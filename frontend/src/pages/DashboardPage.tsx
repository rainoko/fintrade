import Typography from '@mui/material/Typography'

// Placeholder — real composition (StatCards, signal summaries across the
// portfolio) lands in the frontend-dashboard-page task, once
// frontend-common-components and frontend-app-shell-navigation exist.
export default function DashboardPage() {
  return <Typography variant="h4">Dashboard</Typography>
}
