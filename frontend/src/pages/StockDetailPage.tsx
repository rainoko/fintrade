import Typography from '@mui/material/Typography'

// Placeholder — real composition (SignalSummary, ScreensPanel,
// IndicatorsPanel, PriceChart) lands in the frontend-stock-analysis-page and
// frontend-stock-history-chart tasks.
export default function StockDetailPage() {
  return <Typography variant="h4">Stock Detail</Typography>
}
