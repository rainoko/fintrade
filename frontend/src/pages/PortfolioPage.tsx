import Typography from '@mui/material/Typography'

// Placeholder — real composition (PositionsTable, RiskPanel,
// AddPositionDialog) lands in the frontend-portfolio-page and
// frontend-portfolio-risk-panel tasks.
export default function PortfolioPage() {
  return <Typography variant="h4">Portfolio</Typography>
}
