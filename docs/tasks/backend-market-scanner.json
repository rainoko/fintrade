{
  "id": "backend-market-scanner",
  "title": "Market scanner using IBKR's scanner categories, disabled when IBKR isn't available",
  "area": "backend/api",
  "skill": "add-api-endpoint",
  "state": "testing",
  "depends_on": ["backend-ibkr-status-endpoint"],
  "references": [
    "backend/app/data/ibkr_provider.py",
    "docs/ideas.md#market-scanning-ch-56-is-a-new-feature-not-part-of-the-watchlist-or-ticker-detail-view"
  ],
  "description": "Ch. 56 describes scanning a large universe the user doesn't already track (S&P 500, an industry group, etc.) to discover new candidates -- distinct from the watchlist (a curated list of tickers already tracked) and the ticker detail view (analyzes one already-chosen ticker). Per explicit product decision: this uses IBKRProvider.run_scanner's predefined categories (52-week-high/low, hot-by-volume, top % gainers/losers, etc. -- confirmed present in get_scanner_params's option list) when the gateway is enabled and available, and the feature is simply unavailable (a clear 'scanner unavailable, IBKR not connected' state, never a hard error) otherwise. Elder's own bespoke custom-pattern scanner (a precursor-to-divergence detector built with a programmer) is explicitly NOT in scope here -- that remains a separate, much larger idea in docs/ideas.md if ever pursued; this task only wires up IBKR's own existing scan categories.",
  "checklist": [
    { "step": "Add an endpoint exposing the available scanner categories (from IBKRProvider.get_scanner_params) and an endpoint to run a scan (via IBKRProvider.run_scanner), both returning a clear 'unavailable' response (not an error) when IBKR isn't enabled/available -- reuse the same gateway-availability pattern backend-ibkr-status-endpoint establishes.", "done": true },
    { "step": "Decide and record which of IBKR's scan categories to expose as the initial curated set (vs. exposing the raw params list as-is) and any default negative filters (e.g. a minimum average-daily-volume floor, per ch.56's own liquidity-filter advice) -- record the rationale.", "done": true },
    { "step": "Respect IBKRProvider's existing client-side rate limits (1 req/sec for run_scanner, 15-min cache for get_scanner_params) -- do not add a second, conflicting throttle layer.", "done": true },
    { "step": "Unit tests mocking IBKRProvider for: IBKR disabled, gateway unavailable, a successful scan, and the rate-limited case.", "done": true },
    { "step": "Regenerate backend/openapi.json and frontend/src/api/types.ts.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-21T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Both routes (GET /api/ibkr/scanner/params, POST /api/ibkr/scanner/run) live in the existing backend/app/api/routers/ibkr.py alongside GET /api/ibkr/status, rather than a new router file. GET /api/ibkr/scanner/params exposes IBKR's raw scan_type_list from get_scanner_params, passed through as a list[dict] as-is, rather than hand-curating a fixed subset of category codes. POST /api/ibkr/scanner/run's scan_config field is likewise an opaque dict forwarded to IBKRProvider.run_scanner without validation or a server-injected default liquidity filter (e.g. no automatic min-average-volume floor). Both routes report availability via the identical {state, detail} shape GET /api/ibkr/status already established (IBKRGatewayState: disabled/available/gateway_unreachable/not_authenticated), with categories/results populated only when state == 'available'. Being rate-limited (IBKRProvider.run_scanner's own 1-req/sec client-side throttle, IBKRRateLimitedError) is surfaced as an HTTP 429 with the exception's own message as detail, not folded into the {state} shape, since it's a genuine transient error on an otherwise-available scanner, distinct from 'IBKR isn't connected at all'.",
      "rationale": "One router per resource is this codebase's established convention (stocks.py/portfolio.py/watchlist.py, and ibkr.py itself for /status) -- the scanner routes are the same 'ibkr' resource, not a new one. Passing through the raw scan_type_list (rather than a curated subset) was chosen because the actual field shape/available codes are unconfirmed against a live gateway (docs/ideas.md's own admission: 'confirmed not yet checked against a live gateway') -- hand-picking specific category codes risks embedding stale or wrong codes, silently omitting categories IBKR actually offers, or breaking outright if the real field names differ from assumption. This mirrors IBKRProvider.run_scanner's own already-established design philosophy (its docstring: 'this provider doesn't validate scan_config itself, since the option universe is entirely gateway-defined and versioned by IBKR, not something worth duplicating here') -- the same reasoning applies one layer up at the API boundary. For the same reason, no default negative filter (e.g. a volume floor) is injected server-side: the exact IBKR filter code/schema for something like an average-daily-volume floor is unconfirmed against a live gateway, and guessing at one risks constructing a filter that's subtly wrong or silently a no-op; ch.56's liquidity-filter advice is instead documented (in the route docstrings and docs/architecture/API.md) as guidance for the caller to apply via whatever filter option IBKR's own get_scanner_params response actually offers. 429 (not a 'state' value) for rate-limiting was chosen because it's the standard, actionable HTTP status for 'you're calling too fast, retry shortly' -- collapsing it into the same {state, detail} shape used for 'feature unavailable' would blur two very different situations (a permanently-unusable feature right now vs. a fully-usable feature that just needs a brief retry) that a caller needs to handle differently. Also considered eagerly calling get_gateway_status() first in both handlers (mirroring /status exactly) before attempting get_scanner_params()/run_scanner(); rejected in favor of a try/except around the data call itself, falling back to get_gateway_status() only on an IBKRUnavailableError -- this avoids a redundant extra gateway round-trip on every successful/cached call, paying the extra status check only on the (already-failing) unavailable path where it's needed to report which specific state applies instead of parsing IBKRUnavailableError's message string."
    }
  ],
  "git": {
    "branch": "task/backend-market-scanner",
    "pr_number": 214,
    "pr_url": "https://github.com/rainoko/fintrade/pull/214",
    "pushed_at": "2026-09-21T00:19:11Z"
  }
}
