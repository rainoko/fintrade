{
  "id": "backend-profit-target-open-position",
  "title": "Compute a profit target for open positions regardless of today's signal",
  "area": "backend/signals",
  "skill": "verify-elder-signal",
  "state": "planned",
  "depends_on": ["backend-profit-target-weekly-channel"],
  "references": [
    "backend/app/portfolio/profit_target.py",
    "docs/tasks/done/backend-profit-target.json",
    "docs/Analyse.md"
  ],
  "description": "suggest_profit_target is currently BUY-only (only computed for a fresh BUY signal, per backend-profit-target's own prior decision). Ch. 53, read directly, doesn't gate profit targets to entry day only -- a target set at entry is meant to be tracked for the life of the trade, not recomputed only while the signal happens to say BUY. An already-held position whose signal has since drifted to HOLD/SELL currently has no displayed target at all. Depends on backend-profit-target-weekly-channel landing first so this builds on the corrected (weekly-based) target computation, not the one being replaced.",
  "checklist": [
    { "step": "Decide whether an open position's displayed profit target should be computed off the position's actual entry (regardless of today's signal) instead of requiring a fresh BUY signal, and record the rationale -- this revisits backend-profit-target's own prior BUY-only decision explicitly, engaging with why it was made and why ch.53's primary source now changes the analysis.", "done": false },
    { "step": "Implement the decided behavior in GET /api/portfolio/risk (or wherever an open position's target is surfaced).", "done": false },
    { "step": "Update/add tests covering a held position whose signal has drifted to HOLD/SELL, confirming a target is still shown per the decided behavior.", "done": false },
    { "step": "elder-signal-reviewer pass before merge.", "done": false },
    { "step": "Regenerate backend/openapi.json/frontend types if the response shape changes.", "done": false }
  ]
}
