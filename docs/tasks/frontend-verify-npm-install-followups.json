{
  "id": "frontend-verify-npm-install-followups",
  "title": "Non-blocking follow-ups from PR #115 review (frontend-verify-npm-install)",
  "area": "frontend/tooling",
  "skill": null,
  "state": "planned",
  "depends_on": ["frontend-verify-npm-install"],
  "references": [
    "docs/tasks/done/frontend-verify-npm-install.json",
    "https://github.com/rainoko/fintrade/pull/115"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #115 (frontend-verify-npm-install), which fixed the openapi-typescript/typescript npm peer conflict with a scoped `overrides` entry. Neither finding blocks the accept; both are tracked here so they aren't lost once PR #115's comments disappear on merge.",
  "checklist": [
    {
      "step": "Add a short note near frontend/package.json's `overrides` entry (or in docs/architecture/Frontend.md's dependency table, next to the existing openapi-typescript row) explaining why the `openapi-typescript.typescript -> $typescript` override exists -- JSON has no comments, and the full rationale currently lives only in docs/tasks/done/frontend-verify-npm-install.json's decisions array, which a future contributor bumping openapi-typescript or typescript has no discoverable pointer to from the code itself.",
      "done": false
    },
    {
      "step": "Tighten the frontend-verify-npm-install task's decision-entry wording (or add a superseding decisions entry) that claims openapi-typescript 'genuinely works correctly against typescript@6.0.3 at runtime' -- that conclusion was verified via byte-identical regenerated output for the current backend/openapi.json schema only, which has no oneOf/allOf/readOnly/webhooks. pr-reviewer additionally traced openapi-typescript's compiled runtime dist and confirmed it genuinely require('typescript')s and calls real TS compiler-API surface (ts.createSourceFile, ts.factory.createNodeArray, ts.createPrinter().printFile) -- not a type-only, build-time-only relationship as the source's `import type ts from \"typescript\"` might suggest. Consider a one-time smoke test against a synthetic schema exercising oneOf/allOf/readOnly to fully close the verification gap, or at minimum rephrase the decision to scope the claim to what was actually exercised.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
