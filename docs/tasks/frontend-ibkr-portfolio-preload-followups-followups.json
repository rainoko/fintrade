{
  "id": "frontend-ibkr-portfolio-preload-followups-followups",
  "title": "Non-blocking finding from PR #338's review (frontend-ibkr-portfolio-preload-followups)",
  "area": "frontend/portfolio",
  "skill": null,
  "state": "implementing",
  "depends_on": ["frontend-ibkr-portfolio-preload-followups"],
  "references": [
    "docs/tasks/done/frontend-ibkr-portfolio-preload-followups.json",
    "https://github.com/rainoko/fintrade/pull/338"
  ],
  "description": "Tracked follow-up for a non-blocking finding raised in pr-reviewer's review of PR #338 (frontend-ibkr-portfolio-preload-followups) -- doesn't block the accept (PR #338's own decisions entry already explicitly scoped this out, with reasonable rationale), but is worth fixing so it isn't lost once PR #338 merges and the review comment disappears from view.",
  "checklist": [
    {
      "step": "`AddPositionDialog.tsx`'s `<Dialog open={open} onClose={onClose} fullWidth maxWidth=\"sm\">` (line 153) still has the identical Escape-key/backdrop-click dismissal gap `IbkrPreloadDialog.tsx` had before PR #338: only the dialog's own Cancel/Confirm buttons are guarded by `disabled={addPosition.isPending}` (lines 224/227), but MUI's `Dialog` fires `onClose` on Escape and backdrop click regardless of any button's disabled state, so a user can dismiss the dialog mid-`useAddPosition()` mutation. PR #338 fixed this exact pattern in `IbkrPreloadDialog.tsx` via a `handleDialogClose` wrapper that no-ops while the mutation is pending (see that PR's diff and its own `decisions` entry, which explicitly deferred fixing `AddPositionDialog.tsx` in the same PR as a scope call, not an oversight). Apply the same guard here -- either a local `handleDialogClose` wrapper mirroring `IbkrPreloadDialog.tsx`'s, or, now that this pattern exists in two dialogs, consider extracting a small shared helper/hook (e.g. `useGuardedDialogClose(onClose, isPending)`) if that's judged worth the abstraction at this point.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-25T23:28:43Z",
      "recorded_by": "task-worker",
      "decision": "Extracted a shared `useGuardedDialogClose(onClose, isPending)` hook (`frontend/src/features/portfolio/hooks/useGuardedDialogClose.ts`) rather than applying a second copy of `IbkrPreloadDialog.tsx`'s inline `handleDialogClose` wrapper directly to `AddPositionDialog.tsx`. Both dialogs now call the shared hook; `IbkrPreloadDialog.tsx`'s own inline wrapper was replaced with a call to it too, so there is exactly one copy of the guard logic.",
      "rationale": "The parent task's (frontend-ibkr-portfolio-preload-followups) own prior decisions entry explicitly deferred a shared abstraction as 'speculative generality until there's a third case' -- but that was written when only one confirmed instance (IbkrPreloadDialog) existed; this task is itself the second confirmed instance (AddPositionDialog), so extracting now eliminates real, already-existing duplication rather than anticipating a hypothetical future one. This mirrors the codebase's own precedent for the identical judgment call: `useResetOnSubjectChange` (frontend-trade-apgar-followups) was extracted from exactly two dialogs' (`TradeApgarDialog`/`FollowUpReviewDialog`) identical boilerplate, not held back for a third. Rejected leaving two independent inline copies: any future fix to this guard (e.g. also checking a `reason` argument) would then need to land in two places and could silently drift out of sync.",
      "context": "`ClosePositionDialog.tsx`, `FollowUpReviewDialog.tsx`, and `TradeApgarDialog.tsx` were found to have the same unguarded onClose-while-pending pattern during this task, but none of them are in this task's checklist scope -- left untouched and unfixed here; a future task should decide whether to extend `useGuardedDialogClose` to them too."
    }
  ],
  "questions": []
}
