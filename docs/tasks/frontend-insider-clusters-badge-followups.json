{
  "id": "frontend-insider-clusters-badge-followups",
  "title": "Insider-transaction cluster callouts — non-blocking review follow-ups",
  "area": "frontend/pages/stocks",
  "skill": null,
  "state": "planned",
  "depends_on": ["frontend-insider-clusters-badge"],
  "references": [
    "docs/tasks/done/frontend-insider-clusters-badge.json#review",
    "https://github.com/rainoko/fintrade/pull/195"
  ],
  "description": "Non-blocking finding from pr-reviewer's full review of PR #195 (frontend-insider-clusters-badge), tracked here per CLAUDE.md's autonomous-pipeline rule so it isn't lost once that PR merges.",
  "checklist": [
    {
      "step": "The most-recent-window_end_date-first sort comparator (`a.window_end_date < b.window_end_date ? 1 : -1`) is duplicated verbatim between FundamentalDataPanel.tsx's InsiderClusterCallouts (line ~113) and metricHelpContent.ts's insiderClustersHelp.interpretValue (line ~1167) instead of sharing one implementation. Not a correctness bug today, but a maintenance hazard: if the comparator is ever corrected (e.g. a tie-break, or an equal-date edge case) in one copy, it's easy to update only one call site, leaving the callout list and the MetricHelp text silently disagreeing on cluster order for the same ticker. Extract a single shared sort helper (e.g. colocated in metricHelpContent.ts or a small shared util) and have both call sites use it.",
      "done": false
    }
  ]
}
