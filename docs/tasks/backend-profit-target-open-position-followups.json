{
  "id": "backend-profit-target-open-position-followups",
  "title": "Non-blocking follow-ups from backend-profit-target-open-position PR #224 review",
  "area": "backend/signals",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-profit-target-open-position"],
  "references": [
    "docs/tasks/backend-profit-target-open-position.json",
    "https://github.com/rainoko/fintrade/pull/224",
    "backend/app/api/routers/portfolio.py"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #224 (backend-profit-target-open-position), recorded here per CLAUDE.md's convention so they aren't lost once the PR merges. The review's blocking findings (a stop-value inconsistency between RiskPosition.protective_stop and RiskPosition.profit_target's own internal stop, and the frontend never consuming the new field) are tracked directly on backend-profit-target-open-position's own review.comments and must be fixed there before that PR merges -- they are not duplicated here since that task isn't done yet.",
  "checklist": [
    {
      "step": "GET /api/portfolio/risk now runs a full whole-history support/resistance detection pass (detect_support_resistance_zones) plus a second full weekly-Autoenvelope + swing-low pass (inside suggest_profit_target) for every position on every request, uncached -- none of which existed on this endpoint before this task. If this endpoint is polled (RiskPanel's usage suggests it is), this multiplies per-poll CPU cost by N extra heavy passes with no memoization, unlike the already-computed stops/daily_by_id/weekly_by_id this same loop deliberately caches for reuse within one request. Not a correctness issue at today's data sizes, but worth reconsidering (e.g. caching profit_target per (ticker, as-of-date) the way OHLCV itself is cached) if this endpoint's polling frequency or position count grows.",
      "done": false
    },
    {
      "step": "docs/architecture/Backend.md's file-tree listing still describes profit_target.py as 'suggested profit target + reward:risk ratio for a fresh BUY (Analyse.md §7)', which is stale now that GET /api/portfolio/risk also calls it unconditionally for every open position regardless of signal (this task's own change). Reword to reflect that the module itself is signal-agnostic and serves two deliberately-differently-gated callers, consistent with the module's own docstring, docs/Analyse.md §7, and docs/architecture/API.md, all of which already describe this correctly. Found by pr-reviewer's third review pass (PR #224).",
      "done": false
    },
    {
      "step": "ProfitTargetOut.distance_to_target's schema description (backend/app/api/schemas.py) states 'Always > 0 by construction (both target techniques only ever produce a price above current close)', but suggest_profit_target's own domain-layer docstring (backend/app/portfolio/profit_target.py) documents a real degenerate case where the channel-derived target equals current price exactly (distance_to_target == 0) when the weekly Autoenvelope's rolling average deviation is exactly 0 across the whole lookback window. Pre-existing since PR #222/backend-profit-target-weekly-channel (confirmed via git show main:... on both files) and unrelated to this task's own BUY-only-vs-signal-agnostic distinction, so not blocking on backend-profit-target-open-position -- but a real overstated invariant in the OpenAPI contract worth fixing (e.g. hedge the description the same way distance_to_stop's already does with 'can be <= 0 in the rare case...'), and regenerate backend/openapi.json/frontend/src/api/types.ts alongside it. Found by pr-reviewer's third review pass (PR #224), via its code-review skill sub-pass.",
      "done": false
    }
  ]
}
