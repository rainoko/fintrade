{
  "id": "backend-day-trader-timeframe-mode-ibkr-intraday",
  "title": "Day-trader mode: wire IBKR live intraday data fetching for the configured timeframe triple",
  "area": "backend/data",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-day-trader-timeframe-mode"],
  "references": [
    "docs/tasks/backend-day-trader-timeframe-mode.json",
    "docs/architecture/Backend.md#8-optional-ibkr-client-portal-web-api-provider",
    "docs/architecture/Backend.md#11-day-trader-timeframe-mode-foundational-model--settings-in-progress",
    "docs/ideas.md#switchable-trading-mode-positionswing-trader-vs-day-trader--different-triple-screen-timeframe-scales"
  ],
  "description": "Second of four dependent follow-up tasks splitting docs/tasks/backend-day-trader-timeframe-mode.json's original checklist item 4 out of that task (see its `decisions` entry for the full split rationale and what already landed: `app.signals.timeframe`'s generic TimeframeTriple model + `app.trading_mode`'s global settings persistence + `GET`/`PUT /api/settings/trading-mode`). This task wires actual IBKR intraday data fetching for whichever of the day-trader triple's legs need it (the short-term leg always; the intermediate leg too if it's also intraday), so day-trader mode has real data behind it before the signal-engine genericization task (backend-day-trader-timeframe-mode-signal-engine) can be meaningfully tested end to end.\n\nGenuine open design problem to resolve here, not yet solved by anything already landed: IBKR's `/iserver/marketdata/history` endpoint (`app.data.ibkr_provider.IBKRProvider.get_hourly_bars`) only accepts a **fixed, non-composable** set of `bar` values (`_VALID_BAR_INTERVALS`: 1/2/3/5/10/15/30 min, 1/2/3/4/8h, 1d/1w/1m) -- there is no way to request e.g. a literal 25-minute or 39-minute bar the way ch. 39's own day-trading examples use, even though `backend-day-trader-timeframe-mode`'s own product decision was a *fully user-configurable* triple (any positive integer count of minutes/days/weeks, per `app.signals.timeframe.TimeframeInterval`). This task needs to decide (and record as a `decisions` entry) how to reconcile the two: candidates include (a) fetching the nearest IBKR-supported granularity finer than or equal to the configured interval and resampling client-side into the exact requested bar width (mirrors `app.data.stooq_provider.StooqProvider._resample_weekly`'s own daily-to-weekly resampling precedent), (b) restricting the day-trader triple's *legs that need live IBKR data* to only IBKR-supported values (a schema/validation change to `TimeframeInterval`/the settings API, and arguably a step back from the 'fully configurable' decision -- would need explicit reasoning for why it's still consistent with that decision, not just convenient), or (c) something else. Don't guess silently; this is exactly the kind of judgment call CLAUDE.md's decision-memory rule expects recorded with rationale.\n\nMust degrade gracefully to a clear disabled/unavailable state (not a hard error) when IBKR isn't connected or `Settings.ibkr_enabled` is `False`, consistent with every other IBKR-dependent feature's contract in this app (`GET /api/ibkr/status`, `backend-market-scanner`/`backend-market-breadth-indicators`'s established pattern) -- day-trader mode being *configured* must not itself require IBKR to be reachable, only actually *using* it for live data should.\n\nSymbol-to-conid resolution (`IBKRProvider.resolve_conid`) and parameterized bar intervals (`get_hourly_bars`'s `bar_size`) are both already implemented (`backend-ibkr-symbol-resolution`, `backend-ibkr-bar-interval-param`, both done) -- this task is about wiring them into an actual day-trader-mode-aware data-fetching path, not building either from scratch.",
  "checklist": [
    { "step": "Research and record (via `decisions`) how to reconcile IBKR's fixed bar-interval set with the fully-configurable day-trader timeframe triple -- see this task's description for the candidate approaches.", "done": false },
    { "step": "Implement the chosen approach: a function/module that, given the active day-trader `TimeframeTriple` (`app.trading_mode.get_trading_mode_setting`) and a resolved IBKR conid, returns intraday OHLCV bars for whichever leg(s) need them.", "done": false },
    { "step": "Graceful degradation when IBKR is disabled/unreachable/unauthenticated -- a clear unavailable state, never a raw exception surfaced to a caller.", "done": false },
    { "step": "Unit tests with a mocked IBKRProvider/`_request` boundary -- no test may make a live network call, matching this codebase's existing IBKR test convention (`tests/unit/data/test_ibkr_provider.py`).", "done": false },
    { "step": "Update docs/architecture/Backend.md §11's 'Not yet landed' list to move this item to landed, describing what was actually built.", "done": false }
  ],
  "decisions": [],
  "questions": []
}
