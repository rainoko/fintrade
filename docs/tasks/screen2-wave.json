{
  "id": "screen2-wave",
  "title": "Screen 2: Wave (oscillator timing)",
  "area": "backend/signals",
  "skill": "verify-elder-signal",
  "state": "done",
  "depends_on": ["indicator-force-index", "indicator-stochastic"],
  "references": ["docs/Analyse.md#2-triple-screen-trading-system-per-stock-signal-engine"],
  "description": "evaluate_wave() in app/signals/triple_screen.py: evaluates Force Index and Stochastic against the tide direction to detect a pullback (bullish tide) or rally (bearish tide).",
  "checklist": [
    { "step": "Given tide=BULLISH, detect oversold Stochastic + negative Force Index spike as a pullback", "done": true },
    { "step": "Given tide=BEARISH, detect overbought Stochastic + positive Force Index spike as a rally", "done": true },
    { "step": "Return the state dict shape expected by the API (screens.wave.stochastic_k, force_index_2ema, state) per docs/architecture/API.md", "done": true },
    { "step": "Write unit tests covering both tide directions and the neutral/no-signal case", "done": true },
    { "step": "Run pytest --cov=app --cov-report=term-missing and confirm no regression", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-13T00:00:00Z",
      "recorded_by": "verify-elder-signal skill (task-worker)",
      "decision": "evaluate_wave()'s 'state' field uses a three-value vocabulary: 'OVERSOLD_PULLBACK' (tide BULLISH, latest Stochastic %K < 30 AND the latest 2-EMA Force Index is a negative 'spike'), 'OVERBOUGHT_RALLY' (tide BEARISH, %K > 70 AND a positive Force Index spike), and 'NO_WAVE' for every other case (NEUTRAL tide, a directional tide without a matching oscillator extreme, an oscillator extreme without a matching Force Index spike, or NaN indicator values from insufficient warm-up history). Both the Stochastic and Force Index conditions are required together, matching this task's own checklist phrasing ('oversold Stochastic + negative Force Index spike') and docs/Analyse.md §2's framing of the two indicators acting together, not as independent triggers.",
      "rationale": "docs/architecture/API.md's screens.wave.state example value is only 'OVERSOLD_PULLBACK' with a free-text `str` type in app/api/schemas.py (not a Literal enum), so the exact vocabulary -- including the bearish-tide counterpart's name and the no-signal value's name -- was left open and had to be decided here. 'OVERBOUGHT_RALLY' was chosen as the direct mirror of the given 'OVERSOLD_PULLBACK' example (same pullback/rally + oversold/overbought terminology docs/Analyse.md §2 itself uses: 'a pullback ... or rally'). 'NO_WAVE' (rather than e.g. reusing 'NEUTRAL', which docs/Analyse.md §2 already uses for a different concept -- the *tide's* neutral state) was chosen to avoid conflating 'the tide itself is neutral' with 'the tide has a direction but Screen 2 hasn't produced a qualifying oscillator signal yet'; those are different conditions (only the former literally has tide=='NEUTRAL') and collapsing their names risked confusing a future reader of the API response. Considered and rejected treating an oversold Stochastic without a Force Index spike (or vice versa) as a weaker/partial pullback state (e.g. 'WEAK_PULLBACK'): docs/Analyse.md §2 describes the pullback/rally as the joint outcome of both indicators, §6's separate 'oscillator extremity' confidence component already exists to grade *how* oversold/overbought within a real pullback, and NotImplemented confidence-scoring (a separate, not-yet-started task) is the natural home for degrees of partial agreement, not a Screen 2 boolean/tri-state classifier -- so partial matches are simply NO_WAVE here, not a fourth named state Analyse.md never mentions."
    },
    {
      "timestamp": "2026-09-13T00:05:00Z",
      "recorded_by": "verify-elder-signal skill (task-worker)",
      "decision": "'Force Index spike' (docs/Analyse.md §2/§4: 'negative spike in an uptrend', 'positive spike in a downtrend', no numeric definition given) is defined in app.signals.triple_screen._is_force_index_spike as: the latest 2-EMA Force Index value has the required sign, AND its magnitude exceeds 1.0x the standard deviation of the trailing 13-bar window of the same 2-EMA Force Index series (pandas rolling().std(), sample/ddof=1). Returns False (not a spike) if there's insufficient history for that rolling window, if the rolling standard deviation is exactly zero, or if the latest value is NaN.",
      "rationale": "Considered and rejected a pure sign-only definition (any negative value during a bullish tide counts as a 'spike'): that would make 'spike' meaningless as a qualifier -- any single down day would qualify, which doesn't match Elder's own description of Force Index spikes as outsized, attention-grabbing moves distinct from ordinary daily noise, and would make the OVERSOLD_PULLBACK/OVERBOUGHT_RALLY states fire far too often relative to how selective docs/Analyse.md §2 describes Screen 2 as being ('wait for' a pullback/rally, not 'any day the sign matches'). Considered and rejected a fixed absolute-value threshold (e.g. '|Force Index| > $10,000'): Force Index's magnitude scales with a stock's own price times volume, so a single fixed number would be meaningless across tickers of different sizes -- a per-stock, self-relative threshold is required. Chose the trailing rolling standard deviation of the same 2-EMA series (self-relative, scale-free) over alternatives considered: (a) a percentile-of-history threshold -- rejected as needing a much longer lookback window than this function otherwise requires, and no principled percentile cutoff is given by Analyse.md either; (b) comparing the 2-EMA reading against the 13-EMA trend-confirmation variant (also named in docs/Analyse.md §2/§4) -- rejected because the 13-EMA is a *slower*, more heavily smoothed series measuring a different thing (trend confirmation, not entry timing), not a volatility/typical-magnitude baseline for the faster 2-EMA series, so comparing the two directly would conflate two purposes the doc keeps separate. The specific window (13 bars) reuses the Force Index trend-confirmation period Analyse.md §2/§4 already names for a different purpose, rather than inventing an unrelated fourth number; the 1.0x standard-deviation multiplier is a plain 'one standard deviation is enough to call a value statistically outsized' choice with no doc anchor -- both are judgment calls that should be revisited if/when this app has real historical data to backtest signal quality against (see docs/Analyse.md §10's open item on backtested tuning of thresholds generally)."
    }
  ],
  "git": {
    "branch": "task/screen2-wave",
    "pr_number": 21,
    "pr_url": "https://github.com/rainoko/fintrade/pull/21",
    "pushed_at": "2026-09-13T00:20:00Z"
  },
  "review": {
    "reviewed_at": "2026-09-13T20:00:00Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/21",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Re-review from scratch, previous blocking finding verified fixed: evaluate_wave() now guards len(daily_ohlcv) == 0 at the top, returning {stochastic_k: nan, force_index_2ema: nan, state: NO_WAVE} before calling stochastic_oscillator/force_index, matching evaluate_impulse()'s len(daily_ohlcv) < 2 precedent. Independently reproduced (not just re-read): evaluate_wave(pd.DataFrame(columns=[...]), 'BULLISH') now returns the NO_WAVE/NaN shape with no crash. A dedicated regression test (test_empty_daily_ohlcv_degrades_to_no_wave_instead_of_raising) was added using the real unmocked indicators.",
        "file": "backend/app/signals/triple_screen.py",
        "line": 88
      },
      {
        "summary": "Checked the rest of the function for the same edge-case family per re-review instructions: independently constructed a 1-row daily_ohlcv (no crash, degrades to NaN/NO_WAVE -- correct, since unlike evaluate_impulse's _direction() which indexes .iloc[-2] and needs its own < 2 guard, evaluate_wave's .iloc[-1] reads are on rolling()/ewm()-derived series that yield NaN rather than raising on too little history, so a == 0 guard here is the right minimal fix, not an oversight) and a 30-row frame with NaN values injected mid-series into close/high (no crash, correctly classifies via the existing pd.isna(stochastic_k) / _is_force_index_spike NaN checks). No further gaps found in this family.",
        "file": "backend/app/signals/triple_screen.py",
        "line": null
      },
      {
        "summary": "Non-blocking nit (unchanged from previous pass, still not fixed, still not blocking): _is_force_index_spike's `negative: bool` parameter forces double-negative reasoning (`if negative and latest >= 0`, `if not negative and latest <= 0`). Both branches are correct and covered by tests; a signed-multiplier form would read more directly for a future editor.",
        "file": "backend/app/signals/triple_screen.py",
        "line": 43
      },
      {
        "summary": "Non-blocking (from the backgrounded code-review skill pass, high effort): the new guard's docstring is self-contradictory -- it says it mirrors evaluate_impulse's `len(daily_ohlcv) < 2` guard '... in this same module (app/signals/impulse.py)', but evaluate_wave lives in triple_screen.py, a different module than the one just cited, and the condition actually implemented is `== 0`, not `< 2`. Functionally correct (independently verified both 0-row and 1-row degrade safely today, since stochastic_oscillator/force_index tolerate 1-row input by yielding NaN rather than raising), but the wording could mislead a future reader into assuming parity with evaluate_impulse's stricter threshold; worth a docstring wording fix, not blocking.",
        "file": "backend/app/signals/triple_screen.py",
        "line": 84
      },
      {
        "summary": "Non-blocking (from the backgrounded code-review skill pass): no regression test pins the 1-row daily_ohlcv boundary case that the 0-row-vs-<2 guard asymmetry (vs. evaluate_impulse) turns on. Only 0 rows is covered by test_empty_daily_ohlcv_degrades_to_no_wave_instead_of_raising. If stochastic_oscillator/force_index later add stricter single-row validation, nothing here would catch the regression. Worth adding, not blocking (independently verified passes today).",
        "file": "backend/tests/unit/signals/test_triple_screen.py",
        "line": 200
      }
    ],
    "notes": "Re-review from scratch of PR #21 (independent of the prior needs_work pass in this same task file). Checked out task/screen2-wave fresh. Scope: this task's own commits (064ceb5, 62fa505, 800123c -- the last being the fix for the previously-found IndexError) touching evaluate_wave()/_is_force_index_spike() and their tests; the carried-in indicator-stochastic commits (b5876a1/7935d51/507b275, already-accepted PR #12) were confirmed via `git log` to be untouched by this branch's own commits and were not re-reviewed. Independently reproduced the empty-frame fix (no crash, correct NO_WAVE/NaN shape) rather than trusting the diff/commit message alone. Additionally probed the same edge-case family per dispatch instructions: a 1-row frame and a frame with NaN values injected partway through both degrade gracefully with no crash -- see PR comments for why a 1-row guard isn't needed here despite evaluate_impulse's sibling pattern using < 2. Backend suite: backend/.venv/bin/pytest -q -> 86 passed (was 85 pre-fix; +1 for the new regression test), no regressions. Coverage: pytest --cov=app --cov-report=term-missing -> triple_screen.py 96% (only the two untouched evaluate_tide/evaluate_trigger NotImplementedError stub lines uncovered, out of scope); whole-repo 79.42%, below the 90% gate but not a regression -- pre-existing stub modules tracked by other not-yet-started tasks, same accepted pattern as prior PRs. No ruff/mypy configured in backend/pyproject.toml -- nothing to lint. Re-ran verify-elder-signal against docs/Analyse.md Sec.2: Stochastic thresholds (<30/>70, strict) match 'below 30 / above 70' exactly; both Stochastic and Force Index conditions correctly required jointly; only the 2-EMA Force Index is used (entry timing), 13-EMA trend-confirmation and Elder-Ray correctly out of scope (separate confidence-scoring concerns); evaluate_tide/evaluate_trigger confirmed still untouched stubs. Both judgment-call decisions (state vocabulary; Force-Index-spike definition) remain recorded in this task's decisions array with real alternatives-considered rationale -- decision-memory rule satisfied. Returned dict shape matches docs/architecture/API.md's screens.wave exactly. No API route/schema changed -- backend/openapi.json diff confirmed empty, regeneration N/A. No new module/dependency/layer -- architecture-review N/A. No UI surface (pure backend signal-layer Python, no frontend scaffolded, no API route changed) -- browser walkthrough explicitly N/A. Ran the code-review skill (high effort, backgrounded) against the diff in addition to this manual pass; it confirmed the fix (21 tests pass, no remaining correctness bug of blocking severity) and surfaced two further non-blocking nits (a self-contradictory docstring wording, a missing 1-row regression test) beyond the already-fixed 0-row case and the pre-existing double-negation nit -- none blocking. gh pr review --approve attempted first, blocked by GitHub's self-approval rule (worker and reviewer share the raino-agent account) -- posted as a COMMENTED-type PR review with the full findings instead, per the standing fallback process. Task state set to done. Do not merge -- that decision belongs to a human."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-14T21:30:00Z",
    "verdict": "merge",
    "notes": "Redo of an earlier same-day pr-decision pass on PR #21 that predated this schema field (its decision was 'merge' but went unrecorded) -- re-verified independently rather than trusting that prior run. Checked out task/screen2-wave (merge commit 768cc67, merging origin/main's bcd86d4 in). Backend suite: backend/.venv/bin/pytest --cov=app --cov-report=term-missing -> 129 passed, no failures; whole-repo coverage 84.90% (< 90% gate, but pre-existing 0%-covered stub modules -- app/data/*, app/portfolio/exits.py, app/portfolio/risk.py, app/signals/confidence.py, app/signals/engine.py -- unrelated to this PR, not a regression); triple_screen.py itself at 99% (only line 225, the untouched evaluate_trigger NotImplementedError stub). Skimmed the diff vs origin/main myself (git diff origin/main...HEAD --stat: triple_screen.py, test_triple_screen.py, docs/tasks/index.json, docs/tasks/screen2-wave.json only). Focused verification on the merge-conflict resolution pr-reviewer flagged as scrutinized: diffed each function's body (evaluate_wave, _is_force_index_spike from the task/screen2-wave pre-merge side; TideResult/macd_histogram_slope/evaluate_tide from the origin/main side) against the merged file -- byte-identical (only blank-line offsets from my own sed ranges, no content differences). For the test file, counted def test_ occurrences on both pre-merge sides (21 on the task side, 16 on the main side) against the merged file (37) -- matches exactly with no drop/duplication; also diffed the sorted union of both sides' test names against the merged file's sorted test names -- identical, confirming both TestEvaluateWave*/TestIsForceIndexSpike (task side) and TestMacdHistogramSlope/TestEvaluateTideCombinationLogic/TestEvaluateTideEndToEnd (main side) classes are each fully present with none lost. Hand-verified this task's own Force-Index-spike decisions entry independently in a scratch Python REPL (not the test suite itself): pd.Series([0.0]*12+[-50.0]).rolling(13).std() = 13.8675, and abs(-50.0) > 13.8675 is True, matching test_bullish_tide_oversold_stochastic_with_negative_spike_is_pullback's expected OVERSOLD_PULLBACK; separately, pd.Series([-20.0,20.0]*6+[-10.0]).rolling(13).std() = 20.191, and abs(-10.0) > 20.191 is False, matching test_bullish_tide_oversold_without_force_index_spike_is_no_wave's expected NO_WAVE. Confirmed docs/Analyse.md lines 41-42 verbatim: 'Negative spike in an uptrend = buying opportunity; positive spike in a downtrend = selling opportunity' and 'below 30 = oversold, above 70 = overbought' -- matches STOCHASTIC_OVERSOLD/OVERBOUGHT and the BULLISH/negative, BEARISH/positive pairing exactly; the spike magnitude threshold itself has no doc-given numeric definition, consistent with this task's decisions entry saying it had to be decided independently. Re-confirmed pr-reviewer's 'no ruff/mypy configured' claim by grepping backend/pyproject.toml myself -- true, no lint config present. docs/tasks/index.json's screen2-wave entry correctly mirrors state: done. No findings that overturn the accept -- pr-reviewer's non-blocking nits (double-negation param, self-contradictory docstring line, missing 1-row regression test) are genuinely cosmetic and don't affect correctness or the merge-conflict integrity question this run was dispatched to focus on. Accept holds; reporting merge."
  }
}
