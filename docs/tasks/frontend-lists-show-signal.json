{
  "id": "frontend-lists-show-signal",
  "title": "Every ticker list in the UI shows its BUY/HOLD/SELL signal",
  "area": "frontend/pages/portfolio",
  "skill": "add-frontend-feature",
  "state": "implementing",
  "depends_on": ["api-portfolio-position-signal"],
  "references": [
    "frontend/src/features/portfolio/components/PositionsTable.tsx",
    "frontend/src/features/portfolio/components/PositionsGlanceTable.tsx",
    "frontend/src/features/portfolio/components/RiskPanel.tsx",
    "frontend/src/features/watchlist/components/WatchlistTable.tsx",
    "frontend/src/components/common/SignalBadge",
    "docs/tasks/api-portfolio-position-signal.json"
  ],
  "description": "User request: all lists must show the BUY/HOLD/SELL signal, even the watchlist. WatchlistTable (frontend-watchlist-page, merged) already does this via common/SignalBadge -- no change needed there, confirm it still holds. The actual gap is the Portfolio-domain tables: PositionsTable (Portfolio page) and PositionsGlanceTable (Dashboard page) show ticker/quantity/price/P&L but no signal today, since GET /api/portfolio never returned one -- api-portfolio-position-signal (depends_on) adds it. Add a Signal column to both tables using the exact same common/SignalBadge treatment WatchlistTable already established (reuse, do not reinvent), with the same null-signal '—' fallback pattern WatchlistTable uses. Decide whether RiskPanel's table should also get a signal column (it currently shows risk-driven exit_flags, a different concept from a fresh entry signal -- both could usefully coexist) and record as a decisions entry either way.",
  "checklist": [
    { "step": "Add a Signal column (common/SignalBadge, null-safe) to PositionsTable.tsx (Portfolio page), reusing WatchlistTable's exact pattern.", "done": true },
    { "step": "Add a Signal column (common/SignalBadge, null-safe) to PositionsGlanceTable.tsx (Dashboard page), same pattern.", "done": true },
    { "step": "Decide whether RiskPanel's table also gets a signal column, and implement if so; record the decision either way.", "done": true },
    { "step": "Confirm WatchlistTable's existing signal display still works unchanged (regression check only, no code change expected there).", "done": true },
    { "step": "Update/add tests for each changed table's new column (MSW-mocked, including the null-signal case).", "done": true },
    { "step": "Confirm 90% coverage gate still holds; run make e2e to confirm nothing broke on the Portfolio/Dashboard pages.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-18T21:16:45Z",
      "recorded_by": "task-worker",
      "decision": "RiskPanel's per-position table also gets a Signal column, sourced by cross-referencing the already-fetched `positions` prop (PositionOut[], from GET /api/portfolio) by ticker -- not by having the backend add a `signal` field to RiskPosition/GET /api/portfolio/risk.",
      "rationale": "The task's own title ('Every ticker list in the UI shows its BUY/HOLD/SELL signal') is a literal instruction, and RiskPanel's table is a ticker list users look at alongside the others. Exit flags (risk-driven, 'should I get out of a position I already hold') and a fresh entry signal (Elder's Triple Screen, 'would I buy/sell this ticker today') are genuinely different concepts per the task description, but nothing stops them coexisting in the same row -- a HOLD signal next to a stop_hit exit flag is meaningful information, not noise. Rejected: (1) leaving RiskPanel unchanged -- under-delivers on the literal task title for a table that already lists tickers prominently; (2) having the backend add `signal` to RiskPosition -- GET /api/portfolio already returns it per-ticker (api-portfolio-position-signal), so duplicating it into a second response only for RiskPanel's convenience would be scope creep, redundant, and risks the two responses drifting out of sync for a ticker whose signal changes between the two fetches; cross-referencing client-side (the same pattern RiskPanel already uses for its missingTickers note) avoids all of that with no backend change needed. A ticker present in `riskPositions` but with no matching entry in `positions` (or whose PositionOut.signal is null) falls back to the same '—' WatchlistTable/PositionsTable/PositionsGlanceTable use."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/frontend-lists-show-signal",
    "pr_number": 120,
    "pr_url": "https://github.com/rainoko/fintrade/pull/120",
    "pushed_at": "2026-09-18T21:17:26Z"
  },
  "review": {
    "reviewed_at": "2026-09-18T21:24:17Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/120",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "RiskPanel.tsx's new Signal column uses `key: 'signal'` on a `DataTableColumn<RiskPosition>[]`, but `RiskPosition` (GET /api/portfolio/risk's schema) has no `signal` field -- `DataTableColumn<T>.key` is typed `keyof T`, so `tsc -b --noEmit` fails with TS2322 and `yarn build` (tsc -b && vite build) is broken. vitest/eslint/make e2e all stay green since none of them run a full project type-check, so this passed the PR's own stated bar undetected. Fix: reuse a real RiskPosition field name for the key (matching PositionsTable.tsx's own `key: 'id'` synthetic-column precedent), or widen DataTableColumn to accept a synthetic key.",
        "file": "frontend/src/features/portfolio/components/RiskPanel.tsx",
        "line": 115
      }
    ],
    "notes": "Backend (pytest, 565 passed, 99.86% cov) and frontend (vitest, 270 passed, 99.6%/99.2% cov) suites both pass; ruff/mypy/eslint all clean; make e2e 13/13 passed. Browser walkthrough (real Chromium, live backend + a route-mocked edge case) confirmed the Signal column renders consistently across PositionsGlanceTable/PositionsTable/RiskPanel for real positions, the null-signal '—' fallback works in all three, and RiskPanel's client-side positions/riskPositions cross-reference degrades gracefully (no crash, '—' fallback) for a ticker present in the risk response but missing from the portfolio response (the /api/portfolio vs /api/portfolio/risk desync scenario flagged for this review). The RiskPanel signal-column decision is properly recorded in `decisions`. handlers.ts's mockWatchlistSignals -> mockTickerSignals generalization does not mask the null-signal case -- PositionsTable.test.tsx, PositionsGlanceTable.test.tsx, and RiskPanel.test.tsx all retain explicit null-signal fixtures/assertions. Sole blocking finding is the tsc build error above; no non-blocking findings surfaced (independently corroborated by the code-review skill's own pass), so no follow-up task was filed."
  }
}
