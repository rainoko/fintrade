{
  "id": "indicator-stochastic-followups",
  "title": "Indicator: Stochastic — non-blocking review follow-ups",
  "area": "backend/indicators",
  "skill": null,
  "state": "testing",
  "depends_on": ["indicator-stochastic"],
  "references": [
    "docs/tasks/indicator-stochastic.json#review",
    "https://github.com/rainoko/fintrade/pull/12"
  ],
  "description": "Non-blocking finding from pr-reviewer's review of PR #12 (indicator-stochastic), tracked here per CLAUDE.md's autonomous-pipeline rule so it isn't lost once that PR merges.",
  "checklist": [
    {
      "step": "_validate_period's int/bool/range guard in stochastic.py duplicates the identical inline check in app/indicators/ema.py::ema(). force_index.py avoided this by delegating to ema() directly, but stochastic.py can't (it's SMA-, not EMA-, based) — extract a shared _validate_period-style helper once a third indicator needs the same check, rather than a third copy-paste.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-17T17:39:12Z",
      "recorded_by": "task-worker",
      "decision": "Extracted the duplicated int/bool/range period-validation guard from app/indicators/ema.py::ema() and app/indicators/stochastic.py::stochastic_oscillator() into a new shared app/indicators/_validation.py::validate_period(name, value) helper, and had both call sites delegate to it. This was done now even though only two duplicates exist, not three as the checklist item's own text said to wait for.",
      "rationale": "The checklist item's stated precondition ('extract... once a third indicator needs the same check') was explicitly overridden by direct user instruction: a maintained, non-duplicated implementation was judged worth doing immediately rather than waiting for a third copy-paste to accumulate. The refactor is behavior-preserving — same error types, same parametrized error-message wording (stochastic.py's existing _validate_period(name, value) signature was reused as the shared helper's signature so ema.py's call site, which validates a single param literally named 'period', produces byte-identical error text to before) — verified via each indicator's existing test suite plus new direct tests for the helper in tests/unit/indicators/test_validation.py. Rejected keeping the module-level function name with its leading underscore (_validate_period) in the new shared module: the module itself (app/indicators/_validation.py) already signals package-internal-only via its own leading underscore, so the function name didn't need a second one now that it's called from outside its original module."
    }
  ],
  "git": {
    "branch": "task/indicator-stochastic-followups",
    "pr_number": 89,
    "pr_url": "https://github.com/rainoko/fintrade/pull/89",
    "pushed_at": "2026-09-17T17:39:56Z"
  }
}
