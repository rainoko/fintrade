{
  "id": "portfolio-exit-rules-followups",
  "title": "Existing-position exit rules — non-blocking review follow-ups",
  "area": "backend/portfolio",
  "skill": null,
  "state": "done",
  "depends_on": ["portfolio-exit-rules"],
  "references": [
    "docs/tasks/portfolio-exit-rules.json#review",
    "https://github.com/rainoko/fintrade/pull/28"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #28 (portfolio-exit-rules), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges.",
  "checklist": [
    {
      "step": "evaluate_exit_flags() recomputes several indicators from scratch on overlapping windows within a single call: protective_stop(position, daily_ohlcv[:-1]) computes EMA(13) over the pre-today slice, autoenvelope(daily_ohlcv['close']) separately computes EMA(13) over the full (today-inclusive) series, and evaluate_impulse(daily_ohlcv) recomputes EMA(13) + the MACD-Histogram yet again over that same full series -- three independent EMA(13) passes plus a MACD pass per call. evaluate_tide() is also called twice (full weekly_ohlcv, then weekly_ohlcv[:-1]), each independently recomputing MACD-Histogram + EMA(13)/EMA(26) over its own slice. None of this is a correctness issue (mirrors the already-tracked, analogous finding in signal-engine-orchestration-followups for engine.py/impulse.py) and isn't a hot path yet at current per-position, on-demand evaluation call volumes, but worth revisiting -- e.g. sharing the EMA/MACD series across protective_stop/autoenvelope/evaluate_impulse, or a slice-aware cache -- next time exits.py, risk.py, autoenvelope.py, or impulse.py is touched, especially once this feeds a portfolio-wide endpoint evaluating every held position on each request.",
      "done": true
    },
    {
      "step": "The 'six_percent_rule_contributor' decision recorded in docs/tasks/portfolio-exit-rules.json claims 'stop_hit and six_percent_rule_contributor are mutually exclusive by construction' -- but this only holds if position.current_price and daily_ohlcv's latest close represent the same 'today'. evaluate_exit_flags() takes both as independent parameters with no validation or documented precondition tying them together: stop_hit is tested against daily_ohlcv['close'].iloc[-1], while position_risk_pct (feeding both two_percent_rule_breached and six_percent_rule_contributor) is computed against position.current_price via risk.py's pre-existing contract (from the portfolio-risk-rules task, not introduced here). If a future caller (the still-stubbed GET /api/portfolio/risk endpoint, per api-portfolio-risk) ever populates these from data that isn't perfectly in sync (e.g. a stale current_price from a failed/cached price fetch while daily_ohlcv reflects a fresher bar), stop_hit and six_percent_rule_contributor could both fire together despite the decision's claim. Not a live bug today (no wired caller exists yet to violate this), and not a docs/Analyse.md violation (§7 doesn't require these flags be mutually exclusive -- that was only the task-worker's own rationale for the design, not a spec requirement), but worth either (a) adding an explicit documented precondition to evaluate_exit_flags() that daily_ohlcv's latest bar and position.current_price must be as-of the same day, or (b) softening/removing the 'by construction' claim in the decisions entry so a future reader doesn't rely on a guarantee the code doesn't actually enforce. Surface this when api-portfolio-risk (the real caller) is implemented.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-15T00:00:00Z",
      "recorded_by": "task-worker (portfolio-exit-rules-followups)",
      "decision": "Fixed the redundant-computation finding by adding optional, backward-compatible precomputed-series keyword parameters to the four collaborator functions evaluate_exit_flags() calls -- protective_stop(..., short_ema=), autoenvelope(..., mid=), evaluate_impulse(..., ema_13=), and evaluate_tide(..., histogram=, ema_13=, ema_26=) -- all defaulting to None (unchanged behavior: each function still computes its own series internally when the caller doesn't supply one). evaluate_exit_flags() now computes the daily EMA(13) once (over the full, today-inclusive daily_ohlcv['close']) and shares it -- as-is with autoenvelope/evaluate_impulse, sliced via .iloc[:-1] for protective_stop's pre-today window -- and computes the weekly macd_components()/ema(13) once and shares them (again with a .iloc[:-1] slice for the 'previous tide' call) across both evaluate_tide() invocations. Slicing a full-series EMA/MACD computation to derive a shorter window's values is valid because EMA/MACD are causal (a value at index t depends only on data up to and including t, never later data) -- verified both by a hand-argument in each function's updated docstring and by a dedicated unmocked regression test (TestEvaluateTidePrecomputedSeries.test_precomputed_series_causal_slice_matches_recomputing_on_truncated_series in test_triple_screen.py) confirming the sliced-and-shared result exactly equals independently recomputing on the truncated series. This reduces the daily side from 3 independent EMA(13) passes to 1, and the weekly side from 2 independent macd_components()+ema(13) passes to 1 each, per evaluate_exit_flags() call -- verified via call-counting spy tests wrapping the real (unmocked) ema/macd_components functions (TestSharedIndicatorComputation in test_portfolio_exits.py), not just mocked-return assertions, so a regression back to independent recomputation would be caught.",
      "rationale": "Considered and rejected: (1) reimplementing the shared math directly inside exits.py using the low-level ema()/macd_components() primitives instead of going through protective_stop/autoenvelope/evaluate_impulse/evaluate_tide at all -- rejected because it would duplicate each function's own business logic (protective_stop's swing-low-window/volatility-buffer formula, autoenvelope's rolling-deviation-band formula, evaluate_impulse's GREEN/RED/BLUE decision table, evaluate_tide's BULLISH/BEARISH/NEUTRAL table) inside exits.py, creating a second place each could silently drift from its own module's documented contract -- worse for maintainability than the redundant-computation cost it would save. (2) leaving the redundancy entirely deferred, matching the still-unresolved sibling finding in signal-engine-orchestration-followups -- rejected because this specific followups task's checklist wording ('worth revisiting ... next time exits.py, risk.py, autoenvelope.py, or impulse.py is touched') explicitly names this task's own scope as the right moment, unlike the sibling task where no dedicated follow-up task session was addressing those exact files. (3) a slice-aware LRU-style cache keyed by (id(dataframe), window) instead of explicit parameter-passing -- rejected as needing identity-based cache keys on mutable, unhashable pandas objects (fragile, easy to get wrong across process/test boundaries) for no benefit over the simpler explicit-parameter approach, since evaluate_exit_flags() already has direct, single-call-site control over both the daily and weekly series. Optional keyword-only parameters (not positional, not required) were chosen specifically so every existing caller of protective_stop/autoenvelope/evaluate_impulse/evaluate_tide elsewhere in the codebase (engine.py, their own existing test suites) continues to work completely unchanged -- confirmed by the full 330-test suite passing with no test modifications required in those other call sites."
    },
    {
      "timestamp": "2026-09-15T00:00:01Z",
      "recorded_by": "task-worker (portfolio-exit-rules-followups)",
      "decision": "Resolved the same-day precondition finding via option (a) from the checklist item -- added an explicit 'Precondition' section to evaluate_exit_flags()'s docstring in exits.py stating that daily_ohlcv's latest bar and position.current_price must represent the same trading day, that the function does not validate this, and that 'stop_hit'/'six_percent_rule_contributor' mutual exclusivity only holds when that precondition is met -- rather than adding a runtime check that raises when it's violated.",
      "rationale": "A runtime check was considered and rejected as infeasible with the current data contract, not merely undesirable: neither Position (app/portfolio/models.py -- current_price: float | None, with no associated as-of timestamp/date field) nor the daily_ohlcv DataFrame contract this function and protective_stop/autoenvelope/evaluate_impulse already share (lowercase OHLCV columns only, no date column required or guaranteed -- confirmed against every existing caller/test fixture in the test suite, none of which include a date column) carries a date this function could compare. Adding one would mean extending Position's schema (which is also the pydantic model layer the not-yet-implemented GET/POST /api/portfolio endpoints and their OpenAPI contract will be built on) and/or requiring every daily_ohlcv caller to start carrying a date column it doesn't today -- a materially larger, cross-cutting schema change well beyond this followups task's scope, and one that the still-stubbed api-portfolio-risk task (the only prospective real caller) is better positioned to decide when it actually wires a caller and knows what date information it has available (e.g. from the OHLCV cache's own `date` column in app/db/models.py, which does exist at the DB layer but isn't threaded through to this function's DataFrame contract). Documenting the precondition now, rather than silently leaving the earlier 'mutually exclusive by construction' claim unqualified, means a future reader (including api-portfolio-risk's implementer) is warned rather than misled; option (b) (softening/removing the claim entirely) was not chosen in addition, since the claim is accurate and worth keeping as a statement of the *intended* contract -- it's the missing precondition documentation, not the claim itself, that was the gap. This clarifies, without retracting, the 'mutually exclusive by construction' claim recorded in docs/tasks/portfolio-exit-rules.json's decisions array (the six_percent_rule_contributor decision) by making its same-day precondition explicit in code."
    },
    {
      "timestamp": "2026-09-15T20:10:00Z",
      "recorded_by": "task-worker (portfolio-exit-rules-followups, PR #29 needs_work fix)",
      "decision": "Fixed pr-reviewer's blocking finding on PR #29 (evaluate_exit_flags() raising a bare KeyError('close') instead of the documented ValueError for a daily_ohlcv missing 'close') by extracting protective_stop()'s existing column/empty validation out of risk.py's protective_stop() into a new module-level function risk.validate_daily_ohlcv_columns(daily_ohlcv), then calling that validator as the very first statement in evaluate_exit_flags() -- before daily_close = daily_ohlcv['close'] or any other column access -- while leaving the rest of the function (including the daily/weekly EMA/MACD-sharing this task's earlier decisions entry added) unchanged. protective_stop() itself now just calls the extracted validator instead of inlining the checks, so its own behavior/error messages/tests are unaffected. Added a regression test (test_real_malformed_daily_ohlcv_missing_close_raises_value_error_not_key_error in TestEvaluateExitFlagsEndToEnd, test_portfolio_exits.py) asserting evaluate_exit_flags() raises ValueError (matching 'missing required column') rather than KeyError for a daily_ohlcv missing 'close' -- reproduced the bug against the pre-fix code first (confirmed it raised KeyError('close') as the review predicted) before applying the fix, to be sure the test actually exercises the regression.",
      "rationale": "Considered and rejected: (1) simply reordering the existing code so protective_stop() is called before daily_close/daily_ema_13 are computed, letting protective_stop's own inline validation run first as it did pre-PR -- rejected because protective_stop() needs an already-computed short_ema to share (this task's earlier decisions entry's whole point), creating a circular ordering requirement: sharing needs daily_ema_13 computed before protective_stop is called, but validation-first needs protective_stop's check to run before daily_ohlcv['close'] is touched at all. Preserving both properties simultaneously requires the validation logic to be available independently of computing the EMA, which is exactly what extracting it into a standalone function accomplishes. (2) duplicating the two validation lines (empty check + missing-column check) directly at the top of evaluate_exit_flags() instead of extracting a shared function -- rejected because it creates two independent copies of the same contract that could silently drift (e.g. a future change to protective_stop's required-column set or error message would need to be manually mirrored in exits.py, with nothing enforcing that); a single shared function callable from both call sites removes that drift risk entirely, at the cost of one small, well-documented cross-module function. (3) leaving evaluate_exit_flags() to validate only 'close' (the column it accesses directly) rather than protective_stop's full {'low', 'close'} set -- rejected because evaluate_exit_flags() also always calls protective_stop() further down in the same call (irrespective of which flags end up firing), so a daily_ohlcv missing 'low' would still hit protective_stop's own validation moments later regardless; validating the same full set up front just makes that failure happen at the top of the function instead of a few lines in, with an identical resulting exception either way, and keeps the single validator's contract simple ('the columns protective_stop requires') rather than two different partial-validation entry points needing to be kept in sync."
    },
    {
      "timestamp": "2026-09-15T21:20:00Z",
      "recorded_by": "task-worker (portfolio-exit-rules-followups, PR #29 needs_work re-fix -- weekly-side gap)",
      "decision": "Fixed pr-reviewer's second blocking finding on PR #29 (evaluate_exit_flags() raising a bare KeyError('close') instead of the documented ValueError for a weekly_ohlcv missing 'close') at *both* of the two places identified in the review: (1) added a new module-level validate_weekly_ohlcv_columns(weekly_ohlcv) function in app/signals/triple_screen.py (mirroring risk.py's validate_daily_ohlcv_columns) and called it as the first statement of evaluate_tide() immediately after its existing `len(weekly_ohlcv) < 2` short-circuit -- closing the root unguarded access pr-reviewer traced into the PR #28 baseline, for every caller of evaluate_tide(), not just this PR's own new one; and (2) called the same validate_weekly_ohlcv_columns() in evaluate_exit_flags() itself, immediately before its own direct weekly_ohlcv['close'] access (added by this task's earlier MACD/EMA-sharing decision) and before either evaluate_tide() call -- because that direct access runs unconditionally regardless of weekly_ohlcv's row count, whereas evaluate_tide()'s own new guard only runs once there are already >= 2 rows (preserving evaluate_tide's pre-existing, documented <2-row graceful-NEUTRAL-degradation contract, which must not become a new error for a short but otherwise well-formed weekly_ohlcv). Added regression tests at both layers: test_malformed_weekly_ohlcv_missing_close_raises_value_error_not_key_error (and a companion test_short_weekly_ohlcv_missing_close_does_not_raise, confirming the <2-row path is unaffected) plus a small TestValidateWeeklyOhlcvColumns class in test_triple_screen.py, and test_real_malformed_weekly_ohlcv_missing_close_raises_value_error_not_key_error in test_portfolio_exits.py -- reproduced the bug against pre-fix code first (confirmed KeyError('close')) before applying the fix, same as the daily-side fix's methodology. Full suite: 337/337 passing (331 baseline + 6 new tests across both files), 99.36% coverage, exits.py and triple_screen.py both 100% line/branch.",
      "rationale": "Considered and rejected fixing only one of the two locations. Fixing only evaluate_tide() (the traced root cause) and removing evaluate_exit_flags()'s own guard would still leave evaluate_exit_flags() raising a bare KeyError, because its own direct weekly_ohlcv['close'] access (needed to share the weekly MACD/EMA(13) across both evaluate_tide calls, per this task's earlier decisions entry) runs *before* either evaluate_tide() call and unconditionally with respect to row count -- evaluate_tide()'s new guard, sitting after its own `len < 2` check, would never even be reached in that failure case since exits.py's own access fails first. Fixing only evaluate_exit_flags() (mirroring the daily-side, single-call-site fix exactly) and leaving evaluate_tide() itself unguarded was rejected per the review's own suggestion to consider the root cause directly: engine.py's analyse() also calls evaluate_tide(weekly_ohlcv) directly (a separate call path this task doesn't otherwise touch) with no column validation of its own anywhere in that call chain, so leaving evaluate_tide() itself unguarded would leave that caller (and any future one) exposed to the identical bare-KeyError bug this PR is specifically about closing. Validating in both places is not redundant in the way protective_stop()'s re-validation (the separately-tracked non-blocking finding on portfolio-exit-rules-followups-followups.json, deliberately left untouched this round) is: that finding is two validations of the *same* daily_ohlcv on the *same* call path a few lines apart; here, evaluate_exit_flags()'s guard and evaluate_tide()'s guard sit on genuinely different call paths (exits.py's own pre-evaluate_tide access vs. every evaluate_tide() call site including engine.py's), and evaluate_exit_flags() itself exercises both existing evaluate_tide() calls it makes on an already-validated weekly_ohlcv, at which point evaluate_tide()'s own internal check becomes an actually-redundant-but-cheap second validation on this one call path (accepted, matching the same low-cost-cheap-check tradeoff logic in the untouched, separately-tracked protective_stop finding) in exchange for closing the gap on every other call path. A validator requiring the full weekly OHLCV column set (not just 'close') was considered and rejected: evaluate_tide() itself only ever reads 'close' (Screen 1's MACD-Histogram/EMA relationship needs no high/low/volume), so validating anything beyond 'close' would be checking a contract evaluate_tide() doesn't actually have -- unlike protective_stop's {'low','close'}, which it does genuinely require both of."
    }
  ],
  "git": {
    "branch": "task/portfolio-exit-rules-followups",
    "pr_number": 29,
    "pr_url": "https://github.com/rainoko/fintrade/pull/29",
    "pushed_at": "2026-09-15T19:42:47Z"
  },
  "review": {
    "reviewed_at": "2026-09-15T22:15:00Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/29",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Third-round re-review of commit 69f895c, the fix for the weekly_ohlcv KeyError gap found in the prior (2026-09-15T20:40:00Z) pass. Verified both new validation call sites directly against source: (1) app/signals/triple_screen.py's new validate_weekly_ohlcv_columns() is called as evaluate_tide()'s first statement, but genuinely *after* its pre-existing `if len(weekly_ohlcv) < 2: return TideResult(NEUTRAL, 'flat')` short-circuit -- confirmed by reading the function body (lines 159-162) and by directly reverting the two new validation calls and re-running the suite: the <2-row regression test (test_short_weekly_ohlcv_missing_close_does_not_raise) still passed with the calls removed, proving that path never reaches the new validator at all, so evaluate_tide()'s existing graceful-NEUTRAL contract for short history is genuinely unaffected. (2) app/portfolio/exits.py's evaluate_exit_flags() calls the same validator immediately before its own unconditional weekly_ohlcv['close'] access (which runs regardless of row count, unlike evaluate_tide's internal guard) -- correctly placed after the daily-side logic and before either evaluate_tide() call, closing the gap that made this blocking last round.",
        "file": "backend/app/signals/triple_screen.py",
        "line": 159
      },
      {
        "summary": "Independently confirmed the reproduce-then-fix claim for both new regression tests (test_malformed_weekly_ohlcv_missing_close_raises_value_error_not_key_error in test_triple_screen.py, test_real_malformed_weekly_ohlcv_missing_close_raises_value_error_not_key_error in test_portfolio_exits.py), not just trusted the commit message: temporarily removed both validate_weekly_ohlcv_columns() call sites and re-ran the two new tests plus the <2-row companion test -- both new tests genuinely failed with KeyError: 'close' (confirming they exercise the real pre-fix bug, not a vacuous assertion), the <2-row test still passed (confirming the short-history path is untouched), then restored the fix via git checkout and re-ran the full suite clean (337/337).",
        "file": "backend/tests/unit/signals/test_triple_screen.py",
        "line": 318
      },
      {
        "summary": "Full suite re-run on commit 69f895c (branch HEAD): 337/337 passing (331 baseline + 6 new tests across both files, matching the decisions entry's own count), no regressions. pytest --cov=app --cov-report=term-missing -> 99.36% repo-wide (gate 90%), exits.py and triple_screen.py both 100% line/branch. No ruff/mypy config exists in backend/pyproject.toml, so no lint step was invented (unchanged from prior rounds). Dispatched the code-review skill (high effort) against the fix and waited for its result before posting this verdict (per the prior round's own process note) -- it returned zero findings, independently corroborating: the fix closes the root cause inside evaluate_tide() itself (covering every caller, including engine.py's analyse(), which calls evaluate_tide() directly with no validation of its own), and the one intentional redundancy (evaluate_exit_flags validates, then evaluate_tide's own internal call re-validates the same already-good weekly_ohlcv) is explicitly reasoned about in the code comments and this task's decisions entry as an accepted cheap-check tradeoff, consistent with the precedent from the daily-side fix -- not a new finding. verify-elder-signal re-run against docs/Analyse.md #7: this commit is a pure input-validation fix (no change to the Tide/exit-flag decision logic itself), so no methodology drift; the <2-row graceful-NEUTRAL degradation contract (part of Screen 1's documented behavior) is preserved, as verified above. No API route/schema touched (no backend/app/api/ or backend/openapi.json diff between 58ae122 and 69f895c) -- no OpenAPI snapshot check applies. No new module/dependency/layer -- architecture-review not triggered. No frontend exists yet and no UI surface is touched -- browser walkthrough explicitly not applicable, not silently skipped. Checked PR #29 for human comments/reviews (gh pr view --json comments,reviews): all three reviews and both issue comments are from raino-agent (the shared automation account) -- no human override to honor. The two non-blocking findings already tracked on docs/tasks/portfolio-exit-rules-followups-followups.json (weekly MACD/EMA computed unconditionally on <2-row weekly_ohlcv; protective_stop's duplicate column re-validation) remain correctly out of scope for this pass, per this review's own instructions, and required no new entries since the code-review skill surfaced nothing new this round.",
        "file": "backend/pyproject.toml",
        "line": 0
      }
    ],
    "notes": "Third-round re-review of commit 69f895c (the weekly-side fix for the blocking finding from the 2026-09-15T20:40:00Z pass). gh pr checkout 29 (already checked out on task/portfolio-exit-rules-followups, working tree clean at start, no stash needed). Per this round's explicit instruction, waited for the backgrounded code-review skill (high effort) result before posting any verdict, rather than posting an early accept -- it returned zero findings after the daily/weekly-side manual verification (both validation call placements read directly from source, both new regression tests independently confirmed to reproduce-then-fix by temporarily reverting the fix and re-running, the <2-row NEUTRAL contract confirmed unaffected the same way) and the full suite (337/337) plus coverage (99.36%, gate 90%) were already re-run and clean. verify-elder-signal confirmed no methodology drift (pure validation fix, no Analyse.md #7 logic changed). No API/UI surface touched. No human PR comments to honor. Verdict: accepted. Posting as a COMMENTED-type gh pr review (never approve) with the full findings as the body; task state moves to done."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-15T20:11:00Z",
    "verdict": "merge",
    "notes": "Independently re-ran the full backend suite myself: 337/337 passing, 99.36% coverage (gate 90%), matching pr-reviewer's reported numbers exactly. Read the diff of every changed file (exits.py, risk.py, triple_screen.py, autoenvelope.py, impulse.py) directly, not just the review summary: confirmed validate_daily_ohlcv_columns()/validate_weekly_ohlcv_columns() run before any unguarded ['close'] access in both evaluate_exit_flags() and evaluate_tide(), and confirmed evaluate_tide()'s `len(weekly_ohlcv) < 2` short-circuit still executes before validate_weekly_ohlcv_columns() (source order: len check, then validation, then column access), preserving the graceful-NEUTRAL contract. Hand-verified the EMA causal-slice claim myself with an independent script (ema(close,5).iloc[:-1] == ema(close.iloc[:-1],5) on a 14-row series) rather than trusting the decisions entry. Confirmed protective_stop/autoenvelope/evaluate_impulse/evaluate_tide's new params are all keyword-only with None defaults, and grepped every call site (engine.py, all test files) -- no existing caller was broken; engine.py still calls evaluate_tide(weekly_ohlcv)/evaluate_impulse(daily_ohlcv) positionally, unaffected. gh pr view 29 confirms mergeable=MERGEABLE, mergeStateStatus=CLEAN against current main, and no human (non-automation-account) comments/reviews exist to override. Note: review.reviewed_at (2026-09-15T22:15:00Z) does not match the actual GitHub review timestamp (last accepted review posted 2026-09-15T20:09:12Z per `gh pr view 29 --json reviews`) -- flagging the discrepancy, but it's a bookkeeping inaccuracy only; the review content itself was independently confirmed to genuinely correspond to commit 69f895c (current HEAD) and its claims check out."
  }
}
