{
  "id": "frontend-channel-overlay-followups",
  "title": "Non-blocking follow-up from PR #151 review (frontend-channel-overlay)",
  "area": "frontend/pages/stocks",
  "skill": null,
  "state": "planned",
  "depends_on": ["frontend-channel-overlay"],
  "references": [
    "docs/tasks/frontend-channel-overlay.json",
    "https://github.com/rainoko/fintrade/pull/151"
  ],
  "description": "Tracked non-blocking findings from pr-reviewer's review of PR #151 (frontend-channel-overlay), recorded here per CLAUDE.md's 'every non-blocking finding becomes a tracked task' rule so they aren't lost once that PR merges. Distinct from the PR's own blocking finding (the candlestick-occlusion bug), which stays on frontend-channel-overlay itself and must be fixed before that PR can be accepted.",
  "checklist": [
    {
      "step": "Consider a lightweight visual-regression/screenshot check for the chart panes (PriceChart.tsx/OscillatorChart.tsx). No existing test -- neither the mocked vitest unit tests (which stub addSeries/setData and never render real pixels) nor the persisted Playwright e2e suite (which asserts on DOM/console state, not chart pixel content) -- would have caught the PR #151 review's candlestick-occlusion bug (an opaque AreaSeries painting over real candlestick data). That bug was only found via a manual, one-off browser walkthrough. Worth evaluating whether a cheap screenshot-diff check on a couple of representative fixture tickers/ranges is warranted for this chart-rendering-order class of bug, or whether the cost/flakiness of visual-regression tooling outweighs the benefit here -- a genuine judgment call, not a mandate to add one.",
      "done": false
    },
    {
      "step": "Non-blocking (PR #151 retry-round-1 re-review): PriceChart.tsx's post-fix `series.setSeriesOrder(2)` (the candlestick series' target render index) is a literal magic number, not derived from the actual number of series added before it in the effect. Verified correct today by tracing Lightweight Charts v5.2.1's source (addSeries appends to the pane's data-source list; setSeriesOrder splices/reclamps against that list's length at call time) -- with exactly zoneTopSeries and zoneBottomMaskSeries added before it, index 2 is right. But nothing ties the literal `2` to 'however many series exist at this point in the effect': a future change that adds/removes/reorders a series in this effect before line 411 without also updating this number would silently re-occlude candlesticks (the exact bug this PR fixed), and the existing regression test (PriceChart.test.tsx asserting `setSeriesOrder` was called with `2`) would keep passing since it only checks the literal argument, not the resulting z-order relative to the zone series. Consider deriving the target index at call time instead (e.g. `series.setSeriesOrder(zoneBottomMaskSeries.seriesOrder() + 1)`) so it can't drift out of sync with the series actually added above it.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
