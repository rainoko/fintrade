{
  "id": "backend-ibkr-data-provider-followups",
  "title": "Optional IBKR data provider — non-blocking review follow-ups",
  "area": "backend/data",
  "skill": null,
  "state": "testing",
  "depends_on": ["backend-ibkr-data-provider"],
  "references": [
    "docs/tasks/backend-ibkr-data-provider.json",
    "https://github.com/rainoko/fintrade/pull/190"
  ],
  "description": "Non-blocking findings from pr-reviewer's full review of PR #190 (backend-ibkr-data-provider), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges.",
  "checklist": [
    {
      "step": "app.api.dependencies.get_ibkr_provider() constructs a brand-new IBKRProvider (closed at the end of every request) on every dependency resolution, so IBKRProvider's in-memory scanner-params TTL cache and 1-req/sec run_scanner throttle never persist across requests. This is currently dormant (no route consumes get_ibkr_provider yet) but whoever builds the first consuming endpoint needs to choose a longer-lived instance (e.g. a module-level singleton, or move the cache/throttle state to a shared store) rather than assume the per-request FastAPI dependency already provides IBKR-rate-limit protection.",
      "done": true
    },
    {
      "step": "docs/architecture/Backend.md §3's 'Key Dependencies' table still lists `httpx` with the purpose 'via FastAPI TestClient | API integration tests' only -- it wasn't updated to also reflect that `httpx` is now a main/production runtime dependency (app/data/ibkr_provider.py's `IBKRProvider` performs real HTTP calls with it, not just tests). Update the §3 table row to mention IBKRProvider's production use alongside the existing TestClient-testing purpose.",
      "done": true
    },
    {
      "step": "app.api.dependencies.get_ibkr_provider()'s lazy singleton init (`if _ibkr_provider_singleton is None: _ibkr_provider_singleton = IBKRProvider(...)`) is a check-then-set on a plain module-level global with no lock, and FastAPI runs sync dependency generators in a worker thread pool -- two concurrent first-requests can both observe None and each construct their own IBKRProvider (each with independent scanner-params cache / run_scanner throttle state and its own un-closed httpx.Client), silently defeating exactly the cross-request rate-limit protection this singleton was introduced for. Consider a thread-safe lazy-init instead of hand-rolled double-checked locking -- e.g. an `@lru_cache(maxsize=1)`-wrapped factory, matching `get_settings()`'s own already-thread-safe pattern (backend/app/config.py) rather than reinventing it. Currently dormant (no route consumes get_ibkr_provider yet), found during PR #190's re-review (pr-reviewer, code-review skill sub-agent).",
      "done": true
    },
    {
      "step": "The module-global `_ibkr_provider_singleton` has no autouse test fixture resetting it between tests -- only the two tests that directly exercise get_ibkr_provider() remember to `monkeypatch.setattr(..., None)` first. A future test that reaches this dependency indirectly (e.g. hitting a real route through FastAPI's TestClient once one is added, rather than calling get_ibkr_provider() directly) would mutate the plain module attribute via application code's own `global` assignment -- monkeypatch's teardown only reverts changes made through monkeypatch itself, not a later in-process `global` mutation, so such a test could leak a stale IBKRProvider (bound to a previous test's base_url/mock client) into later tests in the same pytest session, non-deterministically depending on execution order. Consider an autouse fixture in tests/unit/api/test_dependencies.py (or conftest.py) that resets the singleton to None on teardown. Found during PR #190's re-review.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-20T10:44:00Z",
      "recorded_by": "task-worker",
      "decision": "Replaced the `@lru_cache(maxsize=1)`-wrapped `_get_ibkr_provider_singleton()` (PR #191's own earlier fix, which pr-reviewer's needs_work review correctly flagged as not actually thread-safe) with an explicit `threading.Lock`-guarded double-checked-locking construction: a module-level `_ibkr_provider_singleton: IBKRProvider | None` global plus `_ibkr_provider_singleton_lock`, checked outside the lock (fast path once set), then re-checked inside the lock before constructing. Added a dedicated `_reset_ibkr_provider_singleton_for_tests()` helper (also lock-guarded) replacing the old `_get_ibkr_provider_singleton.cache_clear()` calls in tests/unit/api/test_dependencies.py's autouse fixture. Added a new regression test, `test_concurrent_first_requests_construct_only_one_singleton`, that races 32 threads (synchronized via a `threading.Barrier` so they call the getter as close to simultaneously as possible) against a cold singleton via `concurrent.futures.ThreadPoolExecutor` and asserts every thread got back the exact same (`is`-identical) instance.",
      "rationale": "This is exactly the fix pr-reviewer's needs_work comment asked for. Verified the new test actually catches the regression it's meant to catch (not just a test that happens to pass either way): temporarily reverted the getter to a lock-free check-then-set with an injected `time.sleep(0.01)` to widen the race window (simulating the old broken lru_cache-era behavior), confirmed the new concurrency test failed deterministically against that broken version (non-identical instances returned across threads), then restored the fixed lock-based implementation and confirmed the test passes reliably (10 consecutive runs, no flakes). A hand-rolled spin-lock or `queue`-based approach was considered and rejected as unnecessary complexity over the standard double-checked-locking idiom with `threading.Lock`, which is the textbook fix for exactly this race and was also pr-reviewer's own explicit suggestion."
    },
    {
      "timestamp": "2026-09-20T10:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Both non-blocking checklist items were fixed directly on PR #190's branch (task/backend-ibkr-data-provider) while addressing that PR's needs_work blocking findings, rather than through a separate branch/PR of this task's own. state is deliberately left as 'planned' rather than flipped to 'done' here.",
      "rationale": "pr-reviewer's original review comment explicitly invited fixing these 'if convenient since you're already in this area' as part of resolving the two blocking findings on the same branch -- doing so avoids a near-duplicate PR re-touching the same two files (app/api/dependencies.py, docs/architecture/Backend.md) moments later. state is left as 'planned' (not flipped to 'done') because moving a task to done -- including the git mv into docs/tasks/done/ -- is documented as pr-reviewer's/pr-merger's call once a PR is actually reviewed and merged, not task-worker's to make unilaterally; pr-reviewer re-reviewing PR #190 will see both checklist items already checked and the actual diff (get_ibkr_provider now a process-wide singleton; Backend.md §3's httpx row updated) and can move this task to done in that same pass once PR #190 merges."
    },
    {
      "timestamp": "2026-09-20T10:20:00Z",
      "recorded_by": "task-worker",
      "decision": "Replaced app.api.dependencies's plain-module-global `_ibkr_provider_singleton` + hand-rolled `if ... is None:` check-then-set with a new private `@lru_cache(maxsize=1)`-wrapped factory function `_get_ibkr_provider_singleton()` that `get_ibkr_provider()` now calls instead of touching a global directly. Updated tests/unit/api/test_dependencies.py's two tests that previously did `monkeypatch.setattr(\"app.api.dependencies._ibkr_provider_singleton\", None)` to no longer need that (the module global no longer exists), and added a new `_reset_ibkr_provider_singleton` autouse pytest fixture in that same file that calls `_get_ibkr_provider_singleton.cache_clear()` before and after every test in the module.",
      "rationale": "This is exactly the fix the checklist item itself suggested (an lru_cache-wrapped factory matching get_settings()'s already-thread-safe pattern in app/config.py), so no alternative was seriously considered -- a hand-rolled threading.Lock double-checked-locking implementation was rejected as reinventing what functools.lru_cache already provides for a zero-argument factory. The autouse fixture placement (test_dependencies.py itself, not a shared conftest.py) mirrors this module's existing pattern of ad hoc get_settings().cache_clear() calls scattered through its own tests rather than centralizing them, since no other test module in the suite currently touches get_ibkr_provider/the IBKR singleton -- if a future test elsewhere in the suite needs the same reset (e.g. an integration test hitting a route that consumes get_ibkr_provider), it should move to a shared conftest.py fixture at that point rather than pre-emptively generalizing here for a need that doesn't exist yet. Verified: full backend test suite (890 tests), ruff, and mypy all pass on the changed files; the pre-existing ruff/mypy findings elsewhere in the backend tree (tests/unit/indicators/test_stochastic.py, tests/unit/signals/test_triple_screen.py, several tests/integration/*.py untyped-def/arg-type findings) were confirmed present on main before this change and are unrelated to this task's scope."
    }
  ],
  "git": {
    "branch": "task/backend-ibkr-data-provider-followups",
    "pr_number": 191,
    "pr_url": "https://github.com/rainoko/fintrade/pull/191",
    "pushed_at": "2026-09-20T10:33:53Z"
  },
  "review": {
    "reviewed_at": "2026-09-20T10:40:53Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/191",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "`_get_ibkr_provider_singleton`'s docstring (app/api/dependencies.py:53) claims `@lru_cache(maxsize=1)` 'gives thread-safe lazy-init for free' -- this is not accurate for what matters here. CPython's lru_cache only holds its internal lock around the cache dict/linked-list bookkeeping; it releases the lock before calling the wrapped function and re-acquires it after. On a cold cache, two threads that both observe a miss each independently execute the factory body (each constructing its own IBKRProvider/httpx.Client/throttle state) and each thread's call returns its own locally-computed instance, not necessarily the one that ends up cached -- reproduced empirically against the real functools.lru_cache (20 concurrent threads on a cold cache -> 20 factory-body executions, 20 distinct instances returned to callers), and independently confirmed by the code-review skill sub-agent. This is the same class of race PR #190's re-review opened this followup task to close; the fix narrows the window (the cache self-heals to one stable instance once the initial cold-cache burst resolves) but does not eliminate it, and the docstring overstates the guarantee. get_settings() gets away with the identical pattern only because a duplicated Settings object during that race window is harmless (no meaningful per-instance mutable state); IBKRProvider has exactly the per-instance state (HTTP client, scanner-params cache, run_scanner throttle) this task exists to protect. Needs an explicit threading.Lock-guarded double-checked-locking construction (or, at minimum, a corrected docstring that accurately describes a bounded/self-healing race window rather than full thread safety) before accept.",
        "file": "backend/app/api/dependencies.py",
        "line": 53
      },
      {
        "summary": "No new/updated test actually exercises concurrent access to _get_ibkr_provider_singleton -- test_enabled_reuses_the_same_singleton_across_calls calls get_ibkr_provider() twice sequentially on one thread, which would also pass under the pre-PR hand-rolled singleton and would not catch the race above. A concurrent regression test (two threads racing _get_ibkr_provider_singleton() on a cold cache, asserting `is` identity) is recommended once the lock-based fix lands.",
        "file": "backend/tests/unit/api/test_dependencies.py",
        "line": 131
      }
    ],
    "notes": "Verified items (2) get_settings() precedent match and (3) fixture reset correctness/scope from the review ask both check out as described in the PR's decisions -- no other test file or route currently reaches get_ibkr_provider, so the module-scoped (not conftest.py) autouse fixture has no current leak risk. Full backend suite (890 passed, 99.93% coverage), ruff, and mypy all pass on the changed files; frontend suite (672 passed, ~99.7% coverage) unaffected since no frontend files are touched. The one blocking item is (1): lru_cache is not actually race-free for this purpose, contrary to the docstring's claim -- see review.comments."
  }
}
