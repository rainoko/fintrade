{
  "id": "backend-ibkr-tickle-keepalive-followups-followups",
  "title": "Non-blocking finding from backend-ibkr-tickle-keepalive-followups's review (PR #268)",
  "area": "backend/data",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-ibkr-tickle-keepalive-followups"],
  "references": [
    "docs/tasks/backend-ibkr-tickle-keepalive-followups.json",
    "https://github.com/rainoko/fintrade/pull/268"
  ],
  "description": "Non-blocking finding from pr-reviewer's code-review pass on PR #268 (backend-ibkr-tickle-keepalive-followups), tracked here per CLAUDE.md's 'every non-blocking finding becomes a tracked task' rule so it isn't lost once that PR merges and its review comment scrolls out of view. The PR itself has no blocking findings -- full backend suite passes (1172 passed, 100% line/branch coverage), ruff/mypy clean on touched files, the asyncio cancellation claim was independently verified against CPython's asyncio.to_thread/Future.cancel() semantics and reproduced locally, the broadened except Exception matches an established repo-wide convention (stooq_provider.py, yfinance_provider.py), no live IBKR calls are made (FINTRADE_IBKR_ENABLED stays false), and the task has no UI surface (backend-only asyncio/docs change).",
  "checklist": [
    {
      "step": "backend/app/main.py's lifespan() finally block comment (around the `await tickle_task` line) hardcodes the referenced task file's path as `docs/tasks/done/backend-ibkr-tickle-keepalive-followups.json`, but at the time this comment was written and committed, that task's state was `testing` and its actual location (per docs/tasks/index.json's path field) was `docs/tasks/backend-ibkr-tickle-keepalive-followups.json` -- not yet under done/. This is the inverse of the anti-pattern CLAUDE.md's task-board section warns against (assuming a path from an id/state rather than resolving it via index.json) -- here assuming a not-yet-true done/ location in a committed code comment. It happens to become accurate once this task is accepted and moved to done/ by pr-reviewer, but the pattern of hardcoding an assumed-future path in source comments is fragile (would go stale again if the task were ever reopened/moved back). Consider rephrasing such comments to reference the task by id only (letting index.json resolve the actual path) rather than a literal path guess.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
