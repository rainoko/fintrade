{
  "id": "backend-ibkr-tickle-keepalive-followups",
  "title": "Non-blocking follow-ups from backend-ibkr-tickle-keepalive's PR #267 review",
  "area": "backend/data",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-ibkr-tickle-keepalive"],
  "references": [
    "docs/tasks/backend-ibkr-tickle-keepalive.json",
    "https://github.com/rainoko/fintrade/pull/267"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #267 (backend-ibkr-tickle-keepalive) -- recorded here per CLAUDE.md's 'every non-blocking finding becomes a tracked task' rule so they aren't lost once that PR merges and its review comment scrolls out of view.",
  "checklist": [
    { "step": "docs/architecture/Backend.md §8 step 4 still reads 'not automated by this app today -- a future task ... would need to add that, itself a currently-open gap'. PR #267 is that future task and closed the gap; update that sentence to reflect the keep-alive is now automated via app.main._ibkr_tickle_loop.", "done": false },
    { "step": "Document/test the shutdown-cancellation edge case where lifespan's tickle_task is cancelled while mid-await asyncio.to_thread(provider.tickle) (an HTTP request already dispatched to the worker thread): Task.cancel() can't interrupt an already-running thread-pool work item, so `await tickle_task` in lifespan's finally will block until that in-flight call completes or its httpx client's ~10s timeout elapses before CancelledError actually propagates. Bounded (no leak/crash) but currently untested -- the existing cancellation test only covers cancellation while the task is sleeping.", "done": false },
    { "step": "Consider widening _ibkr_tickle_loop's except clause from IBKRUnavailableError to a broader Exception (still logging, still not raising) so an unexpected/future exception type from tickle() can't silently stop the while True loop until shutdown surfaces it via await tickle_task.", "done": false }
  ],
  "decisions": [],
  "questions": []
}
