{
  "id": "backend-trade-history-table-followups",
  "title": "Trade-history/ledger table + realized-loss-aware 6% Rule -- non-blocking review follow-ups",
  "area": "backend/db",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-trade-history-table"],
  "references": [
    "docs/tasks/backend-trade-history-table.json#review",
    "https://github.com/rainoko/fintrade/pull/148",
    "docs/Analyse.md#6-rule",
    "docs/architecture/API.md"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #148 (backend-trade-history-table), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges.",
  "checklist": [
    {
      "step": "New tests in backend/tests/integration/test_portfolio_delete_position.py (test_records_closed_trade_with_default_unspecified_exit_reason, line ~168) and backend/tests/integration/test_portfolio_risk.py (TestRealizedLossesThisMonth, lines ~609/657/695) assert against local `date.today()`-derived fixtures/expectations, but the production code that sets/queries the exact same dates (`app.api.routers.portfolio._today()`) is explicitly `datetime.now(UTC).date()`-based, matching every other 'now' helper in this codebase (app.data.cache._utcnow, app.api.routers.watchlist._utcnow). On a machine/CI runner whose local timezone isn't UTC, any run occurring in the local-date-vs-UTC-date skew window (e.g. local UTC-8, any time between local midnight and UTC midnight) makes `trade.exit_date` (UTC-derived) differ from the test's local `date.today()`, failing the exit_date assertion intermittently -- and near a calendar-month boundary, the `this_month`/`last_day_of_prior_month` fixture construction in test_portfolio_risk.py could put a ClosedTradeORM row in the wrong bucket relative to what `_realized_losses_this_month_pct`'s UTC `as_of` actually uses. Currently harmless (dev container and CI both run in UTC per `/etc/timezone`), but the tests should build their expectations from `datetime.now(UTC).date()` (or freeze time) rather than local `date.today()`, for the same reason every other UTC-'now' helper's own tests already do.",
      "done": false
    },
    {
      "step": "frontend/src/features/portfolio/components/RiskSummaryCard.tsx and RiskPanel.tsx's 'Total Open Risk' StatCard label now silently displays a combined open-risk + this-month's-realized-losses figure (per this task's own decision to widen total_open_risk_pct's meaning rather than add a new field name) with no copy change -- deliberately deferred per this task's own `decisions` entry (scoped backend-only; real UI work better paired with the future trade-journal frontend task where the two components can be shown separately). Tracked here so the deferred copy/breakdown work isn't forgotten: a user who took realized losses earlier this month but holds only low-risk open positions currently sees a misleadingly-labeled 'Total Open Risk' figure that could lead them to trim healthy open positions instead of addressing the real (already-realized) cause.",
      "done": false
    },
    {
      "step": "frontend/src/pages/DashboardPage.test.tsx (lines ~69, ~94) and frontend/src/pages/PortfolioPage.test.tsx (line ~58) build RiskResponse MSW fixtures that were not updated to include the new required `realized_losses_this_month_pct` field, unlike every other RiskResponse fixture this PR touched (RiskPanel.test.tsx, RiskSummaryCard.test.tsx, SellFlaggedPositionsCard.test.tsx, frontend/tests/mocks/handlers.ts). Currently harmless -- `HttpResponse.json()` isn't type-checked against `RiskResponse` here and no component under test reads the new field yet -- but it's an inconsistency within this PR's own diff, and the gap would surface as a silent `undefined` (not a compiler/test failure) the moment a future change makes a component read `realized_losses_this_month_pct` (e.g. the label/breakdown follow-up above). Add the field to both fixtures for consistency and future-safety.",
      "done": false
    }
  ]
}
