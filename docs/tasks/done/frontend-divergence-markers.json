{
  "id": "frontend-divergence-markers",
  "title": "Mark bullish/bearish divergences on price and oscillator charts",
  "area": "frontend/pages/stocks",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": [
    "backend-divergence-detection"
  ],
  "references": [
    "frontend/src/features/stocks/components/PriceChart.tsx",
    "frontend/src/features/stocks/components/OscillatorChart.tsx",
    "frontend/src/components/common/InfoBalloon",
    "docs/ideas.md"
  ],
  "description": "Elder calls divergences 'some of the most powerful signals in technical analysis' -- once detected (backend-divergence-detection), make them visually unmistakable: connect the two compared extremes with a line/marker on the relevant chart (price for the swing points, the oscillator panel for MACD-H/Stochastic/RSI), distinct from the existing BUY/SELL transition markers already on PriceChart.tsx. Clicking a divergence marker should open an InfoBalloon explaining which two extremes are being compared, why this counts as a valid divergence (the centerline-crossing rule for MACD-H, or the reference-line-crossing rule for Stochastic/RSI), and what it implies. Per the project's established explanatory pattern (common/InfoBalloon, common/MetricHelp, metricHelpContent.ts, signalExplanation.ts, docs/tasks/frontend-stock-detail-metric-help.json), every new visual element this task adds must be explainable, not just displayed: a help affordance (reuse MetricHelp where it fits) that states what the metric/signal is, how it relates to Dr. Elder's methodology, and, where meaningful, an explanation of the CURRENT value shown -- not a static definition alone. Add an entry to metricHelpContent.ts's registry rather than inventing a parallel explanation mechanism.",
  "checklist": [
    {
      "step": "Design the visual treatment (connecting line/marker between the two compared extremes) distinct from the existing BUY/SELL markers, on both the relevant price and oscillator charts.",
      "done": true
    },
    {
      "step": "Wire click-to-explain via InfoBalloon, reusing the existing pattern from frontend-signal-why-explanation (signalExplanation.ts's balloon-trigger approach) rather than inventing a new interaction model.",
      "done": true
    },
    {
      "step": "Explanation content must name the actual two dates/values being compared for THIS ticker, not a generic divergence definition.",
      "done": true
    },
    {
      "step": "Tests covering a ticker with a known divergence in its history; Storybook story if any new common/ piece was introduced; confirm 90% coverage gate holds.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-19T17:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Only the single currently-qualifying divergence (`AnalysisResponse.divergence`, a singular field) is drawn on both charts -- not a reconstructed history of every past divergence from `GET /api/stocks/{ticker}/indicators`' per-bar `divergence` field.",
      "rationale": "The per-bar field repeats the same divergence across every bar it's still the most-recently-confirmed one for (per backend-divergence-detection's own no-look-ahead design), so turning it into distinct historical markers would need an unbuilt de-duplication pass with no textual basis in docs/ideas.md for how far apart two repeats must be to count as 'the same' vs 'a new' divergence. The checklist's own phrasing ('which two extremes are being compared... for THIS ticker') also reads as singular/current, matching `AnalysisResponse.divergence`'s own shape and every other latest-bar-only field this app already displays this way (support_resistance_zones is the one plural exception, and it's drawn from a genuinely list-shaped backend field, unlike divergence). Rejected reconstructing history as materially more complex for a benefit the checklist doesn't actually ask for."
    },
    {
      "timestamp": "2026-09-19T17:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Divergence markers use a new `theme.palette.divergence.main` color (teal, #00897b) and a `circle` marker shape on both extremes, connected by a dashed `LineSeries` -- one color for both bullish and bearish (distinguished by marker text/legend content instead of a second color), rather than reusing `signal.buy`/`signal.sell` (green/red) the way a 'bullish'/'bearish' label might suggest.",
      "rationale": "The task explicitly requires the treatment be 'distinct from the existing BUY/SELL transition markers' -- reusing green/red for bullish/bearish divergence would read as a second BUY/SELL signal layered on the same chart, exactly what the task says to avoid, especially since a divergence is explicitly NOT wired into the signal engine (backend-divergence-detection's own decisions entry: 'detection + exposure only'). `circle` (vs `arrowUp`/`arrowDown` for BUY/SELL) and a distinct teal color/dashed-line style make the two marker families visually unmistakable from each other at a glance, which was the actual point."
    },
    {
      "timestamp": "2026-09-19T17:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Click-to-explain is implemented via `chart.subscribeClick` (Lightweight Charts) checking the clicked bar's date against the divergence's two extreme dates, opening a new `AnchoredInfoBalloon` export from `common/InfoBalloon.tsx` -- a coordinate-anchored variant of the existing `InfoBalloon` (same title+content Popover chrome, shared via an internal `BalloonContent` helper), rather than trying to reuse `InfoBalloon`'s own child-wrapping trigger API. A `divergenceHelp` `common/MetricHelp` legend row (same content) is ALSO added on both charts as a second, always-reachable way to open the identical explanation.",
      "rationale": "`InfoBalloon`'s existing API assumes a real DOM trigger element (a `ButtonBase` wraps `children` and owns the open/close state via its own `onClick`) -- a Lightweight Charts series marker is drawn on a `<canvas>` with no DOM node of its own to attach that to; the library's own click event only reports page coordinates (`chart.subscribeClick`'s `MouseEventParams.sourceEvent.pageX/pageY`), not a DOM element. Extending `InfoBalloon` itself with a second, externally-controlled open mode (rather than building a separate one-off popover for this feature) keeps the visual/behavioral chrome shared and consistent with the rest of the app's explanatory pattern, per this task's own instruction to reuse the InfoBalloon pattern rather than inventing a new interaction model. The legend row is additional, not a replacement: a canvas marker is a small, imprecise click target (confirmed empirically during manual Playwright verification -- see below), so an always-reachable MetricHelp affordance with the identical content is a meaningful, low-cost addition consistent with every other chart overlay this app already ships (channel, value zone, support/resistance zones, false breakout, RSI), not scope creep."
    },
    {
      "timestamp": "2026-09-19T17:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Divergence explanation content lives in a new `divergenceHelp` entry in `features/stocks/components/metricHelpContent.ts`, following the exact `metricLabel`/`definition`/`elderContext`/`interpretValue(...)` shape every other entry in that registry already uses -- `interpretValue` takes the actual `DivergenceOut` value and names its real dates/prices/indicator values, the centerline-crossing (MACD-H) or reference-line (Stochastic/RSI) rule that actually applied, the Lovvorn bar-spacing, and a Hound-of-the-Baskervilles callout when `aborted` is true.",
      "rationale": "Directly required by the task description ('Add an entry to metricHelpContent.ts's registry rather than inventing a parallel explanation mechanism') and verified end-to-end against a real backend response (see below) that the rendered text genuinely names this ticker's own extreme dates/prices, not a generic definition."
    },
    {
      "timestamp": "2026-09-19T17:10:00Z",
      "recorded_by": "task-worker",
      "decision": "`AnchoredInfoBalloon`'s Storybook stories live in a new, separate `AnchoredInfoBalloon.stories.tsx` file (not appended to `InfoBalloon.stories.tsx`'s existing default export).",
      "rationale": "Storybook's CSF format supports exactly one `component`/`meta` default export per file; `AnchoredInfoBalloon` and `InfoBalloon` are different components with different prop shapes (controlled-by-coordinates vs. wraps-a-child-trigger), so giving each its own file/meta is the correct CSF structure, not a style preference -- the alternative (stories with mismatched `args` typed against the wrong component) would either not type-check or silently mislabel the story's own component in Storybook's UI."
    },
    {
      "timestamp": "2026-09-19T17:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Manually verified the whole feature against a real running backend + frontend (not just mocked component tests) before considering this done: started the backend with `FINTRADE_DATA_PROVIDER_MODE=fixture` (the same deterministic, no-network provider the e2e suite uses), confirmed AAPL's `/analysis` response has a real, non-null bearish MACD-Histogram divergence (`aborted: true`, i.e. a live Hound-of-the-Baskervilles case), then used a throwaway Playwright script to: (1) screenshot the price and oscillator charts showing the teal connecting line + circle markers labeled 'Bearish divergence', visually distinct from the BUY/SELL arrows; (2) open the legend's MetricHelp balloon and confirm its text names the real 2025-12-11/2026-01-16 dates, real prices/indicator values, the centerline-crossing rule, and the Hound-of-the-Baskervilles callout; (3) dispatch a native click event at the exact canvas pixel corresponding to the second extreme's own bar and confirm `AnchoredInfoBalloon` opens with the identical content, anchored near the click. All scratch scripts/servers were discarded afterward; no test-suite files depend on this manual pass.",
      "rationale": "Directly required by this task's dispatch instructions ('Do your own visual verification... at a range/ticker where a divergence is actually detected'). Also surfaced that Playwright's high-level `page.mouse.click()` doesn't reliably reach Lightweight Charts' internal canvas pointer-event listeners in this headless environment (a native `dispatchEvent` with explicit `pointerdown`/`mousedown`/`pointerup`/`mouseup`/`click` was needed instead) -- noted here only as a manual-verification-tooling quirk, not a product bug, since the actual click-handling logic (`clickedDivergenceExtreme`, the pageX/pageY extraction, `AnchoredInfoBalloon`'s open/close) is already directly covered by PriceChart.test.tsx/OscillatorChart.test.tsx's mocked-`chart.subscribeClick` tests, which invoke the exact same handler function the real chart calls."
    },
    {
      "timestamp": "2026-09-19T17:44:00Z",
      "recorded_by": "task-worker",
      "decision": "Post-review fix (PR #158, blocking finding): added `divergenceClick.ts#isDivergenceInRange(divergence, firstDate, lastDate)` -- true only when BOTH of the divergence's own extreme dates fall within the currently visible bar/point range -- and gated the divergence overlay's connecting line, markers, and `chart.subscribeClick` subscription on it in both `PriceChart.tsx` (against `historyQuery.data.bars` filtered by `hasFiniteOhlc`) and `OscillatorChart.tsx` (against `indicatorsQuery.data.points`). When out of range, nothing is drawn on the chart at all for that render -- no partial/clipped line -- following the exact windowing pattern PR #152 already established for `selectDisplayedZones`/`buildFalseBreakoutMarkers` for the same failure class (an out-of-range time-anchored series point stretches Lightweight Charts' time scale to cover the gap, squashing the actual candlestick/oscillator content).",
      "rationale": "Directly required by the review's blocking finding. Verified live via a throwaway Playwright script against a fixture backend (AAPL's real bearish MACD-Histogram divergence, 2025-12-11/2026-01-16): at the 1M preset (which excludes both dates) no divergence line/markers are drawn and no console errors occur; at 1Y and Max (both include the full date range) the divergence draws correctly with a sane axis. Confirms the previously-reported 'entire chart collapses into an unreadable sliver' failure no longer reproduces at 1M."
    },
    {
      "timestamp": "2026-09-19T17:44:00Z",
      "recorded_by": "task-worker",
      "decision": "When the divergence is out of the currently visible range, its legend row (both charts) STAYS visible and keeps naming the real divergence -- rather than hiding the row the way `supportResistanceZoneHelp`'s zone legend hides once `displayedZones` empties (PR #152's precedent) -- with `divergenceHelp.interpretValue`'s new `inVisibleRange` parameter (default `true`, so every pre-existing caller is unaffected) appending an explicit clause naming the divergence's own dates and explaining it isn't drawn at the current range selection (\"Switch to a wider range (e.g. 1Y or Max) to see it plotted.\"). The legend's dot is also dimmed (opacity 0.4) and its caption gets a '(not in current range)' suffix as a lighter-weight visual cue.",
      "rationale": "This task's own review comment explicitly flagged this as a real product/UX judgment call and asked for a decision, citing `selectDisplayedZones`'s legend as precedent for the 'hide' option. Chose 'show but note out-of-range' over hiding because the two cases aren't actually analogous: a zone that fails `selectDisplayedZones`'s relevance filter/cap is PERMANENTLY excluded for that ticker regardless of range (a irrelevant zone never becomes relevant by picking a different date window), so hiding its legend row never contradicts anything the user could see by changing range -- whereas `divergence` is a single, real, currently-qualifying signal (Elder: 'some of the most powerful signals in technical analysis') whose out-of-range-ness is purely a temporary consequence of the user's OWN range selection; hiding it entirely would read as 'no divergence exists for this ticker' rather than the true 'one exists, just switch ranges to see it', which is materially misleading for what this task's own description calls a signal that should be 'visually unmistakable'. The balloon (`AnchoredInfoBalloon`, opened only by clicking an actually-drawn marker) is unaffected in practice since a marker only exists when in range."
    }
  ],
  "git": {
    "branch": "task/frontend-divergence-markers",
    "pr_number": 158,
    "pr_url": "https://github.com/rainoko/fintrade/pull/158",
    "pushed_at": "2026-09-19T17:46:00Z"
  },
  "review": {
    "reviewed_at": "2026-09-19T17:54:57Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/158",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Round 1 blocking findings (axis/time-scale distortion when the divergence overlay's connecting line/markers were drawn unconditionally, regardless of the currently selected range, on both PriceChart.tsx and OscillatorChart.tsx) are fixed. `isDivergenceInRange` (divergenceClick.ts) windows the overlay's line/markers/subscribeClick subscription to the currently visible bar/point range on both charts, matching PR #152's selectDisplayedZones/buildFalseBreakoutMarkers precedent. Re-verified live at 1M (AAPL's real bearish MACD-Histogram divergence, 2025-12-11/2026-01-16, excluded): no line/markers drawn on either chart, axis stays sane on both the candlestick chart and the MACD-Histogram pane, legend shows 'Divergence (not in current range)' dimmed with an accurate explanation naming the real dates. Re-verified at 1Y and Max: overlay draws correctly on both charts with a sane axis, click-to-explain (AnchoredInfoBalloon) opens with correct ticker-specific content anchored near the click, z-ordering/occlusion still correct (candlesticks stay on top). New unit/component tests (divergenceClick.test.ts, metricHelpContent.test.ts, PriceChart.test.tsx, OscillatorChart.test.tsx) directly exercise both the in-range and out-of-range cases, including asserting subscribeClick is not called and no divergence marker/line is added when out of range.",
        "file": "frontend/src/features/stocks/components/divergenceClick.ts",
        "line": 36
      },
      {
        "summary": "The 'legend stays visible but dimmed/annotated when out of range, rather than hidden' decision (differing from supportResistanceZoneHelp's zone-legend 'hide' precedent) is sound: a zone failing selectDisplayedZones' relevance filter is a permanent exclusion for that ticker regardless of range, whereas divergence is a single, real, currently-qualifying signal whose out-of-range-ness is purely a temporary consequence of the user's own range selection -- hiding it would misleadingly read as 'no divergence exists'. Verified the rendered out-of-range legend text is accurate (names the real dates, explains why nothing is drawn, suggests switching to 1Y/Max) and matches the in-range balloon's own content plus the added clause.",
        "file": "frontend/src/features/stocks/components/metricHelpContent.ts",
        "line": 583
      },
      {
        "summary": "Non-blocking, already tracked (frontend-divergence-markers-followups, unchanged by this round): OscillatorChart.tsx's divergence overlay still lives in the single chart-creation useEffect (full teardown/rebuild on divergence resolving late) and the bullish/bearish label/position derivation is still duplicated between PriceChart.tsx and OscillatorChart.tsx rather than factored into divergenceClick.ts. Neither was in scope for this fix and neither regressed.",
        "file": "frontend/src/features/stocks/components/OscillatorChart.tsx",
        "line": 421
      }
    ],
    "notes": "Re-review (retry round 1) of PR #158's post-review fix commit (8395242) on top of the round-1-reviewed base (325e2d4). Full suite re-run: backend (699 tests, 99.91% cov, ruff/mypy clean -- confirmed 0 backend files touched by this PR, git diff main...HEAD -- backend/ is empty) + frontend (473 tests, 1 flaky PortfolioPage.test.tsx timeout under full-suite load reconfirmed as pre-existing/unrelated -- passes alone; 99.68%/98.89%/99.62%/99.78% stmt/branch/fn/line coverage, all above the 90% gate) tests/coverage, eslint/tsc clean, `make e2e` (all 16 persisted specs passed, including stock-analysis.spec.ts's Max-range regression spec), `code-review` skill run against the fix's own diff (b82350a..HEAD, medium effort) -- zero findings, confirmed the `isDivergenceInRange` gating is applied consistently across both charts' draw effects and legend flags, and that `divergenceHelp.interpretValue`'s new `inVisibleRange` parameter defaults to `true` so no pre-existing caller's behavior changed. code-review's own review also surfaced (but explicitly declined to file, as pre-existing/unrelated to this diff) a question of whether the AnchoredInfoBalloon force-closes when the range changes out from under an open balloon -- manually verified live: MUI Popover's own invisible full-viewport backdrop intercepts the *first* click anywhere else on the page (including the range toggle buttons) and closes the balloon on that click alone, so the balloon can never actually go stale in practice -- this is identical, standard behavior to every other InfoBalloon/MetricHelp popover already in the app, not a bug this PR introduced or left open; not filed as a new finding. No new frontend components were added in this fix round (only existing files: divergenceClick.ts, metricHelpContent.ts, PriceChart.tsx, OscillatorChart.tsx and their tests), so no new common/-vs-feature placement assessment was needed beyond round 1's (AnchoredInfoBalloon in components/common/InfoBalloon/, already confirmed correct). No openapi.json regeneration needed (no backend/schema changes in either round). Real-browser walkthrough performed via a throwaway node script against frontend/node_modules/playwright-core (no MCP browser tool available in this session) across 1M/1Y/Max against a fixture backend serving AAPL's real bearish MACD-Histogram divergence: 1M shows no overlay + dimmed 'not in current range' legend on both charts with a sane axis on the candlestick chart and the MACD-Histogram pane; 1Y and Max both show the overlay correctly with a sane axis, and a dispatched native click on the price-chart marker opened AnchoredInfoBalloon with correct ticker-specific content. Zero console errors and zero failed/4xx/5xx network requests across the whole walkthrough. The pre-existing, out-of-scope residual vertical compression from support/resistance zone bands at 1M (task-worker's own noted observation, an already-accepted PR #152 tradeoff) was visually reconfirmed present but not meaningfully worse than before -- not treated as a new finding per this review's own dispatch instructions. Both round-1 non-blocking findings remain correctly tracked on frontend-divergence-markers-followups (unchanged, still `planned`); no new non-blocking findings from this round warranted a followups addition. Posted as a COMMENTED-type PR review (not approve/request-changes)."
  }
}
