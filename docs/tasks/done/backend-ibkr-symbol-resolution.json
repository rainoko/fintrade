{
  "id": "backend-ibkr-symbol-resolution",
  "title": "Resolve a ticker symbol to an IBKR conid",
  "area": "backend/data",
  "skill": null,
  "state": "done",
  "depends_on": [],
  "references": [
    "backend/app/data/ibkr_provider.py",
    "docs/tasks/done/backend-ibkr-data-provider.json",
    "docs/ideas.md#ibkr-client-portal-gateway-is-now-live-and-authenticated--concrete-next-steps"
  ],
  "description": "get_hourly_bars/run_scanner both key off IBKR's own numeric conid, not a ticker symbol -- there's no /iserver/secdef/search (or /trsrv/stocks) wrapper anywhere, and backend-ibkr-data-provider's own decisions entry explicitly deferred researching it ('/iserver/secdef/search wasn't researched as part of that task'). Any real consumer taking a ticker like this app's other endpoints do needs this solved first.",
  "checklist": [
    { "step": "Research /iserver/secdef/search's actual documented request/response shape (mocked in tests, per this module's existing no-live-gateway-in-CI testing constraint) and record findings as a decisions entry.", "done": true },
    { "step": "Implement IBKRProvider.resolve_conid(ticker) -> int | None, degrading gracefully (not raising) on ambiguous or no results -- decide and record how ambiguous matches (multiple conids for one symbol, e.g. across exchanges) are resolved.", "done": true },
    { "step": "Unit tests mocking the endpoint for found, ambiguous, and not-found cases.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Implemented IBKRProvider.resolve_conid(ticker) against /iserver/secdef/search's documented shape (IBKR's Client Portal Web API reference): GET /iserver/secdef/search?symbol=<ticker> returns a JSON array, one entry per candidate contract, each shaped roughly `{\"conid\": \"265598\", \"companyHeader\": \"APPLE INC - NASDAQ\", \"companyName\": \"APPLE INC\", \"symbol\": \"AAPL\", \"sections\": [{\"secType\": \"STK\"}, {\"secType\": \"OPT\", ...}, ...]}` -- conid is returned as a numeric string, symbol matching can be fuzzy/partial (the endpoint is designed for a type-ahead search box, not an exact lookup), and a single top-level entry's `sections` list enumerates every asset class tradeable on that underlying (the stock itself plus any options/warrants/futures), not one entry per asset class.",
      "rationale": "This matches this module's existing no-live-gateway constraint (backend-ibkr-data-provider's own decisions entry: implemented directly against IBKR's documented Web API shapes, every test mocks the _request boundary, live-gateway behavior deferred to manual verification by the user with a real running gateway per FINTRADE_IBKR_ENABLED staying false throughout this task). Not independently re-verified against a live gateway in this session -- same accepted limitation as the rest of this module, recorded rather than silently assumed away."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "resolve_conid filters /iserver/secdef/search's results to entries whose symbol matches the requested ticker exactly (case-insensitive) AND whose sections list includes a \"STK\" entry (excluding pure options/warrants/futures matches and the endpoint's fuzzy partial-symbol matches). If that filtering leaves exactly one DISTINCT conid, it's returned; if it leaves zero or more than one distinct conid, resolve_conid returns None in both cases rather than raising or guessing which candidate was meant (two rows resolving to the same conid, e.g. duplicate entries, are deduplicated first and are NOT treated as ambiguous).",
      "rationale": "The checklist's own phrasing groups 'ambiguous' and 'no results' together as both needing to 'degrade gracefully (not raising)', which reads as an instruction to make both terminal cases produce the same None outcome rather than have resolve_conid pick a 'best guess' first candidate among several real, distinct conids for the same symbol. Considered picking the first result returned by the search endpoint (plausible if IBKR ranks primary listings first) but rejected it: that ranking behavior isn't documented, only observed informally, and silently resolving a genuinely ambiguous ticker to the wrong contract (e.g. the wrong exchange's listing) is a much worse failure mode for a Screen-3-hourly-bars or scanner consumer than surfacing 'could not resolve automatically, needs a manual conid' -- especially since this can't be verified against a live gateway in this session to confirm the ranking assumption holds. A future consumer that wants a 'best effort, pick something' mode can add that as an explicit opt-in parameter later; the safer default is what's implemented here."
    }
  ],
  "git": {
    "branch": "task/backend-ibkr-symbol-resolution",
    "pr_number": 263,
    "pr_url": "https://github.com/rainoko/fintrade/pull/263",
    "pushed_at": "2026-09-22T12:42:44Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T12:47:28Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/263",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "resolve_conid returns int | None but get_hourly_bars takes conid: str -- confirmed with a standalone mypy check that get_hourly_bars(resolve_conid(ticker), ...) fails type-checking today. Not blocking this PR (resolve_conid isn't wired into any consumer yet, so nothing exercises the composition), but the very next task that wires ticker-driven calls through resolve_conid will hit this immediately. Tracked as a follow-up.",
        "file": "backend/app/data/ibkr_provider.py",
        "line": 334
      }
    ],
    "notes": "Full review: gh pr checkout 263 (branch already checked out). Backend suite: `pytest --cov=app --cov-branch` -- 1157 passed, 99.95% total coverage (app/data/ibkr_provider.py at 100%), well above the 90% gate, no regression. Static analysis: mypy clean (60 source files); ruff flags 7 pre-existing issues, all in test files this PR doesn't touch (test_stooq_provider.py, test_autoenvelope.py, test_elder_ray.py, test_force_index.py, test_rsi.py, test_stochastic.py, test_triple_screen.py) -- confirmed via `git diff main...HEAD --name-only` that none of those files are part of this diff, so not attributable to this PR. Confirmed FINTRADE_IBKR_ENABLED=false in backend/.env. Confirmed zero live network calls in the new tests: every TestResolveConid case mocks IBKRProvider._request directly, and TestRequest (the one class that touches the httpx.Client boundary) mocks that Client via mocker.Mock(), never a real socket. Checked internal consistency of the documented /iserver/secdef/search shape (this task's decisions entry) against the rest of the module: _resolve_stk_conid reuses the existing _int_or_none helper the same way _parse_scanner_results does, and the string-vs-int conid representation difference between the two endpoints is handled uniformly by that shared helper regardless -- no internal inconsistency. Ambiguous-match policy (degrade to None for both no-match and >1 distinct conid, never guess) is sound and matches this class's existing error-handling convention: IBKRUnavailableError is reserved for genuine gateway/transport failure (_require_available, _request), never for an ordinary 'couldn't determine a unique answer' business outcome -- get_scanner_params and run_scanner follow the same never-raise-for-business-outcomes / raise-for-gateway-failure split. Both checklist items phrased as 'decide X' (the documented request/response shape; how ambiguous matches are resolved) have corresponding decisions entries with rationale and rejected alternatives -- no gap. docs/architecture/Backend.md was updated to document the new endpoint; no API route/schema changed, so no backend/openapi.json regeneration was needed (confirmed via `git diff main...HEAD --name-only` -- no files under backend/app/api/ touched). area is backend/data (not backend/signals or backend/portfolio) and skill is null, so verify-elder-signal wasn't required; no new module/dependency/layer was added (one new method + one new private helper on an existing class), so architecture-review wasn't required either. No UI surface: this PR touches only backend/app/data/ibkr_provider.py, its unit tests, and docs -- no frontend change and no backend route with a frontend consumer -- so the browser walkthrough (step 9) and the persisted e2e suite (step 9a) do not apply; stated explicitly rather than silently skipped. One non-blocking finding (conid type mismatch between resolve_conid's int return and get_hourly_bars's str parameter -- verified with a standalone mypy check) filed as a followup task since resolve_conid isn't wired into any consumer in this PR."
  }
}
