{
  "id": "frontend-close-position-dialog",
  "title": "Real Close-Position dialog (exit reason + optional manual exit override)",
  "area": "frontend/pages/portfolio",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": [
    "backend-close-position-manual-exit"
  ],
  "references": [
    "frontend/src/features/portfolio/components/PositionsTable.tsx",
    "frontend/src/features/portfolio/components/AddPositionDialog.tsx",
    "docs/ideas.md#no-manual-sell--close-position-form--only-a-bare-confirm-delete-dialog"
  ],
  "description": "PositionsTable.tsx's close flow is currently a bare ConfirmDialog with zero user-editable fields, wired to useDeletePosition, whose mutation signature is just (id: string). 'Buy' already has a real form (AddPositionDialog); 'sell' has no equivalent. Build a ClosePositionDialog mirroring AddPositionDialog's shape: an exit_reason selector (the backend endpoint already accepts this param, it's just never been surfaced) plus the optional manual exit price/date override once backend-close-position-manual-exit lands.",
  "checklist": [
    {
      "step": "Build ClosePositionDialog with an exit_reason <select> (matching the ExitReason taxonomy already defined) replacing PositionsTable's plain ConfirmDialog for closing a position.",
      "done": true
    },
    {
      "step": "Add the optional manual exit price/date override fields, defaulting to unset (today's live-price behavior unchanged).",
      "done": true
    },
    {
      "step": "Update useDeletePosition's mutation signature to pass the new parameters (exit_reason, and the optional override fields).",
      "done": true
    },
    {
      "step": "Component tests covering a default close (no override, just exit_reason) and an override close (manual exit price/date supplied).",
      "done": true
    },
    {
      "step": "Decide placement (components/common vs. feature-specific) per Frontend.md's placement guidance, and add a Storybook story if it belongs in common/.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "ClosePositionDialog lives under features/portfolio/components/, not components/common/ -- no Storybook story added.",
      "rationale": "It renders PositionOut/ExitReason directly, both portfolio domain concepts (Frontend.md's placement test: 'anything that references portfolio/stock domain concepts belongs under features/<domain>/components/'), and it is wired to a real mutation (useDeletePosition) rather than taking only generic presentational props. This mirrors the placement AddPositionDialog/FollowUpReviewDialog/TradeApgarDialog already made for themselves -- all four are portfolio-domain form dialogs, none live in common/. Considered treating it as domain-agnostic like common/ConfirmDialog (which it replaces) since its shape -- title, body, Cancel/Confirm -- looks generic at a glance; rejected because unlike ConfirmDialog it owns real form fields tied to ExitReason/PositionOut.entry_date, not just a caller-supplied title/body string, so it can't be reused for an unrelated confirm without dragging portfolio-specific validation along."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "useDeletePosition() stays instantiated in PositionsTable (as before), not inside ClosePositionDialog itself -- isPending/isError/error/onConfirm/onCancel are passed into the dialog as props instead.",
      "rationale": "PositionsTable's own doc comment documents a pre-existing double-click-race guard: the row's own Delete icon button is disabled while `deletePosition.isPending` for that row's id specifically, so a second click can't reopen the dialog and fire a second DELETE for the same position while the first is still in flight (mirroring WatchlistTable's own Remove-button guard). AddPositionDialog/FollowUpReviewDialog/TradeApgarDialog each own their mutation internally, but none of them have an equivalent 'also disable a sibling trigger button outside the dialog' requirement -- adopting that same self-contained-mutation convention here would mean two independent useDeletePosition() instances (one in the dialog, one implicitly needed for the row button's disabled state), which don't share pending/variables state with each other. Lifting the single mutation instance up to PositionsTable and threading its state down as props was the only way to keep both the dialog's own pending/error UI and the row's pending-disable guard driven by the same underlying mutation. Rejected: giving ClosePositionDialog its own useDeletePosition() call and dropping the row-level guard entirely -- rejected because MUI's Dialog only blocks interaction with the underlying page while it's open (see the next decision for why it can, in practice, still be dismissed early while pending), so the guard is not actually fully redundant. See the next decision for the specific bug this surfaced (reset() called on cancel while still pending) and its fix."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "ClosePositionDialog only closes on a successful close (mirroring AddPositionDialog/FollowUpReviewDialog's own convention of staying open with an inline ErrorState on failure), not synchronously on every Confirm click like the old bare common/ConfirmDialog did. PositionsTable's onCancel handler (fired by the dialog's Cancel button, a backdrop click, or Escape) only calls the mutation's own `reset()` when `deletePosition.isPending` is false at that moment.",
      "rationale": "Staying open on error lets the user see exactly what went wrong and retry/cancel from the same dialog instead of the error appearing after the dialog has already vanished -- better UX, and consistent with every other mutation-backed dialog in this feature. This surfaced a real bug during testing: ClosePositionDialog's own Cancel button is disabled while `isPending` (so it can't be clicked mid-flight), but a backdrop click still fires the Dialog's `onClose` regardless of pending state (matching every other dialog in this app -- none of them gate backdrop-click/Escape dismissal on pending either). The first implementation unconditionally called `deletePosition.reset()` in that handler to clear a stale error from a previous failed close before a different row's dialog reopens; doing that unconditionally also flips `isPending` back to `false` immediately even though the DELETE request is still genuinely running in the background, which defeated the row icon's own double-click-race guard (confirmed by a failing test: after a backdrop-click cancel mid-flight, the row's Delete button was no longer disabled even though the earlier request hadn't resolved yet). The fix -- skip `reset()` while still pending -- keeps `isPending` accurate for the guard's purpose (a real in-flight request is still a real in-flight request even if the dialog itself has been dismissed) while still clearing stale errors for the common case (cancelling once nothing is in flight). Escape was tried first as the more natural way to exercise/describe this path in a test, but MUI's own Button moves focus off itself the instant `loading` disables it, which stops the keydown from reaching the Modal's listener at all in this environment -- backdrop click has no such focus dependency and was used instead, both in the dialog's own behavior and in the regression test."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The manual exit price/date override is a checkbox ('Backfill a historical exit price/date instead of today's live price') that reveals two additional fields (Exit Price, Exit Date) only once checked; unchecked (the default) sends neither field, preserving today's live-price behavior byte-for-byte. Client-side validation (exit price positive/finite, exit date not before the position's own entry_date) only runs while the checkbox is checked, mirroring the backend's own validation for the same two cases.",
      "rationale": "Matches the checklist's own 'defaulting to unset' requirement literally -- an always-visible pair of optional text fields would invite a half-filled state (e.g. a price with no date) that then has to be silently ignored or specially validated, whereas a single checkbox makes 'am I overriding or not' an explicit, unambiguous choice mirroring the backend's own both-or-neither contract. Considered a bare two-field form with no checkbox, treating both as simply optional the way AddPositionDialog treats its own 'Notes' field; rejected because exit price/date aren't independently optional the way notes are -- they're a mutually-dependent pair (backend 422s on exactly one being supplied), and a checkbox makes that pairing legible in the UI itself rather than only in a validation error message a user has to trigger to discover."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "api/portfolio.ts's deletePosition() forwards a supplied exitPrice/exitDate independently onto the query string (each present iff it was supplied), rather than only including either when both are present together.",
      "rationale": "ClosePositionDialog's own client-side validation is what actually enforces 'both or neither' before onConfirm/the mutation is ever called, so this api-layer function forwarding a caller's mismatched single value straight through (rather than silently coalescing it into 'as if neither was given') means a caller that does get it wrong anyway -- a bug in some future consumer, or a direct unit test of the api function itself -- sees the backend's own real 422 for exactly that case, not a request that silently looks identical to the ordinary no-override default and masks the mistake. Rejected: requiring both together at this layer too (as the mutation-variables type's own shape doesn't statically prevent one being supplied without the other) -- redundant with ClosePositionDialog's validation and would need its own separate error surface for what's already a well-documented backend 422."
    },
    {
      "timestamp": "2026-09-22T11:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed PR #261's blocking review finding by scoping which position a close-attempt error belongs to, rather than passing `deletePosition.error` into ClosePositionDialog unconditionally. PositionsTable now derives `belongsToOpenDialog` (`pendingDelete !== null && deletePosition.variables?.id === pendingDelete.id`): while true, the error renders inside ClosePositionDialog exactly as before (unchanged for the common case the reviewer explicitly verified as correct); while false -- dialog dismissed mid-flight, never reopened, or reopened for a *different* position -- the same error instead renders as a page-level banner above the table (`Failed to close <ticker>:` + common/ErrorState), with the ticker looked up from `deletePosition.variables?.id` against the live `positions` list (falling back to 'a position' if that position is no longer present).",
      "rationale": "This combines both options the review comment offered rather than picking only one: (b) the dialog itself can now never show a stale error misattributed to an unrelated later-opened dialog, since the id check gates it; and (a) a failure that settles with no dialog open (or a different one open) is still surfaced somewhere unambiguous instead of vanishing, addressing the review's other reported case directly. The page-level banner mirrors WatchlistTable's own existing convention (an always-visible `removeWatchlistItem.isError && <ErrorState .../>` block above its table) rather than inventing a new failure-surfacing pattern for this table specifically -- the only addition beyond that precedent is the ticker-attribution line, needed here because (unlike WatchlistTable's single always-on banner) this table's error can belong to a position other than whichever the user is currently looking at. Considered doing only (b) (scope-and-suppress, per the review's second option) and leaving no-dialog-reopened failures silently invisible -- rejected because a background failure a user never learns about (e.g. they cancelled and moved on to a different task, not just a different position) is a worse outcome than a small, clearly-labeled banner, and CLAUDE.md's testing conventions already established (WatchlistTable) that a page-level banner is this codebase's answer to 'a delete mutation can fail after its own confirm dialog is gone.' Considered removing the in-dialog ErrorState entirely and only ever using the banner -- rejected because the reviewer explicitly verified the in-dialog-error-while-still-open behavior as correct UX (retry/cancel from the same dialog) and no part of the finding asked for that to change. Verified with a new regression test reproducing the review's exact repro (cancel AAPL via backdrop while pending -> let the delayed DELETE resolve as 404 -> open ZZZZ's dialog) asserting the ZZZZ dialog shows no alert while the AAPL-attributed banner persists, plus a second test covering the ticker-lookup fallback when the failed position is no longer in the list; both pass, and PositionsTable.tsx now has 100% line/branch coverage (up from the pre-fix 96.66% branch figure)."
    }
  ],
  "git": {
    "branch": "task/frontend-close-position-dialog",
    "pr_number": 261,
    "pr_url": "https://github.com/rainoko/fintrade/pull/261",
    "pushed_at": "2026-09-22T10:53:31Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T11:37:04Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/261",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Round 2 (this review): the round-1 misattribution/invisibility bug is genuinely fixed. Reproduced round 1's exact repro in a real Chromium browser against both the pre-fix commit (2ae1bf9, checked out in a scratch worktree) and the fixed HEAD (31cd62f): pre-fix showed AAPL's stale 'Position not found' error rendered inside MSFT's freshly-opened dialog with no page-level surfacing at all when no dialog was reopened; fixed HEAD shows a correctly-attributed 'Failed to close AAPL' banner and a clean MSFT dialog with no misattributed error.",
        "file": "frontend/src/features/portfolio/components/PositionsTable.tsx",
        "line": 98
      },
      {
        "summary": "Round 2: confirmed the double-click-race guard from round 1 is genuinely untouched -- git show 31cd62f only touches the new belongsToOpenDialog/banner logic and doc comments; handleCancelClose, handleConfirmClose, and the row IconButton's disabled condition are byte-for-byte unchanged. Verified live in the browser too: AAPL's own Delete button stayed genuinely disabled for the duration of a held-pending request after a backdrop-dismiss, and clicking it fired no second dialog/DELETE.",
        "file": "frontend/src/features/portfolio/components/PositionsTable.tsx",
        "line": 197
      },
      {
        "summary": "Non-blocking: a code-review pass raised a theoretical concern that PositionsTable's single shared useDeletePosition() instance could let a second, different position's confirmed close orphan the first's still-in-flight error entirely (TanStack Query's mutation observer detaches from the previous mutation on a new mutate() call). Verified empirically this is not reachable through the UI -- ClosePositionDialog's Confirm button receives isPending unscoped from the shared mutation, so any open dialog's Confirm button is disabled table-wide while any close is in flight, preventing the second mutate() call that would be needed to trigger it. This safety is incidental (not a documented/tested invariant), so a future per-row isPending refactor could silently reintroduce it. Tracked in frontend-close-position-dialog-followups.",
        "file": "frontend/src/features/portfolio/components/PositionsTable.tsx",
        "line": 267
      }
    ],
    "notes": "Round 2 verification: backend 1144 passed/99.95% coverage, frontend 787/788 passed (1 pre-existing AddPositionDialog.test.tsx timeout flake under full-coverage-run load, reproduces only under load, unrelated to this PR)/99.75%-99.81% coverage across statements/branches/functions/lines; PositionsTable.tsx itself at 100%. Static analysis (ruff, mypy, eslint, tsc -b --noEmit, profit-target wording guard) all clean. make e2e: 21/21 specs pass across all 6 spec files. No backend/schema changes, so no openapi.json regen needed. The round-2 decisions entry is well-reasoned. Accepting; one new non-blocking finding (the incidental-not-documented overlap-prevention mechanism) filed on frontend-close-position-dialog-followups alongside the two pre-existing ones from round 1."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-22T11:46:35Z",
    "verdict": "merge",
    "notes": "Independently re-ran backend suite (1144 passed, 99.95% coverage) and full frontend coverage suite (788/788 passed on one run; a second run reproduced the exact same pre-existing AddPositionDialog.test.tsx timeout flake under load pr-reviewer described, 787/788, unrelated to this PR), plus PositionsTable.tsx isolated at 100% line/branch/function coverage. Confirmed via `git diff 2ae1bf9..31cd62f` that round 2 touches only PositionsTable.tsx/PositionsTable.test.tsx (plus task JSON) and leaves handleCancelClose/handleConfirmClose/the row IconButton's disabled condition byte-for-byte untouched, matching the round-1 double-click-guard claim. Read the new regression test (PositionsTable.test.tsx:303-343) and confirmed it genuinely reproduces the round-1 misattribution repro (backdrop-cancel AAPL mid-flight, let its DELETE 404 in the background, then open ZZZZ's dialog) and asserts no stray error appears inside ZZZZ's dialog while the AAPL-attributed banner persists. Also skimmed the full PR diff (api/portfolio.ts, useDeletePosition.ts, ClosePositionDialog.tsx) and confirmed no human PR comments exist to override the accept. validate_tasks.py reports no board violations. Accept holds."
  }
}
