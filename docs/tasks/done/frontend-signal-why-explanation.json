{
  "id": "frontend-signal-why-explanation",
  "title": "Stock detail: clicking the signal (BUY/HOLD/SELL) opens an explanation of why",
  "area": "frontend/pages/stocks",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": [],
  "references": [
    "frontend/src/features/stocks/components/SignalSummary.tsx",
    "backend/app/signals/engine.py",
    "docs/Analyse.md#5-signal-logic-buy--sell--hold",
    "docs/tasks/frontend-stock-detail-metric-help.json"
  ],
  "description": "User request: on the stock detail page, clicking the signal (BUY/HOLD/SELL) itself must open a balloon (popover) or modal explaining WHY that specific signal resulted for this ticker right now -- not a generic definition of what BUY/HOLD/SELL means (that's frontend-stock-detail-metric-help's job for individual metrics), but the actual causal chain: which of the four conditions (Tide direction, Impulse gate, Wave pullback/rally, Trigger fired) were met or not met, using this ticker's real current screen values, per docs/Analyse.md §5's BUY/SELL/HOLD rules (_determine_signal in backend/app/signals/engine.py is the source of truth for the exact logic to explain). Decide balloon (Popover/rich Tooltip anchored to the signal) vs modal (Dialog) based on how much explanatory text is actually needed -- record as a decisions entry; may reuse whatever shared help/popover component frontend-stock-detail-metric-help builds if that has landed by the time this is worked, or build its own self-contained one if not (these are independent tasks, no hard ordering between them).\n\nIMPORTANT design constraint to resolve as part of this task: GET /api/stocks/{ticker}/analysis's screens.wave.state only reflects TODAY's evaluate_wave() result, but _determine_signal actually uses a 5-trading-day lookback (wave_showed_pullback/wave_showed_rally in backend/app/signals/engine.py's _wave_lookback) -- so a signal can be correctly explained by 'Wave showed an oversold pullback 3 days ago' even though today's screens.wave.state is NO_WAVE, and the current API response has no way to distinguish that case from 'the wave condition was never met.' Decide how to handle this: (a) extend the API response to also expose the wave_showed_pullback/wave_showed_rally booleans (or equivalent) so the explanation can be fully accurate -- likely requiring a small backend follow-up task, since this task's own skill is add-frontend-feature not add-api-endpoint -- or (b) build the explanation using only currently-exposed fields with an explicit, honest caveat when the wave condition's true state is ambiguous from what's available. Record the choice and rationale as a decisions entry; if (a), file the backend change as its own task (following the api-watchlist/api-stocks-indicator-history precedent of a dedicated backend task, not folded into a frontend-skill task) rather than attempting a backend schema change under this task's own skill.",
  "checklist": [
    { "step": "Resolve the wave-lookback data-availability question above (extend the API vs. an honest frontend-only approximation) and record as a decisions entry; file a dedicated backend task if the API needs to change.", "done": true },
    { "step": "Decide balloon/Popover vs modal/Dialog based on the actual explanatory content length, and whether to reuse a shared help component from frontend-stock-detail-metric-help if it already exists; record as a decisions entry.", "done": true },
    { "step": "Implement the click-to-explain trigger on the signal display in SignalSummary.tsx, opening the chosen balloon/modal.", "done": true },
    { "step": "Write the explanation logic covering every BUY/SELL/HOLD case per docs/Analyse.md §5 -- for HOLD specifically, name which of the four conditions is actually missing/blocking for THIS ticker's current values, not a generic 'conditions not met' message (mirror the kind of per-condition breakdown already given conversationally in this session's own chat history for reference on the expected level of detail).", "done": true },
    { "step": "Tests covering a representative BUY, SELL, and at least two different HOLD cases (e.g. Neutral tide vs. a directional tide missing just the wave/trigger condition) to confirm the explanation text is actually condition-specific, not templated the same for every HOLD.", "done": true },
    { "step": "Confirm 90% coverage gate still holds, Storybook build succeeds if a new common/ component was added, and run make e2e to confirm nothing broke on the stock detail page.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-18T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Chose option (b) for the Wave-lookback data-availability gap: build the explanation from currently-exposed fields (screens.tide.trend, screens.impulse, screens.wave.state, screens.trigger.fired/reference), with an explicit, honest 'can't be confirmed from today's data alone' caveat for the one HOLD configuration where the Wave condition's true 5-day-lookback state is genuinely undeterminable from what the API exposes today -- rather than attempting option (a) (extending WaveScreen with wave_showed_pullback/wave_showed_rally) under this task's own add-frontend-feature skill. Filed the (a) extension as its own dedicated backend task, docs/tasks/api-stocks-analysis-wave-lookback.json (add-api-endpoint skill, state planned, no depends_on since it's an independent enhancement), following the api-watchlist/api-stocks-indicator-history precedent of a dedicated backend task for a schema change rather than folding it into a frontend-skill task.",
      "rationale": "Tracing _determine_signal's four-gate logic (backend/app/signals/engine.py) shows the ambiguity is narrower than it first looks: (1) for a fresh BUY/SELL, the signal's own existence already proves wave_showed_pullback/_rally was true at some point in the lookback -- no ambiguity, just uncertainty about *which* day, worded honestly ('within the last few trading days') rather than falsely claiming 'today'. (2) For a HOLD where Tide/Impulse/Trigger all otherwise satisfy a would-be directional trade today, Wave must be the reason by elimination (if it had also qualified, _determine_signal would have returned BUY/SELL instead) -- fully determinable without any new field. (3) Only the remaining HOLD configurations (Tide Neutral, or a directional Tide where Impulse/Trigger already independently block) are genuinely ambiguous from today's snapshot alone, and for exactly those cases signalExplanation.ts renders met: null with an explicit caveat naming the other confirmed blocker(s) rather than guessing Wave's true state. This closes most of the practical gap today without a backend change, while the filed follow-up task remains available to close the remainder (the genuinely ambiguous case) later without blocking this feature's landing now.",
      "alternatives_considered": "Option (a) implemented directly under this task despite the add-frontend-feature skill (rejected: out of this skill's scope, and CLAUDE.md's task-board precedent explicitly calls for a dedicated backend task for a schema change rather than folding it in). Not filing the backend follow-up at all and leaving the ambiguous case permanently un-closeable (rejected: the gap is real and worth tracking, not silently dropped). Presenting today's screens.wave.state as if it were authoritative for the lookback in the ambiguous case, omitting the caveat (rejected: would produce a confidently-worded but sometimes-false explanation, exactly the kind of methodology-fidelity bug verify-elder-signal/architecture-review would flag)."
    },
    {
      "timestamp": "2026-09-18T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Chose a balloon (MUI Popover, anchored to the clicked signal badge) over a modal (Dialog), and built a new self-contained common/InfoBalloon component rather than reusing anything from frontend-stock-detail-metric-help.",
      "rationale": "Every explanation this task renders is bounded: one headline sentence plus exactly four condition lines (Tide/Impulse/Wave/Trigger), each itself one short sentence -- content that reads fine inline near the signal it's explaining and doesn't need the viewport-taking focus a Dialog implies (which docs/architecture/Frontend.md's existing ConfirmDialog reserves for actual yes/no decisions, not read-only explanations). Checked frontend-stock-detail-metric-help (docs/tasks/frontend-stock-detail-metric-help.json) before starting: still state 'planned', no common/MetricHelp (or equivalent) component exists anywhere under frontend/src/components/common/ -- confirmed via a repo-wide search -- so there is nothing to reuse yet, per that task's own explicitly-independent, no-hard-ordering relationship with this one. Built components/common/InfoBalloon (generic trigger + Popover, no domain knowledge) plus features/stocks/components/signalExplanation.ts + SignalExplanationContent.tsx (the Elder Triple Screen domain logic/rendering) as two separate pieces along the same common/-vs-feature-specific line every other component in this codebase follows (e.g. SignalBadge vs ScreensPanel) -- InfoBalloon is intentionally generic enough that a later frontend-stock-detail-metric-help pass could reuse it for its own metric popovers instead of building a second, near-duplicate 'click a trigger, get a Popover' component.",
      "alternatives_considered": "A modal/Dialog (rejected: content length doesn't warrant full-viewport focus, and a Popover keeps the explanation visually anchored to the signal it explains). A plain MUI Tooltip instead of a Popover (rejected: Tooltip's hover/focus-only trigger model doesn't suit a tap/click-to-open, dismiss-on-click-away interaction as well as Popover, and MUI's own docs steer 'rich, interactive content' toward Popover). Waiting for frontend-stock-detail-metric-help to land first and building nothing until then (rejected: the two tasks are explicitly independent with no ordering requirement, and blocking on an unstarted sibling task would stall this one for no documented reason)."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/frontend-signal-why-explanation",
    "pr_number": 125,
    "pr_url": "https://github.com/rainoko/fintrade/pull/125",
    "pushed_at": "2026-09-18T23:14:46Z"
  },
  "review": {
    "reviewed_at": "2026-09-18T23:23:41Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/125",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Worked through signalExplanation.ts's case analysis by hand against backend/app/signals/engine.py's _determine_signal/_wave_lookback for every reachable HOLD sub-case (Neutral tide; directional tide with Impulse blocking, both when Wave already shows/showed today and when it doesn't; directional tide where Wave never matched, provable by elimination since Tide/Impulse/Trigger otherwise satisfy the setup; directional tide with Trigger not fired). Every case's met/false/null value is correct and matches the backend's real boolean logic; the one flagged 'genuinely ambiguous' Wave case (HOLD, Wave not shown today, and at least one of Impulse/Trigger is already an independent confirmed blocker) is in fact the only case where Wave's true 5-day state can't be determined from what GET /api/stocks/{ticker}/analysis exposes today -- confirmed by tracing the AND-gate structure of _determine_signal itself, not just trusting the task's own decisions-entry claim. No conflation found between the four distinct ways a HOLD can arise.",
        "file": "frontend/src/features/stocks/components/signalExplanation.ts",
        "line": null
      },
      {
        "summary": "Browser walkthrough (Playwright driven directly against a manually-started backend fixture-mode + Vite dev server, since no chromium-cli/Playwright MCP tool was available in this session): clicked the signal badge on /stocks/AAPL (a real ticker, Neutral-tide HOLD case), confirmed the balloon opens anchored below the badge with text that matches the ticker's actual screens.* values exactly (Tide Neutral, Impulse BLUE 'not evaluated', Wave/Trigger 'not evaluated'), with no console errors and no >=400 network responses. Also verified keyboard-open (focus + Enter), Escape-to-close, and click-away-to-close all work. make e2e: all 13 specs passed, including this PR's new stock-analysis.spec.ts assertions for the click-to-open balloon and Escape-to-close.",
        "file": null,
        "line": null
      },
      {
        "summary": "docs/tasks/api-stocks-analysis-wave-lookback.json (the filed backend follow-up for the wave-lookback data gap) is well-formed, validated by scripts/validate_tasks.py, correctly filed as its own separate task file (not folded into this task's own commit inappropriately), and correctly added to docs/tasks/index.json in the same PR.",
        "file": "docs/tasks/api-stocks-analysis-wave-lookback.json",
        "line": null
      },
      {
        "summary": "Non-blocking: signalExplanation.ts's ambiguous-Wave HOLD detail text has a subject-verb agreement bug when both the Impulse gate and Trigger are already-confirmed blockers (otherBlockers.join(' and ') produces a plural subject but the fixed singular verb 'isn't met' still follows -- e.g. 'the Impulse gate and Trigger also isn't met'). Reachable and unexercised by any existing test. Filed as a checklist item on frontend-signal-why-explanation-followups.",
        "file": "frontend/src/features/stocks/components/signalExplanation.ts",
        "line": null
      },
      {
        "summary": "Non-blocking: the Trigger 'not fired' detail can render a self-contradictory 'reference: Not applicable' alongside a directional close-above/below claim, reachable when evaluate_trigger returns reference='not_applicable' due to fewer than 2 daily bars even though the weekly-derived tide is already directional (a rare edge case, but reproducible directly, and matches the exact fixture SignalExplanationContent.test.tsx's ambiguousWaveScreens already uses without asserting on this text). Filed as a checklist item on frontend-signal-why-explanation-followups.",
        "file": "frontend/src/features/stocks/components/signalExplanation.ts",
        "line": null
      },
      {
        "summary": "Non-blocking: no test exercises the reachable combination where today's Wave state already shows the qualifying pullback/rally (waveMetToday true) but the overall signal is still HOLD because Impulse or Trigger blocks it -- numerically covered by branch coverage via the BUY/SELL cases sharing the same code path, but not semantically asserted for this specific HOLD combination. Filed as a checklist item on frontend-signal-why-explanation-followups.",
        "file": "frontend/src/features/stocks/components/signalExplanation.test.ts",
        "line": null
      }
    ],
    "notes": "Full review: backend pytest --cov=app --cov-fail-under=90 (573 passed, 99.86% coverage), frontend vitest run --coverage (286 passed, 47 files, 99.64%/99.3%/100%/99.63% stmt/branch/func/line) -- both well above the 90% gate, no regression. Static analysis clean: ruff (app/), mypy (39 source files), eslint, tsc -b --noEmit. yarn build-storybook succeeds (new InfoBalloon.stories.tsx). scripts/validate_tasks.py: no violations. code-review skill (medium effort) found the two non-blocking wording bugs listed above and nothing else. No backend route/schema changed by this PR itself (api-stocks-analysis-wave-lookback is a separate, not-yet-implemented follow-up task), so no openapi.json staleness question applies. common/InfoBalloon is correctly placed (generic trigger+Popover, no domain knowledge, only presentational props) vs. signalExplanation.ts/SignalExplanationContent.tsx correctly placed under features/stocks (Elder Triple Screen domain logic) -- no placement violation either direction. Both checklist items phrased as 'decide X' (wave-lookback gap, balloon vs modal) have corresponding decisions entries with real rationale and rejected alternatives, not bare 'changed X' records. Working tree was clean at review start and end; no stray uncommitted changes from another agent were found."
  }
}
