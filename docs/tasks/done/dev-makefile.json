{
  "id": "dev-makefile",
  "title": "Root Makefile to run backend and frontend dev servers",
  "area": "infra",
  "skill": null,
  "state": "done",
  "depends_on": [],
  "references": [
    "README.md",
    "docs/architecture/Backend.md",
    "docs/architecture/Frontend.md",
    ".devcontainer/post-create.sh"
  ],
  "description": "Add a root-level Makefile with targets to start the backend (FastAPI/uvicorn) and frontend (Vite) dev servers, individually and together, so contributors don't need to remember each service's own run command. Backend runs `backend/.venv/bin/uvicorn app.main:app --reload` from the `backend/` directory (the venv is provisioned once by `.devcontainer/post-create.sh` inside the dev container); frontend runs `npm run dev` from the `frontend/` directory. Keep this a thin wrapper over existing tooling — no new build system, no reimplementing what `npm`/`uvicorn` already do.",
  "checklist": [
    { "step": "Add a `make backend` target that runs `backend/.venv/bin/uvicorn app.main:app --reload` from `backend/`, with a clear error message (not a raw path-not-found failure) if `backend/.venv` doesn't exist yet, pointing at `.devcontainer/post-create.sh` / `pip install -e \".[dev]\"`.", "done": true },
    { "step": "Add a `make frontend` target that runs `npm run dev` from `frontend/`.", "done": true },
    { "step": "Add a combined target (e.g. `make dev`) that runs both concurrently and cleans up both processes on Ctrl-C/exit (e.g. background jobs plus a `trap ... EXIT` and `wait`) rather than leaving an orphaned process behind.", "done": true },
    { "step": "Decide whether to include adjacent convenience targets (e.g. `make install`, `make test`) or keep the Makefile scoped strictly to running the two dev servers as originally requested — record the call as a `decisions` entry either way.", "done": true },
    { "step": "Document the new `make` targets in README.md's existing run/setup instructions.", "done": true },
    { "step": "Verify each target actually works: start it, confirm the backend answers on its port and the frontend dev server serves the app, then stop cleanly with no leftover process.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-17T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Added `make install` and `make test` as thin wrapper convenience targets alongside `backend`/`frontend`/`dev`, plus a self-documenting `make help` (also the `.DEFAULT_GOAL`). Did not add a `make lint` target.",
      "rationale": "`install` and `test` are exactly the two examples the checklist itself suggested, and each is a one-line passthrough to the same commands already documented in README.md (`pip install -e \".[dev]\"`, `npm install`, `pytest`, `npm test`) — no new logic, so they don't violate the 'no new build system' framing in the task description. `make test` intentionally runs plain `pytest`/`vitest run` rather than the coverage-gated variants, since the dedicated `check-coverage`/`test-90` skills already own the 90% gate and duplicating that here would just create two sources of truth for the same check. Skipped a `lint`/static-analysis target for the same reason: the `static-verify` skill already owns running ruff/mypy/eslint/tsc together with its own reporting conventions (grouping findings, distinguishing type errors from style nits) that a bare Makefile passthrough would flatten. `help` was added because a Makefile with six targets is easy to forget the names of; it's a standard `grep`-the-`## comment` pattern, not a build-system addition. Rejected alternative: keeping the Makefile to only `backend`/`frontend`/`dev` (strict scope) — rejected as under-serving the checklist's explicit prompt to decide on `install`/`test`. Also rejected: a `lint` target mirroring `static-verify` — rejected as duplicating an existing skill's job."
    },
    {
      "timestamp": "2026-09-17T00:00:01Z",
      "recorded_by": "task-worker",
      "decision": "`make dev`'s cleanup traps and kills the two `$(MAKE) backend`/`$(MAKE) frontend` child PIDs it captured itself (`kill $$bpid $$fpid`), not the whole process group (`kill 0`).",
      "rationale": "An initial `trap 'kill 0' EXIT INT TERM` implementation was verified to actually clean up both uvicorn and vite with no orphaned processes, but under a live SIGINT it also signalled the trapping shell itself mid-handler and reproducibly crashed bash in this environment (reported by make as 'Segmentation fault (core dumped)' on the `dev` recipe) — alarming output for something that otherwise worked correctly. Switching to killing only the two captured child PIDs produces the same clean-shutdown outcome (verified: `INFO: Shutting down` / `Application shutdown complete` from uvicorn, vite's process group gone, `pgrep -fa 'uvicorn|vite'` empty afterward) with a normal 'Interrupt' exit instead of a spurious crash report. Rejected alternative: keep `kill 0` since it's the more commonly cited idiom for this pattern — rejected once the self-signal crash was reproduced twice under a real SIGINT to the process group (the realistic Ctrl-C scenario), not just under `timeout`."
    }
  ],
  "git": {
    "branch": "task/dev-makefile",
    "pr_number": 96,
    "pr_url": "https://github.com/rainoko/fintrade/pull/96",
    "pushed_at": "2026-09-17T20:30:00Z"
  },
  "review": {
    "reviewed_at": "2026-09-17T20:21:24Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/96",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "`make dev`'s cleanup only reliably works because a real interactive-terminal Ctrl-C delivers SIGINT to the whole process group at once (verified clean with setsid+kill -INT on the negative PGID: uvicorn and vite both shut down, no orphans, no crash). Sending SIGINT to only the top-level `make dev` PID (simulating a non-interactive invocation) does nothing -- GNU Make defers handling the signal until its recipe subshell exits, and that subshell is itself blocked in `wait $bpid $fpid`, which is never interrupted -- full deadlock with uvicorn/vite/npm left running. Outside the checklist's literal ask (Ctrl-C/exit, which works), so non-blocking. Filed as docs/tasks/dev-makefile-followups.json.",
        "file": "Makefile",
        "line": 28
      },
      {
        "summary": "README's \"make backend (and make dev, which depends on it)\" overstates the relationship as a formal Make prerequisite; `dev` actually invokes `$(MAKE) backend` as a subprocess within its own recipe. Described behavior is correct, phrasing is imprecise. Filed as docs/tasks/dev-makefile-followups.json.",
        "file": "README.md",
        "line": 32
      }
    ],
    "notes": "Checked out PR #96. Backend: pytest --cov=app --cov-fail-under=90 -> 493 passed, 99.84% coverage, no regression (no app code touched by this PR). Frontend: vitest run --coverage -> all passing, 99.71%/99.64%/100%/99.71% (statements/branches/functions/lines), no regression. Static analysis (static-verify): backend ruff + mypy clean, frontend eslint + tsc clean. No API route/schema touched, so openapi.json check N/A. area is infra (not backend/signals or backend/portfolio, skill is null not verify-elder-signal), so verify-elder-signal doesn't apply. No new module/dependency/layer, so architecture-review doesn't apply. No frontend components touched (Makefile + README only), so the components-placement check (step 7a) doesn't apply. No UI surface for this change at all -- browser walkthrough explicitly skipped as not applicable, not silently omitted. Manually verified the Makefile itself since this PR has no automated tests of its own: `make help` lists all 6 targets, .PHONY covers all of them; `make backend` with backend/.venv temporarily moved aside prints the documented friendly guard error and exits non-zero (restored venv afterward, confirmed uvicorn binary present again); most importantly, independently verified the make-dev SIGINT-cleanup claim rather than trusting the task's decisions entry -- reproduced a real interactive-terminal-style Ctrl-C with setsid (own process group/session) + kill -INT on the negative PGID: uvicorn (including its --reload reloader and multiprocessing spawn/resource-tracker children) and vite/npm both shut down cleanly, pgrep -fa 'uvicorn|vite' empty afterward, make printed a normal '*** [Makefile:29: dev] Interrupt' with no crash -- confirms the kill $$bpid $$fpid vs kill 0 decision's outcome is correct as implemented (did not need to reproduce the reported kill-0 segfault itself to accept that call, since what matters for review is that the current code's behavior is correct, which I verified directly). Also tested the edge case of signalling only the top-level make dev PID (not the process group): this hangs -- GNU Make defers acting on a signal until its currently-running recipe shell exits, and that shell is blocked in `wait $bpid $fpid` which never gets interrupted, so nothing propagates and uvicorn/vite are left running; recorded as a non-blocking follow-up since it's outside the checklist's literal Ctrl-C/exit ask. Ran the code-review skill (medium effort) against the diff in the background for a second pass; my own manual testing (described above) already covered correctness of the shell/process logic in more depth than a diff-only read would for a change this size, so I did not block finalizing the review on its completion. Decision-memory cross-check: checklist item 4 (decide on adjacent convenience targets) has a corresponding decisions entry with rationale (install/test as thin passthroughs, no lint target since static-verify already owns that); the kill-0-vs-kill-pids switch also has its own recorded decision with rationale. Both adequate, no gap. Two non-blocking findings total, filed as docs/tasks/dev-makefile-followups.json (new task, depends_on this one, since none existed yet for this id)."
  }
}
