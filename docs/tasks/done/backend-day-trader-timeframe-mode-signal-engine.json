{
  "id": "backend-day-trader-timeframe-mode-signal-engine",
  "title": "Day-trader mode: genericize Triple Screen (Tide/Wave/Trigger) evaluation over the active timeframe triple",
  "area": "backend/signals",
  "skill": "verify-elder-signal",
  "state": "done",
  "depends_on": [
    "backend-day-trader-timeframe-mode",
    "backend-day-trader-timeframe-mode-ibkr-intraday"
  ],
  "references": [
    "docs/tasks/backend-day-trader-timeframe-mode.json",
    "docs/Analyse.md",
    "docs/architecture/Backend.md#5-signal-engine",
    "docs/architecture/Backend.md#10-day-trader-timeframe-mode-foundational-model--settings-in-progress"
  ],
  "description": "Third of four dependent follow-up tasks splitting docs/tasks/backend-day-trader-timeframe-mode.json's original checklist items 5 and 9 out of that task (see its `decisions` entry for the full split rationale and what already landed: `app.signals.timeframe`'s generic TimeframeTriple model + `app.trading_mode`'s global settings persistence + `GET`/`PUT /api/settings/trading-mode`; and backend-day-trader-timeframe-mode-ibkr-intraday's real intraday data fetching, a hard prerequisite here since there's no meaningful way to test a generic evaluation against day-trader-mode data without it).\n\nThis is the core methodology change: `app.signals.triple_screen.evaluate_tide`/`evaluate_wave`/`evaluate_trigger` and `app.signals.engine.analyse`/`analyse_history` currently hard-code weekly (Tide) / daily (Wave) / daily-as-Trigger-approximation (Screen 3) -- see `app.signals.engine._weekly_through_bar_date`'s calendar-week truncation logic and every 'weekly'/'daily'-named parameter throughout `engine.py`. This task adapts that evaluation to operate generically over whichever `TimeframeTriple` is currently active (`app.trading_mode.get_trading_mode_setting`) -- swing mode's own weekly/daily behavior must remain byte-for-byte unchanged (it's this app's default and only mode in production today; regression risk here is real, not hypothetical), while day-trader mode evaluates the same Screen 1/2/3 + Impulse + confidence logic over the configured triple's long-term/intermediate/short-term data instead.\n\nNeeds an `elder-signal-reviewer` pass before merge (docs/tasks/backend-day-trader-timeframe-mode.json's original checklist item 9) given ch. 39's own framing of what changes and what stays invariant when timeframes shift: confirm (don't assume) that the Impulse System, confidence scoring (docs/Analyse.md §6), and the 6% Rule/protective-stop risk logic (docs/Analyse.md §7) all remain timeframe-agnostic in principle once Screen 1/2/3 themselves are genericized. Also confirm docs/Analyse.md §7's own weekly-chart-specific profit-target/stop rule (ch. 39 p.161's 'set profit targets using long-term charts, stops on intermediate-timeframe charts') generalizes correctly to 'long-term chart' / 'intermediate-timeframe chart' meaning whichever timeframe currently fills that role, not literally weekly/daily -- this app's own `suggest_profit_target`/`protective_stop` currently hard-code that literal weekly/daily split (see docs/Analyse.md's 'Swing-style' profit-target section) and are explicitly out of scope for this task's own checklist unless the elder-signal-reviewer pass finds they must change too.",
  "checklist": [
    { "step": "Decide (and record via `decisions`) how deep the genericization goes for this task's own scope: at minimum Screen 1/2/3 (`triple_screen.py`) + the Impulse gate's timeframe assumptions in `engine.py`; explicitly decide whether `portfolio/risk.py`/`portfolio/profit_target.py`'s hard-coded weekly/daily split is in scope here or deferred to yet another follow-up.", "done": true },
    { "step": "Genericize `app.signals.triple_screen.evaluate_tide`/`evaluate_wave`/`evaluate_trigger` to accept whichever long-term/intermediate/short-term data the active `TimeframeTriple` implies, with swing mode's existing weekly/daily behavior preserved exactly (regression tests, not just new-behavior tests).", "done": true },
    { "step": "Genericize `app.signals.engine.analyse`/`analyse_history`'s weekly/daily-specific truncation and parameter naming (`_weekly_through_bar_date` and friends) the same way.", "done": true },
    { "step": "Unit/integration tests for the generic evaluation, including a mocked IBKR intraday provider (no test may make a live network call) exercising an actual day-trader-mode signal computation end to end.", "done": true },
    { "step": "elder-signal-reviewer pass before merge -- confirm Impulse/confidence/6%-Rule timeframe-agnosticism per this task's own description, don't assume it.", "done": true },
    { "step": "Update docs/architecture/Backend.md §11's 'Not yet landed' list accordingly.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-24T18:47:18Z",
      "recorded_by": "task-worker",
      "decision": "Scope for this task's own PR: genericize `app.signals.triple_screen` (Tide/Wave/Trigger) and `app.signals.engine` (`analyse`/`analyse_history`'s orchestration, plus a new `analyse_day_trader` entry point). `app.portfolio.risk`/`app.portfolio.profit_target`/`app.portfolio.exits`'s own hard-coded weekly/daily split (the 6% Rule/protective-stop's `_SWING_LOW_WINDOW_DAYS` trading-day window, the weekly-chart profit-target channel, `evaluate_exit_flags`'s `tide_flipped_bearish` flag on `weekly_ohlcv`) is deferred to a follow-up task, not touched here. `app.data.day_trader_intraday` (a sibling, already-`done` task's module) is extended in this PR to also fetch the `long_term` leg, resolving a non-blocking finding from that task's own PR #311 review.",
      "rationale": "The task's own checklist explicitly permits deferring the portfolio/risk-layer split, and the actual code shows why that's the right call here specifically: `app.portfolio.risk.protective_stop`'s SafeZone-style swing-low window and `app.portfolio.profit_target.suggest_profit_target`'s weekly-chart channel are genuinely calendar-day/calendar-week-specific concepts (a '~2 weeks' lookback, a literal weekly-resampled OHLCV fetch) that would need their own dedicated design decisions (what does a '10 trading day' SafeZone window mean when the active short-term leg is 2-minute bars?) rather than a mechanical rename -- exactly the kind of judgment call that deserves its own task and its own elder-signal-reviewer scrutiny, not a rushed addendum to an already-large signal-engine change. Extending `app.data.day_trader_intraday` to fetch `long_term` was judged in-scope (not deferred) because `backend-day-trader-timeframe-mode-ibkr-intraday-followups.json`'s own checklist explicitly named this task as the one that should decide it, and because ch. 39's own canonical day-trading examples (25-min/5-min/2-min, 39-min/8-min) are fully intraday on all three legs -- without this extension, this task's own end-to-end test (checklist item 4) couldn't exercise Screen 1/Tide against real day-trader-mode data for the book's own primary examples at all, which would have made 'genericize the evaluation' an unverifiable claim for the case that matters most."
    },
    {
      "timestamp": "2026-09-24T18:47:24Z",
      "recorded_by": "task-worker",
      "decision": "`app.signals.engine.analyse`'s existing public parameters (`daily_ohlcv`, `weekly_ohlcv`) and `app.signals.triple_screen.evaluate_tide`/`evaluate_wave`'s existing parameters are NOT renamed to generic names -- genericity is achieved by documenting the general contract (a short added docstring paragraph on each) and, for `analyse()`, by adding a new optional `short_term_ohlcv` parameter, rather than by renaming what's already there. `evaluate_trigger`'s own `daily_ohlcv` parameter IS renamed, to `ohlcv`. The private, zero-external-caller `_weekly_through_bar_date` helper and `_WAVE_LOOKBACK_DAYS` constant in `engine.py` ARE renamed (`_long_term_through_bar_date`/`_WAVE_LOOKBACK_BARS`), since checklist item 3 explicitly names the former by example.",
      "rationale": "`evaluate_tide`/`evaluate_wave`/`analyse()`'s `daily_ohlcv`/`weekly_ohlcv` parameters already accept an arbitrary `pd.DataFrame` -- nothing in their implementation is actually specific to calendar weeks/days, only the parameter *names* and the surrounding prose are. Renaming them would touch roughly 150 occurrences of those identifiers across `engine.py`/`triple_screen.py`'s own extremely dense narrative docstrings (confirmed via grep before starting), for zero behavioral difference, at real risk of introducing a subtle prose error mid-edit that a passing test suite would never catch (a wrong word in a docstring doesn't fail an assertion) -- directly working against this task's own explicit 'be extremely careful, given real regression risk on swing mode' instruction, since verifying a rename of that size by eye is itself expensive and error-prone. `evaluate_trigger` is the deliberate exception: unlike Tide/Wave, its own *meaning* genuinely changes depending on what's fed in (swing mode's daily-bar EOD approximation vs. day-trader mode's literal short-term-timeframe rule) -- see its own rewritten docstring -- so renaming its parameter to a neutral `ohlcv` and rewriting that one function's (much shorter, ~35-line) docstring to explain both cases was worth the smaller, more contained risk. Confirmed via `grep` before renaming that every real caller of `evaluate_trigger`/`analyse`/`analyse_history` in `app/` and `tests/` uses positional arguments only (no `daily_ohlcv=`/`weekly_ohlcv=` keyword usage anywhere), so even the parameters that keep their old names would have tolerated a rename without breaking a caller -- the decision not to rename them is about diff-size/review-risk, not caller compatibility."
    },
    {
      "timestamp": "2026-09-24T18:47:26Z",
      "recorded_by": "task-worker",
      "decision": "`app.signals.engine.analyse`'s Screen 3 (Trigger) genericization is a new optional `short_term_ohlcv: pd.DataFrame | None = None` parameter -- when `None` (the default, every existing caller's behavior), Trigger evaluates on `daily_ohlcv` exactly as before; when supplied, Trigger evaluates on `short_term_ohlcv` instead, while Screen 1 (Tide)/Screen 2 (Wave)/the Impulse gate/every `indicators` entry are completely unaffected (still read `daily_ohlcv`/`weekly_ohlcv`). A new `analyse_day_trader()` function is the day-trader-mode entry point -- a thin wrapper supplying `long_term_ohlcv`/`intermediate_ohlcv`/`short_term_ohlcv` explicitly and delegating straight to `analyse()`, with no DB/IBKR dependency of its own.",
      "rationale": "This maps directly onto ch. 39's own three-tier structure: Tide is the long-term screen, Wave is the medium-term/intermediate screen, and Trigger is specifically the short-term/intraday screen (docs/Analyse.md §2's own 'Screen 3 -- The Ripple (entry trigger, short-term/intraday)' heading) -- so Trigger is the *only* one of the three that should ever read a genuinely distinct short-term series in day-trader mode; Wave/the Impulse gate/every daily indicator correctly stay on whichever data plays the intermediate role (verified directly: `evaluate_impulse`'s own docstring already documents its `ohlcv` parameter as timeframe-agnostic, confirming the Impulse gate needed no change at all here). A default of `None` reproducing the exact pre-existing call path was chosen specifically so swing mode's own regression tests (unchanged assertions, still passing) constitute genuine proof of no behavior change, rather than requiring a new code path to be proven equivalent to an old one that no longer exists. `analyse_day_trader()` staying a pure function (no DB Session/IBKR provider parameter) keeps `app.signals.engine` a DB- and IBKR-free domain module, matching docs/architecture/Backend.md §5's existing description of this module -- resolving *which* frames to fetch for the currently active `TimeframeTriple` is left to `backend-day-trader-timeframe-mode-api` (a dependent follow-up task, not yet started), which is where DB/IBKR wiring for an actual HTTP route belongs."
    },
    {
      "timestamp": "2026-09-24T18:47:28Z",
      "recorded_by": "task-worker",
      "decision": "`analyse_history()`'s own day-trader-mode support (a walk-forward historical replay producing a per-bar signal history over intraday data, for a future day-trader-mode chart overlay) is deferred to a follow-up task, not built here. `_long_term_through_bar_date` (renamed from `_weekly_through_bar_date`) does gain a `long_term_unit: TimeframeUnit = TimeframeUnit.WEEK` parameter with a new direct `<= bar_date` branch for `DAY`/`MINUTE` units (covered by dedicated unit tests), but `analyse_history()`'s own public signature is unchanged and its one internal call site still passes the default `WEEK` unit -- exactly its pre-existing behavior.",
      "rationale": "`app.data.day_trader_intraday`'s current shape (`get_intraday_bars_for_triple`/`get_active_day_trader_intraday_bars`) only ever fetches 'the most recent N days of bars, as of now' -- a signal-computation snapshot, the right shape for `analyse()`/`analyse_day_trader()`'s single-point-in-time evaluation (this task's checklist item 4's own 'actual day-trader-mode signal computation' framing, singular). Walking forward through *historical* intraday bars for a growing per-bar replay (what `analyse_history` does for swing mode) needs a categorically different IBKR fetch shape -- paginating backward through a much longer intraday history window while still respecting IBKR's per-call pagination bounds (`IBKRProvider._MAX_PAGINATION_PAGES`/`_MAX_BARS_PER_PAGE`) -- that's a genuine, unsolved data-fetching design problem in its own right, not a mechanical extension of this task's own engine-layer changes. Building `_long_term_through_bar_date`'s `DAY`/`MINUTE` branch now (rather than waiting for that follow-up) was still worth doing: it's cheap, correct, and directly testable in isolation (see `TestLongTermThroughBarDateGenericUnits`), so the follow-up task inherits a ready building block instead of needing to touch this function's own look-ahead-avoidance logic itself."
    },
    {
      "timestamp": "2026-09-24T19:33:06Z",
      "recorded_by": "task-worker",
      "decision": "Addressed `pr-decision`'s needs_work override on PR #312 (test-strength gap: `test_fully_intraday_triple_produces_a_real_buy_signal`'s fixtures didn't actually prove `short_term_ohlcv` drives Screen 3, since the intermediate leg's own last two bars would independently fire the same bullish trigger) by (1) adding one more bar to `_intermediate_bars()` -- a slight recede after the existing rally bar -- so the intermediate leg's own last two bars (close 101.5 vs. prior high 102.8) no longer independently satisfy `evaluate_trigger`'s crossing rule, while every other assertion in the existing test (tide/impulse/wave/signal/confidence/breakdown) still holds unchanged; and (2) adding a new `test_short_term_leg_genuinely_drives_the_trigger_not_the_intermediate_leg` test alongside it (not a replacement) that calls the engine both the correct way (`analyse_day_trader` with `short_term_ohlcv` wired) and the simulated-regression way (`analyse(ticker, intermediate_ohlcv, long_term_ohlcv)`, no `short_term_ohlcv` -- i.e. Screen 3 silently falling back to the intermediate leg) and asserts the two runs genuinely diverge (`trigger.fired` True vs. False, `signal` BUY vs. HOLD) while Tide/Wave/Impulse stay identical between the two calls, isolating the divergence to the trigger-input wiring specifically. The `review` object below (verdict: needs_work, from PR #312's first review round) is left as-is as the historical record of that round rather than edited in place -- a fresh `pr-reviewer` pass on this updated branch is expected to re-review and overwrite it, per this task's dispatch instructions.",
      "rationale": "A new, narrower discriminating test was added alongside the existing broad end-to-end test rather than replacing it, per this task's own instruction to preserve both properties (a real BUY signal computed end to end, and proof that the short-term leg specifically drives Trigger) rather than trading one kind of coverage for the other. Verified the fix does what it claims exactly the way `pr-decision` did: directly computed `_intermediate_bars()`'s own last two bars by hand (confirmed non-firing, 101.5 <= 102.8) and `_short_term_bars()`'s (confirmed firing, 99.5 > 96.0); then, as an extra confirmation beyond arithmetic, temporarily edited `engine.py`'s `trigger_ohlcv = daily_ohlcv if short_term_ohlcv is None else short_term_ohlcv` line to unconditionally read `daily_ohlcv` (simulating the exact regression this test exists to catch) and re-ran the test file -- both the existing end-to-end test and the new discriminating test failed as expected -- then reverted `engine.py` back to its committed state (confirmed via `git diff` showing no change) and re-ran to confirm both pass again. Full backend suite re-run after the fix: 1320 passed (up from 1319, the one new test), 100% line/branch coverage; `ruff check .` and `mypy` (scoped to `app/`, per `[tool.mypy]`'s own `files = [\"app\"]` config) both clean; `scripts/check_profit_target_wording.py` clean. No functional code in `app/` was touched -- `pr-decision` had already confirmed `engine.py`'s actual wiring is correct today, so this was purely a test-strength fix, matching the dispatch instruction's own framing."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/backend-day-trader-timeframe-mode-signal-engine",
    "pr_number": 312,
    "pr_url": "https://github.com/rainoko/fintrade/pull/312",
    "pushed_at": "2026-09-24T19:35:24Z"
  },
  "review": {
    "reviewed_at": "2026-09-24T19:41:40Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/312",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "RESOLVED (round 2): `pr-decision`'s needs_work override (round 1) found `test_fully_intraday_triple_produces_a_real_buy_signal`'s fixtures didn't discriminate the correct short_term_ohlcv wiring from a simulated regression (Trigger silently falling back to the intermediate leg) -- every assertion passed identically either way. task-worker's fix (commit 3fd9a00) adds one receding bar to `_intermediate_bars()` so its own last two bars no longer independently satisfy the bullish crossing rule, and adds `test_short_term_leg_genuinely_drives_the_trigger_not_the_intermediate_leg`, which calls the engine both the correct way and the simulated-regression way and asserts `trigger.fired`/`signal` genuinely diverge while Tide/Wave/Impulse stay identical. I independently reproduced the exact check pr-decision and task-worker both describe, not just trusting their write-ups: temporarily edited `engine.py`'s `trigger_ohlcv = daily_ohlcv if short_term_ohlcv is None else short_term_ohlcv` line to unconditionally read `daily_ohlcv` (simulating the regression) and re-ran `tests/integration/test_day_trader_mode_signal_engine.py` -- both the original end-to-end test and the new discriminating test failed as expected (`assert False is True` on `trigger.fired`). Reverted the edit (`git diff` clean afterward) and re-ran -- all 3 tests in the file pass again. The fixture-discrimination gap is genuinely closed.",
        "file": "backend/tests/integration/test_day_trader_mode_signal_engine.py",
        "line": 78
      },
      {
        "summary": "Non-blocking findings from round 1 (volume-confirmation reading the intermediate leg's volume even when short_term_ohlcv drives Trigger; a stale `_weekly_through_bar_date` docstring reference in timeframe.py; a task-board bookkeeping gap on backend-day-trader-timeframe-mode-ibkr-intraday-followups.json; sequential-not-concurrent 3-leg IBKR fetch latency) remain correctly tracked on backend-day-trader-timeframe-mode-signal-engine-followups.json -- no new findings from this round's re-review.",
        "file": "docs/tasks/backend-day-trader-timeframe-mode-signal-engine-followups.json"
      }
    ],
    "notes": "Round 2 re-review of PR #312, dispatched specifically to independently verify task-worker's fix for pr-decision's round-1 needs_work override (the integration test's fixture-discrimination gap -- see comments above and this task's decisions entry for the full history). Re-ran everything: backend `pytest --cov=app` -- 1320 passed, 100% line/branch coverage (up one test from round 1's 1319). Frontend `yarn test:coverage` -- 882 passed, 99.77%/99.08%/99.79%/99.82% stmt/branch/func/line coverage. Backend `ruff check .` and `mypy` clean; frontend `yarn lint` and `yarn tsc -b --noEmit` clean; `scripts/check_profit_target_wording.py` clean; `scripts/validate_tasks.py` -- 308 task files validated, no violations. Confirmed via `git diff 7421aee...HEAD --stat` that only the test file plus task-board JSON changed since pr-decision's override commit -- no app/ code changed, so the round-1 elder-signal-reviewer pass (Screen 1/2/3, Impulse gate, confidence weights, portfolio/risk deferral-safety) and the openapi.json byte-identical check both still hold unchanged; not re-run from scratch since nothing they cover moved. No UI-facing work in this PR (backend app/signals + app/data/day_trader_intraday.py + docs only, confirmed unchanged from round 1) -- browser walkthrough (step 9) and persisted e2e suite (step 9a) both explicitly skipped as not applicable, matching round 1's determination. No leftover `pr_decision` field on this task (pr-decision only writes that field on a merge decision, and round 1 ended in an override, not a merge, so there was never one to clear). The pre-existing 'not mine, do not discard' git stash entry was left completely untouched throughout -- no `git stash` command was run at any point in this review."
  }
}
