{
  "id": "frontend-daily-homework-page-followups-followups",
  "title": "Non-blocking findings from PR #272's review (Daily Homework follow-ups PR)",
  "area": "frontend/pages",
  "skill": null,
  "state": "done",
  "depends_on": ["frontend-daily-homework-page-followups"],
  "references": [
    "docs/tasks/frontend-daily-homework-page-followups.json#review",
    "https://github.com/rainoko/fintrade/pull/272"
  ],
  "description": "Non-blocking findings from pr-reviewer's full review of PR #272 (frontend-daily-homework-page-followups), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges.",
  "checklist": [
    {
      "step": "`DailyHomeworkForm.tsx` calls `useYesterdayTradingSuggestion()` unconditionally (no `enabled` gate), so the suggestion request still fires -- and is left in flight -- even when today's entry already exists and its result is provably unused. This contradicts the component's own doc comment, which claims the suggestion query 'is skipped entirely once an existing entry makes it moot' -- verified via a real browser walkthrough (existing-entry case): the request was seen firing even with a 10s artificial delay on it, while the form+banner rendered immediately from the existing entry. The parent checklist item this PR closed only asked to stop *waiting* on the query in that case, which was correctly implemented -- but the fetch itself remains wasteful. The codebase already has an established `enabled:`-gating pattern elsewhere (`useIndicatorHistory.ts`, `useStockHistory.ts`, `useStockAnalysis.ts`) that could gate this call on `!existingEntry` once `todayQuery` has settled. Also correct the doc comment's 'skipped entirely' wording to describe what actually happens (the wait is skipped, not the fetch) regardless of whether the fetch itself gets fixed. NOT actually resolved by PR #273's `enabled: !existingEntry` gate (see `review.comments` on this task, PR #273's review): reopened by pr-reviewer -- a fresh/cold page load to a day whose entry already exists still dispatches the wasted suggestion request (`existingEntry` is `null`/falsy until `todayQuery` resolves, so `enabled` is `true` from the very first render regardless of the eventual outcome, and the fetch is typically already in flight by the time `existingEntry` becomes truthy -- confirmed via a live browser walkthrough reproducing the original evidence, and independently by the `code-review` skill). Only a *warm remount* (an already-cached `todayQuery` across a same-session re-mount) is actually fixed. RESOLVED (second attempt, post-PR #273 needs_work): `DailyHomeworkForm` now gates on `enabled: todayQuery.isSuccess && !existingEntry` -- this query is never enabled at all across a cold load whose entry turns out to already exist, since `existingEntry` can't retroactively become truthy once `todayQuery` has settled with `null`. `HomeworkQuestionsForm` was restructured to accept a live `suggestedScore` prop and apply it to `yesterday_trading_score` via a render-time state adjustment (guarded by a `hasEditedYesterdayScore` flag and an `initialResult` check), since the suggestion fetch can no longer have resolved by the time the form first mounts for a genuinely new entry. See the `decisions` entry below for the full rationale, including why an effect (the originally-suggested approach) was rejected in favor of the render-time-adjustment pattern already established elsewhere in this codebase (`useResetOnSubjectChange`, `AddPositionDialog`). Verified via new unit tests covering both the cold-load-with-existing-entry (zero suggestion dispatches) and cold-load-with-new-entry (prefill lands after an async resolve, without blocking initial render; not clobbered by a prior user edit) cases, 100%-branch coverage on `DailyHomeworkForm.tsx`, and a full `make e2e` pass (22/22, including `daily-homework.spec.ts`).",
      "done": true
    },
    {
      "step": "`HomeworkScoreBanner.tsx`'s 'is this the high-yellow (9-10) too-perfect band' condition (`band === 'yellow' && totalScore >= 9`) is duplicated three times (`iconFor`, the inline JSX label-suffix check, and implicitly as `totalScore >= 9` inside `messageFor`'s yellow branch) instead of being computed once and reused. A future change to the threshold or band naming could update one or two of the three and silently leave the others out of sync (e.g. the icon shows but the label suffix doesn't). Consider a single `const isTooPerfect = band === 'yellow' && totalScore >= 9` computed once in the component and passed to/reused by all three call sites.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T18:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Gated `useYesterdayTradingSuggestion` on `enabled: !existingEntry` (not `todayQuery.isSuccess && !existingEntry`), and accepted that this only prevents the fetch on a *warm* remount (today's entry already cached, so `existingEntry` is known synchronously on first render) -- not on every cold load.",
      "rationale": "Verified empirically (a throwaway test, not committed) that TanStack Query's `enabled: false` does not abort a fetch already in flight, and that this API client never forwards the queryFn's injected AbortSignal to `fetch` anywhere in the codebase -- so no gating strategy here can cancel an already-dispatched request; only preventing the dispatch itself is possible. Considered and rejected gating strictly on `todayQuery.isSuccess && !existingEntry` (delaying suggestionQuery until todayQuery settles): it would guarantee zero dispatch on every cold load, but empirically breaks the prefill feature entirely and permanently, since `HomeworkQuestionsForm` mounts in the exact same render where `todayQuery.isSuccess` first becomes true, and its `useState(initialScores)` initializer runs during that render -- strictly before TanStack Query's own effect (which starts the now-newly-enabled suggestion fetch) has a chance to run -- so the suggestion's resolved value would never be captured by the initial mount, by construction, every single time, not just occasionally (verified via a failing test asserting the prefilled '2 — Well' value, which never appeared even after an extended `waitFor`). Also considered and rejected adding AbortSignal-forwarding through the API client so `enabled: false` could cancel an in-flight cold-load fetch too: no other hook in this codebase forwards the injected signal, so this would be a new, broader architectural pattern change rather than this task's narrow `enabled:`-gating fix. `enabled: !existingEntry` instead starts the suggestion fetch concurrently with `todayQuery` whenever the entry's existence isn't known yet (i.e. `existingEntry` is `null` during loading, same as before this fix), preserving the existing (already-tested) prefill timing for a cold load, while still preventing the fetch on a warm remount -- verified via a test using a shared QueryClient across two mounts, where the second mount's cached `todayQuery` data makes `existingEntry` available on its very first render, so `enabled` starts `false` and the suggestion endpoint is never hit a second time. This is a real, common case (revisiting the page in the same session shortly after submitting today's entry) and the best fix achievable without regressing the prefill feature or expanding scope."
    },
    {
      "timestamp": "2026-09-22T19:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Superseded the previous `enabled: !existingEntry`-only attempt (reopened by pr-reviewer on PR #273 for not actually preventing the cold-load-with-existing-entry dispatch) with two coupled changes: (1) `DailyHomeworkForm`'s `useYesterdayTradingSuggestion` gate is now `enabled: todayQuery.isSuccess && !existingEntry`, requiring `todayQuery` to have genuinely resolved before the suggestion query can ever be enabled; (2) `HomeworkQuestionsForm` no longer folds the suggestion into its `useState(initialScores)` initializer -- it now takes a live `suggestedScore` prop and applies it to `yesterday_trading_score` via a guarded render-time state adjustment (an `if (suggestedScore !== prevSuggestedScore) { ...; if (!initialResult && !hasEditedYesterdayScore && suggestedScore !== null) setScores(...) }` block, with `prevSuggestedScore` and `hasEditedYesterdayScore` both plain `useState`), not an initializer or a `useEffect`.",
      "rationale": "Change (1) alone is what actually closes the reopened finding: `todayQuery.isSuccess` is only ever true once `todayQuery` has genuinely resolved, so `existingEntry` can no longer be read as falsy-because-still-loading the way the old bare `!existingEntry` gate allowed -- once `todayQuery` settles and reveals an existing entry, this query is never enabled at any point in the component's lifetime, verified via a new test asserting zero suggestion-endpoint dispatches across an entirely fresh render (no shared/warm QueryClient, unlike the old warm-remount-only test this PR's previous attempt relied on). The tradeoff this creates -- the suggestion fetch can no longer start until after `todayQuery` resolves, so for a genuinely new entry it hasn't even started, let alone resolved, by the time `HomeworkQuestionsForm` first mounts -- is exactly the 'useState initializer can't capture a value that arrives after mount' obstacle PR #273's review predicted, which change (2) exists to solve. Rejected a `useEffect` for change (2) (the review's own suggested shape) for two reasons discovered during implementation, not just style preference: first, this codebase's own ESLint config enforces `react-hooks/set-state-in-effect` (an error, not a warning) against calling `setState` synchronously inside a `useEffect` body, which the natural 'prefill once suggestion resolves' effect does directly; second, guarding that effect against overwriting a user's own edit needs to read a 'has the user touched this field' flag during the effect, and this codebase already has a documented, established alternative for exactly this shape of problem -- adjusting state synchronously during render, guarded by a `prevValue !== value` comparison stored in its own `useState` (see `useResetOnSubjectChange.ts`'s and `AddPositionDialog.tsx`'s own doc comments, both citing React's 'adjusting state when a prop changes' guidance at https://react.dev/learn/you-might-not-need-an-effect) -- so this task adopted that existing pattern rather than introducing a second, inconsistent way to solve the same category of problem. A first pass at the render-time adjustment used a `useRef` for `hasEditedYesterdayScore` (reasoning: flipping it shouldn't itself trigger a re-render) but ESLint's `react-hooks/refs` rule (also an error in this config) rejects reading a ref's `.current` during render -- refs are only safe to read in effects/handlers -- so `hasEditedYesterdayScore` was changed to plain `useState` instead, which is what the render-time-adjustment pattern requires for anything read during render anyway. Verified end to end: a new test seeds a resolvable-on-demand suggestion response and confirms the still-neutral field switches to the suggested value only after the response is manually resolved (proving the render doesn't block on it and the adjustment fires once it lands); another new test confirms a suggestion that resolves after the user has already answered `yesterday_trading_score` themselves never overwrites that answer; the pre-existing 'no score'/'suggestion request fails' tests were also strengthened (they previously asserted a value identical to the pre-resolution default, so `waitFor` was passing before the suggestion had actually settled -- a real, if incidental, coverage gap this task's own investigation surfaced via a genuinely uncovered branch on the `??` fallback at the relevant line, not something the original PR #273 review flagged). Full verification: `DailyHomeworkForm.tsx` at 100% statement/branch/function/line coverage; full frontend suite 825/825 passing (825/825 on a clean run; two unrelated, already-documented pre-existing flakes -- `AddPositionDialog`'s and `FollowUpReviewDialog`'s own 5000ms `waitFor` timeouts -- were observed on other runs and confirmed to pass in isolation, matching this exact task's own prior review notes) at 99.76%/99.31%/99.78%/99.82% statements/branches/functions/lines; `eslint`/`tsc` clean; `make e2e` 22/22 passing including `daily-homework.spec.ts`."
    }
  ],
  "git": {
    "branch": "task/frontend-daily-homework-page-followups-followups",
    "pr_number": 273,
    "pr_url": "https://github.com/rainoko/fintrade/pull/273",
    "pushed_at": "2026-09-22T19:02:59Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T19:28:54Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/273",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Round-2 fix verified as genuinely correct, not just re-asserted. Reproduced all three scenarios live in a real browser against the running app (not just the unit tests): (1) cold-load-with-existing-entry -- seeded today's entry via the API, did a fresh navigation to /homework, and confirmed via a network-request trace that `yesterday-trading-suggestion` is never dispatched at all (0 requests observed, vs. the previous round's fix which still fired it); (2) cold-load-with-genuinely-new-entry -- confirmed the form renders immediately at the neutral default (`/today` resolves, form renders ~1.2s in), the suggestion request only starts once `/today` has resolved (confirmed sequential, not concurrent, via request timestamps), and the field switches in-place to the suggested value once it resolves, with zero console errors; (3) user-edits-before-suggestion-resolves -- answered 'yesterday' manually with the suggestion request artificially delayed 3s, then waited past that delay and confirmed the user's own answer was never clobbered. All three match the `decisions` entry's claims exactly.",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.tsx",
        "line": 309
      },
      {
        "summary": "Confirmed the render-time-adjustment pattern (`prevSuggestedScore`/`hasEditedYesterdayScore`, the `if (suggestedScore !== prevSuggestedScore)` block) is a genuinely adapted application of `useResetOnSubjectChange`'s established shape, not a blind copy-paste: the guard conditions differ appropriately (an extra `!initialResult && !hasEditedYesterdayScore && suggestedScore !== null` check this use case needs that the source pattern doesn't), and the `useState` (not `useRef`) choice for `hasEditedYesterdayScore` is correctly justified by this codebase's own `react-hooks/refs` lint rule (verified: the rule is in fact configured and would reject reading a ref during render).",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.tsx",
        "line": 138
      },
      {
        "summary": "Non-blocking (tracked as a follow-up, see below): a dedicated `code-review` skill pass plus my own `--reporter=verbose` run confirmed three real, if minor, quality observations -- the render-time-adjustment logic duplicates `useResetOnSubjectChange`'s shape instead of reusing/extracting it; the new 'does not overwrite a user's own answer' test (and two strengthened pre-existing tests) synchronize via a fixed `setTimeout(20ms)` rather than `waitFor`, which genuinely produces React 'not wrapped in act(...)' warnings (confirmed, not hypothetical) -- a latent flakiness risk, not yet an actual failure; and the `todayQuery.isSuccess`-gating gates every new-entry cold load's suggestion fetch behind `/today`'s round trip, not just the existing-entry case, which is already accepted as a documented tradeoff in this task's own `decisions` entry but is worth reconsidering later (e.g. combining the suggestion into `/today`'s response) since it would also let the render-time-adjustment machinery be removed entirely. None of these are blocking -- filed as `frontend-daily-homework-page-followups-followups-followups`.",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.test.tsx",
        "line": 379
      }
    ],
    "notes": "Verified independently end to end. Backend: pytest, 1194 passed, 100% statement/branch coverage (well above the 90% gate) -- no regression. Frontend: a clean sequential `vitest run --coverage --no-file-parallelism` run (avoiding the same pre-existing AddPositionDialog/FollowUpReviewDialog 5000ms `waitFor` timeout flakes this task's own prior rounds already documented, which I independently reproduced and confirmed pass in isolation, unrelated to this PR) shows 825/825 passing at 99.76%/99.31%/99.78%/99.82% statements/branches/functions/lines, with `DailyHomeworkForm.tsx` itself at 100% on all four metrics. ruff/mypy/eslint/tsc/the profit-target-wording guard are all clean. No backend route or schema changed in this PR (diff touches only `frontend/src/features/homework/` and the task board), so no `openapi.json` regeneration was required -- confirmed via `git diff --stat` against `main`. `make e2e`: 22/22 specs pass, including `daily-homework.spec.ts`'s nav/submit/reload-persistence flow -- no regression. Cross-checked the task's `decisions` entries against `docs/architecture/Frontend.md`'s React-state guidance and the cited prior art (`useResetOnSubjectChange.ts`, `AddPositionDialog.tsx`) -- the rejection of a `useEffect`-based approach is well-supported (this codebase's ESLint config does enforce both `react-hooks/set-state-in-effect` and `react-hooks/refs` as errors, confirmed by inspecting `eslint.config.js` and by the clean `yarn lint` result on the actual chosen implementation). Neither checklist item is phrased as 'decide X', so no decision-record gap applies here. No new module/dependency/layer was added (no `architecture-review` needed); `area` is `frontend/pages` and `skill` is `null`, so `verify-elder-signal` doesn't apply; `HomeworkScoreBanner`/`DailyHomeworkForm` are both already-established feature components with no placement change in this PR (nothing new to assess under the common/-vs-feature-specific placement check). All three non-blocking findings above are filed as checklist items on the new follow-up task `frontend-daily-homework-page-followups-followups-followups` (`depends_on: [frontend-daily-homework-page-followups-followups]`) rather than left only in this PR's comments."
  }
}
