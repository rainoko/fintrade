{
  "id": "frontend-kangaroo-tail-markers",
  "title": "Mark Kangaroo Tail patterns on the price chart",
  "area": "frontend/pages/stocks",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": [
    "backend-kangaroo-tail-pattern"
  ],
  "references": [
    "frontend/src/features/stocks/components/PriceChart.tsx",
    "docs/ideas.md"
  ],
  "description": "Mark detected Kangaroo Tails (backend-kangaroo-tail-pattern) directly on the price chart at the tall bar itself, distinct from every other marker type already present. Since this is a candlestick pattern most users won't recognize by name, the explanation needs to actually show what makes the bar unusual (its range vs. the recent average), not just assert 'this is a Kangaroo Tail.' Per the project's established explanatory pattern (common/InfoBalloon, common/MetricHelp, metricHelpContent.ts, signalExplanation.ts, docs/tasks/frontend-stock-detail-metric-help.json), every new visual element this task adds must be explainable, not just displayed: a help affordance (reuse MetricHelp where it fits) that states what the metric/signal is, how it relates to Dr. Elder's methodology, and, where meaningful, an explanation of the CURRENT value shown -- not a static definition alone. Add an entry to metricHelpContent.ts's registry rather than inventing a parallel explanation mechanism.",
  "checklist": [
    {
      "step": "Design a distinct marker for Kangaroo Tail bars (direction-aware: bearish vs. bullish reversal), placed at the tail bar itself.",
      "done": true
    },
    {
      "step": "Implement on PriceChart.tsx.",
      "done": true
    },
    {
      "step": "MetricHelp/InfoBalloon content explaining the pattern concretely (this bar's range vs. the recent average, why the close snapping back to the open matters, the suggested halfway-through-the-tail stop) using this ticker's own actual numbers, not a generic definition.",
      "done": true
    },
    {
      "step": "Tests + Storybook story; confirm 90% coverage gate holds.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-19T20:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Marker: a `square` shape (the one `SeriesMarkerShape` this chart doesn't already use for BUY/SELL `arrowUp`/`arrowDown`, divergence `circle`, or false-breakout `arrowDown`/`arrowUp`), in a new dedicated `theme.palette.kangarooTail.main` magenta (`#ad1457`), positioned `aboveBar` for an `'up'` (bearish) tail and `belowBar` for a `'down'` (bullish) one -- i.e. on the side the tail bar itself physically points toward (its own new high/low), not the side of the implied reversal the way `buildFalseBreakoutMarkers`'s arrow does. Also draws a dashed `createPriceLine` at `suggested_stop` labeled 'Kangaroo Tail stop', same treatment as the false-breakout stop line.",
      "rationale": "The task calls for a marker 'distinct from every other marker type already present' -- `square` is the only shape not already claimed, and a brand-new palette color (rather than reusing `warning.main`/`divergence.main`/`info.main`, all already meaningfully attached to a different overlay on this same chart) keeps the visual vocabulary unambiguous, verified with a dedicated `theme.test.ts` case asserting the new color differs from every one of those. Positioning at the tail's own physical side (rather than mirroring false breakout's 'mark the reversal side' convention) reads more naturally here: the marker sits directly at/above the pattern itself (the unusual bar), not at a separate later confirmation point the way a false breakout's marker sits at its own `reentry_date`, which is a genuinely different bar than the breakout itself. Rejected alternatives: reusing `circle` with a different color (rejected -- shape reuse would read as 'another divergence-like thing' at a glance, undermining the distinctness requirement); a custom SVG/plugin-based marker (rejected as unjustified complexity when the library's own 4-shape enum has an unclaimed option)."
    },
    {
      "timestamp": "2026-09-19T20:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Windowed to the currently visible bar range via a new `isKangarooTailInRange(tail, firstDate, lastDate)` (single-date check, `tail.tail_date >= firstDate && tail.tail_date <= lastDate`) -- both the marker and the `suggested_stop` price line are omitted entirely (not drawn at all, not drawn-but-clipped) whenever the tail's own date falls outside the current range/interval selection, exactly mirroring `isDivergenceInRange`'s established pattern (PR #152/#158) for the same axis-distortion-avoidance reason.",
      "rationale": "This session's own explicit instruction (and this batch's repeated PR #152/#158 history) flags this exact failure class -- an unwindowed time-anchored overlay stretches Lightweight Charts' time scale to cover a far-away point, squashing the real candlestick content. Unlike a 2-point divergence line, a single-bar marker/price-line has no 'partially in range' state to reason about -- one date is either in the visible window or it isn't -- so the check is simpler than `isDivergenceInRange`'s two-extremes-both-required test, but the same 'omit the whole overlay for this render, not just clip it' response applies. The price line is included in the same windowing gate (not drawn unconditionally the way `series.createPriceLine` calls elsewhere on this chart sometimes are) because an out-of-range price line would still render at its own price level with no corresponding visible bar to anchor it to, which reads as orphaned/confusing rather than simply absent."
    },
    {
      "timestamp": "2026-09-19T20:30:00Z",
      "recorded_by": "task-worker",
      "decision": "The tail bar's own `open`/`close` (needed for the 'why the close snapping back to the open matters' explanation, per this task's own description) are looked up from the currently-fetched `/history` bars by matching `tail.tail_date` against each bar's `date` (`kangarooTailBar` in `PriceChart.tsx`), rather than requesting a backend schema change to add `open`/`close` fields to `KangarooTailOut`. `kangarooTailHelp.interpretValue` (`metricHelpContent.ts`) accepts this looked-up bar as an optional parameter and degrades gracefully (describing the pattern from `high`/`low`/`range_multiple`/`suggested_stop` alone, omitting the open/close sentence) when it's `undefined` -- which happens whenever the tail's date isn't covered by the currently selected range/interval, a genuinely different condition from `isKangarooTailInRange` (the bar can be missing on a first render before `/history` resolves even when the tail IS in range).",
      "rationale": "`KangarooTailOut` (per the `backend-kangaroo-tail-pattern` task's own decisions) deliberately exposes only what its *detection* algorithm needed (`high`/`low`, not `open`/`close` -- the shape-matching test only ever reads range/retracement-from-extreme, computed from high/low plus the body-position check documented there) -- reopening that schema for a field the backend detection logic itself never needed would be scope creep onto an already-reviewed, merged task, and the exact same `open`/`close` values are already available for free from `/history`, a query this component already makes. The graceful degradation (rather than requiring `tailBar` and hiding the whole explanation when it's missing) means the legend/balloon still explains something concrete (the range-vs-average comparison, the stop) even when a user is looking at a shorter preset that happens not to include the tail bar's own row in the currently-fetched `/history` response, rather than going silent. Rejected alternative: request a `KangarooTailOut.open`/`.close` addition as a blocking prerequisite on the already-`done`/merged backend task -- rejected as unnecessary backend churn for data this component can derive itself from an endpoint it already calls."
    },
    {
      "timestamp": "2026-09-19T20:30:00Z",
      "recorded_by": "task-worker",
      "decision": "The Kangaroo Tail legend row stays visible (naming the real, current tail) even when `kangarooTailInVisibleRange` is `false`, with `kangarooTailHelp.interpretValue`'s own out-of-range clause explaining why nothing is currently drawn -- the same convention `divergenceHelp`'s legend already uses, not the support/resistance zone legend's 'hide the row entirely once nothing survives the relevance filter' convention.",
      "rationale": "Matches this task's own closest sibling precedent exactly: like a divergence, a confirmed Kangaroo Tail is a single, real, currently-qualifying pattern for this ticker whose out-of-range-ness is a temporary range-selection artifact, not a permanent exclusion the way a zone failing `isZoneRelevant`'s price-distance filter is (that zone was excluded because it's judged not relevant to the current price at all, a standing fact about the zone, not the range). Hiding the Kangaroo Tail legend row entirely at e.g. the 1M preset would read as 'no tail detected for this ticker' rather than the true 'a tail exists, just not in this window', which is worse information for a user than a dimmed row with an explicit note."
    },
    {
      "timestamp": "2026-09-19T20:30:00Z",
      "recorded_by": "task-worker",
      "decision": "No click-to-explain `AnchoredInfoBalloon` was added for the Kangaroo Tail marker itself (unlike divergence's marker-click balloon) -- the legend row's own `MetricHelp` affordance is the only way to open the explanation.",
      "rationale": "The task's own checklist only requires 'MetricHelp/InfoBalloon content ... using this ticker's own actual numbers', which the legend's `MetricHelp` already fully satisfies (same content, same numbers, reachable in one click) -- divergence's additional marker-click path exists because a divergence is drawn as two separate markers plus a connecting line spanning a wide date range, where clicking directly on either extreme is a meaningfully different, more precise interaction than the legend row for a user already looking at that part of the chart; a Kangaroo Tail is a single marker at a single bar, so the legend's own help icon is already exactly as close to the marker as a second click-affordance would be, making a second interaction path redundant complexity rather than added value. Rejected alternative: add `chart.subscribeClick`/`AnchoredInfoBalloon` wiring mirroring divergence's -- rejected as unjustified surface-area growth (a second click-handling effect, a second balloon-anchor state variable) for no real gain in discoverability over the existing legend-only pattern support/resistance zones and false breakouts already use successfully."
    }
  ],
  "git": {
    "branch": "task/frontend-kangaroo-tail-markers",
    "pr_number": 165,
    "pr_url": "https://github.com/rainoko/fintrade/pull/165",
    "pushed_at": "2026-09-19T20:21:37Z"
  },
  "review": {
    "reviewed_at": "2026-09-19T20:28:32Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/165",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "theme.palette.kangarooTail.main (#ad1457, magenta) sits fairly close in hue to signal.sell (#c62828, red) -- theme.test.ts only checks hex inequality, not perceptual distance. Not blocking because the marker shape (square vs. arrowDown) already makes them unambiguous on the chart itself, but worth reconsidering a hue further from the red family in a future pass. Filed as frontend-kangaroo-tail-markers-followups.",
        "file": "frontend/src/theme/theme.ts",
        "line": 122
      }
    ],
    "notes": "Full review: backend pytest (741 passed, 99.92% cov) and frontend vitest (506 passed, 99.7% statements/98.88% branches) both clean and well above the 90% gate -- the worker's claimed 'one pre-existing PortfolioPage flake' did not reproduce here (506/506 green). ruff/mypy/eslint/tsc all clean. code-review (medium) found no correctness bugs, traced the frontend avgRange = barRange / range_multiple math against the backend's actual range_multiple = bar_range / baseline_range formula (app/signals/kangaroo_tail.py) and confirmed it inverts correctly. verify-elder-signal not triggered (area is frontend/pages/stocks, not backend/signals or backend/portfolio) but spot-checked kangarooTailHelp's prose against docs/Analyse.md row 13/line 130-140 (3-bar pattern, 2.5x/10-day baseline, halfway stop, hard next-bar confirmation gate) and the backend's DEFAULT_LOOKBACK=10 -- all consistent. No new module/dependency/component added (existing PriceChart.tsx/metricHelpContent.ts/theme.ts modified only), so architecture-review and the components/common/ vs feature placement check (step 7a) don't apply. No API route/schema changed by this PR (KangarooTailOut already existed from the merged backend-kangaroo-tail-pattern task) -- openapi.json correctly untouched. /history and /analysis both call the same provider.get_daily_ohlcv(ticker) (same SQLite-cached data), and the tail bar is always a past, confirmed bar (never the still-forming latest bar), so the open/close lookup by date cannot see a data-consistency mismatch between the two endpoints. Re-read the checklist wording myself: item 3 only requires MetricHelp/InfoBalloon content with the ticker's own numbers, which kangarooTailHelp.interpretValue fully satisfies via the legend's MetricHelp -- the 'no click-to-explain balloon' decision is a reasonable, non-blocking scope call, not a checklist gap. Browser walkthrough: ran the actual dev app (fixture backend + Vite frontend) and used a Playwright script to intercept AAPL's /analysis response and inject a hand-crafted Kangaroo Tail (fixture data has no naturally-occurring one), then drove the real UI. Confirmed: (1) default 1Y range draws a magenta square marker aboveBar at the tail bar plus a dashed 'Kangaroo Tail stop' price line, axis stays sane (180-300); (2) MetricHelp popup opens and shows the exact numbers from the injected tail (range 1.68, 3.0x the ~0.56 average, open/close, suggested stop 265.60, tip/base) -- content matches the ticker's own actual numbers as required; (3) switching to 1M pushes the tail out of range -- marker and price line both correctly disappear (not just clipped), legend dims and appends '(not in current range)', axis stays sane (no distortion); (4) switching to Max brings it back in range -- marker/price line/undimmed legend all correctly return, axis sane (150-325). No browser console errors, no failed network requests, at any step. Also ran make e2e (16/16 specs passed) as the persisted regression suite alongside the exploratory walkthrough. Decisions array is thorough and well-justified (marker shape/color/position, windowing mirroring isDivergenceInRange, open/close-from-/history sourcing, legend-stays-visible-when-out-of-range convention, no second click-affordance) -- no undecided 'decide X' checklist items found. One non-blocking finding filed as a follow-up task (frontend-kangaroo-tail-markers-followups) per CLAUDE.md's tracked-finding requirement."
  }
}
