{
  "id": "frontend-ticker-link",
  "title": "Every ticker in the UI links to its stock detail page (shared TickerLink component)",
  "area": "frontend/components/common",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": [],
  "references": [
    "frontend/src/features/portfolio/components/PositionsGlanceTable.tsx",
    "frontend/src/features/portfolio/components/PositionsTable.tsx",
    "frontend/src/features/portfolio/components/RiskPanel.tsx",
    "frontend/src/components/common/DataTable/DataTable.tsx"
  ],
  "description": "User request: wherever a ticker appears in the UI, it must be a link to that ticker's stock detail page (/stocks/:ticker), using one shared common/ component rather than each place reimplementing its own link. Current state: PositionsGlanceTable already links its ticker column, but does so with its own inline `<Link component={RouterLink}>` (duplicated logic, no shared component); PositionsTable and RiskPanel both render their ticker column as plain unlinked text (`{ key: 'ticker', header: 'Ticker', sortable: true }`, no render function). Add a new components/common/TickerLink component (ticker text, links to /stocks/:ticker, properly URI-encoded) and use it everywhere a ticker is displayed as a reference to another entity -- including PositionsGlanceTable (refactored to use it instead of its own inline Link), PositionsTable, and RiskPanel. Do NOT link a ticker to itself where it's already the current page's own subject (e.g. the ticker shown in StockDetailPage's own header, or the text typed into TickerSearchBox) -- those aren't references to a different page. Future ticker-displaying features (e.g. the watchlist page) should use this component too, but making sure of that is each of those tasks' own responsibility, not retroactively in scope here.",
  "checklist": [
    { "step": "Create frontend/src/components/common/TickerLink (component + Storybook story + test), following existing common/ component conventions.", "done": true },
    { "step": "Audit every place a ticker is rendered across frontend/src and apply TickerLink to every reference-to-another-entity case, skipping the current-page's-own-subject case (decide the exact boundary and record as a decisions entry if any spot is ambiguous).", "done": true },
    { "step": "Refactor PositionsGlanceTable's existing inline ticker Link to use the new TickerLink component instead of duplicating the logic.", "done": true },
    { "step": "Add a linked ticker column (via TickerLink) to PositionsTable and RiskPanel, which currently render ticker as plain unlinked text.", "done": true },
    { "step": "Update/add tests wherever ticker-rendering behavior changed (PositionsTable/RiskPanel now render an anchor, not plain text) and for TickerLink itself.", "done": true },
    { "step": "Confirm 90% coverage gate still holds and Storybook build succeeds.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-18T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Applied TickerLink to features/watchlist/components/WatchlistTable.tsx (added by the already-merged frontend-watchlist-page, PR #106) in addition to the three sites the task description names (PositionsGlanceTable, PositionsTable, RiskPanel).",
      "rationale": "The task's own description explicitly calls this out: 'Future ticker-displaying features (e.g. the watchlist page) should use this component too, but making sure of that is each of those tasks' own responsibility, not retroactively in scope here.' WatchlistTable's own docstring/decisions (recorded by frontend-watchlist-page) explained it deliberately left ticker as plain text specifically because TickerLink 'doesn't exist yet.' Now that it exists, applying it there is squarely 'wherever a ticker appears in the UI' per this task's title, and the alternative (leaving one of only four ticker-displaying tables unlinked) would defeat the point of having a shared component at all. Rejected alternative: leave WatchlistTable untouched and file a followup task instead -- rejected as needless process overhead for a one-line, zero-risk change already inside this task's stated scope."
    },
    {
      "timestamp": "2026-09-18T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Did not touch PriceChart.tsx, OscillatorChart.tsx, IndicatorsPanel.tsx, ScreensPanel.tsx, or SignalSummary.tsx -- every ticker mention in those files is either a loading/empty-state message string (e.g. 'Loading price history for {ticker}...') or a doc comment, never a rendered, clickable ticker label referencing a different page.",
      "rationale": "All five live inside StockDetailPage, which is already 'the current page's own subject' the task explicitly excludes (same exception as the page's own header and TickerSearchBox's input). A ticker embedded in a loading/empty sentence isn't a UI element a user could click to navigate anywhere, so there is no separate 'reference to another entity' to link -- turning ticker substrings inside prose into links would also be a much bigger, unrequested UI change (partial-text linking) outside this task's scope of adding a shared link *component*."
    },
    {
      "timestamp": "2026-09-18T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "TickerLink's Storybook story wraps itself in a local <MemoryRouter> decorator (scoped to TickerLink.stories.tsx only) rather than adding a router decorator to .storybook/preview.tsx globally.",
      "rationale": "TickerLink is the only common/ component that renders a react-router Link, so a global decorator would be unused overhead for every other story. A per-story decorator keeps the router dependency visible exactly where it's needed, matching how PositionsGlanceTable/PositionsTable/RiskPanel/WatchlistTable's own component tests each wrap themselves in <MemoryRouter> individually rather than relying on a shared global test harness change."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/frontend-ticker-link",
    "pr_number": 109,
    "pr_url": "https://github.com/rainoko/fintrade/pull/109",
    "pushed_at": "2026-09-18T10:31:27Z"
  },
  "review": {
    "reviewed_at": "2026-09-18T10:36:33Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/109",
    "verdict": "accepted",
    "comments": [],
    "notes": "Full review: backend 559 tests/99.86% coverage (untouched, no regression), frontend 262 tests/99.59% statements/99.2% branches (gate 90%, both clean). Static analysis (ruff/mypy/eslint/tsc) all clean. code-review skill (medium effort): no findings -- verified DataTable sort correctness unaffected by new render functions, no double-MemoryRouter wrapping, URI-encoding symmetry with StockDetailPage's useParams decoding, no other ticker-displaying surface missed. e2e suite (make e2e): 13/13 Playwright specs passed. Browser walkthrough (seeded AAPL position + GOOGL watchlist item, drove Chromium directly): confirmed real <a href=\"/stocks/AAPL\"> links and successful navigation from Dashboard's PositionsGlanceTable, Portfolio's PositionsTable and RiskPanel, and Watchlist's WatchlistTable; confirmed StockDetailPage's own <h1> ticker heading is NOT a link (closest('a') === null) and TickerSearchBox's input is plain text -- both correctly excluded; no console errors or failed/4xx/5xx requests during the walkthrough. Placement check (Frontend.md Sec3): TickerLink takes only ticker:string, no fetching, no domain knowledge -- correctly placed under components/common/ with colocated test + 2-variant Storybook story, not a duplicate of anything existing. Scope check: WatchlistTable addition is a reasonable, well-documented scope extension (task description explicitly named the watchlist page as an in-scope future consumer, and WatchlistTable's own prior docstring had already deferred exactly this to this task) -- not creep. Exclusion boundary spot-checked across StockDetailPage's chart/panel components: every ticker mention there is a loading-message string or doc comment, never a clickable label, correctly excluded and documented. OpenAPI: N/A, frontend-only change. Decision-memory cross-check: the one 'decide X' checklist item has three corresponding decisions entries, no undecided judgment call found. No blocking or non-blocking findings; no followup task filed."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-18T10:40:28Z",
    "verdict": "merge",
    "notes": "Re-ran backend pytest (559 passed, 99.86% coverage) and frontend vitest (262 passed, 99.59% statements/99.2% branches) plus `make e2e` (13/13 Playwright specs) -- all match review's reported numbers exactly. eslint/tsc clean. Skimmed the full diff myself: TickerLink is a minimal common/ component, all four consumer sites (PositionsGlanceTable, PositionsTable, RiskPanel, WatchlistTable) correctly wired, StockDetailPage header and TickerSearchBox's input confirmed untouched/plain-text by reading their source directly. Independently drove the one path not covered by the existing e2e suite: a scratch Playwright spec added TSLA to the Watchlist, clicked its TickerLink, and confirmed real navigation to /stocks/TSLA with the correct heading against the real backend (spec removed after verifying). Task bookkeeping (done/ move, index.json mirror) consistent. Accept holds."
  }
}
