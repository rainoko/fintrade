{
  "id": "backend-trade-grading",
  "title": "A-trade grading (buy grade / sell grade / trade grade) for closed trades",
  "area": "backend/api",
  "skill": "add-api-endpoint",
  "state": "done",
  "depends_on": [
    "backend-trade-history-table",
    "backend-channel-envelope-exposure"
  ],
  "references": [
    "docs/Analyse.md",
    "docs/ideas.md"
  ],
  "description": "Elder ch. 55 gives three exact, checkable formulas for grading a completed trade, unlockable once backend-trade-history-table exists: `Buy grade = (day's high - buy price) / (day's high - day's low)` (>50% is 'very good'); `Sell grade = (sell price - day's low) / (day's high - day's low)` (>50% is 'very good'); `Trade grade = (sell price - buy price) / (channel high - channel low, measured on entry day)` -- >=30% capture is an 'A' trade, ~10% a 'C' trade. All three are pure functions once entry/exit price+date exist (from backend-trade-history-table) and that day's OHLC + channel bounds are available (from backend-channel-envelope-exposure). Preferred over grading by raw P&L alone because it accounts for how much was realistically available to capture that day, not just what was captured.",
  "checklist": [
    {
      "step": "Implement the three grade formulas as pure functions taking a closed-trade row + that day's OHLC + channel bounds.",
      "done": true
    },
    {
      "step": "Hand-computed reference test reproducing the book's own worked ADSK example (buy grade 97%, sell grade 35%, trade grade 32%) exactly.",
      "done": true
    },
    {
      "step": "Expose grades on a closed-trade list/detail endpoint (new, or extending whatever endpoint backend-trade-history-table introduced for reading closed trades).",
      "done": true
    },
    {
      "step": "Regenerate backend/openapi.json/frontend types; document the formulas and thresholds in docs/Analyse.md.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "backend-trade-history-table's own task never actually built a closed-trade read endpoint (only DELETE /api/portfolio/positions/{id}, which writes closed_trades rows) -- confirmed via grep across app/api/routers/ before starting. Added a new endpoint, GET /api/portfolio/closed-trades, returning every closed_trades row (most recently exited first: exit_date descending, id descending as a same-day tiebreaker) each annotated with buy_grade_pct/sell_grade_pct/trade_grade_pct.",
      "rationale": "The checklist item itself says 'new, or extending whatever endpoint backend-trade-history-table introduced for reading closed trades' -- since that task introduced none, 'new' is the only applicable branch, not a judgment call between two real options. Ordering newest-first (rather than _ordered_positions's entry_date-ascending convention used for the positions list) was chosen because trade *history* is read for recency (what did I just close, and how did it grade) rather than portfolio composition -- mirrors WatchlistResponse's own oldest-first-for-a-different-reason precedent in spirit (each list picks the order that matches how it's actually consumed) rather than blindly copying _ordered_positions's ascending convention for a list serving a different purpose."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Grading logic lives in a new app/portfolio/grading.py module: three pure formula functions (buy_grade_pct/sell_grade_pct/trade_grade_pct, each returning float | None) plus grade_trade (combines all three, pure) and grade_closed_trade (the data-fetching wrapper: takes a ticker's already-fetched daily_ohlcv frame + a closed trade's entry/exit price+date, derives the eight grade_trade arguments -- that day's own high/low for entry and exit, plus the entry-day Autoenvelope channel bounds via app.indicators.autoenvelope.autoenvelope -- and returns an all-None TradeGrade rather than raising when entry_date/exit_date isn't an exact row in the (drop_malformed_daily_bars-filtered) frame). The router's own new helper, _grade_closed_trades, fetches each distinct ticker's daily_ohlcv at most once per request (shared across multiple closed trades for that ticker) and degrades a whole ticker's grades to all-None on a DataProviderError, mirroring _compute_position_signal's/enrich_positions_with_price's existing per-ticker degrade-gracefully convention.",
      "rationale": "Matches this codebase's established pure-domain-function-plus-thin-router-glue split (app.portfolio.risk/exits hold pure formulas over an already-fetched frame; the router owns all I/O and per-ticker fetch/cache/degrade logic) rather than folding I/O into the grading module itself, which would make grade_trade's formulas harder to unit-test in isolation (the ADSK reference-value test needs zero I/O) and would duplicate the fetch-once-per-ticker sharing logic that already exists as a pattern (_realized_losses_this_month_pct/_compute_position_signal) rather than reusing it. grade_closed_trade (not grade_trade directly) is the router's actual call site because a closed trade's grade needs values (day OHLC, channel bounds) derived from a whole-history frame, not just handed eight bare floats -- keeping that derivation as its own testable function (rather than inlined in the router) lets tests/unit/test_portfolio_grading.py's TestGradeClosedTrade class verify the wiring (correct rows/columns pulled for the right dates) independently of the router/DB/HTTP layer, consistent with how autoenvelope()/ema() etc. are unit-tested without a FastAPI TestClient anywhere in the stack. Rejected alternative: compute grades inline in the GET /api/portfolio/closed-trades handler itself -- rejected because it would mix HTTP/DB concerns with the actual math in one function, making both harder to test and diverging from every other computation in this codebase (protective_stop, evaluate_exit_flags, realized_losses_pct) that instead lives in app.portfolio.* as testable pure(-ish) functions the router calls."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "grade_closed_trade calls app.signals.engine.drop_malformed_daily_bars(daily_ohlcv) with its default require_full_ohlc_on_latest_bar=True (i.e. every bar, not just the latest, needs a full non-NaN OHLC) before looking up entry_date/exit_date -- unlike GET /api/portfolio/risk's more lenient require_full_ohlc_on_latest_bar=False call.",
      "rationale": "entry_date/exit_date are essentially never the very latest bar in the ticker's currently-fetched history (a closed trade's exit date is, at the newest, 'recent', not literally 'today' by the time this endpoint is called later), so the lenient carve-out GET /api/portfolio/risk needs specifically for its own latest-bar desync concern doesn't apply here -- what grading actually needs is a real, non-NaN high/low for two arbitrary historical days, which is exactly what the stricter default guarantees across the whole frame. Using the lenient variant instead would risk silently computing a grade from a malformed (NaN open/high/low, valid-close-only) bar on a day that happens to still be the latest fetched bar, producing NaN-poisoned or wrong grade values instead of the intended null-when-ungradeable behavior."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "ClosedTradeOut.exit_reason is typed as a new schemas.py Literal (ExitReasonOut, duplicating ExitReason's 8 string values) rather than importing app.portfolio.models.ExitReason directly into schemas.py.",
      "rationale": "Matches this module's own established convention: Signal/Trend/Impulse/ConfidenceBand are all Literal type aliases defined directly in schemas.py rather than importing the domain-layer types they mirror (app.signals.engine has no such enums to import from in the first place, by design -- see docs/architecture/Backend.md's layering), and schemas.py has zero existing imports from app.portfolio or app.signals anywhere. Rejected alternative: import ExitReason directly as the field type (as DELETE /api/portfolio/positions/{id}'s Query(...) param already does, but that's a router, not the shared schemas.py module) -- rejected because it would be the first domain-type import into schemas.py, a layering precedent this task doesn't need to set just to avoid duplicating 8 short string literals that (per ExitReason's own docstring) are themselves meant to be a stable, rarely-changing taxonomy."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "No frontend client function (frontend/src/api/portfolio.ts) or MSW mock handler was added for GET /api/portfolio/closed-trades in this PR -- only backend/openapi.json and the regenerated frontend/src/api/types.ts changed on the frontend side.",
      "rationale": "docs/tasks/frontend-trade-journal.json already exists as a separate, already-planned task (depends_on: [backend-trade-history-table, backend-trade-grading], skill add-frontend-feature) whose entire scope is building the actual UI consumer of this endpoint, including its own MetricHelp-explained grade display, placement decision, and tests -- adding an unused client function/mock here now would be dead code with no consumer until that task lands, and duplicate/preempt a placement and data-shape decision that task's own skill (add-frontend-feature) is meant to make deliberately. Matches the identical, already-accepted precedent set by backend-channel-envelope-exposure (PR #146), whose review explicitly confirmed 'no UI-facing surface in this PR... no existing frontend code reads the new fields yet' was correct scoping, not an omission."
    }
  ],
  "git": {
    "branch": "task/backend-trade-grading",
    "pr_number": 149,
    "pr_url": "https://github.com/rainoko/fintrade/pull/149",
    "pushed_at": "2026-09-19T11:01:50Z"
  },
  "review": {
    "reviewed_at": "2026-09-19T11:07:11Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/149",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Independently re-derived the book's ADSK example by hand from the review prompt's own inputs (buy $51.77 on high $52.49/low $51.75; sell $53.78 on high $54.49/low $53.39; entry-day channel upper $53.87/lower $47.61) and got 97.297...% / 35.454...% / 32.108...%, matching the claimed 97%/35%/32% and the exact formulas in app/portfolio/grading.py (buy_grade_pct/sell_grade_pct/trade_grade_pct) and their reference-value test (tests/unit/test_portfolio_grading.py::TestGradeTradeReferenceValues) exactly, including the entry-day-not-exit-day channel measurement for trade_grade_pct."
      },
      {
        "summary": "Confirmed grade_closed_trade/_grade_closed_trades never raise/500 on missing data: entry_date/exit_date not an exact row, a malformed entry-day bar, warm-up-window NaN channel bounds, and a per-ticker DataProviderError all degrade to null grade fields on an otherwise fully-populated closed-trade row, each covered by both unit tests (TestGradeClosedTrade) and integration tests (TestGetClosedTradesGradingDegradesGracefully)."
      },
      {
        "summary": "The strict (require_full_ohlc_on_latest_bar=True) malformed-bar filtering decision is sound: entry/exit dates are essentially never the latest fetched bar for a closed trade, so the stricter default (needed for a real, non-NaN high/low on two arbitrary historical days) doesn't create the false-null risk the lenient GET /api/portfolio/risk carve-out exists to avoid."
      },
      {
        "summary": "backend/openapi.json and frontend/src/api/types.ts are genuinely in sync -- re-ran `python scripts/export_openapi.py` and `yarn generate:api-types` against the checked-out branch and both produced zero diff. GET /api/portfolio/closed-trades has response_model, operation_id, summary, and a full docstring; it declares no responses={} error cases, which is correct and consistent with GET /api/portfolio's own precedent (no path params, no request body, and grading degrades gracefully rather than raising, so there's genuinely no documentable error case beyond FastAPI's default). schemas.py's new fields (ClosedTradeOut, ExitReasonOut) all carry descriptions per the API documentation standard, and ExitReasonOut's 8 literals match ExitReason's 8 enum values exactly.",
        "file": "backend/app/api/routers/portfolio.py"
      },
      {
        "summary": "Full suites pass and exceed the 90% gate: backend 639 passed, 99.89% coverage (grading.py and the router additions both 100%); frontend 349 passed, coverage 99.6-100% across the board. ruff/mypy/eslint/tsc all clean. scripts/validate_tasks.py reports the task board clean.",
        "file": "backend/app/portfolio/grading.py"
      },
      {
        "summary": "No UI surface in this PR (only the generated frontend/src/api/types.ts changed on the frontend side, confirmed via git diff --stat -- frontend/) -- the step-9 browser walkthrough and step-9a persisted e2e suite are correctly skipped, and the decision to defer the actual frontend consumer to the already-planned frontend-trade-journal task is recorded with rationale.",
        "file": "frontend/src/api/types.ts"
      },
      {
        "summary": "Non-blocking (efficiency, not correctness): per-ticker daily-history fetch is shared across a ticker's closed-trade rows in _grade_closed_trades's frames dict, but grade_closed_trade's more expensive per-call work -- drop_malformed_daily_bars over the whole frame and the full autoenvelope() rolling-window computation -- is redundantly recomputed once per row instead of once per ticker, so a ticker with many round-trip closed trades does O(n) redundant dropna/autoenvelope passes over an identical frame. Tracked as docs/tasks/backend-trade-grading-followups.json rather than left only here.",
        "file": "backend/app/api/routers/portfolio.py",
        "line": 179
      }
    ],
    "notes": "Checked out PR #149. Ran backend pytest --cov (639 passed, 99.89%, grading.py 100%) and frontend vitest --coverage (349 passed, 99.6-100%) -- both match the PR's own claims exactly. ruff/mypy/eslint/tsc all clean. Re-ran scripts/export_openapi.py and yarn generate:api-types against the branch: zero diff, confirming both committed snapshots are genuinely current, not just claimed. Ran architecture-review: app/portfolio/grading.py follows the established pure-domain-function-plus-thin-router-glue pattern (mirrors app/portfolio/risk.py/exits.py), the router stays thin, schemas.py holds all shape/description, no new dependency, no coverage-config changes. Ran verify-elder-signal's spirit against docs/Analyse.md/docs/ideas.md's own ch. 55/ch. 33 notes (line 144-146, 109) -- the implemented formulas, the >50%/>=30%/~10% thresholds, and the entry-day-specific channel measurement for trade_grade_pct all match the source material verbatim, and docs/Analyse.md/docs/architecture/API.md were both updated in this PR to describe the new endpoint/formulas accurately. Ran the code-review skill (medium) against the diff in the background; it independently corroborated no correctness bugs and surfaced the one efficiency finding recorded above. All 4 checklist items are done with 5 decisions entries covering every judgment call made (new-vs-extend endpoint, module split, strict malformed-bar filtering, ExitReasonOut vs importing ExitReason, deferring the frontend consumer) -- no checklist item is phrased as 'decide X' without a matching decisions entry. No frontend components were added or changed in this PR (only generated types.ts), so the frontend component-placement check (step 7a) doesn't apply. No human PR comments/reviews existed on PR #149 before this review. One non-blocking finding filed as docs/tasks/backend-trade-grading-followups.json (new, since no prior follow-up task existed for this id) rather than left only as a PR comment."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-19T11:12:30Z",
    "verdict": "merge",
    "notes": "Independently re-ran backend pytest --cov=app (639 passed, 99.89% coverage, grading.py 100%) and frontend vitest --coverage (49 files / 349 passed, 99.69% stmt coverage) -- both match pr-reviewer's numbers exactly. Hand-derived the book's ADSK example myself from the prompt's own inputs against grading.py's actual formulas (buy_grade_pct/sell_grade_pct/trade_grade_pct in app/portfolio/grading.py) and got 97.29729729729688% / 35.45454545454546% / 32.108626198083044%, matching both the ~97/35/32 targets and TestGradeTradeReferenceValues's asserted values to floating-point precision. Skimmed the full diff (git diff origin/main...HEAD): grade_closed_trade/_grade_closed_trades never raise -- entry/exit date lookups, drop_malformed_daily_bars filtering, and per-ticker DataProviderError all degrade to null grade fields on an otherwise-populated row (confirmed via TestGetClosedTradesGradingDegradesGracefully); GET /api/portfolio/closed-trades declaring no responses={} error cases is consistent with GET /api/portfolio/risk's identical precedent (no path params/body, degrades rather than raises). Ran ruff/mypy/eslint/tsc myself: mypy/eslint/tsc clean; ruff flagged 6 pre-existing issues in files this PR never touches (test_stooq_provider.py, test_autoenvelope.py, test_elder_ray.py, test_force_index.py -- confirmed absent from the PR's diff --stat), so not attributable to this PR. scripts/validate_tasks.py reports the task board clean, and backend-trade-grading-followups.json genuinely exists with the efficiency finding tracked as a checklist item. Accept holds."
  }
}
