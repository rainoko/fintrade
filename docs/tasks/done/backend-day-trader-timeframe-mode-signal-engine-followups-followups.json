{
  "id": "backend-day-trader-timeframe-mode-signal-engine-followups-followups",
  "title": "Non-blocking finding from PR #315's review (backend-day-trader-timeframe-mode-signal-engine-followups)",
  "area": "backend/signals",
  "skill": null,
  "state": "done",
  "depends_on": ["backend-day-trader-timeframe-mode-signal-engine-followups"],
  "references": [
    "docs/tasks/done/backend-day-trader-timeframe-mode-signal-engine-followups.json",
    "https://github.com/rainoko/fintrade/pull/315"
  ],
  "description": "Non-blocking finding from pr-reviewer's full review of PR #315 (backend-day-trader-timeframe-mode-signal-engine-followups), which accepted the PR -- backend suite (1341 passed, 100% line/branch coverage), ruff/mypy/profit-target-wording-guard/validate_tasks.py all clean, no API surface touched (no OpenAPI regen needed), no UI surface (no browser walkthrough/e2e needed). The `score_volume_confirmation` keep-as-is rationale (rolling-average-scale-mismatch argument) was independently checked and is analytically sound: `analyse_day_trader` maps `intermediate_ohlcv` onto `analyse()`'s `daily_ohlcv` parameter, so both `latest_volume` and its 20-period rolling average are computed from the same (intermediate-leg) series in day-trader mode exactly as in swing mode, and swapping only the numerator to `short_term_ohlcv`'s much-finer-grained latest bar while leaving the denominator computed from the coarser intermediate leg would indeed systematically bias that arm toward reading 'below average' (a finer bar's volume is mechanically smaller than a coarser leg's per-bar rolling average), confirming the PR's reasoning rather than just trusting it.",
  "checklist": [
    {
      "step": "`docs/Analyse.md` §6's \"Volume confirmation ... trigger bar volume is above 20-day average\" row is not updated to document the day-trader-mode-specific behavior this PR's `decisions` entry and `app/signals/engine.py` docstring/comment now describe in detail: that the volume-confirmation component deliberately continues comparing the *intermediate* leg's latest bar/rolling average even in day-trader mode, not the actual short-term leg Trigger itself now evaluates against. `docs/Analyse.md` §2's Screen 3 bullet and §9's \"partially resolved\" note both got a parallel day-trader-mode footnote when Screen 3 itself changed (PR #312) -- §6's table has no equivalent footnote for this narrower, OR'd-with-Force-Index-spike component, so a reader of Analyse.md alone (without cross-referencing `engine.py`'s docstring or this task's `decisions` entry) would not learn that \"trigger bar volume\" is not literally what day-trader mode computes here. Not blocking (the original checklist item that prompted this PR only asked for a decision to be recorded, which it was, via docstring/inline-comment/`decisions` entry), but worth adding a short parallel footnote to §6 (or a cross-reference to the `decisions` entry) so the methodology doc stays the single source of truth without requiring a reader to already know to look elsewhere.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-25T01:53:19Z",
      "recorded_by": "task-worker",
      "decision": "Added a day-trader-mode footnote to docs/Analyse.md §6's \"Volume confirmation\" table row, appended to the existing \"Scoring\" cell rather than as a separate table or a numbered footnote marker below the table.",
      "rationale": "Matched the existing style §2's Screen 3 bullet (line 77) and §9's Screen 3 note (line 384) already use for this exact kind of day-trader-mode caveat: a bolded '**Day-trader mode** (`task-id`): ...' clause appended directly onto the pre-existing prose it qualifies, in-line, rather than a separate footnote apparatus -- both of those precedents modify the sentence/bullet in place instead of adding an asterisk/superscript marker plus a below-the-table note. A markdown table cell can hold arbitrarily long prose (this doc already has far longer single lines elsewhere, e.g. §2's Force Index bullet), so no table-structure change was needed. Content-wise, the footnote restates (in the reader-facing terms §6 itself uses -- 'intermediate leg' vs 'short-term leg', 'rolling average', 'trigger bar volume') the same keep-as-is rationale already recorded in app/signals/engine.py's `analyse()` docstring paragraph near `short_term_ohlcv` and in `backend-day-trader-timeframe-mode-signal-engine-followups`'s own `decisions` entry (the rolling-average-scale-mismatch argument, independently re-verified sound by both that task's pr-reviewer and pr-decision passes), and points back to that task by id for the full derivation rather than duplicating the entire multi-paragraph rationale into the methodology doc itself. This closes the gap PR #315's review flagged: a reader of Analyse.md alone, without cross-referencing engine.py or the task board, can now learn that 'trigger bar volume' is not literally what day-trader mode computes for this component, matching what §2/§9 already do for Screen 3 itself."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/backend-day-trader-timeframe-mode-signal-engine-followups-followups",
    "pr_number": 322,
    "pr_url": "https://github.com/rainoko/fintrade/pull/322",
    "pushed_at": "2026-09-25T01:54:36Z"
  },
  "review": {
    "reviewed_at": "2026-09-25T01:59:11Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/322",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Confirmed this is genuinely a docs+task-board-only diff via `git diff main...HEAD --stat` (docs/Analyse.md, this task's own JSON, docs/tasks/index.json -- no backend/frontend code, no OpenAPI snapshot).",
        "file": "docs/Analyse.md",
        "line": 227
      },
      {
        "summary": "Read the new §6 footnote against `backend/app/signals/engine.py`'s `analyse()` docstring paragraph (lines 405-415) and inline comment at the `volume = daily_ohlcv[\"volume\"]` call site (lines 645-649), plus `backend-day-trader-timeframe-mode-signal-engine-followups`'s `decisions` entry -- the footnote accurately restates that both halves of the volume-confirmation OR deliberately keep reading the intermediate leg's latest bar/20-period rolling average, not the short-term leg, and correctly attributes the rolling-average-scale-mismatch rationale (a single finer-grained bar's volume being mechanically smaller than a coarser leg's rolling average). No misstatement or oversimplification found.",
        "file": "backend/app/signals/engine.py",
        "line": 649
      },
      {
        "summary": "Style genuinely matches §2's Screen 3 bullet (line 77) and §10's 'partially resolved' note (line ~384, not §9 as this PR's own decisions entry and PR description mislabel it -- see non-blocking finding below): a bolded '**Day-trader mode** (`task-id`): ...' clause appended in-line to the existing prose, not a separate footnote-marker apparatus. Consistent with precedent.",
        "file": "docs/Analyse.md",
        "line": 77
      },
      {
        "summary": "Re-ran scripts/check_profit_target_wording.py (clean, 264 files scanned), scripts/validate_tasks.py (clean, 315 task files), the full backend suite (1400 passed, 100% line/branch coverage via --cov-fail-under=90), ruff check (clean), mypy (clean), frontend eslint (clean), and frontend tsc -b --noEmit (clean). No regression. No API/schema surface touched, so no OpenAPI regen needed. No UI surface (docs/task-board only) -- browser walkthrough and the persisted e2e suite (steps 9/9a) don't apply, noted explicitly rather than silently skipped. Ran the code-review skill against the diff: one non-blocking finding (see below), no correctness bugs.",
        "file": null,
        "line": null
      },
      {
        "summary": "Non-blocking: this PR's own new `decisions` entry (and its PR description's 'Test plan' line) cites the Screen 3 'partially resolved' precedent as living in '§9' (Data Sources (free)) when it actually lives in §10 (Open Questions / Next Steps) -- confirmed directly against docs/Analyse.md's own section headers (line 381 is `## 10. Open Questions / Next Steps`; the cited line 384 falls inside it, not inside §9 at lines 346-380). This mislabeling was already present in the checklist item text inherited from PR #315's review rather than introduced fresh, but this PR's decisions entry repeats it as a freshly-asserted citation without catching it. Doesn't affect the actual docs/Analyse.md §6 footnote content added (which is accurate and doesn't itself cite §9/§10), only a task-JSON metadata citation -- tracked as a follow-up rather than blocking.",
        "file": "docs/tasks/done/backend-day-trader-timeframe-mode-signal-engine-followups-followups.json",
        "line": 24
      }
    ],
    "notes": "Docs+task-board-only PR, confirmed via git diff --stat. The new docs/Analyse.md §6 footnote was independently verified accurate against backend/app/signals/engine.py's docstring/inline comment and the referenced decisions entry -- no misstatement or oversimplification. Style matches §2/§10's existing day-trader-mode footnote format (the task's own dispatch instructions and this PR's decisions entry call the §10 precedent '§9', which is a pre-existing mislabeling this PR repeats but didn't introduce -- filed as a non-blocking follow-up, not blocking since it doesn't affect the actual Analyse.md content). Full backend suite (1400 tests, 100% coverage), backend ruff/mypy, frontend eslint/tsc, profit-target wording guard, and task-board validation all independently re-run and clean. No API surface touched (no OpenAPI regen needed); no UI surface (browser walkthrough/e2e steps explicitly noted as not applicable). code-review skill run against the diff surfaced the same §9/§10 finding and nothing else. One non-blocking finding filed on docs/tasks/backend-day-trader-timeframe-mode-signal-engine-followups-followups-followups.json rather than left only in this PR's comments."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-25T02:03:07Z",
    "verdict": "merge",
    "notes": "Re-verified independently: `git diff main...HEAD --stat` shows only docs/Analyse.md, docs/tasks/index.json, and task JSON files (this task's move to done/ plus the new followups-followups-followups task) -- no code/OpenAPI diff. Read the new §6 footnote directly against engine.py's analyse() docstring (lines 405-415) and inline comment at the `volume = daily_ohlcv[\"volume\"]` call site (lines 645-649): accurate, both match. Confirmed line 381 of docs/Analyse.md is literally `## 10. Open Questions / Next Steps` (line 384's 'partially resolved' note sits inside it, not §9 at lines 346-380), so the §9/§10 mislabel finding is real, and it's confined to a decisions-entry citation, not the §6 content itself -- genuinely non-blocking, and its followup task (backend-day-trader-timeframe-mode-signal-engine-followups-followups-followups.json, state planned, index.json mirrored) is correctly filed. Re-ran scripts/check_profit_target_wording.py (clean, 264 files) and scripts/validate_tasks.py (clean, 316 task files) myself. Re-ran the full backend suite: 1400 passed, 100% line/branch coverage -- matches pr-reviewer's reported numbers exactly."
  }
}
