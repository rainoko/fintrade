{
  "id": "frontend-daily-homework-page",
  "title": "'Am I ready to trade?' daily self-test form",
  "area": "frontend/pages",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": ["backend-daily-homework-self-test"],
  "references": [
    "docs/ideas.md#daily-homework--am-i-ready-to-trade-self-test-ch-57"
  ],
  "description": "A small daily form for the ch. 57 psychological readiness self-test, showing the color-coded (red/yellow/green) summary score, backed by backend-daily-homework-self-test.",
  "checklist": [
    { "step": "Build a form for the 5 questions with the book's own 0/1/2 answer options (feeling physically, yesterday's trading result, planning done, mood, schedule busyness).", "done": true },
    { "step": "Show the summed score with the book's own color-banding (<=4 red, 5-6 yellow, 7-8 green, 9-10 yellow).", "done": true },
    { "step": "Wire to the backend endpoint via a TanStack Query hook.", "done": true },
    { "step": "Decide page placement (a new standalone route vs. part of an existing page/dashboard) and record.", "done": true },
    { "step": "Component tests covering each color band.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Placement: a new standalone route (`/homework`, `DailyHomeworkPage`) with its own top-level `NavDrawer` entry ('Daily Homework'), not folded into `DashboardPage` as an embedded card.",
      "rationale": "Every existing card DashboardPage composes (RiskSummaryCard, SellFlaggedPositionsCard, PositionsGlanceTable) is derived from the exact same `usePortfolio`/`usePortfolioRisk` data the Portfolio page already fetches -- DashboardPage.tsx's own doc comment is explicit that it's 'an at-a-glance summary composed entirely from data/hooks the Portfolio page and its risk panel already wired... no new API calls'. The daily homework self-test is a genuinely separate concern with its own backend resource (`daily_homework_entries`, three new endpoints) and no portfolio/ticker data of its own, the same shape of 'standalone destination with no ticker/position to drill in from' that justified `/methodology` getting its own nav entry despite also being a small, self-contained page (frontend-methodology-explainer's own precedent). Rejected embedding it as a Dashboard widget: it would mix an unrelated new data-fetch into a page explicitly scoped to reusing existing portfolio hooks, and a 5-question form (five selects + a submit button) doesn't fit the compact 'at a glance' card format the rest of that page uses -- it would either dominate the page or need to be collapsed/hidden, adding its own placement complexity for no benefit over a one-click nav destination. A standalone page also leaves room for this feature to grow (a history/trend view over `GET /api/daily-homework`, not built in this task) without further crowding the Dashboard."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "`DailyHomeworkForm`'s initial answers come from two blocking queries resolved before the interactive form ever mounts (`useDailyHomeworkToday`, `useYesterdayTradingSuggestion`), not from a `useEffect` that seeds local state after the component mounts.",
      "rationale": "An effect-based 're-seed local state from a query result once it arrives' pattern is exactly the case React's own guidance (and this repo's `eslint-plugin-react-hooks` `react-hooks/set-state-in-effect` rule, which failed the build on the first draft of this component) calls out as unnecessary: the seed value is already computable during render once both queries have settled, so the child `HomeworkQuestionsForm` component simply mounts with that value as its initial `useState`, never re-synced afterward. Waiting for both queries (both fast, local-only endpoints) before rendering the interactive fields was chosen over rendering immediately with a neutral default and letting a later-arriving suggestion silently overwrite an in-progress answer -- the latter would need the same forbidden effect-based approach to implement correctly (or a manual 'has the user touched this field yet' flag purely to work around the timing race), for a one-time, sub-second wait that's imperceptible in practice. A failed `useYesterdayTradingSuggestion` fetch is treated as 'no suggestion available' (silently falls back to the neutral default) rather than blocking the form or showing an error, since it's a pure optional enhancement over Elder's fully-manual version of that one question -- only `useDailyHomeworkToday`'s failure blocks (showing `common/ErrorState`), since not knowing whether today's entry already exists could otherwise produce an incorrect Save/Update experience."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Every question defaults to the neutral middle answer (1) when there's no already-recorded entry for today, except `yesterday_trading_score`, which instead defaults to `GET /api/daily-homework/yesterday-trading-suggestion`'s `suggested_score` whenever one is available.",
      "rationale": "Defaulting every question to 0 would render the form already showing a 'red'/'don't trade' band before the user has answered anything, which misrepresents an unanswered form as the worst possible score; defaulting to 2 is the opposite, overly optimistic bias. 1 (neutral/middle) is the only value that doesn't bias the initial band in either direction. `yesterday_trading_score` is the one exception because the backend already computes a genuine, data-derived suggestion for it specifically so 'most days require zero extra typing' for that one question (per `backend-daily-homework-self-test`'s own `decisions` entry) -- using the neutral default there instead would silently discard that suggestion endpoint's entire purpose. An already-recorded entry for today always takes priority over both the neutral default and the suggestion, since it's the user's own previously-submitted true answer, never silently replaced."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "`HomeworkScoreBanner` (the red/yellow/green summary banner) lives under `features/homework/components/`, not `components/common/`, despite taking only a plain `{ totalScore, band }` prop with no fetch of its own.",
      "rationale": "Unlike `components/common/SignalBadge` (a pure color-by-string-union chip with no interpretive text), this banner bakes in wording specific to this self-test's own meaning of 'red'/'yellow'/'green' -- notably that the *same* 'yellow' band means two different things depending on `totalScore` (5-6 'trade cautiously' vs. 9-10 Elder's own 'with everything so perfect, any change is bound to be for the worse' caution), which only makes sense in the context of this specific 0-10 homework score. No other domain in this app currently has red/yellow/green bands with this meaning (the closest analog, `confidence_band`, uses a completely different Low/Medium/High scale via `common/ConfidenceGauge`), so there's no real current reuse case to justify hoisting it to `common/`; if a second red/yellow/green-with-interpretive-copy use case appears later, consolidating at that point (per the skill's own near-duplicate guidance) is preferable to speculatively generalizing now."
    }
  ],
  "git": {
    "branch": "task/frontend-daily-homework-page",
    "pr_number": 271,
    "pr_url": "https://github.com/rainoko/fintrade/pull/271",
    "pushed_at": "2026-09-22T16:51:25Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T17:10:59Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/271",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Backend: ruff/mypy clean, 1194 tests pass, 100% line/branch coverage (no backend changes in this PR -- it consumes the already-merged backend-daily-homework-self-test endpoints, confirmed backend/openapi.json needs no regeneration since no schema/route changed).",
        "file": null,
        "line": null
      },
      {
        "summary": "Frontend: eslint/tsc clean. vitest --coverage: 820/820 tests pass (84/84 files), 99.76% statements / 99.3% branches / 99.78% functions / 99.82% lines, well above the 90% gate. A run under full parallel load intermittently timed out one unrelated pre-existing test (AddPositionDialog.test.tsx's 'sends the trimmed notes as entry_notes' case, not touched by this PR) -- reproduced the identical timeout on a clean `main` checkout in a separate git worktree, confirming it's a pre-existing environment-timing flake unrelated to this change, not a regression. All 820 tests pass with `--testTimeout=20000`.",
        "file": null,
        "line": null
      },
      {
        "summary": "e2e suite (`make e2e`): 21/21 specs pass (dashboard, navigation, portfolio, stock-analysis, watchlist). No spec exists yet for the new /homework flow itself -- filed as a non-blocking follow-up (frontend-daily-homework-page-followups) rather than blocking, since the flow is covered by MSW component tests plus this review's own real-browser walkthrough.",
        "file": null,
        "line": null
      },
      {
        "summary": "Real-browser walkthrough (fixture-mode backend + vite dev server, driven via a Playwright script since no interactive Playwright MCP tool was available this session): nav entry renders and routes correctly, form loads with the neutral default (1) for every question except yesterday_trading_score prefilled from the suggestion endpoint, submitting persists the entry (verified surviving a full page reload), and all four color bands render correctly with distinct, methodology-faithful copy -- red '0/10 -- Don't trade today', low-yellow '5/10 -- Trade cautiously', green '7/10 -- Good to trade', high-yellow '9/10' with Elder's own 'with everything so perfect...' caution text, verbatim-matching docs/ideas.md's ch. 57 entry. No console errors, no failed network requests observed.",
        "file": "frontend/src/features/homework/components/HomeworkScoreBanner.tsx",
        "line": null
      },
      {
        "summary": "Verified the data-loading decision (blocking on both useDailyHomeworkToday/useYesterdayTradingSuggestion before HomeworkQuestionsForm mounts, seeding via initial useState only) does not produce a stale-initial-value bug: HomeworkQuestionsForm's initialScores prop is only consumed by useState's initializer, never re-synced by an effect, so a later background refetch (e.g. after the invalidateQueries a successful submit triggers) cannot silently overwrite in-progress answers -- confirmed by reading the component and by the browser walkthrough (editing answers across multiple submits never reset unrelated fields).",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.tsx",
        "line": 216
      },
      {
        "summary": "Accessibility: each of the 5 Select fields uses the InputLabel+labelId pattern already established elsewhere in this codebase (ClosePositionDialog.tsx, TradeApgarDialog.tsx), giving each combobox a correct accessible name -- confirmed via role-based queries in both the component tests and the browser walkthrough. Submit errors route through the existing common/ErrorState (role=\"alert\").",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.tsx",
        "line": null
      },
      {
        "summary": "Placement decisions all check out against Frontend.md §3 and this codebase's own precedent: DailyHomeworkForm/HomeworkQuestionsForm correctly stay feature-specific (domain-specific, fetch-owning); the two-components-per-file shape has existing precedent (ScreensPanel.tsx, TradeJournalPanel.tsx, FundamentalDataPanel.tsx). HomeworkScoreBanner's common/-vs-feature placement call (feature-specific, since its 'yellow' wording is specific to this 0-10 score and there's no current second red/yellow/green-with-interpretive-copy use case) is well-reasoned and its comparison to common/ConfidenceGauge (a genuinely different Low/Medium/High scale) holds up on inspection.",
        "file": "frontend/src/features/homework/components/HomeworkScoreBanner.tsx",
        "line": null
      },
      {
        "summary": "All 4 recorded `decisions` entries are substantive (what + rationale + rejected alternatives), and the one checklist item phrased as 'decide' (page placement) has a matching decisions entry -- no undocumented judgment call found.",
        "file": "docs/tasks/frontend-daily-homework-page.json",
        "line": null
      },
      {
        "summary": "Non-blocking: no e2e spec for the new /homework flow, and HomeworkScoreBanner's two 'yellow' bands share an identical icon/color and are distinguished only by body text (a fast glance could conflate the 5-6 'trade cautiously' and 9-10 'too perfect' cases, which mean very different things). Both filed as checklist items on frontend-daily-homework-page-followups rather than left only in this comment.",
        "file": null,
        "line": null
      },
      {
        "summary": "code-review (medium effort, backgrounded) finding, verified: DailyHomeworkForm's loading gate is `todayQuery.isLoading || suggestionQuery.isLoading`, so the whole 5-question form (not just the yesterday-trading field) stays behind LoadingState until *both* queries settle. In production (main.tsx's real QueryClient sets `retry: 1`, unlike tests' `retry: false`), a slow/failing suggestion endpoint adds a real, if bounded, extra delay before any field renders -- larger than the 'sub-second, imperceptible' wait this task's own decisions entry assumes for the happy path. Not a correctness bug (no wrong data, no permanent hang, still falls back to the neutral default once settled) and the decisions entry does explicitly choose to wait on 'two blocking queries' by design, so treated as non-blocking, but the assumption that this is always imperceptible doesn't hold under retry/backoff -- filed as a follow-up.",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.tsx",
        "line": 221
      },
      {
        "summary": "code-review (medium effort, backgrounded) finding, verified: when today's entry already exists, `scoresFromEntry(existingEntry)` is used and `suggestedScore` is discarded entirely in that branch -- yet the render gate still waits on `suggestionQuery` to settle before showing anything. A user re-opening the page after already recording today's entry waits on a network call whose result is provably unused for their case. Real, avoidable inefficiency (skip waiting on `suggestionQuery` once `todayQuery` resolves with a non-null `entry`), but bounded and non-data-affecting -- filed as a follow-up rather than blocking.",
        "file": "frontend/src/features/homework/components/DailyHomeworkForm.tsx",
        "line": 228
      }
    ],
    "notes": "Checked out PR #271. Backend: ruff/mypy clean, pytest 1194 passed, 100% coverage. Frontend: eslint/tsc clean, vitest --coverage 820/820 passed (99.76%/99.3%/99.78%/99.82%, gate is 90%) -- one unrelated test flaked under parallel load and was confirmed to reproduce identically on a clean `main` worktree, ruling it out as a regression from this PR. e2e suite (make e2e): 21/21 passed. Real-browser walkthrough performed via a standalone Playwright script (no interactive Playwright MCP tool available this session) against a freshly started fixture-mode backend + vite dev server: golden path (nav, prefill, submit, reload-persistence) and all 4 color bands verified visually and via DOM assertions, no console errors or failed requests. Cross-checked the 5-question wording and exact color-band thresholds/copy against docs/ideas.md's ch. 57 entry -- verbatim match. All 4 `decisions` entries are substantive; the 'decide placement' checklist item has a matching decision. No backend/schema changes in this PR, so backend/openapi.json correctly needs no regeneration. The backgrounded code-review skill (medium effort) returned two findings about DailyHomeworkForm's loading gate (blocks the whole form on the optional suggestion query; still waits on that query even when its result is provably unused because today's entry already exists) -- verified both directly against the code and main.tsx's real `retry: 1` QueryClient config, judged real but bounded (no wrong data, no permanent hang, no crash) so non-blocking, and added to the follow-up task alongside the two findings from my own manual review. Four non-blocking findings total filed as docs/tasks/frontend-daily-homework-page-followups.json rather than left only as PR comments -- none change the accept verdict."
  }
}
