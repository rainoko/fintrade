{
  "id": "data-provider-stooq-followups",
  "title": "Data provider: Stooq — non-blocking review follow-ups",
  "area": "backend/data",
  "skill": null,
  "state": "done",
  "depends_on": ["data-provider-stooq"],
  "references": [
    "docs/tasks/data-provider-stooq.json#review",
    "https://github.com/rainoko/fintrade/pull/19",
    "docs/architecture/Backend.md#2-module-layout"
  ],
  "description": "Non-blocking findings from pr-reviewer's review(s) of PR #19 (data-provider-stooq), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges.",
  "checklist": [
    {
      "step": "Fix _to_stooq_symbol (backend/app/data/stooq_provider.py) treating any '.' in the ticker as an existing market suffix, so a literal dotted class-share ticker like 'BRK.B' skips the '.us' suffix and produces the wrong Stooq symbol (Stooq/yfinance both use a hyphen for class shares, e.g. brk-b.us). Low impact since this app's own ticker convention already uses hyphens, so it only bites on non-canonical input, but worth a fix + regression test.",
      "done": true
    },
    {
      "step": "Remove the unused `import urllib.error` in backend/app/data/stooq_provider.py — all transport failures go through the broad `except Exception` in _fetch_daily/_fetch_csv, never a specific urllib.error type. Dead code.",
      "done": true
    },
    {
      "step": "Add app/data/exceptions.py to docs/architecture/Backend.md §2's module-layout diagram — the module's existence is well-justified and recorded in data-provider-stooq's decisions, just not reflected in the architecture doc's diagram yet.",
      "done": true
    },
    {
      "step": "backend/tests/unit/data/test_provider_contract.py still scopes `_PROVIDER_CLASSES` to StooqProvider only, and its docstring still says YFinanceProvider is an unmerged stub on main. That premise no longer holds: PR #18 (data-provider-yfinance) merged to main before this branch's own 2026-09-15 merge-conflict-resolution commit, and this task's decisions/index.json both already record yfinance as done. Per the original decisions entry's own stated plan ('adding YFinanceProvider once it lands on main is a one-line change'), append YFinanceProvider to _PROVIDER_CLASSES with its own mocked-fetch fixture and update the docstring, so the 'shared contract test proving interchangeability' checklist item is actually cross-provider rather than Stooq-only.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-16T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "_to_stooq_symbol now treats a trailing '.us' as the only case of an already-fully-qualified Stooq symbol; every other '.' in the input ticker is converted to '-' (Stooq/yfinance's class-share notation) before the '.us' suffix is appended, instead of the old rule of 'any dot means don't touch it'.",
      "rationale": "This provider only ever targets US equities (docs/Analyse.md §9 default market), so '.us' is the one legitimate pre-supplied suffix an existing test (test_does_not_double_suffix_ticker_that_already_has_a_market_suffix) already depends on being passed through unchanged. Treating every other dot as class-share notation rather than as a suffix fixes 'BRK.B' -> 'brk-b.us' (previously 'brk.b', an invalid Stooq symbol) without breaking that existing suffix-passthrough case, and without needing a country-code allowlist this app has no other use for.",
      "alternatives_considered": "Considered maintaining an allowlist of known non-US market suffixes (.uk, .de, etc.) to distinguish 'already suffixed' from 'class share', but rejected it as unnecessary complexity: this app's data providers are documented as US-equity-only, so '.us' is the only suffix that can legitimately already be present."
    },
    {
      "timestamp": "2026-09-16T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Extended backend/tests/unit/data/test_provider_contract.py's parametrized protocol-conformance tests to cover both StooqProvider and YFinanceProvider, and added two new dedicated shape-output tests for YFinanceProvider (daily/weekly) alongside the existing Stooq ones, rather than trying to parametrize the shape-output tests themselves across both classes.",
      "rationale": "The two providers mock at different boundaries (StooqProvider._fetch_csv vs yf.Ticker), so a single parametrized shape test body can't drive both without an awkward per-class dispatch table; separate, symmetrical test methods (mirroring test_yfinance_provider.py's own mocking pattern) are simpler to read and keep each provider's contract-shape assertion self-contained. Reused the yfinance fixtures already on main (tests/fixtures/yfinance/aapl_daily.csv, aapl_weekly.csv) rather than recording new ones, since they already satisfy the 'own mocked-fetch fixture' requirement from the task checklist. Rejected: a single dispatch-table-driven parametrized test across both provider classes, which would have obscured each provider's distinct mocking boundary behind indirection for no real gain given there are only two providers."
    }
  ],
  "git": {
    "branch": "task/data-provider-stooq-followups",
    "pr_number": 53,
    "pr_url": "https://github.com/rainoko/fintrade/pull/53",
    "pushed_at": "2026-09-16T00:00:00Z"
  },
  "review": {
    "reviewed_at": "2026-09-16T10:05:21Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/53",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Independently verified the Stooq class-share ticker convention claim: Yahoo Finance's real ticker for Berkshire Hathaway Class B is 'BRK-B' (hyphen), and Stooq mirrors the same US-equity ticker spelling lowercased with a '.us' suffix, so 'brk-b.us' is the correct Stooq symbol -- 'brk.b.us' (the pre-fix output) is not a symbol Stooq recognizes. Attempted to confirm directly against Stooq's live CSV endpoint but it's behind a JS proof-of-work bot challenge; did not attempt to solve/bypass that (out of scope for a review, and consistent with this project's own 'no live network calls in tests' policy anyway). Confidence in the fix rests on well-established, independently-checkable domain knowledge of Yahoo/Stooq's shared US-equity ticker convention, not on trusting the PR's own docstring.",
        "file": "backend/app/data/stooq_provider.py",
        "line": 108
      },
      {
        "summary": "Full backend suite: 460 passed. stooq_provider.py at 100% line/branch coverage, overall 99.83% -- well above the 90% gate. No ruff/mypy config exists in backend/pyproject.toml, so no lint/type-check step beyond pytest+coverage was run, per CLAUDE.md's 'don't invent tool config that isn't there' instruction.",
        "file": "backend/pyproject.toml"
      },
      {
        "summary": "All four checklist items genuinely implemented and match their decisions entries: the .us-only-passthrough fix with a real regression test (BRK.B -> brk-b.us, confirmed by direct call), the dead urllib.error import removed (grepped clean across app/ and tests/), app/data/exceptions.py added to Backend.md's module diagram with an accurate one-line description, and test_provider_contract.py now genuinely parametrizes both StooqProvider and YFinanceProvider (protocol conformance + separate shape-output tests per provider, matching the decisions entry's stated rationale for not trying to share one parametrized shape test across two different mocking boundaries).",
        "file": "backend/tests/unit/data/test_provider_contract.py"
      },
      {
        "summary": "No API route/schema touched by this PR -- backend/openapi.json correctly untouched, no regeneration needed.",
        "file": "backend/openapi.json"
      },
      {
        "summary": "No frontend exists yet and this PR has no UI-facing surface (an internal data-provider symbol-normalization fix) -- browser walkthrough explicitly skipped as inapplicable rather than silently omitted.",
        "file": "backend/app/data/stooq_provider.py"
      },
      {
        "summary": "code-review skill (medium effort, single-pass): found one genuine edge case -- _to_stooq_symbol's `endswith('.us')` early return short-circuits before the new dot-to-hyphen conversion, so a ticker with both a class-share dot and an explicit '.us' suffix (e.g. 'BRK.B.US') still produces the invalid 'brk.b.us' instead of 'brk-b.us'. Confirmed directly (`StooqProvider._to_stooq_symbol('BRK.B.US')` -> `'brk.b.us'`). Pre-existing (the old any-dot-means-passthrough rule produced the identical wrong result for this input), not a regression introduced by this PR, and a narrow doubly-non-canonical input -- non-blocking. Tracked as docs/tasks/data-provider-stooq-followups-followups.json.",
        "file": "backend/app/data/stooq_provider.py",
        "line": 108
      },
      {
        "summary": "Own finding: narrowing the passthrough case to only a trailing '.us' is a real (if narrow) behavior change -- an already-correctly-suffixed non-US ticker like 'SAP.DE' previously passed through unchanged is now mangled to 'sap-de.us' (confirmed: `StooqProvider._to_stooq_symbol('SAP.DE')` -> `'sap-de.us'`). Low real-world impact given this app's providers are documented/decided as US-equity-only (this task's own decisions entry, and data-provider-stooq's original decision), and the decisions entry explicitly considered and rejected a country-code allowlist as unnecessary complexity -- a reasonable, deliberate tradeoff, not a bug, but worth a visible test/doc note rather than only living in a decisions-array rationale. Non-blocking. Tracked as docs/tasks/data-provider-stooq-followups-followups.json.",
        "file": "backend/app/data/stooq_provider.py",
        "line": 108
      },
      {
        "summary": "Decisions cross-check: both checklist items requiring judgment (the .us-vs-class-share normalization rule, and how to structure the cross-provider contract test) have corresponding decisions entries with rationale and rejected alternatives -- no undocumented judgment call.",
        "file": "docs/tasks/data-provider-stooq-followups.json"
      }
    ],
    "notes": "No blocking findings. Tests, coverage, and the ticker-convention claim all independently verified; two narrow non-blocking edge cases found (one pre-existing, one a documented scope tradeoff) and filed as docs/tasks/data-provider-stooq-followups-followups.json."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-16T10:08:09Z",
    "verdict": "merge",
    "notes": "Re-ran full backend suite myself: 460 passed, 99.83% overall coverage, stooq_provider.py 100%. Called StooqProvider._to_stooq_symbol directly for BRK.B -> brk-b.us (fix confirmed), aapl.us -> aapl.us (passthrough preserved), BRK.B.US -> brk.b.us and SAP.DE -> sap-de.us (both non-blocking edge cases reproduced exactly as pr-reviewer described, correctly filed to data-provider-stooq-followups-followups.json). Skimmed the full diff (stooq_provider.py, both test files, Backend.md); confirmed urllib.error import removed and no ruff/mypy config exists in backend/pyproject.toml."
  }
}
