{
  "id": "frontend-trade-journal-followup-review",
  "title": "Surface trades due for their two-months-later follow-up review",
  "area": "frontend/pages/portfolio",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": ["backend-trade-journal-followup-review"],
  "references": [
    "docs/ideas.md#expanding-the-trade-journal-entry-notes-a-two-months-later-follow-up-and-strategy-segmented-equity-curves-ch-59"
  ],
  "description": "Surface trades the backend now identifies as due for a two-months-later follow-up review, and let the user record that review.",
  "checklist": [
    { "step": "Add a 'due for follow-up' indicator/section in TradeJournalPanel (or a dedicated panel) listing trades due for review.", "done": true },
    { "step": "Add a small form/dialog to record follow_up_notes for a given trade.", "done": true },
    { "step": "Component tests for both: the due-list rendering and submitting a follow-up review.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Built a dedicated `TradeFollowUpDuePanel` component (GET /api/portfolio/closed-trades?due_for_follow_up=true) rendered on PortfolioPage alongside — not inside — `TradeJournalPanel`, rather than folding the due-list into `TradeJournalPanel` itself.",
      "rationale": "TradeJournalPanel's whole DataTable is sorted/keyed around every closed trade ever, with one row per trade and a Notes column tucked in among nine other columns; the due-for-follow-up list is a small, action-oriented subset (usually 0-1 rows) whose entire point is prompting an action (open a review dialog), not another column to scan in a wide historical table. A second `DataTable` instance with its own three columns (ticker/exit date/realized P&L) plus a 'Record Review' action column reads far more like SellFlaggedPositionsCard's existing 'positions needing attention' precedent than like another row of TradeJournalPanel. Folding it into TradeJournalPanel (e.g. a highlighted row style or an inline action per row) was considered and rejected: it would require plumbing the review dialog's open/selected-trade state through a component that already owns nine columns of rendering logic, and would make the 'due right now' set need to be recomputed client-side from the same `useClosedTrades()` unfiltered response rather than just querying the endpoint's own `due_for_follow_up` filter directly — two fetches (one the API already offers a dedicated query parameter for) is simpler than one fetch plus client-side date-window recomputation duplicating backend logic the endpoint already encapsulates."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "`useDueForFollowUpTrades()` is a separate TanStack Query hook/key (`portfolioKeys.dueForFollowUpTrades`, nested one level under `portfolioKeys.closedTrades`) from the existing `useClosedTrades()`, rather than one hook taking a parameter with a single shared cache entry.",
      "rationale": "The unfiltered journal table and the due-list are genuinely different server responses (different query strings, different item sets) that both need to be independently cached and independently refetchable — TanStack Query already models 'the same resource under different filters' as distinct query keys (see how `usePortfolioRisk`/`useClosedTrades` are already separate top-level keys nested under `portfolioKeys.all` for exactly this reason). Nesting `dueForFollowUpTrades` one level under `closedTrades` (rather than a same-level sibling) means `useRecordFollowUpReview`'s existing broad `portfolioKeys.all` invalidation (matching the app's established convention of invalidating the whole `['portfolio']` prefix on any portfolio-affecting write) refreshes both the full journal and the due-list in one call via TanStack Query's default prefix-matching invalidation, with zero new invalidation logic required in the mutation hook itself. A single `useClosedTrades(dueForFollowUp?: boolean)` hook keying its query solely off a fixed key (ignoring the parameter) was considered and rejected — it would either silently share one cache entry between two genuinely different response shapes (a bug: switching between the filtered and unfiltered view would show stale data from whichever was fetched first) or require the caller to thread a variable key manually anyway, which the two-named-hooks approach does more explicitly and readably at each call site."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "`FollowUpReviewDialog` closes immediately (calling `onClose`) on a successful `POST .../follow-up-review`, with no intermediate 'success' screen/message — unlike `AddPositionDialog`, which shows an explicit 'Added X' / 'Merged into X' message before the user clicks Done.",
      "rationale": "AddPositionDialog's success message exists because the outcome is ambiguous from the user's own input alone (a duplicate ticker silently merges rather than creating a new position, per API.md's documented merge behavior) — the message is the *only* way the user learns which of two outcomes happened. Recording a follow-up review has exactly one outcome (the note is saved) with no such hidden branching to disclose, and the visible, immediate effect (the trade disappearing from the 'Due for Follow-Up Review' list once the due-query refetches) already confirms success without an extra modal step end users would need to dismiss. An AddPositionDialog-style intermediate success screen with a 'Done' button was considered and rejected as an unnecessary extra click for an action with no ambiguous outcome to disclose."
    }
  ],
  "git": {
    "branch": "task/frontend-trade-journal-followup-review",
    "pr_number": 248,
    "pr_url": "https://github.com/rainoko/fintrade/pull/248",
    "pushed_at": "2026-09-22T04:16:16Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T04:30:55Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/248",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "No Playwright e2e spec exercises the new 'Due for Follow-Up Review' panel/FollowUpReviewDialog flow end to end -- portfolio.spec.ts wasn't extended and no new spec was added, even though this adds a new API round trip and query filter the running e2e-suite backend now serves live. Non-blocking (thorough component tests plus a live headless-Chromium walkthrough both confirm the flow works); tracked as a checklist item on frontend-trade-journal-followup-review-followups so it isn't lost once this PR merges.",
        "file": "frontend/tests/e2e/portfolio.spec.ts",
        "line": null
      }
    ],
    "notes": "Full review. Backend pytest --cov: 1139 passed, 99.95% coverage (unchanged -- no backend files touched by this PR; confirmed backend/openapi.json and the generated frontend/src/api/types.ts already carry FollowUpReviewIn/due_for_follow_up from the already-merged backend-trade-journal-followup-review PR #221, so no regeneration was needed here). Frontend vitest run --coverage: 745 passed/76 files, statements 99.73%/branches 99.21%/functions 99.74%/lines 99.79% (all new files -- TradeFollowUpDuePanel.tsx, FollowUpReviewDialog.tsx, useRecordFollowUpReview.ts -- at 100%). static-verify: ruff, mypy, eslint, tsc -b, and the profit-target stale-wording guard all clean. Read app/api/routers/portfolio.py's get_closed_trades/record_follow_up_review and app/api/schemas.py's FollowUpReviewIn directly: the due_for_follow_up query param, the 8-10-week inclusive window, the POST body shape, and the 404 case all match what frontend/src/api/portfolio.ts sends/expects exactly. Traced the new mutable MSW closed-trades store (handlers.ts) for state leakage: resetPortfolioStore() (called in every consuming test's beforeEach) cascades into resetClosedTradesStore(), and the relative-dated fixture row (isoDateWeeksAgo(9), recomputed fresh each read via Date.now()) sits comfortably 1 full week inside both edges of the 8-10-week due window, so it isn't a boundary-flake risk. code-review skill (medium effort) launched against the diff; independent manual line-by-line read of every changed file found no correctness bugs and confirmed strong reuse (DataTable/TickerLink/SignedCurrency/ErrorState/LoadingState all reused as-is, no near-duplicate of an existing common/ component introduced). Placement check (Frontend.md Sec.3): both new components reference ClosedTradeOut/trade-id domain concepts directly, correctly placed under features/portfolio/components/, not components/common/ -- no placement finding. e2e-test skill: yarn playwright install chromium confirmed present, ports 8000/5173 freed first, make e2e ran all 18 specs green (dashboard/navigation/methodology/portfolio/stock-analysis/watchlist), including the app's new closed-trades?due_for_follow_up=true call firing cleanly on every portfolio-page visit. Live browser walkthrough (real headless Chromium via the installed playwright package, backend on FINTRADE_DATA_PROVIDER_MODE=fixture + a scratch sqlite db, frontend on the real Vite dev server, closed-trade rows seeded via the real add-position/delete-position endpoints then backdated 9 weeks via direct DB edit to land in the due window): the due panel renders the seeded trade (ticker link, exit date, realized P/L, Record Review button); clicking it opens FollowUpReviewDialog pre-titled with the ticker; typing notes and clicking Save Review closes the dialog immediately with no intermediate confirmation screen (matching the 3rd decisions entry) and the trade visibly drops off the due list, confirmed still gone after a full page reload; console messages and network responses were clean throughout (no errors, no 4xx/5xx outside the deliberate 404 test). Edge case: submitting blank notes, then whitespace-only notes, both correctly show the 'Follow-up notes are required.' validation error, leave the dialog open, and fire no network request -- verified live in the browser, not just in the mocked unit test. All three decisions entries (dedicated panel vs. folding into TradeJournalPanel; separate nested query key vs. one parameterized hook; immediate close on success vs. an AddPositionDialog-style success screen) are present, each with a concrete rationale including the alternative considered and rejected -- no undecided judgment call, and no checklist item phrased as 'decide X' was left without a matching decisions entry. scripts/validate_tasks.py clean both before and after this review's own task-board edits. One non-blocking finding (missing e2e spec for the new flow) filed as a checklist item on the new frontend-trade-journal-followup-review-followups task rather than left only in this comment."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-22T04:39:11Z",
    "verdict": "merge",
    "notes": "Independently re-ran both suites: backend pytest --cov 1139 passed/99.95% coverage, frontend vitest run --coverage 745 passed/76 files with statements 99.73%/branches 99.21%/functions 99.74%/lines 99.79% -- both match pr-reviewer's numbers exactly, and eslint/tsc -b were re-run clean. Skimmed every changed file in git diff origin/main...HEAD: confirmed no backend files touched, verified the due_for_follow_up query param/8-10-week window/POST follow-up-review body+404 shape in app/api/routers/portfolio.py and schemas.py match frontend/src/api/portfolio.ts exactly, confirmed resetPortfolioStore() cascades into resetClosedTradesStore() and the isoDateWeeksAgo(9) fixture is recomputed fresh via Date.now() each reset (no boundary-flake risk), and confirmed all three decisions entries have concrete rationale with a rejected alternative. Verified frontend-trade-journal-followup-review-followups.json exists with the e2e-spec-gap checklist item tracked. No blocking issues found; accept holds."
  }
}
