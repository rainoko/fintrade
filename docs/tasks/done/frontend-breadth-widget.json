{
  "id": "frontend-breadth-widget",
  "title": "Watchlist \"personal breadth\" indicator widget",
  "area": "frontend/pages/watchlist",
  "skill": "add-frontend-feature",
  "state": "done",
  "depends_on": [
    "backend-watchlist-breadth-proxy"
  ],
  "references": [
    "frontend/src/features/watchlist/components",
    "docs/ideas.md"
  ],
  "description": "Surface the new personal-breadth proxy (backend-watchlist-breadth-proxy) as a small widget on the watchlist page (e.g. '9 of 15 watchlist tickers are currently BULLISH') -- explicitly framed as a lightweight stand-in for true market breadth (NH-NL etc.), not the real thing, since it only reflects the user's own tracked tickers rather than the whole market. Be honest about this distinction in the explanation content rather than letting it read as more authoritative than it is.",
  "checklist": [
    {
      "step": "Design and implement the breadth widget on the watchlist page.",
      "done": true
    },
    {
      "step": "MetricHelp/inline content explicitly framing this as a personal-watchlist proxy, not true market breadth, with a one-line explanation of why (no broad-universe data source, per docs/ideas.md).",
      "done": true
    },
    {
      "step": "Tests + Storybook story; confirm 90% coverage gate holds.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-20T00:37:34Z",
      "recorded_by": "task-worker",
      "decision": "New feature component `PersonalBreadthCard` under `features/watchlist/components/` (not `components/common/`), rendered directly on `WatchlistPage` above the existing `WatchlistTable`, owning its own `useWatchlistBreadth` (`GET /api/watchlist/breadth`) fetch/loading/error state -- following the exact same pattern as `features/portfolio/components/RiskSummaryCard.tsx` on the Dashboard page (a feature card that owns its own query and is composed directly into a thin page).",
      "rationale": "Every value this widget renders (a tracked ticker, a Screen 1/Tide trend count) is a Triple Screen/watchlist domain concept, not a generic display primitive, so it fails Frontend.md \u00a73's common/-vs-feature placement test for `components/common/` the same way RiskSummaryCard/SellFlaggedPositionsCard do -- it composes existing common/ primitives (StatCard, MetricHelp, EmptyState, ErrorState, LoadingState) rather than duplicating any of their behavior. Rejected alternative: bolting the widget's markup directly into WatchlistPage.tsx itself, which would violate Frontend.md \u00a73's 'pages stay thin, no business logic' rule the same way DashboardPage avoids inlining RiskSummaryCard's own logic."
    },
    {
      "timestamp": "2026-09-20T00:37:34Z",
      "recorded_by": "task-worker",
      "decision": "No Storybook story added for `PersonalBreadthCard`.",
      "rationale": "The add-frontend-feature skill's own Storybook requirement (Frontend.md \u00a74) is scoped to `components/common/` components -- confirmed by precedent: `RiskSummaryCard.tsx` and `SellFlaggedPositionsCard.tsx` (the two closest analogues, same 'feature card that owns its own query' shape) have colocated tests but no `.stories.tsx` file. `PersonalBreadthCard` isn't a domain-agnostic display primitive callers pass props into (it fetches its own data internally), so a story would need MSW-mocked network state Storybook doesn't provide out of the box here, the same reason those two cards don't have one either. Its every state (loading, error, empty, mixed breakdown, all-unavailable, singular-count wording) is instead covered directly in `PersonalBreadthCard.test.tsx` against MSW-mocked responses."
    },
    {
      "timestamp": "2026-09-20T00:37:34Z",
      "recorded_by": "task-worker",
      "decision": "`watchlistKeys.breadth` (`['watchlist', 'breadth']`) nests under `watchlistKeys.all`, so `useAddWatchlistItem`/`useRemoveWatchlistItem`'s existing `invalidateQueries({ queryKey: watchlistKeys.all })` also refetches the breadth query via TanStack Query's prefix-matching invalidation. A portfolio-side edit (adding/removing a position on the Portfolio page) is deliberately *not* wired to invalidate this query, even though the breadth denominator spans the watchlist+portfolio union.",
      "rationale": "Mirrors `portfolioKeys.risk`'s own nesting rationale (features/portfolio/hooks/queryKeys.ts) for the watchlist-side edits, which are the common case for a widget placed on the Watchlist page itself. Cross-invalidating from `useAddPosition`/`useDeletePosition` (in `features/portfolio/hooks/`) would require either that feature importing `watchlistKeys` (a feature-to-feature dependency Frontend.md's module layout doesn't otherwise have anywhere) or a new shared cross-feature query-key module -- both heavier than accepting that the breadth widget can be up to `staleTime` (60s, `main.tsx`'s global QueryClient default, already the accepted staleness window used by e.g. `useStockAnalysis`) stale after a portfolio-only edit made on a different page. Rejected alternative: a standalone top-level `['breadth']` query key not nested under either feature's prefix, which would lose the free invalidation-on-watchlist-edit behavior entirely for no benefit."
    },
    {
      "timestamp": "2026-09-20T00:37:34Z",
      "recorded_by": "task-worker",
      "decision": "Widget layout: a heading ('Personal Breadth (Watchlist + Portfolio)') with a `MetricHelp` balloon, three `StatCard`s (Bullish/Bearish/Neutral, each showing count + percentage), an `EmptyState` when nothing is tracked yet, and a small caption footnote naming `unavailable_count` when it's non-zero -- rather than a single-sentence widget matching the task description's literal example ('9 of 15 watchlist tickers are currently BULLISH').",
      "rationale": "The backend's response spans three trend buckets (BULLISH/BEARISH/NEUTRAL) plus an explicit unavailable count, not just a single BULLISH fraction -- showing only the BULLISH count would silently discard the BEARISH/NEUTRAL breakdown and the honesty-relevant unavailable-ticker signal the backend task deliberately added (rather than guessing or dropping unavailable tickers). Reused `StatCard` (already used identically by RiskSummaryCard/DashboardPage) rather than inventing a new stat-tile component, per the skill's 'check for an existing common/ component before building a new one' step. The dynamic sentence form ('X of Y tracked tickers are currently BULLISH...') from the task description instead lives in the `MetricHelp` balloon's `valueInterpretation` (`personalBreadthHelp.ts`), where it's paired with the full BULLISH/BEARISH/NEUTRAL/unavailable breakdown plus the honest personal-proxy framing, rather than being the widget's only visible content."
    },
    {
      "timestamp": "2026-09-20T01:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed the blocking `alignItems` finding by switching to `sx={{ alignItems: 'center' }}` on the inner `<Stack>` (PersonalBreadthCard.tsx), and bundled the two non-blocking grammar fixes tracked on frontend-breadth-widget-followups into the same commit rather than leaving them for a separate followups PR.",
      "rationale": "The `alignItems` fix is the exact convention every other `<Stack>` usage in the repo already follows, per pr-reviewer's finding -- no alternative considered. For the two grammar fixes (footnote pluralization keyed off tracked_ticker_count instead of unavailable_count; 'is'/'are' agreement keyed off the computable-ticker count), bundled them since the dispatching instruction called them cheap to bundle while already touching these exact two files for the blocking fix; both required updating the verbatim strings pinned in PersonalBreadthCard.test.tsx's existing assertions. Verified with `yarn tsc -b --noEmit` specifically (not plain `tsc --noEmit`, since that's the config that missed the original bug), the full frontend suite (534 tests, coverage unchanged at ~99%+), `yarn eslint .`, and `yarn build`, all clean."
    }
  ],
  "git": {
    "branch": "task/frontend-breadth-widget",
    "pr_number": 171,
    "pr_url": "https://github.com/rainoko/fintrade/pull/171",
    "pushed_at": "2026-09-20T00:39:05Z"
  },
  "review": {
    "reviewed_at": "2026-09-20T01:21:21Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/171",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "Confirmed fix: PersonalBreadthCard.tsx:45 now uses sx={{ alignItems: 'center' }} instead of the bare alignItems=\"center\" prop -- matches the exact convention used identically by OscillatorChart.tsx, SignalSummary.tsx, PriceChart.tsx, and TradeJournalPanel.tsx. yarn tsc -b --noEmit and yarn build both now exit 0 clean (re-ran both myself specifically, not the weaker plain tsc --noEmit that missed this originally). A live browser walkthrough (Playwright against the fixture-provider dev stack) shows zero console errors/warnings on WatchlistPage load, and make e2e's watchlist.spec.ts (which exercises GET /api/watchlist/breadth) passes with no console warning either -- no longer reproduces.",
        "file": "frontend/src/features/watchlist/components/PersonalBreadthCard.tsx",
        "line": 45
      },
      {
        "summary": "Confirmed fix: the unavailable-count footnote now pluralizes 'ticker(s)' off tracked_ticker_count (the total population the noun phrase attaches to) instead of unavailable_count. Correct and matches PersonalBreadthCard.test.tsx's updated assertion.",
        "file": "frontend/src/features/watchlist/components/PersonalBreadthCard.tsx",
        "line": 78
      },
      {
        "summary": "Confirmed fix: personalBreadthHelp.ts's unavailableClause now pluralizes off trackedTickerCount too, consistent with the computable-ticker clause's own pattern.",
        "file": "frontend/src/features/watchlist/components/personalBreadthHelp.ts",
        "line": 53
      },
      {
        "summary": "Non-blocking (new, found via code-review this round): the 'is'/'are' fix resolves the exact case the original finding cited (computable=1, bullishCount=1) but keys the verb off computable (the denominator) rather than bullishCount (the actual grammatical subject of 'X ... are currently BULLISH'). Still wrong whenever bullishCount and computable differ, e.g. bullish_count=1, computable=4 renders '1 of 4 computable tickers (25.0%) are currently BULLISH' (should be 'is'), and wrong again when bullish_count=0, computable=1 (renders 'is' for a zero count). The only test added (computable=1 AND bullishCount=1, which can't distinguish the two variables) doesn't catch this. Cosmetic -- doesn't affect computed values, tests, lint, or build -- so not blocking, but tracked as a fresh finding on frontend-breadth-widget-followups rather than left only in this comment.",
        "file": "frontend/src/features/watchlist/components/personalBreadthHelp.ts",
        "line": 57
      }
    ],
    "notes": "Re-review (retry round 1) of PR #171 after the prior needs_work verdict. Re-ran everything from scratch on the current branch tip (commit 2acf19c): backend pytest 746 passed, 99.92% coverage; frontend vitest 99.55%/98.66%/99.65%/99.64% (statements/branches/functions/lines), all >=90% gate. Backend ruff/mypy clean. Frontend yarn lint clean. yarn tsc -b --noEmit exits 0 (the exact command the original miss was about) and yarn build also exits 0 clean (864KB bundle chunk-size warning is pre-existing/unrelated, not an error). Read the fix diff directly: the Stack alignItems change is correct and idiomatic, confirmed against 5 other identical sx={{ alignItems: 'center' }} usages elsewhere in the codebase (OscillatorChart.tsx, SignalSummary.tsx x3, PriceChart.tsx x7, TradeJournalPanel.tsx). The two bundled grammar fixes (footnote pluralization off tracked_ticker_count; personalBreadthHelp's unavailableClause pluralization) are both genuinely correct, and frontend-breadth-widget-followups.json's checklist items 1-2 are correctly marked done; item 3 (is/are agreement) is only a partial fix -- correct for the literal case the original finding cited but still buggy in general (see comments above; a fresh checklist entry was added rather than marking item 3 undone, since the original narrow claim it made is in fact resolved). Live browser walkthrough via Playwright against the fixture-data dev stack: WatchlistPage renders the Personal Breadth widget (3 StatCards + footnote), opened the MetricHelp balloon and confirmed its dynamic sentence renders correctly against live backend data (tracked=15, unavailable=13, computable=2, all-NEUTRAL), zero console errors/warnings/pageerrors and zero failed network requests throughout. make e2e (16/16 specs, including watchlist.spec.ts's add/view/remove flow exercising GET /api/watchlist/breadth) passes clean with no console-error regression. No backend/API changes in this PR, so no openapi.json regeneration applies. No checklist item phrased as 'decide X' was left undecided. No pr_decision present from a prior pass to clear. Accepting; the one new non-blocking finding is filed on frontend-breadth-widget-followups (still 'planned', now with 1 of 4 items outstanding)."
  },
  "pr_decision": {
    "confirmed_at": "2026-09-20T01:26:45Z",
    "verdict": "merge",
    "notes": "Independently re-ran on branch tip (commit e745990): yarn tsc -b --noEmit exits 0, yarn build exits 0 (same pre-existing 864KB chunk-size warning only), yarn eslint . clean, yarn vitest run --coverage: 56 files/534 tests passed, coverage 99.55%/98.66%/99.65%/99.64% -- exact match to pr-reviewer's reported numbers. Backend pytest also re-run: 746 passed, 99.92% coverage, matching exactly (no backend changes in this PR's diff). Read the full diff (git diff main...task/frontend-breadth-widget) myself: confirmed PersonalBreadthCard.tsx:45 uses sx={{ alignItems: 'center' }}, and spot-checked the convention is real (not just claimed) by grepping the codebase -- 13 files including OscillatorChart.tsx, PriceChart.tsx, SignalSummary.tsx, TradeJournalPanel.tsx use the identical sx={{ alignItems: 'center' }} pattern, none use the bare alignItems= prop on a Stack. Also independently confirmed the one filed non-blocking finding (is/are keyed off `computable` instead of `bullishCount` in personalBreadthHelp.ts's valueInterpretation ternary) is real from reading the diff, and it's correctly tracked as an open (not done) item on frontend-breadth-widget-followups.json -- genuinely cosmetic, doesn't affect any computed value/test/build, so it doesn't change the merge verdict."
  }
}
