{
  "id": "backend-profit-target-weekly-channel-followups-followups",
  "title": "Non-blocking follow-ups from backend-profit-target-weekly-channel-followups PR #223 review",
  "area": "backend/signals",
  "skill": null,
  "state": "done",
  "depends_on": ["backend-profit-target-weekly-channel-followups"],
  "references": [
    "docs/tasks/backend-profit-target-weekly-channel-followups.json",
    "https://github.com/rainoko/fintrade/pull/223",
    "scripts/check_profit_target_wording.py"
  ],
  "description": "Non-blocking findings from pr-reviewer's full review of PR #223 (backend-profit-target-weekly-channel-followups, the check_profit_target_wording.py guard script), recorded here per CLAUDE.md's convention so they aren't lost once the PR merges. All six were verified as real, demonstrated gaps in the guard's own robustness (confirmed by directly constructing inputs and running the affected functions), not currently-live false positives/negatives against the real repo tree -- and critically, none of them defeat the guard's actual protection for the 8 known regression-prone files: the per-file positive check (check_known_files_mention_weekly) operates on whole-file text at the character-window level, not per physical line, so it independently still catches a reversion in any of those 8 files even where the repo-wide negative check's line-based scanning is fooled (verified directly by reverting schemas.py's `source` field to the old stale wording, wrapped across lines matching the file's own dominant style for long Field descriptions elsewhere -- the negative check missed it, the positive check still caught it). That's why these are follow-ups rather than blocking findings.",
  "checklist": [
    {
      "step": "check_banned_patterns() scans each physical line independently (text.splitlines()), so it misses a banned phrase split across two source lines by Python implicit string concatenation -- a style already used throughout backend/app/api/schemas.py's own IndicatorsOut field descriptions (ATR, ADX, +DI/-DI, channel_upper/channel_lower, RSI all wrap this way), just not (currently) ProfitTargetOut's own fields. Verified directly: a description=(\"...today's \"\\n    \"Autoenvelope/channel height...\") input produces zero violations from check_banned_patterns(). This only matters for a brand-new file outside the 8-file known-locations list (the known 8 are independently protected by the positive per-file check regardless of line-wrapping, verified directly) -- worth having the negative check join adjacent lines (or scan the whole file text with re.DOTALL/single-string search) before matching, so its own docstring's 'anywhere in the repo, including a brand-new file' claim actually holds.",
      "done": true
    },
    {
      "step": "_WEEKLY_RE = re.compile(r'\\bweek(ly)?\\b') doesn't match the plural 'weeks' on its own (\\b right after the non-'ly' branch fails since 'weeks' has no boundary between 'week' and the following 's') -- verified directly: _WEEKLY_RE.search('100 weeks') is None while _WEEKLY_RE.search('100 weekly') matches. Currently harmless since every real occurrence in the repo pairs 'weeks' with 'weekly' nearby, but a future, fully-correct edit phrasing the warm-up window as e.g. '~100 weeks of price history' without the adjective 'weekly' anywhere nearby would be wrongly flagged as a stale-daily-channel regression by check_known_files_mention_weekly. Fix the regex to r'\\bweeks?(ly)?\\b' (or equivalent) so the plural noun form is recognized on its own.",
      "done": true
    },
    {
      "step": "_iter_scanned_files() calls REPO_ROOT.rglob('*') over the entire repo tree and only filters out node_modules/.venv/.git/dist/build/coverage/etc. after full enumeration, rather than pruning those directories during traversal (e.g. an os.walk with dirnames[:] = [d for d in dirnames if d not in _EXCLUDED_DIR_NAMES]). Since this script now runs as static-verify's own step 5 -- which pr-reviewer and task-qa-reviewer both run unconditionally on every task -- this wasted walk over tens of thousands of node_modules/.venv/.git files repeats on every PR review and QA pass and will only get slower as those directories grow.",
      "done": true
    },
    {
      "step": "_PROFIT_TARGET_CONTEXT_RE's `profit[\\s_]target` alternative doesn't match the hyphenated form 'profit-target' -- the literal text of methodologyContent.ts's own start_anchor (id: 'profit-target'). Verified directly: the id text alone doesn't match. In practice the check currently still passes because the same entry's `name` field ('Profit Target (+ reward:risk ratio)') independently matches the space-separated form -- a genuine structural field of the entry, not throwaway incidental prose, so the practical fragility is lower than it first appears, but it's still an unnecessary gap: add a hyphenated alternative (`profit[\\s_-]target`) so the anchor's own id text is self-sufficient context, not reliant on a second field happening to also mention it.",
      "done": true
    },
    {
      "step": "_extract_region() silently falls back to scanning to the end of the file when end_anchor fails to match, with no violation/warning emitted -- asymmetric with the explicit 'this guard's start anchor no longer matches -- update...' violation raised when start_anchor fails. If a known file's structure changes so the end anchor stops matching (e.g. profitTargetHelp becomes the last export in a metricHelpContent.ts, or profit-target becomes the last id: entry in methodologyContent.ts), the scanned region would silently balloon to include unrelated trailing content (e.g. the genuinely-different DAILY trade-grading description) with no signal to a maintainer that the anchor needs updating -- worth emitting the same kind of 'anchor no longer scoping correctly, update this guard' violation in that case too, for symmetry with the start-anchor check.",
      "done": true
    },
    {
      "step": "scripts/check_profit_target_wording.py has one ruff finding (SIM103: `_should_scan_for_banned_patterns` could return `not _is_test_file(path)` directly instead of the trailing if/return). Not part of the enforced backend `ruff check app/` gate (scripts/ at the repo root is out of scope, same precedent as scripts/validate_tasks.py) so non-blocking, but scripts/validate_tasks.py itself -- the sibling script this one explicitly mirrors -- is fully ruff-clean, so this is a small, easy inconsistency worth tidying up to match.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-21T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed check_banned_patterns()'s cross-line-concatenation blind spot by recognizing Python's own implicit-string-concatenation syntax specifically (a regex matching `quote, whitespace spanning a newline, matching quote` and dropping just the quote pair, preserving every newline) rather than switching to a whole-file re.DOTALL scan or blanket whitespace-collapsing normalization.",
      "rationale": "A whole-file re.DOTALL/single-string search (the alternative floated in the checklist item itself) would also match a banned phrase split across two genuinely unrelated lines/paragraphs that just happen to have the right words in the right order with nothing else between -- a broader match surface than the bug class this guard exists for, and it would make every violation's line-number reporting harder (a match spanning many lines has no single obvious 'the' line to report). Targeting the specific implicit-concatenation syntax keeps the fix narrow to the actual reported gap (schemas.py's own Field description wrapping style) while leaving every other newline in a file inert to the banned-pattern regexes, exactly as before. It also preserves exact line-number reporting for free, since only quote characters are removed, not newlines -- a plain `joined_text.count(\"\\n\", 0, match.start())` still maps correctly back to the original file's physical lines."
    },
    {
      "timestamp": "2026-09-21T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "On a failed end_anchor (checklist item 5), emit a violation but still fall back to scanning from the start anchor to end of file, rather than skipping the file's positive check entirely the way a failed start_anchor does.",
      "rationale": "A failed start_anchor means there is no valid region at all to scan, so skipping is the only option. A failed end_anchor still leaves a well-defined region start; the fallback-to-end-of-file region is a superset that still legitimately contains the real profit-target description (just with extra, possibly-unrelated trailing content risked), so scanning it can still catch a real regression even while the guard's own anchor needs a maintainer's attention -- silently disabling the positive check entirely on a stale end_anchor would be a worse outcome than an occasional extra false positive from bleeding into trailing content."
    },
    {
      "timestamp": "2026-09-21T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Refactored _iter_scanned_files() to os.walk with dirnames[:] pruning (checklist item 3) but left the now-redundant _EXCLUDED_DIR_NAMES membership check inside _should_scan_for_banned_patterns() in place, as a defense-in-depth safety net for any future caller of that predicate outside the pruned os.walk loop, with a comment noting the redundancy.",
      "rationale": "Removing it would save a negligible amount of work per call and add a hidden coupling: _should_scan_for_banned_patterns() would only be a correct 'should this file be scanned' predicate when called from inside _iter_scanned_files()'s own pruned walk, silently wrong if called on an arbitrary path from anywhere else. Verified the traversal refactor is fully behavior-preserving by diffing the real repo's scanned-file set (217 files, byte-for-byte identical sorted path list) between the pre-change rglob-based implementation and the new os.walk-based one."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/backend-profit-target-weekly-channel-followups-followups",
    "pr_number": 226,
    "pr_url": "https://github.com/rainoko/fintrade/pull/226",
    "pushed_at": "2026-09-21T08:11:40Z"
  },
  "review": {
    "reviewed_at": "2026-09-21T08:20:04Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/226",
    "verdict": "accepted",
    "comments": [
      {
        "summary": "A banned-phrase violation caught via the new implicit-string-concatenation join has its printed 'found' text excerpt missing a boundary quote character (an artifact of the join removing quote chars but not newlines) -- cosmetic diagnostic-clarity nit only; line number is still correct.",
        "file": "scripts/check_profit_target_wording.py",
        "line": 218
      },
      {
        "summary": "_STRING_CONCAT_JOIN_RE only recognizes implicit concatenation between two literals using the same quote character (backreference-tied); Python also permits mixed-quote-style concatenation, which this join won't catch. Not exercised by the current codebase's wrapping style, so no live gap today.",
        "file": "scripts/check_profit_target_wording.py",
        "line": 201
      },
      {
        "summary": "ruff still reports SIM110 on _is_excluded_path (pre-existing, not introduced by this PR). Checklist item 6 aimed for parity with validate_tasks.py's fully ruff-clean state but that parity isn't fully achieved yet.",
        "file": "scripts/check_profit_target_wording.py",
        "line": 157
      }
    ],
    "notes": "All 6 claimed fixes verified directly: (1) implicit-string-concatenation split now caught (pre-fix vs post-fix repro, main's _iter_scanned_files/check_banned_patterns confirmed to miss it), (2) plural 'weeks' now matches _WEEKLY_RE (pre-fix None, post-fix matches), (3) os.walk directory-pruning refactor confirmed behavior-preserving by running both the pre-fix rglob-based and post-fix os.walk-based _iter_scanned_files() against the real repo tree in-process and diffing the resulting sorted file lists -- 217 files, byte-for-byte identical, not just the PR's own claim taken on faith, (4) hyphenated 'profit-target' now matches _PROFIT_TARGET_CONTEXT_RE (pre-fix None, post-fix matches), (5) failed end_anchor now emits a symmetric violation while still scanning the fallback region (verified via direct _extract_region call returning the new 3-tuple, and via the PR's own new regression test), (6) the named SIM103 finding is gone per ruff (a separate pre-existing SIM110 remains, tracked as a follow-up, not part of this checklist item's claim). scripts/tests/test_check_profit_target_wording.py: 46/46 passed via `python3 -m unittest discover -s scripts/tests`. Backend: pytest --cov=app --cov-fail-under=90 -> 1109 passed, 99.95% coverage. Frontend: vitest run --coverage -> 704 tests passed, 99%+ across all metrics. ruff check app/ and mypy (backend) clean; yarn lint and yarn tsc -b --noEmit (frontend) clean. The real check_profit_target_wording.py run against the repo itself: 'Scanned 217 file(s) ... no stale daily-channel wording found.' Not UI-facing (scripts/ + docs/tasks/ only) so browser walkthrough/e2e suite skipped explicitly. area is backend/signals (inherited) but this diff touches no app/signals or app/portfolio code, so the Elder-methodology checklist doesn't apply. No API/schema changes, so no OpenAPI snapshot concern. All judgment calls in this PR are recorded in decisions[] per CLAUDE.md. Three non-blocking findings filed as checklist items on new follow-up task backend-profit-target-weekly-channel-followups-followups-followups (none existed yet for this task id)."
  }
}
