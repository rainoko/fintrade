{
  "id": "frontend-market-scanner-page",
  "title": "Scanner page -- run a scan, review candidates, add hits to the watchlist",
  "area": "frontend/pages",
  "skill": "add-frontend-feature",
  "state": "testing",
  "depends_on": [
    "backend-market-scanner"
  ],
  "references": [
    "docs/ideas.md#market-scanning-ch-56-is-a-new-feature-not-part-of-the-watchlist-or-ticker-detail-view"
  ],
  "description": "A new page, not a section of the watchlist or ticker detail view (per docs/ideas.md's own scoping): pick a scan category, run it, review the resulting candidate list, and add any worth tracking to the watchlist (POST /api/watchlist, already built) -- each hit's drill-down is the existing ticker detail view. Must clearly show a disabled/unavailable state (not an error) when IBKR isn't connected, consistent with the backend's own degrade-gracefully contract.",
  "checklist": [
    {
      "step": "Build a new Scanner page: category picker, a 'run scan' action, and a results list with each hit linking to the ticker detail view (TickerLink) and an 'add to watchlist' action per row.",
      "done": true
    },
    {
      "step": "Show a clear, non-error disabled state when the backend reports IBKR unavailable/disabled.",
      "done": true
    },
    {
      "step": "Add the page to app navigation.",
      "done": true
    },
    {
      "step": "Component tests covering: available with results, available with zero results, and the disabled/unavailable state.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "New common/UnavailableState component (with its own Storybook stories + test) renders the 'the feature is disabled/not reachable right now, nothing is actually broken' case -- distinct from both common/ErrorState (genuine ApiError, red/alert styling, role=\"alert\") and common/EmptyState (real data, just zero of it). ScannerPage renders it whenever GET /api/ibkr/scanner/params's state !== 'available'; ScannerPanel renders it again for the same non-'available' outcome from POST /api/ibkr/scanner/run (reachable if IBKR drops between the params check and the run). This is the first frontend feature that needs to distinguish this three-way split (broken / disabled-but-fine / empty-but-fine) since this is the first UI built against any of the /api/ibkr/* routes.",
      "rationale": "Reusing EmptyState for the disabled state was rejected: this task's own checklist explicitly calls out 'available with zero results' and 'the disabled/unavailable state' as two scenarios a reviewer needs to be able to tell apart at a glance, and EmptyState's framing ('no data to show you') is a poor fit for 'this data source can't even be reached right now' -- conflating them under one component/message would blur exactly the distinction the checklist calls out. Reusing ErrorState (e.g. by synthesizing a fake ApiError for a 200 response) was also rejected: `disabled`/`gateway_unreachable`/`not_authenticated` are deliberately not ApiErrors at all (API.md, backend-market-scanner's own decisions) precisely so the frontend doesn't have to render them as failures -- forcing them through ErrorState's alert-role/red-icon treatment would fight that contract and require inventing a synthetic error object for something that isn't one. A new, small, genuinely reusable common component (no domain knowledge, just heading+optional message) was the cleaner fit and will likely be reused once frontend-ibkr-status-indicator (still planned) needs the same treatment."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The category picker only exposes IBKR's own scan-type `code`/`display_name` (GET /api/ibkr/scanner/params's `categories`, normalized via features/scanner/scannerCategories.ts's `toScannerCategories`). `scan_config`'s `instrument`/`location` fields are hardcoded to `'STK'`/`'STK.US.MAJOR'` (the exact defaults `IBKRScannerRunRequest.scan_config`'s own schema `examples` document) rather than exposed as further pickers.",
      "rationale": "GET /api/ibkr/scanner/params's backend handler only ever forwards IBKR's `scan_type_list` -- it deliberately discards `instrument_list`/`location_tree` entirely (see backend-market-scanner's own decisions entry: 'passing through the raw scan_type_list... rather than hand-curating'), so there is no server-exposed option list this frontend could build an instrument/location picker from without inventing one client-side from scratch (duplicating gateway-versioned option data the backend explicitly chose not to model, for the same staleness-risk reason that task gave). Building a full instrument/location/filter-builder UI is also out of scope for this task's own 'pick a scan category, run it' framing (task description) -- that is a materially larger feature (a generic scan-config form) than what was asked for here. STK/STK.US.MAJOR was chosen over any other fixed pair specifically because it's the literal example the backend schema itself documents, not a value invented for this task."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "ScannerResultsTable imports and reuses features/watchlist/hooks/useAddWatchlistItem.ts directly for its per-row 'Add to watchlist' action, rather than writing a second POST /api/watchlist mutation hook under features/scanner/hooks/. This is the first cross-feature hook import in the codebase (every prior feature's hooks are only ever consumed from within that same feature's own components/pages).",
      "rationale": "useAddWatchlistItem already encapsulates the correct behavior end to end (idempotent no-op on a duplicate add per API.md, invalidates watchlist queries on success) -- duplicating it under features/scanner/hooks/ would risk the two implementations silently drifting (e.g. one gaining a bug fix or an invalidation the other doesn't). Frontend.md \u00a73's placement rules explicitly restrict *component* placement (`components/common/` vs `features/<domain>/components/`, checked by pr-reviewer) but say nothing about restricting a mutation hook -- a thin wrapper over one `useMutation` call with no feature-specific rendering -- to only being called from within its own feature. Considered lifting `useAddWatchlistItem` (or the underlying `addWatchlistItem` API call) into a shared, feature-agnostic location instead; rejected since it already lives exactly where Frontend.md's hook-placement rule says a watchlist-domain hook belongs ('features/portfolio/hooks/usePortfolio.ts... lives under the feature it serves'), and moving it would only be justified if a third consumer made the watchlist feature itself feel like the wrong owner -- not yet the case here."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "common/ErrorState gained a fourth documented status case (429, alongside 404/422/503/0): a distinct 'Too many requests' heading/icon, plus a new `Try again in {retryAfterSeconds}s.` line rendered when `ApiError.retryAfterSeconds` is non-null. Previously 429 fell through to the generic 'Something went wrong' default case, since no earlier frontend feature had ever called a rate-limited backend route (POST /api/ibkr/scanner/run is the first).",
      "rationale": "API.md documents 429 as a real, expected outcome of POST /api/ibkr/scanner/run (IBKRProvider's 1-request/second client-side throttle) and api/client.ts already parses the response's Retry-After header into `ApiError.retryAfterSeconds` specifically so a caller doesn't have to -- leaving ErrorState's generic fallback in place for this endpoint's very first UI consumer would both mislabel a normal, actionable, retry-shortly outcome as 'Something went wrong' and leave that already-parsed retry delay unused. Rendering the retry delay as its own Typography line (not appended into `detail`'s string) was chosen so a future backend wording change to `detail` can never accidentally duplicate or contradict this app-owned line."
    },
    {
      "timestamp": "2026-09-22T01:30:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed the review's blocking finding by extracting a new `ScannerAddToWatchlistButton({ ticker })` component that calls `useAddWatchlistItem()` and owns its own `added` boolean state, and having ScannerResultsTable's action-column `render` return that component per row instead of a shared `useAddWatchlistItem()`/`addedTickers` Set at the table level. Added a regression test staggering two rows' POST /api/watchlist responses (MSFT resolving before AAPL despite AAPL being clicked first) proving both rows independently flip to 'Added'.",
      "rationale": "The bug was structural, not a state-tracking bug: TanStack Query's MutationObserver.mutate() detaches from whatever mutation it was previously observing as soon as a different mutate() call reattaches the same observer, so one useAddWatchlistItem() instance shared across rows could only ever track the most recently clicked row's onSuccess/isError. Reconciling addedTickers against some other source of truth (e.g. polling the watchlist query) was considered and rejected -- it would still leave a shared mutation's isPending/isError wired to only the latest click, and would add a second, redundant success-detection path instead of fixing the actual observer-sharing root cause. Giving each row its own hook call (one observer per row, matching how DataTable's render(row) already produces one React element per row) is the same pattern this codebase already uses for other per-row state and requires no changes to useAddWatchlistItem itself."
    },
    {
      "timestamp": "2026-09-22T02:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed round 2's first blocking finding by rewriting the regression test's two clicks to use synchronous `fireEvent.click()` (no `await` in between) instead of two sequential `await user.click()` calls. Verified the fix is real by checking out the pre-fix commit (61e9377) into a separate git worktree, copying the rewritten test file over unmodified, and running it there: the concurrency test now correctly fails against the old shared-mutation code (only 1 of 2 rows reaches 'Added') and passes against this branch's fix.",
      "rationale": "The reviewer proved `await user.click(a); await user.click(b)` never exercises genuine concurrency -- userEvent's returned promise doesn't resolve until React has flushed that click's own state updates, which given the mocked POST's short delay meant the first click's onSuccess had already fired before the second click was even dispatched. `Promise.all([user.click(a), user.click(b)])` was considered (also genuinely concurrent, since neither individual click promise is awaited before the second fires) but `fireEvent.click()` was chosen instead because it's what the reviewer's own comment named as the more direct fix, dispatches synchronously with no promise semantics to reason about at all, and needed no `userEvent.setup()` in this particular test now that no other user-event-only behavior (hover, typing) is exercised by it."
    },
    {
      "timestamp": "2026-09-22T02:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed round 2's second blocking finding by replacing ScannerAddToWatchlistButton's failed-add rendering of the full common/ErrorState block with a compact Tooltip-wrapped error IconButton (aria-label and Tooltip title both carry the failure's ApiError.detail; clicking it retries the same mutate() call). No existing 'compact inline error state for a table cell' component or convention was found anywhere in the codebase (grepped for Tooltip usage and every other per-row-mutation-error case -- PositionsTable.tsx, WatchlistTable.tsx -- both render ErrorState as a sibling above their whole table, never per-row/per-cell), so this is a new, narrowly-scoped pattern rather than a reuse of an existing one.",
      "rationale": "A full ErrorState block (icon + heading + body, meant to occupy a page/section) inside a <TableCell> makes that cell the tallest in its row and stretches every other cell in the same row to match, per the reviewer's finding. An icon-sized affordance keeps the row's height unchanged regardless of error state. A Tooltip (rather than inline Typography text, which would still add vertical space, just less of it) was chosen so the failure detail is available on demand (hover/focus) without permanently growing the cell at all; wrapping the retry action in the same control (rather than a separate icon plus a separate retry button) avoids adding a second interactive element to an already-narrow action column. Promoting this to a components/common/ component was considered and rejected for now -- with exactly one caller, extracting a common component ahead of a second real use case would be speculative generalization; if a second per-row-mutation-error case appears (e.g. frontend-position-risk-columns or a future per-row table action), that is the point to lift this into common/, not before."
    }
  ],
  "git": {
    "branch": "task/frontend-market-scanner-page",
    "pr_number": 243,
    "pr_url": "https://github.com/rainoko/fintrade/pull/243",
    "pushed_at": "2026-09-22T00:58:38Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T01:31:56Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/243",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "Round 2 (re-review of commit 8b03144's fix). The production fix itself is correct: I independently reproduced the original shared-mutation race against the pre-fix code (checked out commit 61e9377 in a separate worktree) using true concurrent fireEvent.click() calls on two rows with staggered mock responses -- confirmed only 1 of 2 rows ever reached 'Added' on the old code, and both rows correctly reach 'Added' against this PR's ScannerAddToWatchlistButton extraction. However the committed regression test ('resolves two concurrent adds on different rows independently (regression, PR #243)') does NOT actually exercise this race: `await user.click(addButtons[0])` fully awaits @testing-library/react's act()-wrapping until AAPL's mocked POST (delay 40ms) resolves and its onSuccess callback fires, before the test even dispatches the second click on MSFT -- confirmed by instrumenting both the old and new component with timestamped console logs (ONSUCCESS AAPL fires ~12ms before CLICK MSFT is even dispatched). I re-ran this exact test file, completely unmodified, against the pre-fix ScannerResultsTable.tsx (commit 61e9377) and all 6 tests passed, including this 'regression' one -- proof it would not have caught the original bug and will not catch a reintroduction of it. The two clicks need to be genuinely concurrent (e.g. `fireEvent.click()` for both with no await in between, or firing both `user.click()` promises and only then awaiting `Promise.all`) rather than sequential `await user.click()` calls.",
        "file": "frontend/src/features/scanner/components/ScannerResultsTable.test.tsx",
        "line": 68
      },
      {
        "summary": "Round 2 regression introduced by this fix: ScannerAddToWatchlistButton now renders `<ErrorState>` (a full page/section-scale icon+heading+body block, per components/common/ErrorState/ErrorState.tsx) directly inside the DataTable's per-row action `<TableCell>` (DataTable.tsx renders `column.render(row)` straight inside `<TableCell>`) on a failed add. Every other place in this codebase with the identical 'DataTable + per-row mutation error' shape -- PositionsTable.tsx (`deletePosition.isError && <Box sx={{mb:2}}><ErrorState .../></Box>` rendered above `<DataTable>`) and WatchlistTable.tsx (same pattern) -- renders ErrorState as a sibling above the whole table, never inside a cell, specifically because it's not designed to fit inline. The pre-fix ScannerResultsTable did this too (ErrorState was a sibling of <DataTable>, shared table-wide). Moving the add-watchlist mutation to be per-row also moved its error rendering into the cell, which will visually break that row's height relative to the rest of the table on a 422/404/503/429 from POST /api/watchlist -- an HTML table row expands every cell to the tallest one. No existing test catches this since the failure test only asserts `getByRole('alert')` exists, not where it renders. Needs each row's error surfaced without embedding ErrorState's full block layout inside a table cell (e.g. lift failed-ticker state back up to ScannerResultsTable and render ErrorState(s) above the table, or use a slimmer inline error affordance for the per-row case).",
        "file": "frontend/src/features/scanner/components/ScannerResultsTable.tsx",
        "line": 34
      }
    ],
    "notes": "Round 2 re-review of task-worker's fix for round 1's blocking shared-mutation-race finding. Backend unaffected (no changes; pytest 1139 passed, 99.95% coverage; ruff/mypy clean). Frontend: vitest 742 passed, coverage 99.73%/98.91%/99.73%/99.79% (statements/branches/functions/lines), all comfortably above the 90% gate; yarn lint and tsc -b --noEmit both clean; scripts/check_profit_target_wording.py clean. make e2e: all 18 existing specs pass, no regression (no scanner-specific e2e spec exists yet -- already tracked in frontend-market-scanner-page-followups, not duplicated here). No browser/Playwright MCP tool was available in this session to do an interactive walkthrough of the scanner golden path (also blocked in practice: this dev environment has FINTRADE_IBKR_ENABLED=false with no live IBKR gateway or mock available, same constraint noted in docs/ideas.md/Backend.md -- only the disabled state is reachable without one); in its place I did a more rigorous direct reproduction of the specific race in question (see comments), which a hand-driven browser click sequence could not reliably control the timing of anyway. No API/schema changes in this diff, so backend/openapi.json is correctly untouched. Both this round's findings are new regressions/gaps introduced specifically by the round-1 fix commit (8b03144), not carryovers from round 1's original finding, which is itself correctly resolved. Verdict: needs_work -- two blocking findings (a regression test that doesn't test the regression it claims to, and a layout regression from rendering ErrorState inside a table cell)."
  }
}
