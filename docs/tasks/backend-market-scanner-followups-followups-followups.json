{
  "id": "backend-market-scanner-followups-followups-followups",
  "title": "Non-blocking follow-ups from backend-market-scanner-followups-followups review (PR #239)",
  "area": "backend/api",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-market-scanner-followups-followups"],
  "references": [
    "docs/tasks/done/backend-market-scanner-followups-followups.json",
    "https://github.com/rainoko/fintrade/pull/239"
  ],
  "description": "Non-blocking findings from the pr-reviewer pass on PR #239 (the typed `Retry-After` OpenAPI header added to POST /api/ibkr/scanner/run's and POST /api/ibkr/breadth/snapshot's 429 responses), tracked here per CLAUDE.md's convention so they aren't lost once the PR merges.",
  "checklist": [
    {
      "step": "The generated `\"Retry-After\"?: number` field on both 429 responses (frontend/src/api/types.ts) is currently unreachable from any actual frontend code: the hand-rolled frontend/src/api/client.ts `request()`/`ApiError` never surfaces `response.headers` to callers at all (`ApiError` only carries `status`/`detail`). Before any future feature actually reads Retry-After to schedule a UI retry, thread response headers through the client (or a dedicated typed accessor) -- and note that a real `Headers` instance is read via `.get('Retry-After')` (returns a string) rather than bracket access on a plain object, so the generated `number` type needs an explicit parse step, not a direct cast.",
      "done": false
    },
    {
      "step": "The `Retry-After` header spec in backend/app/api/routers/ibkr.py's `responses={}` (both routes) omits `required: true`, even though `_rate_limited_http_exception` unconditionally sets this header on every 429 these two routes raise. Add `required: true` so the generated type is `\"Retry-After\": number` (non-optional) instead of `\"Retry-After\"?: number`, and regenerate backend/openapi.json + frontend/src/api/types.ts.",
      "done": false
    },
    {
      "step": "The `headers: {\"Retry-After\": {...}}` dict literal (and its enclosing 429 `description` string) is duplicated verbatim between run_ibkr_scanner's and record_ibkr_breadth_snapshot's `responses={}` in backend/app/api/routers/ibkr.py. Factor it into a shared module-level constant (e.g. `_RETRY_AFTER_HEADER_SPEC`) referenced by both routes' `responses={}` dicts so the two 429 specs can't silently drift out of sync.",
      "done": false
    }
  ]
}
