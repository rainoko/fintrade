{
  "id": "backend-ibkr-bar-interval-param",
  "title": "Parameterize IBKRProvider's hardcoded 1h bar interval",
  "area": "backend/data",
  "skill": null,
  "state": "planned",
  "depends_on": [],
  "references": [
    "backend/app/data/ibkr_provider.py",
    "docs/ideas.md#switchable-trading-mode-positionswing-trader-vs-day-trader--different-triple-screen-timeframe-scales"
  ],
  "description": "get_hourly_bars hardcodes _BAR_INTERVAL = \"1h\" -- only hourly bars are implemented today, not the finer granularities (5-min/25-min/2-min, or 39-min/8-min) a future day-trader-mode feature would need. Parameterize the bar interval now so it's ready, without building the full day-trader-mode feature itself (that remains a separate, much larger idea explicitly flagged in docs/ideas.md as needing its own design pass -- not in scope here).",
  "checklist": [
    { "step": "Generalize get_hourly_bars into a bar-interval-parameterized method (or add a `bar` parameter to it), validating the requested value against IBKR's actually-documented accepted bar values.", "done": false },
    { "step": "Default the parameter to today's '1h' behavior so no existing caller's behavior changes.", "done": false },
    { "step": "Record which bar values were confirmed vs. assumed (per this module's mocked-only testing constraint) as a decisions entry.", "done": false },
    { "step": "Update existing tests for the new parameter and add tests for at least one other interval value.", "done": false }
  ]
}
