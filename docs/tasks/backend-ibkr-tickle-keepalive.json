{
  "id": "backend-ibkr-tickle-keepalive",
  "title": "Automate the IBKR gateway /tickle session keep-alive",
  "area": "backend/data",
  "skill": null,
  "state": "testing",
  "depends_on": ["backend-ibkr-status-endpoint"],
  "references": [
    "backend/app/data/ibkr_provider.py",
    "docs/architecture/Backend.md",
    "docs/ideas.md#ibkr-client-portal-gateway-is-now-live-and-authenticated--concrete-next-steps"
  ],
  "description": "docs/architecture/Backend.md documents keeping the gateway session alive with a periodic GET /tickle call roughly once a minute, but this is currently a manual step for whoever does the browser login -- nothing in IBKRProvider or the FastAPI app itself ever calls it, so an authenticated session will silently expire on its own. Add a background repeating task that calls /tickle on an interval, only when Settings.ibkr_enabled is True. Depends on backend-ibkr-status-endpoint landing first so there's a way to observe/verify the keep-alive is actually working.",
  "checklist": [
    { "step": "Add a tickle() method to IBKRProvider calling GET /tickle, following the same _request boundary/error-handling convention as its other methods.", "done": true },
    { "step": "Add a FastAPI lifespan background task that calls tickle() on an interval, started only when Settings.ibkr_enabled is True, and cleanly cancelled/stopped on app shutdown.", "done": true },
    { "step": "Decide and record the polling interval (docs/architecture/Backend.md says 'roughly once a minute' -- pick and justify a concrete value).", "done": true },
    { "step": "Unit tests for the tickle() call itself, and for the lifespan task being a no-op when ibkr_enabled is False.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "IBKRProvider.tickle() goes through the same _request boundary as every other method (so gateway-down/non-200/unparseable-body all surface as IBKRUnavailableError), but deliberately skips the _require_available()/get_gateway_status() pre-check that get_hourly_bars/get_scanner_params/run_scanner/resolve_conid all do first.",
      "rationale": "Those other methods gate on availability because a data-fetching call against an unauthenticated gateway is pointless. /tickle's entire purpose is refreshing a session that's expected to still be authenticated -- gating it behind a status check would call GET /iserver/auth/status immediately before every single /tickle, doubling request volume against the gateway for no benefit; if the session actually expired, /tickle itself fails the same way the status check would have."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Polling interval for the lifespan /tickle loop is 45 seconds (app.main._IBKR_TICKLE_INTERVAL_SECONDS), not the literal 60s docs/architecture/Backend.md's 'roughly once a minute' names.",
      "rationale": "docs/ideas.md's own research note on this gap warns 'IBKR's session timeout is short' without pinning an exact number, so 45s buys a safety margin below the letter of 'roughly once a minute' in case the real timeout is shorter than expected. Unlike /iserver/scanner/params and /iserver/scanner/run (documented, enforced 1-req/15-min and 1-req/sec rate limits respectively), IBKR's docs don't document any rate limit on /tickle itself, so polling somewhat more often than exactly once a minute costs nothing. Rejected picking exactly 60s (no margin, and 'roughly' already signals it's not meant to be exact) and picking something much shorter e.g. 10s (no evidence the timeout is that aggressive, and needlessly chatty against the gateway)."
    },
    {
      "timestamp": "2026-09-22T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The lifespan task's IBKRProvider instance is obtained via next(get_ibkr_provider()) (app.api.dependencies) rather than duplicating the Settings.ibkr_enabled check and constructing a fresh IBKRProvider directly in app.main.",
      "rationale": "Reuses that dependency's existing gate and process-wide singleton (same HTTP client, so the tickle loop's calls share connection/cookie state with every other IBKR call the app makes) as a single source of truth instead of a second, independently-maintained enabled check. get_ibkr_provider has no cleanup logic after its single yield in either branch, so calling next() once without ever closing the generator loses nothing."
    },
    {
      "timestamp": "2026-09-22T15:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed the reproducible flake in TestLifespanIbkrTickleGate::test_enabled_logs_and_keeps_looping_when_tickle_fails (and the same structural risk in test_enabled_starts_a_task_that_calls_tickle) by replacing the fixed-200-iteration `await asyncio.sleep(0)` busy-wait with a real-timer poll: a new module-level `_wait_until(predicate, timeout=5.0, interval=0.01)` helper that awaits `asyncio.sleep(interval)` in a loop wrapped in `asyncio.wait_for(..., timeout=timeout)`.",
      "rationale": "The reviewer's root cause was correct: `_ibkr_tickle_loop` calls `provider.tickle()` via `asyncio.to_thread`, a real thread-pool round trip, so 200 bare `sleep(0)` cooperative yields (no real wall-clock time) aren't guaranteed to let two such round trips land under load (coverage instrumentation, a busy CI host). Rejected an asyncio.Event set from inside the mock's side_effect -- that side_effect runs in the to_thread worker thread, and asyncio.Event isn't safe to .set() from a thread other than the loop's own (would need call_soon_threadsafe, adding complexity for no real benefit over a small real sleep). Rejected just raising the iteration count (e.g. 2000) -- still a fixed-count guess rather than a real condition wait, and either flakes again under enough load or wastes time in the common case. The chosen fix gives the worker thread genuine wall-clock time between checks while still bounding the wait so a real regression (loop never calling tickle()) fails promptly instead of hanging; verified with 40 repeated runs of tests/unit/test_main.py (plain) and 15 repeated runs with --cov=app (matching the reviewer's exact repro conditions) with zero failures, plus one full backend suite run (1170 passed, 100% coverage)."
    }
  ],
  "git": {
    "branch": "task/backend-ibkr-tickle-keepalive",
    "pr_number": 267,
    "pr_url": "https://github.com/rainoko/fintrade/pull/267",
    "pushed_at": "2026-09-22T14:21:19Z"
  },
  "review": {
    "reviewed_at": "2026-09-22T14:34:04Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/267",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "Flaky test: backend/tests/unit/test_main.py::TestLifespanIbkrTickleGate::test_enabled_logs_and_keeps_looping_when_tickle_fails intermittently fails (reproduced twice in ~10 runs of the full suite, with and without --cov): 'assert fake_provider.tickle.call_count >= 2 -- AssertionError: assert 1 >= 2'. The test mocks _IBKR_TICKLE_INTERVAL_SECONDS to 0.0 and busy-waits `await asyncio.sleep(0)` up to 200 times for tickle.call_count >= 2, but each tickle() call goes through asyncio.to_thread (a real thread-pool round trip); under load, 200 cooperative yields aren't reliably enough for two full thread-pool round trips to land before the test gives up. test_enabled_starts_a_task_that_calls_tickle has the same structural risk (only needs one call, so less likely to flake in practice). BLOCKING.",
        "file": "backend/tests/unit/test_main.py",
        "line": 83
      },
      {
        "summary": "docs/architecture/Backend.md §8 step 4 still says the /tickle keep-alive is 'not automated by this app today ... itself a currently-open gap' -- this PR is exactly that future task and should have updated that sentence; it's now stale. Non-blocking, filed as backend-ibkr-tickle-keepalive-followups.",
        "file": "docs/architecture/Backend.md",
        "line": 138
      },
      {
        "summary": "Shutdown-cancellation edge case: cancelling tickle_task while mid-await asyncio.to_thread(provider.tickle) can't interrupt the already-running worker thread, so lifespan's `await tickle_task` in finally will block until that in-flight call completes or its httpx timeout (~10s) elapses before CancelledError propagates. Bounded (no leak/crash) but untested -- the existing cancellation test only covers cancellation while sleeping. Non-blocking, filed as backend-ibkr-tickle-keepalive-followups.",
        "file": "backend/app/main.py",
        "line": 78
      },
      {
        "summary": "Minor robustness nit (sharpened after an independent code-review pass reproduced it): _ibkr_tickle_loop only catches IBKRUnavailableError around tickle(); if it ever raised anything else, the task finishes with that exception stored, tickle_task.cancel() is then a no-op at shutdown, and `await tickle_task` inside `contextlib.suppress(asyncio.CancelledError)` re-raises it -- propagating out of lifespan()'s own finally block, not just silently pausing the loop. No current code path triggers this (_request's raise sites are exhaustively IBKRUnavailableError today), so it's latent, not live. Non-blocking, filed as backend-ibkr-tickle-keepalive-followups.",
        "file": "backend/app/main.py",
        "line": 48
      }
    ],
    "notes": "Verified by reading code (not just trusting test descriptions) that: the lifespan wiring is a genuine no-op when Settings.ibkr_enabled is False (backend/.env still has FINTRADE_IBKR_ENABLED=false); the task is cancelled cleanly on shutdown for the tested case; _request's three raise sites all funnel into IBKRUnavailableError so a failed tickle only ever logs and continues; and the tickle()-skips-_require_available() reasoning is sound since the loop never retries early on failure (no tight-loop hammering). All 3 decisions entries are well-reasoned and cover the checklist's 'decide' items. ruff/mypy clean on every file this PR touches; the ruff findings visible on a repo-wide check are pre-existing in unrelated, untouched files. No API/frontend surface touched, so openapi.json correctly untouched and no browser walkthrough/e2e run was needed (explicitly noted rather than skipped silently). Verdict is needs_work solely due to the reproducible test flake above -- everything else passed."
  }
}
