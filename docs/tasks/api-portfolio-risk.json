{
  "id": "api-portfolio-risk",
  "title": "API: GET /api/portfolio/risk",
  "area": "backend/api",
  "skill": "add-api-endpoint",
  "state": "testing",
  "depends_on": ["portfolio-risk-rules", "portfolio-exit-rules"],
  "references": ["docs/architecture/API.md#get-apiportfoliorisk"],
  "description": "Implement the route body in app/api/routers/portfolio.py (currently a 501 stub). Route signature and response_model (RiskResponse) are already wired.",
  "checklist": [
    { "step": "Route signature + RiskResponse schema already defined", "done": true },
    { "step": "Implement handler: compute per-position protective_stop/position_risk_pct plus portfolio total_open_risk_pct and exit_flags", "done": true },
    { "step": "Write integration test covering a clean portfolio, a 2%-breaching position, and a 6%-breaching portfolio", "done": true },
    { "step": "Run pytest --cov=app --cov-report=term-missing and confirm no regression", "done": true },
    { "step": "PR review fix: guard get_risk()'s total_open_risk_pct(account, stops) call against ValueError (non-positive account.equity.total) the same way the per-position loop guards its own calls, with a covering test", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-15T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Extracted GET /api/portfolio's mark-to-market fetch-and-degrade-gracefully loop into a new app/portfolio/pricing.py module (EnrichedPosition dataclass + enrich_positions_with_price()/positions_value() functions, built on the existing app.portfolio.models domain models) rather than implementing GET /api/portfolio/risk with its own independent copy of that logic, and refactored GET /api/portfolio's handler to use the same shared function. This does the extraction the docs/tasks/api-portfolio-get-followups.json task's first checklist item calls for, ahead of that task itself.",
      "rationale": "GET /api/portfolio/risk unavoidably needs the exact same per-position current-price fetch (app.portfolio.risk.position_risk_pct's precondition is a populated position.current_price) plus the same degrade-gracefully-per-ticker semantics GET /api/portfolio already established (a failed price fetch excludes the position from any value/percentage sum rather than defaulting to 0 or falling back to cost basis) -- reimplementing that loop a second time inline in the risk handler would have produced two copies of nontrivial, already-subtle logic (the NaN-poisoning guard, the empty-frame check, the DataProviderError catch) that would silently drift from each other over time. The alternative (implement api-portfolio-risk independently, leave extraction to the other task) was rejected because api-portfolio-get-followups hasn't been scheduled yet and there's no guarantee it runs before further changes touch either handler; doing the extraction now, while both call sites and their exact needs are freshly in view, is lower-risk than writing a second copy today and reconciling two implementations later. EnrichedPosition additionally carries the fetched daily_ohlcv frame itself (not just the price), so GET /api/portfolio/risk can reuse that same frame for protective_stop/evaluate_exit_flags instead of re-fetching -- a need GET /api/portfolio never had, so this one extra field was added to the shared module rather than to the domain Position model itself. The docs/tasks/api-portfolio-get-followups.json task's first checklist item is left in place but effectively satisfied by this change; its remaining items (Backend.md diagram, positions ordering, session expire_on_commit, sequential-fetch latency) are unrelated and untouched here."
    },
    {
      "timestamp": "2026-09-15T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "A position that GET /api/portfolio/risk can't fully evaluate -- current price fetch failed, daily history has fewer than 2 rows, weekly history fetch failed, or protective_stop()/evaluate_exit_flags() raised ValueError for a malformed frame -- is silently excluded from the response's `positions` list entirely (and so also excluded from total_open_risk_pct, via app.portfolio.risk.total_open_risk_pct's existing 'skip positions missing from stops' behavior), rather than appearing with null/placeholder risk fields.",
      "rationale": "Unlike PositionOut (GET /api/portfolio), every field on RiskPosition (protective_stop, position_risk_pct, two_percent_rule_breached, exit_flags) is non-nullable in the documented schema (docs/architecture/API.md), so there is no partial representation available for a position whose risk genuinely can't be computed -- inventing nullable fields here would be a bigger, undocumented schema change for an edge case, and silently defaulting protective_stop/position_risk_pct to 0 would misrepresent an unknown risk as a *known-zero* one, which is worse than omitting the position (mirrors GET /api/portfolio's own precedent of excluding a bad-price position from positions_value rather than defaulting it to 0). Rejected alternative: return the position with a 200 but flag it via a synthetic exit_flags entry like 'risk_unavailable' -- rejected because exit_flags is documented as drawn specifically from Analyse.md §7's five named exit conditions, and overloading it with a data-availability signal would blur that contract for frontend consumers filtering/rendering it. docs/architecture/API.md's GET /api/portfolio/risk section was updated to document this exclusion explicitly."
    },
    {
      "timestamp": "2026-09-15T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The router's `protective_stop`/`position_risk_pct` display values for a position (used for the `protective_stop`/`position_risk_pct`/`two_percent_rule_breached` response fields) are computed with the stop excluding today's own bar (`protective_stop(position, daily_ohlcv.iloc[:-1])`), the same 'stop in force as of today' semantics `app.portfolio.exits.evaluate_exit_flags` uses internally for its own `stop_hit`/`two_percent_rule_breached` flag logic, rather than a today-inclusive stop.",
      "rationale": "evaluate_exit_flags computes its own stop/risk_pct internally and doesn't expose them, so the router has no choice but to independently call protective_stop/position_risk_pct for the values it needs to display -- keeping those calls' semantics identical to evaluate_exit_flags' internal ones (rather than, say, a today-inclusive 'current' stop) ensures the top-level `two_percent_rule_breached` boolean field can never disagree with whether `'two_percent_rule_breached'` appears in that same position's `exit_flags` list, which would otherwise be a confusing self-contradiction in one API response. The cost is recomputing protective_stop/position_risk_pct a second time (once directly in the handler, once inside evaluate_exit_flags) per risk-eligible position; this was accepted as a reasonable single-request tradeoff rather than restructuring evaluate_exit_flags's signature to also return its internal stop/risk_pct, which would be a larger, riskier change to an already-reviewed module for a per-request cost that's small (a handful of already-cached OHLCV rows, no extra network/DB fetch)."
    },
    {
      "timestamp": "2026-09-15T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The 2%/6% rule threshold constants (2.0/6.0) used for the response's `two_percent_rule_breached`/`six_percent_rule_breached` fields are re-declared as local constants in app/api/routers/portfolio.py rather than imported from app.portfolio.exits's identical (but module-private, underscore-prefixed) `_TWO_PERCENT_RULE_THRESHOLD`/`_SIX_PERCENT_RULE_THRESHOLD`.",
      "rationale": "exits.py's own thresholds are themselves not imported from app.portfolio.risk (which doesn't define them at all) -- there was no existing public/shared home for these two float constants to import from without also promoting exits.py's private constants to public, a change to an already-reviewed module out of scope for this task. Duplicating two named float constants with a comment cross-referencing the module they must stay in sync with was judged lower-risk than that wider change; both mirror docs/Analyse.md §7 and are exercised by tests on both sides (tests/unit/test_portfolio_exits.py, tests/integration/test_portfolio_risk.py) that would catch a drift."
    },
    {
      "timestamp": "2026-09-15T23:15:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed the pr-reviewer needs_work finding: wrapped get_risk()'s total_open_risk_pct(account, stops) call in try/except ValueError, defaulting total_risk to 0.0 (and so six_percent_rule_breached to False) whenever it raises, instead of letting the ValueError propagate as an unhandled 500. Added test_non_positive_equity_total_degrades_total_open_risk_pct_to_zero (tests/integration/test_portfolio_risk.py) covering negative cash driving account.equity.total <= 0 with an otherwise fully-computable position in `stops`.",
      "rationale": "total_open_risk_pct calls position_risk_pct internally for every position in `stops`, and position_risk_pct raises ValueError whenever account.equity.total <= 0 regardless of which position triggered it -- this is not a per-position data problem, it's an account-wide precondition failure, so 0.0 (meaning 'no known open risk') is the correct degrade-gracefully value, consistent with decision entry #2's 'silently excluded whenever risk can't be computed' contract and with every other can't-be-computed case this endpoint already handles (failed price fetch, insufficient history, malformed frame). Rejected alternative: exclude any position whose current_price is non-positive from `stops` up front (the review comment's second suggested fix) -- rejected because that only handles the zero/negative-current-price trigger, not the negative-cash trigger (a position can have a perfectly valid positive price while cash is negative enough to make account.equity.total <= 0 on its own), so it would have left the underlying precondition unguarded and the bug only partially fixed. Note the per-position loop below already independently hits and catches the same ValueError for the same reason once account.equity.total <= 0, so `positions` also correctly ends up empty in this scenario without any additional change there."
    }
  ],
  "git": {
    "branch": "task/api-portfolio-risk",
    "pr_number": 33,
    "pr_url": "https://github.com/rainoko/fintrade/pull/33",
    "pushed_at": "2026-09-15T21:52:34Z"
  },
  "review": {
    "reviewed_at": "2026-09-15T22:30:00Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/33",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "Blocking: get_risk()'s call to total_open_risk_pct(account, stops) (app/api/routers/portfolio.py) is not wrapped in try/except ValueError, unlike the per-position loop directly below it. total_open_risk_pct calls position_risk_pct internally, which raises ValueError whenever account.equity.total <= 0. As soon as `stops` contains at least one position, a non-positive account.equity.total (e.g. negative cash, or a data provider returning a non-NaN but zero/negative close for that position's current_price -- _latest_close's NaN-only check in app/portfolio/pricing.py doesn't guard against a non-NaN zero/negative price) makes this call raise, producing an unhandled 500 for the whole request instead of the graceful per-position exclusion this endpoint documents and tests everywhere else (its own decisions entry #2 promises 'silently excluded ... whenever its risk can't be computed'). No test exercises this path (every fixture uses positive cash and positive prices). Independently confirmed by the code-review skill run against this diff. Fix by guarding this call the same way the per-position loop guards its own position_risk_pct/evaluate_exit_flags calls, or by excluding non-positive-price positions from `stops` up front.",
        "file": "backend/app/api/routers/portfolio.py",
        "line": 264
      }
    ],
    "notes": "Checked out PR #33, ran full backend suite: 396 passed, 99.64% line/branch coverage (gate 90%, backend/pyproject.toml) -- no regression from main. No ruff/mypy configured in backend/pyproject.toml (confirmed via grep), so pytest+coverage is the full static-analysis surface for this repo today. Regenerated backend/openapi.json via scripts/export_openapi.py and diffed against the committed snapshot -- byte-identical, confirming it's current (matches the PR's own 'docstring-only diff' claim). docs/architecture/API.md was updated for the new degrade-gracefully-exclusion behavior. Ran code-review (medium) against the diff -- it independently found the same total_open_risk_pct/ValueError bug above, plus a minor non-blocking inefficiency (weekly OHLCV fetched before protective_stop is attempted in the first-pass loop, wasting a fetch for positions later excluded on the daily side) -- filed as a follow-up. Ran verify-elder-signal against docs/Analyse.md §7: the router's wiring of app.portfolio.risk/app.portfolio.exits is correct -- current (not entry-time) equity/price used throughout, exit flags evaluated independently of any fresh-entry signal, protective_stop is the swing-low-minus-volatility-buffer SafeZone calc (not a fixed-percent stop), and the today-excluded stop used for the router's own protective_stop/position_risk_pct display values correctly mirrors evaluate_exit_flags' internal semantics so two_percent_rule_breached can never disagree with the exit_flags list (decision #3, sound). Ran architecture-review against the new app/portfolio/pricing.py module: correctly placed in the portfolio/ package, correct dependency direction (imports only data/db/portfolio.models, imported only by api/), no new external dependency, but docs/architecture/Backend.md's §2 module layout tree and §6 narrative were not updated to mention it -- same category of gap as the api-portfolio-get-followups task's analogous finding for app/api/dependencies.py, treated as non-blocking there and here too, filed as a follow-up rather than blocking this PR. All four decisions recorded (extraction-now, silent-exclusion, today-excluded-stop, duplicated-thresholds) are sound and well-reasoned; no checklist item is phrased as an undecided 'decide X', so no missing-decision gap. No UI-facing surface (frontend isn't scaffolded yet, and this PR is backend-only) -- browser walkthrough explicitly skipped rather than silently omitted, per instructions. Filed docs/tasks/api-portfolio-risk-followups.json for the two non-blocking findings (Backend.md doc staleness, weekly-fetch ordering) per CLAUDE.md's autonomous-pipeline rule. Verdict is needs_work solely for the total_open_risk_pct/ValueError blocking finding above; everything else passed."
  }
}
