{
  "id": "frontend-oscillator-chart",
  "title": "Frontend: oscillator chart (Stochastic, Force Index) beneath the price chart",
  "area": "frontend/pages/stocks",
  "skill": "add-frontend-feature",
  "state": "testing",
  "depends_on": ["api-stocks-indicator-history"],
  "references": [
    "frontend/src/features/stocks/components/PriceChart.tsx",
    "frontend/src/features/stocks/components/IndicatorsPanel.tsx",
    "docs/Analyse.md#4-indicator-summary-table",
    "docs/Analyse.md#screen-2--the-wave-medium-term-daily-chart",
    "docs/tasks/api-stocks-indicator-history.json"
  ],
  "description": "User request: the oscillator(s) used by Screen 2 (docs/Analyse.md — Stochastic %K(5,3,3) and Force Index, 2-period EMA) must also appear as a chart in the UI, sourced from the backend (no client-side recomputation, same principle as frontend-chart-signal-overlay). Add a new chart pane — a separate synced panel beneath/alongside the price chart (the conventional oscillator-pane layout, since these values are on a different scale than price and don't belong on the same axis as candlesticks) — plotting the historical Stochastic %K and Force Index series from the api-stocks-indicator-history endpoint, with reference lines at the documented oversold/overbought thresholds (30/70 for Stochastic per docs/Analyse.md §4). If MACD Histogram (Screen 1's weekly slope input, also table-listed in §4) is also wanted as a plotted oscillator rather than just the point-in-time value already in IndicatorsPanel, include it here too — decide and record as a decisions entry. Additional chart panels beyond what's listed here may be added if needed for clarity (e.g. splitting Stochastic and Force Index into separate panes instead of one shared one) — record any such addition as a decisions entry explaining why.",
  "checklist": [
    { "step": "Decide which oscillator(s) to plot (Stochastic %K required per the user's request; Force Index required; MACD Histogram optional) and the pane layout (one shared oscillator pane vs. multiple), recording as a decisions entry.", "done": true },
    { "step": "Wire a TanStack Query hook for the oscillator series (reuse the hook added by frontend-chart-signal-overlay for the same endpoint if it covers this data, rather than duplicating the fetch).", "done": true },
    { "step": "Implement the oscillator pane using Lightweight Charts (or another already-in-use charting approach — check PriceChart.tsx's library first, prefer consistency over introducing a second charting library) as a new component under features/stocks/components/, synced in time range/interval with PriceChart.", "done": true },
    { "step": "Add reference lines/shading for the documented oversold/overbought thresholds (docs/Analyse.md §4) so the chart is readable against the methodology, not just raw numbers.", "done": true },
    { "step": "Handle loading/error/empty states consistently with the rest of the page (reuse LoadingState/ErrorState/EmptyState).", "done": true },
    { "step": "Tests: component test(s) for the new oscillator chart, keeping the suite's no-live-network invariant (MSW-mocked); confirm 90% coverage gate still holds and Storybook still builds if any new common/ component was added.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-18T09:52:09Z",
      "recorded_by": "task-worker",
      "decision": "Plot all three eligible oscillators from GET /api/stocks/{ticker}/indicators -- Stochastic %K(5,3,3), Force Index (2-EMA), AND MACD Histogram (daily-cadence, the Impulse System's own slope input per app/signals/engine.py's `histogram_series = macd_components(daily_close).histogram` -- NOT Screen 1's weekly Tide MACD-H, which is a separate computation the endpoint doesn't expose per-bar) -- as three stacked panes in one new `OscillatorChart` component, rather than Stochastic + Force Index only.",
      "rationale": "MACD Histogram was explicitly offered as optional in the task description, conditioned on it adding value beyond IndicatorsPanel.tsx's existing point-in-time StatCard. Checked IndicatorsPanel.tsx directly: it renders MACD Histogram as a single number with no trend/shape context, which is exactly the information a chart adds and a single stat can't -- whether the histogram is rising or falling is the literal Impulse System determinant (GREEN/RED/BLUE color, docs/Analyse.md's Impulse System section), so seeing its shape over time is genuinely more useful than the current snapshot, not redundant with it. The data is already returned by the endpoint at zero extra fetch cost (no new backend work, no new query). Rejected leaving it out: would have left a clearly-flagged 'decide and record' checklist item unresolved for no stated reason, and IndicatorsPanel's own doc comment already frames it as *not* a chart precisely because no historical series existed before this task's dependency (api-stocks-indicator-history) closed that gap -- the reasoning that justified leaving it as a stat-only value no longer holds.",
      "revisit_if": "Product feedback finds three stacked panes too dense/tall for the page -- could demote MACD Histogram to an optional toggle rather than always-shown, without reversing the underlying 'it adds value' judgment."
    },
    {
      "timestamp": "2026-09-18T09:52:09Z",
      "recorded_by": "task-worker",
      "decision": "Three SEPARATE panes (native Lightweight Charts v5 multi-pane support, one `IChartApi` instance, `chart.addSeries(definition, options, paneIndex)` with paneIndex 0/1/2) rather than one shared oscillator pane, and rather than three fully separate `IChartApi` instances with manually-synced cross-chart panning.",
      "rationale": "Stochastic %K is a bounded 0-100 oscillator; Force Index (2-EMA) is unbounded and volume-weighted, observed running into the tens of thousands in the fixture data (e.g. -18234.5); MACD Histogram is typically a small price-denominated value (single digits in the fixture data). Sharing one y-axis/pane across all three would make at least two of them visually flat/unreadable against the third's scale -- rejected. Using native multi-pane support (v5's own `addSeries(..., paneIndex)`, which auto-creates a pane at a not-yet-used index) rather than three independent `createChart()` instances with manual `subscribeVisibleLogicalRangeChange`/`setVisibleLogicalRange` cross-chart sync: the native approach shares one real time (x) axis by construction (same chart object), needs no hand-rolled sync code that could itself drift/break, and is the mechanism the library actually ships for exactly this 'stacked, time-synced sub-panes' case.",
      "revisit_if": "A future task wants per-pane independent zoom/scroll (e.g. zooming Force Index without moving Stochastic) -- native panes share one time scale by design, so that would need a different mechanism (back to separate chart instances with only their y-axes desynced, x-axes synced)."
    },
    {
      "timestamp": "2026-09-18T09:52:09Z",
      "recorded_by": "task-worker",
      "decision": "OscillatorChart is its own React component with its own container/`createChart()` call and its own `useIndicatorHistory` call (not a prop-drilled `chart`/`points` from PriceChart), composed via a new thin wrapper `StockCharts.tsx` that mirrors PriceChart's range/interval selection into OscillatorChart via two new optional callback props on PriceChart (`onRangeChange`/`onIntervalChange`) rather than converting PriceChart into a fully controlled component (range/interval state and its ToggleButtonGroup controls lifted entirely out of it).",
      "rationale": "PriceChart.tsx has a real, recent crash history (frontend-chart-signal-overlay, PR #107 -- a cross-effect cleanup-ordering bug on the SAME chart instance). Embedding OscillatorChart's panes directly inside PriceChart's own chart/effect, or passing PriceChart's live `IChartApi` down as a prop for a child component to mutate, would add a second consumer touching that same fragile chart object and risk reintroducing a similar effect-interaction class of bug. The chosen design keeps OscillatorChart's own chart lifecycle to a SINGLE effect (create-on-data-change, tear-down-on-cleanup) with no sibling effect touching the same chart object -- the exact pattern that caused zero problems for PriceChart's own (first, simpler) candlestick effect, and avoids the two-effect crash class entirely by construction. The range/interval 'sync' is achieved via a small, additive change to PriceChart (two new optional callback props called from its EXISTING handler functions, zero changes to its chart-creation/cleanup effect bodies) rather than a larger refactor of a component with recent crash history. Both `useIndicatorHistory` calls (PriceChart's overlay, OscillatorChart's panes) use the identical `ticker`/`range` queryKey when mounted together, so TanStack Query dedupes them into one real network request -- confirmed via the e2e run showing exactly one `GET /indicators?range=...` request per page load/range change, not two. Rejected: (a) passing `chart`/`points` as props from PriceChart to a child OscillatorChart -- couples two crash-history-adjacent chart-mutation effects onto one shared mutable `IChartApi`, and makes OscillatorChart untestable in isolation (would require mounting through PriceChart, or fabricating a fake chart object as a prop rather than mocking the whole `lightweight-charts` module the same way every other chart component here does); (b) converting PriceChart into a fully controlled component (range/interval + selector UI lifted to StockCharts) -- correct in the abstract, but a materially larger diff to the fragile file for no behavioral difference from the callback-mirror approach, and would have required either duplicating the ToggleButtonGroup UI in StockCharts or moving it out of PriceChart (breaking existing PriceChart.test.tsx assertions that render PriceChart standalone with its own controls).",
      "revisit_if": "A third sibling pane is ever added that also needs range/interval sync -- at 3+ mirrored consumers the callback-mirror pattern gets less clean than a real lift; converting PriceChart to controlled would become the better tradeoff at that point."
    },
    {
      "timestamp": "2026-09-18T09:52:09Z",
      "recorded_by": "task-worker",
      "decision": "OscillatorChart's own `enabled` prop (driven by `interval === 'daily'` in StockCharts, same convention as PriceChart's own overlay gating) shows an explanatory EmptyState message ('...are only available for the Daily interval.') when disabled, rather than silently rendering nothing the way PriceChart's on-chart overlay section does when its own `overlayEnabled` is false.",
      "rationale": "PriceChart's existing decisions entry explicitly flagged silent omission as a tradeoff worth revisiting ('could add a persistent caption... instead of silently omitting the overlay section'). OscillatorChart is its own standalone panel further down the page (not a layer drawn on top of the candlesticks), so an empty message here reads as informative rather than as visual clutter competing with the chart underneath it -- the tradeoff that justified silence for the on-chart overlay doesn't apply the same way to a separate section that would otherwise just vanish with no explanation for why.",
      "revisit_if": "User feedback finds the message noisy/redundant once seen a few times -- could collapse to a smaller inline caption instead of a full EmptyState treatment."
    },
    {
      "timestamp": "2026-09-18T09:52:09Z",
      "recorded_by": "task-worker",
      "decision": "Force Index and MACD Histogram get a dotted zero-price baseline (via `createPriceLine({ price: 0, ... })`) instead of a documented oversold/overbought threshold pair, and their histogram bars are colored by sign (theme.palette.signal.buy for >= 0, .sell for < 0) as a conventional 'momentum direction' visual, not a literal per-bar BUY/SELL claim.",
      "rationale": "docs/Analyse.md §2/§4 documents a numeric oversold/overbought threshold (30/70) only for the Stochastic Oscillator -- Force Index and MACD Histogram are described purely in terms of sign/spike behavior ('Negative spike in an uptrend = buying opportunity; positive spike in a downtrend = selling opportunity' for Force Index), which is explicitly trend-context-dependent, not a fixed threshold this component could draw a reference line at without inventing a number the methodology doesn't specify. A zero baseline is the correct, honest reference for a signed oscillator with no documented magnitude threshold, and sign-based bar coloring (green above zero, red below) is the near-universal MACD-histogram/Force-Index charting convention on every mainstream trading platform, independent of Elder's own trend-context caveat about what a positive/negative reading *means* for a trade decision -- the color here signals direction, not a recommendation, and no code computes or displays a derived BUY/SELL judgment for either series (avoiding a client-side re-implementation of Screen 2's actual oversold/overbought logic, which the backend already owns).",
      "revisit_if": "Analyse.md is ever updated with an explicit numeric Force Index/MACD Histogram threshold -- at that point a real reference line (like Stochastic's 30/70) should replace the zero baseline."
    }
  ],
  "questions": []
}
