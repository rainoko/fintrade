{
  "id": "backend-divergence-detection",
  "title": "MACD-Histogram / Stochastic / RSI divergence detection (bullish & bearish)",
  "area": "backend/signals",
  "skill": "add-indicator",
  "state": "testing",
  "depends_on": [
    "backend-swing-point-detector",
    "backend-indicator-rsi"
  ],
  "references": [
    "backend/app/indicators/macd.py",
    "backend/app/indicators/stochastic.py",
    "backend/app/signals/triple_screen.py",
    "docs/Analyse.md",
    "docs/ideas.md"
  ],
  "description": "Confirmed via grep: zero divergence logic anywhere in this codebase, despite Elder repeatedly calling it one of his strongest signal types. Exact algorithm captured in docs/ideas.md from the primary source (ch. 23/26/27): MACD-Histogram divergence requires the indicator to cross its own centerline between the two compared extremes ('an absolute must for a true divergence' per the book -- no crossover, no divergence); Stochastic/RSI divergence is simpler, no centerline-crossing requirement, just a direct second-vs-first extreme comparison, strongest when the first extreme is beyond the oscillator's own overbought/oversold reference line and the second is back inside it. Also implement Kerry Lovvorn's empirical refinement (most tradable divergences have their two extremes 20-40 bars apart, closer to 20 is better, and the second extreme should be no more than half the height/depth of the first) as configurable filters, and the 'Hound of the Baskervilles' signal (a formed divergence that price then ignores is itself a strong continuation signal in the opposite direction -- the one case Elder uses a stop-and-reverse). Expose divergence state (none/bullish/bearish, which indicator, extreme dates) as a new field on the analysis response; wiring it into confidence scoring or _determine_signal is a separate, later decision -- out of scope for this task, which is detection + exposure only.",
  "checklist": [
    {
      "step": "Implement bullish/bearish divergence detection for MACD-Histogram (centerline-crossing required) using backend-swing-point-detector.",
      "done": true
    },
    {
      "step": "Implement the same for Stochastic and RSI (no centerline-crossing requirement, simpler comparison) -- reuse the same swing-point utility.",
      "done": true
    },
    {
      "step": "Implement Kerry Lovvorn's 20-40-bar-spacing and half-depth filters as named, documented, and independently testable checks (not hardcoded inline).",
      "done": true
    },
    {
      "step": "Implement the 'Hound of the Baskervilles' continuation-signal detection (a formed divergence price subsequently ignores).",
      "done": true
    },
    {
      "step": "Hand-verifiable reference tests reproducing the exact chart examples described in docs/ideas.md (the DJIA 2007-2009 bullish/bearish divergence examples) as closely as fixture data allows.",
      "done": true
    },
    {
      "step": "Expose divergence state on GET /api/stocks/{ticker}/analysis and the indicator-history endpoint; regenerate openapi.json and frontend types; document in docs/Analyse.md.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "docs/ideas.md does not actually contain a DJIA 2007-2009 divergence chart example (confirmed via grep for 'DJIA'/'2007'/'2008'/'2009' across docs/ideas.md and docs/Analyse.md -- zero hits); the checklist's wording describing one appears to be inaccurate. Built hand-computed synthetic price/indicator fixtures instead (tests/unit/signals/test_divergence.py's FIXTURE_A and its variants -- exact arithmetic sequences with every filter boundary independently verifiable by inspection, per the add-indicator skill's reference-value-test bar), plus one additional fixture (tests/integration/test_stocks_analysis.py's/test_stocks_indicator_history.py's _divergence_daily_ohlcv) that drives the app's own real RSI/Stochastic/MACD-Histogram math end to end (confirmed empirically, not hand-derived by hand -- RSI's 9-day rolling-average math over a 38-bar span isn't hand-tractable the way a small synthetic-indicator fixture is) to prove the real (not synthetic) wiring path also produces a correct result. This is the closest reproduction of the checklist's intent the actually-available fixture data allows.",
      "rationale": "Rejected: inventing a plausible-looking DJIA numeric series from memory and presenting it as 'the book's own chart' would misrepresent an unverifiable claim as fact; skipping hand-verified tests entirely would violate the add-indicator skill's explicit reference-value-test requirement. The chosen approach keeps every number in the test suite either independently hand-checkable or empirically reproducible against this app's own already-tested indicator functions."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Swing points for divergence detection are found on PRICE (close) alone, via app.signals.swing_points -- never independently on the indicator series. Each indicator's value is then read at exactly those two price-swing dates, rather than detecting swing points on the indicator series too and fuzzily matching the two series' extrema by date proximity.",
      "rationale": "docs/ideas.md's own 'Implementation shape' note phrases the algorithm the same way ('compare the latest swing extreme in price against the indicator's value at the prior comparable swing extreme'). Anchoring on one series' swings keeps detection deterministic (no tolerance-window matching heuristic to invent and justify) and means every indicator needs only one already-computed swing-point pass, shared via app.signals.divergence.current_divergence's single swing_lows/swing_highs call over price -- rejected the two-independent-swing-series alternative as materially more complex (needing a fuzzy date-matching tolerance with no textual basis for its width) for no clear accuracy benefit, since a genuine price/indicator divergence's turning points are, in practice, at the same bars anyway."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Investigated the swing-point-detector-followups task's flagged concern (plateau/tie-merging in app.signals.swing_points shifting a reported extreme's date, and whether that could distort the 20-40-bar Lovvorn spacing measurement here) and concluded no mitigation is needed. Marked that followup's checklist item done in docs/tasks/backend-swing-point-detector-followups.json.",
      "rationale": "The spacing filter (is_spacing_valid) measures bar-count distance via price.index.get_loc on each SwingPoint's own reported index -- if a plateau run were merged to its middle bar per swing_points.py's documented convention, that bar (not the run's true first/last occurrence) is what bars_apart is computed from, a real shift bounded by run_length // 2. In practice this doesn't matter here: (1) an exact multi-bar tie in a continuous float close/indicator series is vanishingly rare outside synthetic data (the same conclusion the swing-point-detector task itself reached), and (2) even if it did occur, Lovvorn's own 20-40-bar range is already a wide empirical band with an explicit soft preference ('closer to 20 is better'), not a single exact cutoff -- a shift of a handful of bars from a plateau wouldn't flip a genuine divergence from qualifying to non-qualifying except in an already-marginal edge case, and an edge case at exactly the 20 or 40 boundary is exactly the kind of borderline call the book's own 'closer to 20 is better' phrasing already treats as weaker anyway. No code changes made for this; not worth a defensive workaround for a combination (multi-bar exact tie + landing exactly on the spacing boundary) this unlikely."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "Kerry Lovvorn's half-depth filter measures each extreme's 'depth' as its distance from a reference line: MACD-Histogram's own zero centerline (explicit in the book); for Stochastic/RSI (which have no signed centerline the same way -- their scale is bounded 0-100, not centered on zero), reused their own overbought/oversold reference line (30/70) as the reference instead of inventing an unrelated 50-midline or 0/100-bound convention.",
      "rationale": "The book invokes that exact same reference line in the same breath as the depth rule for Stochastic/RSI ('strongest when the first extreme is beyond the oscillator's own overbought/oversold reference line') -- reusing it keeps the depth and strength-flag math consistent and textually grounded, rather than introducing a second, unexplained reference point. RSI itself has no OB/OS reference line defined anywhere else in this codebase (docs/Analyse.md exposes it for computation + display only); reused Stochastic's existing 30.0/70.0 constants (app.signals.triple_screen.STOCHASTIC_OVERSOLD/OVERBOUGHT) as RSI's convention too, rather than a different pair -- Elder's own book also treats 30/70 as RSI's classic reference lines (ch. 27), and using the same numbers for both oscillators keeps app.signals.divergence's depth/strength math uniform."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "When more than one indicator (MACD-Histogram/Stochastic/RSI) independently qualifies with a divergence sharing the exact same second-extreme date (expected to be common in practice, since all three are checked against the same price swing points), app.signals.divergence.current_divergence breaks the tie by a fixed priority order: MACD-Histogram, then Stochastic, then RSI.",
      "rationale": "docs/ideas.md repeatedly calls MACD-Histogram 'his usual choice' for divergence work and Stochastic/RSI get 'the exact same... treatment, slightly simpler' -- reads as MACD-H being the primary/preferred indicator, Stochastic next (already computed pre-existing in this app, RSI newest). Rejected returning a list of every qualifying indicator's divergence instead of a single winner: the task's own description says 'Expose divergence state (none/bullish/bearish, which indicator, extreme dates) as a new field' (singular), and AnalysisResponse.divergence follows that singular-field shape, matching every other latest-bar-only field on that response rather than introducing the only list-shaped field that isn't already a list-of-zones/list-of-breakdown-items concept."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "'Hound of the Baskervilles' (Divergence.aborted) is defined as: at any point after the divergence's second extreme date, price closes beyond that same second extreme's own price level in the 'wrong' direction (a new low below it for an aborted bullish divergence, a new high above it for an aborted bearish one). This is detection + exposure only -- no stop-and-reverse trading action is implemented, and aborted is not wired into _determine_signal or confidence scoring.",
      "rationale": "docs/ideas.md's own phrasing ('price does the opposite of what it implies -- keeps falling despite a bullish divergence') doesn't specify an exact numeric trigger (a bar count, a percentage move, etc.) beyond 'ignores it', so this is the simplest, most literal, and most directly checkable reading: the second extreme's own price is the natural line in the sand a genuine reversal should hold above (bullish) or below (bearish); price breaching it again is precisely 'the opposite of what it implies'. Not implementing the stop-and-reverse trading action itself matches this task's own description ('detection + exposure only... a separate, later decision -- out of scope for this task') -- confidence-scoring/signal-engine wiring for aborted, same as for the base divergence signal itself, is left for a future task."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "app.signals.engine.analyse_history does not call app.signals.divergence.current_divergence fresh per bar (the 'compute internally when not given' path every other analyse() passthrough uses, and what analyse() itself falls back to for a single, non-history call). Instead it precomputes one DivergenceSwingCache over the full daily close series up front and calls confirmed_divergence_as_of (a no-look-ahead, already-confirmed-swings-only lookup) per bar, passed into analyse() via a new divergence= passthrough parameter guarded by a private _DIVERGENCE_NOT_GIVEN sentinel (since, unlike every other passthrough parameter, None is itself a legitimate 'no divergence' answer here, not just 'not supplied').",
      "rationale": "app.signals.swing_points.find_swing_points's core scan is an unvectorized Python-level loop (not a pandas/numpy vectorized rolling-window operation like the EMA/MACD/Stochastic passes elsewhere in this module), so re-running it on every one of analyse_history's up-to-thousands of per-bar calls would be a real O(history_length^2) Python-loop cost -- confirmed by direct estimation (~35k Python-level operations per full-series swing-point scan at a 5,000-bar history, times 5,000 calls) as measurably slower than the already-accepted O(i)-per-bar costs elsewhere in this module (e.g. the confidence-scoring branch's volume.rolling(...).mean() call, which is vectorized C code even at O(i)). Rejected the simpler 'just call current_divergence() fresh every bar, matching elder_bull_power/bear_power's own not-precomputed precedent' alternative for this specific reason: elder_bull_power/bear_power are themselves vectorized O(i) operations (a High/Low - EMA(13) subtraction), not a per-bar Python loop, so that precedent doesn't transfer to swing-point detection. The chosen precompute-and-slice design mirrors the same pattern engine.py already uses for ema_13_full/histogram_full/etc, and its no-look-ahead correctness (a swing point at position p is only usable once p + window <= the current bar) is directly covered by tests/unit/signals/test_divergence.py's TestDivergenceSwingCache tests and confirmed end to end against the real API in tests/integration/test_stocks_indicator_history.py's TestDivergenceField."
    },
    {
      "timestamp": "2026-09-19T16:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed the review's blocking finding by adding `divergence: null` to both pre-existing `AnalysisResponse` test fixtures that predated the new required field (frontend/tests/mocks/handlers.ts's analysisFixture, frontend/src/features/stocks/components/PriceChart.test.tsx's baseAnalysis) -- `null` is a valid, already-schema-legal value for this field (docs/ideas.md's own semantics: no currently-qualifying divergence), and neither fixture's own tests assert anything about the divergence field, so a non-null value would add noise without adding coverage.",
      "rationale": "Re-verified per the review's own instruction: `yarn tsc -b --noEmit` and `yarn build` (which runs `tsc -b && vite build`) both now succeed cleanly (previously only `vitest run` was checked, which doesn't type-check via esbuild transform and so missed this). Full frontend suite re-run afterward: 414/414 vitest tests pass, coverage 99.64%/98.97%/99.6%/99.75% (statements/branches/functions/lines), eslint clean. Backend untouched by this fix; its own suite (699 tests) still passes unchanged."
    }
  ],
  "git": {
    "branch": "task/backend-divergence-detection",
    "pr_number": 156,
    "pr_url": "https://github.com/rainoko/fintrade/pull/156",
    "pushed_at": "2026-09-19T15:45:05Z"
  },
  "review": {
    "reviewed_at": "2026-09-19T15:58:37Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/156",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "BLOCKING: `yarn tsc -b --noEmit` fails with 2 real type errors. `frontend/src/api/types.ts`'s regenerated `AnalysisResponse.divergence` field is required (no `?`, matches `app/api/schemas.py`'s `DivergenceOut | None` with no `default=None`), but two pre-existing test fixtures that construct `AnalysisResponse`-shaped objects were not updated to include it: `tests/mocks/handlers.ts` and `src/features/stocks/components/PriceChart.test.tsx`. `vitest run` passes (414/414) only because it doesn't type-check via esbuild transform, but `yarn build` (which runs `tsc -b && vite build`) would fail outright. The PR's own claim of 'frontend tsc/vitest clean' does not hold. Fix: add `divergence: null` (or an appropriate `DivergenceOut` value) to both fixtures.",
        "file": "frontend/tests/mocks/handlers.ts",
        "line": 53
      },
      {
        "summary": "Same blocking tsc error, second occurrence: AnalysisResponse mock is missing the now-required `divergence` field.",
        "file": "frontend/src/features/stocks/components/PriceChart.test.tsx",
        "line": 142
      },
      {
        "summary": "Verified: task-worker's claim that docs/ideas.md contains no DJIA/2007-2009 divergence chart example is accurate (grepped docs/ideas.md and docs/Analyse.md for 'DJIA'/'2007'/'2008'/'2009' -- zero hits in both). The synthetic hand-computed fixtures plus one real-RSI-math integration fixture (tests/integration/test_stocks_analysis.py's/test_stocks_indicator_history.py's _divergence_daily_ohlcv) are a reasonable substitution, not a shortcut to avoid harder fixtures -- 28 hand-verified reference tests in tests/unit/signals/test_divergence.py cover every filter boundary (spacing 19/20/40/41, exact-half depth ratio, centerline-crossing required-vs-not-required, Hound-of-the-Baskervilles both directions) by direct inspection.",
        "file": "backend/tests/unit/signals/test_divergence.py",
        "line": 0
      },
      {
        "summary": "Verified: both cross-task followup edits are accurate and transparent (visible in the PR's own diff plus a documented decisions entry, not silent). docs/architecture/Backend.md's module-layout tree now lists support_resistance.py (backend-support-resistance-followups' flagged gap), swing_points.py, and divergence.py all three under signals/. The swing-point-detector-followups plateau/tie-merging item's own narrower question (does the middle-bar merge convention distort the 20-40-bar Lovvorn spacing measurement?) is reasonably answered -- the spacing filter's own soft 'closer to 20 is better' framing absorbs a small mid-run shift. See the next comment for a related but distinct gap that reasoning did not cover.",
        "file": "docs/architecture/Backend.md",
        "line": 36
      },
      {
        "summary": "NON-BLOCKING (tracked as docs/tasks/backend-divergence-detection-followups.json): confirmed_divergence_as_of's no-look-ahead guarantee has a narrow leak for a swing point born from a run of 3+ exact-value-tied consecutive bars. Demonstrated concretely: for vals=[10,9,8,7,6,5,5,5,5,5,6,7,8,9,10,...] (a 5-bar plateau at indices 5-9), swing_lows(window=3) over the FULL series reports the merged point at index 7; the cache's confirm threshold for that point is 7+window(3)=10, but swing_lows on the series truncated to position 10 (what a genuinely causal per-bar recomputation would see) still reports index 6, not 7 -- convergence to index 7 only happens once truncated at position 11. So at position 10, confirmed_divergence_as_of reports a swing point one bar earlier than a truly causal recomputation would -- a real, if narrow, look-ahead leak. Requires an exact multi-bar tie in real float price/indicator data (vanishingly rare with real market data, matching this task's own 'vanishingly rare outside synthetic data' reasoning elsewhere), but this is a distinct claim from the spacing-distortion question the swing-point-detector-followups item's 'no mitigation needed' conclusion actually analyzed -- that conclusion did not cover this confirm-threshold correctness question, so marking that followup item done based on it was slightly premature on this narrower point (though reasonable on the point it did address).",
        "file": "backend/app/signals/divergence.py",
        "line": 651
      },
      {
        "summary": "NON-BLOCKING (tracked as docs/tasks/backend-divergence-detection-followups.json): `_evaluate_pair` calls `is_beyond_reference_line(...)` for RSI without explicitly passing `oversold=_RSI_OVERSOLD, overbought=_RSI_OVERBOUGHT` -- it silently relies on the function's own defaults, which are `_STOCHASTIC_OVERSOLD`/`_STOCHASTIC_OVERBOUGHT`. Correct today only because both constant pairs are numerically 30.0/70.0, but they were deliberately kept as distinct named constants (per this task's own decisions entry) specifically so RSI's reference line could diverge from Stochastic's later -- if either pair changes independently without also fixing this call site, RSI's `beyond_reference_line` would silently compute against the wrong thresholds, and no existing test would catch it. Found by a code-review pass; verified directly against the source.",
        "file": "backend/app/signals/divergence.py",
        "line": 355
      },
      {
        "summary": "Verified: MACD-Histogram centerline-crossing requirement is genuinely enforced as a hard filter (macd_centerline_crossed returns False -> _evaluate_pair returns None entirely, not merely flagged weak), directly covered by test_no_centerline_crossing_means_no_divergence_at_all. Stochastic/RSI correctly have no centerline requirement (beyond_reference_line is informational only, never gates). Lovvorn's 20-40-bar spacing and half-depth filters are implemented as named, independently-testable functions (is_spacing_valid, is_second_extreme_shallow_enough) with direct boundary tests (19/20/40/41 bars; exactly-half/just-over-half ratios). Hound of the Baskervilles (_is_aborted) correctly checks price beyond the second extreme's own level after its date, covered both directions. Full backend suite (699 tests) and frontend suite (414 tests, run serially to rule out worker-contention flakiness) pass; backend coverage 99.91% (divergence.py itself 100%), frontend coverage 99.6%/99%/99.6%/99.75% (statements/branches/functions/lines) -- both well above the 90% gate. ruff/mypy clean. backend/openapi.json regenerated locally and diffs clean against the committed snapshot. docs/Analyse.md/API.md/Backend.md updates are accurate and thorough.",
        "file": "backend/app/signals/divergence.py",
        "line": 0
      }
    ],
    "notes": "No UI surface: this PR is backend-only plus a regenerated frontend/src/api/types.ts (no rendering code consumes the new field yet -- frontend-divergence-markers is a separate, still-planned task) -- browser walkthrough (review step 9) and the persisted e2e suite (step 9a) both explicitly skipped as not applicable. Verdict is needs_work solely for the blocking frontend tsc failure (frontend/tests/mocks/handlers.ts and src/features/stocks/components/PriceChart.test.tsx missing the newly-required AnalysisResponse.divergence field) -- the backend divergence-detection logic itself (Elder methodology conformance, no-look-ahead precompute path modulo the tracked plateau edge case, coverage, architecture placement) is sound. Frontend flaky test observed and confirmed unrelated: src/pages/PortfolioPage.test.tsx's 'adds a new position end to end' test times out under full parallel vitest run but passes consistently in isolation and serially (--no-file-parallelism) -- pure worker-resource contention, not a regression (PortfolioPage.tsx/test.tsx untouched by this PR)."
  }
}
