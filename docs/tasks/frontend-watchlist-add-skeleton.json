{
  "id": "frontend-watchlist-add-skeleton",
  "title": "Watchlist: animated skeleton row while a newly-added ticker's data is still loading",
  "area": "frontend/pages/watchlist",
  "skill": "add-frontend-feature",
  "state": "testing",
  "depends_on": [],
  "references": [
    "frontend/src/features/watchlist/components/AddTickerForm.tsx",
    "frontend/src/features/watchlist/components/WatchlistTable.tsx",
    "frontend/src/features/watchlist/hooks/useAddWatchlistItem.ts",
    "docs/tasks/done/frontend-watchlist-page.json"
  ],
  "description": "User request: adding a ticker to the watchlist should immediately show an animated skeleton placeholder, since it can take a moment before the ticker actually appears with real data -- so the user gets visual feedback that something is happening rather than the UI looking unresponsive.\n\nCurrent behavior: AddTickerForm.tsx's submit button already shows a loading spinner while POST /api/watchlist itself is in flight, but POST just adds the ticker row (fast) -- useAddWatchlistItem.ts's onSuccess invalidates the whole watchlist query, triggering a GET /api/watchlist refetch that computes a signal for EVERY watched ticker (including the brand-new one, whose signal has never been computed/cached before) -- this refetch, not the POST, is the actual source of the perceptible delay before the new ticker's real row (with its ticker/date/signal/confidence) appears in WatchlistTable.tsx. The button's own spinner clears once the POST resolves, before the slower part is even done, which is exactly the gap the user is describing.\n\nUse MUI's Skeleton component for the animated placeholder -- this is a genuinely new pattern in this codebase (the existing common/LoadingState is a whole-page spinner, not a per-row/inline skeleton), so this task introduces the first one; decide whether it's worth generalizing into a reusable common/ component now or is fine as a one-off in WatchlistTable for this single use case, and record as a decisions entry.",
  "checklist": [
    { "step": "Decide where 'which ticker is currently being added' state lives so both AddTickerForm (which owns the mutation) and WatchlistTable (which renders the row) can see it -- e.g. lift to WatchlistPage, or another mechanism -- and when exactly the skeleton appears/disappears (on submit vs. on POST success; disappears once the ticker's real row is present in the refetched list, or on error). Record as a decisions entry.", "done": true },
    { "step": "Implement an animated skeleton row in WatchlistTable.tsx for the pending ticker, using MUI's Skeleton component.", "done": true },
    { "step": "Handle the error case: if POST fails, remove the skeleton and surface it via the existing ErrorState path in AddTickerForm rather than leaving an orphaned skeleton row.", "done": true },
    { "step": "Handle the already-watched/idempotent-no-op case (POST/API.md) sensibly -- the row already exists, so the skeleton either doesn't apply or resolves immediately, not a lingering placeholder for a row that was already there.", "done": true },
    { "step": "Tests: submitting a valid new ticker shows the skeleton then the real row; a failing add clears the skeleton and shows the error; a duplicate add doesn't leave a stray skeleton (MSW-mocked, including a deliberately delayed mock response to actually exercise the in-between skeleton state).", "done": true },
    { "step": "Confirm 90% coverage gate still holds, Storybook covers the new skeleton state if it became its own common/ component, and run make e2e to confirm nothing broke on the watchlist add flow.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The 'which ticker is currently being added' state that both AddTickerForm and WatchlistTable need is read directly off useAddWatchlistItem's shared TanStack Query mutation-cache entry, via `useMutationState({ filters: { mutationKey: watchlistKeys.add, status: 'pending' } })` inside WatchlistTable -- not lifted into a common ancestor (WatchlistPage) and prop-drilled down to both children. The skeleton appears the instant `mutate()` dispatches (on submit) and persists through both the POST and the GET refetch it triggers, because useAddWatchlistItem.ts's `onSuccess` now *awaits* `queryClient.invalidateQueries` instead of firing it off. It disappears on error (mutation status flips to 'error', excluded by the 'pending' filter) or once the refetch completes and the real row is present in `items` (checked every WatchlistTable render).",
      "rationale": "AddTickerForm and WatchlistTable are siblings under WatchlistPage with no other state they need to share, and useMutationState is a native TanStack Query mechanism for exactly this (observing a mutation's state from anywhere, keyed by `mutationKey`), keeping the app within 'TanStack Query only' (Frontend.md §2) without introducing prop-drilling or a new Context. Awaiting invalidateQueries in onSuccess keeps the mutation's `status` at 'pending' (matching the useMutationState filter) for the entire window during which the new ticker's real data isn't yet visible -- exactly the gap the task describes -- and as a side effect keeps AddTickerForm's own submit button loading/disabled for that same whole window instead of appearing to finish early. Rejected alternative: lifting the `useAddWatchlistItem()` call itself into WatchlistPage and passing the resulting mutation object down as a prop to both AddTickerForm and WatchlistTable -- rejected because it would force AddTickerForm's public API and its existing standalone tests to change for a concern that is purely about cross-component *visibility*, not mutation *ownership*, which useMutationState already solves without that restructuring."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "MUI's Skeleton is used directly inline in WatchlistTable's per-column `render` callbacks as a one-off, not generalized into a new components/common/ wrapper despite being the first per-row/inline skeleton pattern in this codebase.",
      "rationale": "Every existing common/ component (SignalBadge, ConfidenceGauge, LoadingState, ErrorState, ...) encapsulates real domain- or convention-specific behavior (coloring rules, band thresholds, a message layout) beyond what the underlying MUI primitive already provides. There is exactly one usage site here (WatchlistTable's own columns) and no shared prop surface or behavior to abstract -- each cell just needs a differently-sized MUI Skeleton with no domain logic attached, the same way AddTickerForm already uses raw MUI Button/TextField directly without a common/ wrapper. Revisit this if a second per-row skeleton use case appears elsewhere (e.g. PositionsTable) and the two genuinely need to share behavior, not just both happen to render MUI's Skeleton."
    },
    {
      "timestamp": "2026-09-19T00:00:00Z",
      "recorded_by": "task-worker",
      "decision": "The already-watched/idempotent-no-op add case is handled by simply never adding a synthetic skeleton row for a ticker that's already present in the current `items` list at render time -- i.e. 'the skeleton doesn't apply', the first of the two options the checklist itself allows, rather than 'resolves immediately'.",
      "rationale": "A real row for that ticker is already visible on screen the instant the (idempotent) POST is submitted, so there is nothing to placeholder and nothing to explicitly resolve -- checking `items.some(item => item.ticker === pendingTicker)` against the already-fetched list (not any newly introduced state) is sufficient and avoids ever rendering a duplicate/ghost row alongside the real one."
    }
  ],
  "questions": []
}
