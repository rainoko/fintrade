{
  "id": "backend-indicator-atr-adx-followups",
  "title": "ATR/Directional System -- non-blocking review follow-ups",
  "area": "backend/indicators",
  "skill": null,
  "state": "planned",
  "depends_on": ["backend-indicator-atr-adx"],
  "references": [
    "docs/tasks/backend-indicator-atr-adx.json#review",
    "https://github.com/rainoko/fintrade/pull/175"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #175 (backend-indicator-atr-adx), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges.",
  "checklist": [
    {
      "step": "True Range is computed twice from the same high/low/close whenever atr/plus_di/minus_di aren't all supplied to app.signals.engine.analyse(), since app.indicators.atr.atr() and app.indicators.directional_system.plus_minus_di() each independently call true_range() instead of sharing one pass. Every call path that computes these from scratch -- app.signals.engine.analyse_history's upfront full-history precompute (engine.py, the atr_full/plus_di_full/minus_di_full block), and any direct analyse() call with no atr/plus_di/minus_di passed in (e.g. via portfolio.get_risk's position-signal computation or watchlist's signal computation) -- runs true_range(high, low, close) twice over the full daily series: once inside atr() and once inside plus_minus_di(). Cheap in absolute terms (a vectorized O(n) pass), but avoidable -- engine.py could compute true_range once and pass it into both, or atr()/plus_minus_di() could accept an optional precomputed true_range series the way analyse() already accepts precomputed atr/plus_di/minus_di. Found by a code-review pass during PR #175's review.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
