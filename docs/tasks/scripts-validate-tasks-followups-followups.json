{
  "id": "scripts-validate-tasks-followups-followups",
  "title": "Non-blocking follow-up from PR #114 review (scripts-validate-tasks-followups)",
  "area": "tooling",
  "skill": null,
  "state": "planned",
  "depends_on": ["scripts-validate-tasks-followups"],
  "references": [
    "docs/tasks/done/scripts-validate-tasks-followups.json#review",
    "https://github.com/rainoko/fintrade/pull/114"
  ],
  "description": "Non-blocking finding from pr-reviewer's review of PR #114 (scripts-validate-tasks-followups), tracked here per CLAUDE.md's 'every non-blocking finding becomes a tracked task' rule so it isn't lost once that PR merges.",
  "checklist": [
    {
      "step": "docs/tasks/done/scripts-validate-tasks-followups.json's second `decisions` entry (recorded_by task-worker, covering the index_by_id list-keying fix, the shebang drop, and the is_invalid_str_choice helper) carries `timestamp: \"2026-09-18T00:00:00Z\"` -- an exact-midnight value that looks like a placeholder rather than the real time the decision was made. The corresponding commit (f16ff55, 'Fix validate_tasks.py's index.json duplicate-id overwrite bug') was actually authored at 2026-09-18T12:27:07Z per `git log`, ~12.5 hours off from the recorded timestamp. `scripts/validate_tasks.py`'s own validator only checks that `decisions[].timestamp` is a non-empty string, not that it's plausible/real, so this slipped through undetected. Not blocking (doesn't affect the decision's actual content or the code it describes), but worth correcting the timestamp to the real commit time (or removing it if the exact real time can't be reconstructed) so the decision record doesn't misrepresent when the call was made -- and worth a reminder that a fabricated/rounded timestamp on a `decisions` entry is the same class of problem CLAUDE.md/pr-merger already guard against for `review.reviewed_at`.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
