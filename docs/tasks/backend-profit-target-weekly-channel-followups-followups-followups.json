{
  "id": "backend-profit-target-weekly-channel-followups-followups-followups",
  "title": "Non-blocking follow-ups from backend-profit-target-weekly-channel-followups-followups PR #226 review",
  "area": "backend/signals",
  "skill": null,
  "state": "testing",
  "depends_on": ["backend-profit-target-weekly-channel-followups-followups"],
  "references": [
    "docs/tasks/backend-profit-target-weekly-channel-followups-followups.json",
    "https://github.com/rainoko/fintrade/pull/226",
    "scripts/check_profit_target_wording.py"
  ],
  "description": "Non-blocking findings from pr-reviewer's full review of PR #226 (backend-profit-target-weekly-channel-followups-followups, hardening check_profit_target_wording.py's 6 robustness gaps). All 6 fixes in PR #226 were verified as genuinely working (direct pre-fix-vs-post-fix repro for the implicit-string-concatenation join, the plural-weeks regex, the hyphenated profit-target context regex, and the end-anchor symmetry fix; a real before/after file-set diff on the actual repo tree -- 217 files, byte-for-byte identical sorted paths -- for the rglob->os.walk refactor; ruff confirming the named SIM103 finding is gone), so none of these are correctness bugs in the fixes themselves -- just small residual gaps worth tracking so they aren't lost once PR #226 merges.",
  "checklist": [
    {
      "step": "When check_banned_patterns() catches a violation via the new implicit-string-concatenation join (_join_implicit_string_concatenation), the printed 'found' text excerpt for that line has its boundary quote character stripped -- an artifact of the join step only removing quote characters, not newlines. E.g. a violation on a schemas.py-style split description prints `\"Current price + 30% of today's` without its trailing `\"`. The line number is still correct and the phrase is still identifiable, but the printed excerpt no longer exactly matches the original source line, which could confuse a maintainer eyeballing the message against the file. Worth reporting the original (pre-join) line text for the excerpt while still using the joined text's positions for matching/line-number computation.",
      "done": true
    },
    {
      "step": "_STRING_CONCAT_JOIN_RE (`([\\\"'])(\\s*\\n\\s*)\\1`) only recognizes implicit string concatenation between two literals using the *same* quote character (the backreference `\\1` requires an exact match). Python also permits mixed-quote-style concatenation (e.g. `\"foo\" 'bar'`), which this join won't catch -- not exercised by the current codebase's actual Field-description wrapping style (schemas.py is consistently double-quoted), so no live false-negative today, but the module's own doc comment claim of 'recognizing Python's own implicit-string-concatenation syntax' is slightly broader than what's implemented. Worth broadening the join regex to accept either quote character on each side independently (not tied by backreference) if this is meant to be a general-purpose join rather than narrowly scoped to the observed schemas.py style.",
      "done": true
    },
    {
      "step": "`ruff check` still reports one finding on scripts/check_profit_target_wording.py: `_is_excluded_path`'s SIM110 (a `for`/`if`/`return True` loop that could be `return any(path == excluded or excluded in path.parents for excluded in _EXCLUDED_PATHS)`). This finding pre-dates PR #226 (present on `main` before that PR too) and is not part of the enforced `ruff check app/` gate, so it's non-blocking either way -- but PR #226's own checklist item 6 rationale for fixing the sibling SIM103 finding was explicitly 'worth tidying up to match' scripts/validate_tasks.py's fully ruff-clean state, and that parity still isn't fully achieved with this SIM110 finding left in place. Worth fixing for the same consistency reason.",
      "done": true
    },
    {
      "step": "(Round 4 review, PR #226) The round-3->round-4 redesign (per-line negative scan + separate tokenize-based `_iter_implicit_concat_runs`/`_find_run_violations` for genuine implicit string concatenation) was verified thorough -- including a new probe specifically constructed for this review round (a banned phrase split exactly at the join point between two implicitly-concatenated string literals, e.g. `\"today's \" \"Autoenvelope\"`, plus mid-word and three-way-join variants) -- all correctly still caught via `_find_run_violations`'s decode-and-concatenate approach, which is insensitive to exactly where the phrase is split. However, the redesign only recognizes a split via genuine *implicit concatenation between two or more separate STRING tokens*; a banned phrase split by a real embedded newline *within a single* multi-line string (e.g. a triple-quoted docstring using one string literal, not two concatenated ones) is caught by neither the per-line scan (crosses no real newline by design) nor the run-based check (no second STRING token exists to form a run) -- confirmed directly (a single triple-quoted docstring wrapping the banned phrase across two lines is silently unflagged). This is an inherent, deliberately-accepted tradeoff of the current design (the alternative -- joining across any real newline -- is exactly what produced round 3's false-positive regression), not a bug in this round's fix, but it narrows what the module docstring's 'catches a literal regression anywhere in the repo' claim actually covers. Worth a short doc-comment addition (near `_iter_implicit_concat_runs` or the module docstring) making this scope boundary explicit, so a future contributor doesn't assume broader coverage than what's actually implemented.",
      "done": true
    },
    {
      "step": "(Round 4 review, PR #226) `_find_run_violations`'s `reported` dedup set is keyed only by `lineno` (`if lineno in reported: continue`), unlike the per-line scan's own dedup key of `(pattern_index, lineno)`. If two different `_BANNED_PATTERNS` entries both match within the same implicit-concatenation run and resolve to the same reported line, only one violation is now emitted instead of two -- a minor undercount (cosmetic reporting only; the guard still fails the check and points at the right line), not a missed regression, but worth aligning with the per-line scan's dedup granularity for consistency.",
      "done": true
    },
    {
      "step": "(Round 6 review, PR #226) `_decode_string_token_with_offsets` returns `None` for a byte-string literal (`\"b\" in prefix_lower`), which is the correct, safe choice for a single byte-string piece -- but a run made entirely of byte-string literals (e.g. `b\"today's \" b\"Autoenvelope\"`, valid since Python only forbids mixing bytes and non-bytes literals in one run, not multiple bytes literals) shares the exact same cross-line-miss structural gap as round 6's three BLOCKING f-string/same-line/mixed-quote findings: `_find_run_violations` returns `[]` for the whole run the moment any one piece fails to decode, so a banned phrase split across a byte-string-only run is silently missed too. Verified directly. Not filed as blocking alongside the other three because byte literals holding this kind of descriptive prose (Field descriptions, docstrings) is not a realistic pattern anywhere in this codebase's actual style -- but whichever fix addresses the round-6 f-string finding (which requires broadening what `_iter_implicit_concat_runs`/`_find_run_violations` treat as a recognizable-but-inert piece rather than an entirely run-breaking token) should double-check it doesn't leave this byte-string case newly reachable-but-still-silently-wrong, and ideally add a regression test pinning that a byte-string-only run is still safely skipped (not mis-reported) after that fix.",
      "done": true
    },
    {
      "step": "(Round 7 review, PR #226) In `_iter_implicit_concat_runs`'s f-string branch, `member = tok._replace(end=tokens[end_index].end)` extends the FSTRING_START stand-in's `.end` to the f-string's real end position, with an accompanying comment claiming this is so 'a following plain string's adjacency check compares against where the f-string actually ends.' But round 7's own adjacency-check simplification (dropping the different-physical-line and matching-quote-character requirements) removed every position-based comparison from the adjacency check entirely -- reaching a candidate member with `prev_member` still on record is now the sole condition. Confirmed by a repo-wide grep for `.end`/`span.tok.end` usage that nothing downstream (`_find_run_violations`, `_physical_line_for_offset`, `_span_index_for_offset`) ever reads a run member's `.end` for an f-string (decode always returns `None` for it before any span/position math involving it runs). This is now dead computation with a stale, misleading comment -- independently confirmed by a backgrounded `code-review` (effort high) pass against the same round-7 diff, which found the identical gap via its own from-scratch grep. Worth dropping the `_replace` (using the FSTRING_START token as-is) or correcting the comment, so a future reader doesn't infer a position-based adjacency check that no longer exists.",
      "done": true
    },
    {
      "step": "(Round 7 review, PR #226) None of the 4 new round-7 regression tests exercise `_find_fstring_end_index`'s nesting-depth-tracking branch (a genuinely nested f-string, e.g. `f\"outer {f'inner'}\"`) or its defensive fallback return -- confirmed via `coverage run --branch -m unittest discover -s scripts/tests` against `scripts/check_profit_target_wording.py`: lines 302->296 (the depth-increment branch on a nested FSTRING_START) and 307 (the fallback) are never hit by the existing 63 tests. Manually verified directly (outside the test suite) that a nested f-string is handled correctly -- `_find_fstring_end_index` correctly returns the *outer* FSTRING_END's index, not the inner one, confirmed via a depth-tracking dump -- so this is a coverage gap for correctly-behaving code, not a live bug. But given this exact function's history (three separate axes of bugs across 6 rounds before this one), and that nested f-strings were explicitly one of the round-7 dispatch's named stress-test scenarios, it's worth locking this in with an actual regression test (e.g. `f\"outer {f'inner {1}'} today's Autoenvelope\"` implicitly concatenated to a trailing real string) rather than relying on a one-off manual review check.",
      "done": true
    }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-24T15:24:21Z",
      "recorded_by": "task-worker",
      "decision": "Checklist item 1 (quote-stripped 'found' excerpt) is stale/already resolved -- no code change made. Verified directly: `_find_run_violations`'s current `display_text` construction (`tok.line.strip()` per distinct start line, deduplicated and joined with a space) already reports the untouched original raw source line(s), quote characters included -- confirmed with a probe reproducing the exact schemas.py-style split-description example the checklist item names, which prints the full `'\"Current price + 30% of today\\'s \" \"Autoenvelope/channel height.\"'` excerpt intact. `git log` confirms `_join_implicit_string_concatenation`/the quote-stripping join this item names never existed in any commit that landed on `main` (checked both PR #223's and PR #226's merged diffs) -- it was an intermediate, never-merged draft from an earlier review round of PR #226 that was superseded by the tokenize-based `_iter_implicit_concat_runs`/`_find_run_violations` redesign before merging.",
      "rationale": "Amending the checklist text in place (rather than appending this note) would hide that a real finding was reported at review time against a draft that was legitimately still evolving -- the append-only decisions convention lets a future reader see both what was found and when it stopped applying, without needing to diff PR review history."
    },
    {
      "timestamp": "2026-09-24T15:24:21Z",
      "recorded_by": "task-worker",
      "decision": "Checklist item 2 (_STRING_CONCAT_JOIN_RE's same-quote-character backreference) is likewise stale/already resolved -- no code change made. `grep -n \"_STRING_CONCAT_JOIN_RE\\|STRING_CONCAT\"` against the current script returns nothing; this regex-based join was superseded by the tokenize-based `_iter_implicit_concat_runs`, whose adjacency check (per its own module-level comment) explicitly imposes no matching-quote-character requirement at all -- `\"a\" 'b'` is recognized as ordinary, valid implicit concatenation, confirmed by the existing `test_mismatched_quote_style_concatenation_is_flagged` regression test. Same `git log` check as item 1 confirms this regex never existed in a merged commit either.",
      "rationale": "The task's own checklist text already flagged this item as 'likely already resolved by PR #226's later rounds -- verify first,' so this decision records that verification's outcome rather than re-implementing a fix against code that no longer has the described gap."
    },
    {
      "timestamp": "2026-09-24T15:24:21Z",
      "recorded_by": "task-worker",
      "decision": "Fixed the SIM110 finding (checklist item 3) by rewriting `_is_excluded_path`'s `for`/`if`/`return True`/`return False` loop as `return any(path == excluded or excluded in path.parents for excluded in _EXCLUDED_PATHS)`, exactly as ruff's own suggested rewrite proposed. Confirmed `ruff check scripts/` is now fully clean (previously the sole finding).",
      "rationale": "No alternative considered -- ruff's suggested rewrite is behavior-preserving and directly matches the consistency goal the checklist item names (parity with scripts/validate_tasks.py's fully ruff-clean state)."
    },
    {
      "timestamp": "2026-09-24T15:24:21Z",
      "recorded_by": "task-worker",
      "decision": "Added the doc comment for checklist item 4 as a new paragraph inside the existing module-level comment block above `_NON_BREAKING_TOKEN_TYPES` (immediately after the paragraph describing when a run-based match is reported), rather than in the top-of-file module docstring or as a docstring addition on `_iter_implicit_concat_runs` itself.",
      "rationale": "That comment block is already where the run-detection/run-reporting design's scope tradeoffs are documented (the f-string-as-opaque-member choice sits in the very next paragraph) and is the location a future contributor reading `_iter_implicit_concat_runs`'s implementation would already be reading; duplicating the same explanation in the module docstring risked the two drifting out of sync, and `_iter_implicit_concat_runs`'s own docstring is deliberately kept short (describing what it returns, not every scope boundary of the overall guard design)."
    },
    {
      "timestamp": "2026-09-24T15:24:21Z",
      "recorded_by": "task-worker",
      "decision": "Removed the dead `member = tok._replace(end=tokens[end_index].end)` position-extension entirely (checklist item 7) -- now `member = tok` (the FSTRING_START token as-is) -- and rewrote the stale comment to explain why no position extension is needed, rather than keeping the `_replace` call and only fixing the comment.",
      "rationale": "Confirmed via grep (`\\.end\\b`) that nothing downstream ever reads a run member's tokenize `.end` attribute for an f-string member specifically (the only other `.end`-shaped names in the file are `_PieceSpan.end`, a distinct field, and `re.Match.end()`, an unrelated method) -- keeping the `_replace` call would have left a functioning-but-pointless allocation on every f-string member with no reader depending on it, whereas dropping it is strictly simpler and the risk (some future change re-introducing a position-based adjacency check that then silently regresses for f-strings) is exactly what the corrected comment now calls out explicitly."
    }
  ],
  "questions": []
}
