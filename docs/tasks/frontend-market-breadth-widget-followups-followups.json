{
  "id": "frontend-market-breadth-widget-followups-followups",
  "title": "Non-blocking findings from PR #287's review (MarketBreadthCard followups)",
  "area": "frontend/pages/watchlist",
  "skill": null,
  "state": "planned",
  "depends_on": [
    "frontend-market-breadth-widget-followups"
  ],
  "references": [
    "docs/tasks/done/frontend-market-breadth-widget-followups.json#review",
    "https://github.com/rainoko/fintrade/pull/287"
  ],
  "description": "Non-blocking findings from pr-reviewer's full review of PR #287 (frontend-market-breadth-widget-followups), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges. Neither blocked acceptance -- tests/coverage/static analysis/code-review/browser walkthrough/e2e all passed clean.",
  "checklist": [
    {
      "step": "No frontend/tests/e2e/*.spec.ts spec asserts on PersonalBreadthCard at all (confirmed via grep across every spec file). PR #287's own task-worker discovered this while working checklist item 3 (whose premise -- 'matching the level of e2e attention PersonalBreadthCard already gets' -- turned out false) and correctly recorded it as a decision rather than expanding that task's scope to fix it. The underlying gap is real: PersonalBreadthCard (features/watchlist/components/PersonalBreadthCard.tsx) has thorough vitest coverage but zero persisted e2e regression coverage, unlike MarketBreadthCard which now has an explicit disabled-state e2e assertion (frontend/tests/e2e/watchlist.spec.ts). Consider adding an equivalent e2e assertion for PersonalBreadthCard's rendered Bullish/Bearish/Neutral stat cards on the watchlist page.",
      "done": false
    },
    {
      "step": "Both of PR #287's new `decisions` entries on frontend-market-breadth-widget-followups.json are timestamped `2026-09-23T00:00:00Z` -- an exact-midnight value that doesn't match the PR's actual push time (git log shows commits at 2026-09-23T23:53-23:54Z, i.e. roughly 24h later same calendar day). Looks like a placeholder/rounded value rather than a real `date -u` capture. Doesn't affect the decisions' validity, but worth using a real captured timestamp for future `decisions` entries the same way `review`/`pr_decision` timestamps are already required to.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
