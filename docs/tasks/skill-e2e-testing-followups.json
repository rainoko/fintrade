{
  "id": "skill-e2e-testing-followups",
  "title": "e2e-test skill wiring — non-blocking review follow-up",
  "area": "tooling",
  "skill": null,
  "state": "planned",
  "depends_on": ["skill-e2e-testing"],
  "references": [
    "docs/tasks/skill-e2e-testing.json#review",
    "https://github.com/rainoko/fintrade/pull/105"
  ],
  "description": "Non-blocking finding from pr-reviewer's review of PR #105 (skill-e2e-testing), tracked here per CLAUDE.md's autonomous-pipeline rule so it isn't lost once that PR merges.",
  "checklist": [
    {
      "step": "`.claude/agents/pr-reviewer.md`'s new step 9a says a non-flake failing e2e spec is \"review-blocking\", while `.claude/agents/task-qa-reviewer.md`'s new step 5a says the same underlying signal is \"at least a `major` gap\" (task-qa-reviewer's severity scale is blocker/major/minor, and 5a doesn't say which of those a failing e2e spec collapses to when it's more severe than baseline `major` — e.g. one that contradicts Analyse.md/API.md would otherwise qualify as `blocker` per step 6's own definition). Not a defect introduced by this PR specifically (the two agents already phrase other shared checks slightly differently elsewhere), but worth reconciling explicitly so a task-qa-reviewer run doesn't under-rank a failure that pr-reviewer would treat as an outright block.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
