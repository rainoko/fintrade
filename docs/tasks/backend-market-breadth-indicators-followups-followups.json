{
  "id": "backend-market-breadth-indicators-followups-followups",
  "title": "IBKR-scanner breadth snapshot concurrent-insert fix — non-blocking findings from PR #242's review",
  "area": "backend/api",
  "skill": null,
  "state": "planned",
  "depends_on": [
    "backend-market-breadth-indicators-followups"
  ],
  "references": [
    "docs/tasks/done/backend-market-breadth-indicators-followups.json#review",
    "https://github.com/rainoko/fintrade/pull/242"
  ],
  "description": "Non-blocking findings from pr-reviewer's review of PR #242 (backend-market-breadth-indicators-followups), tracked here per CLAUDE.md's autonomous-pipeline rule so they aren't lost once that PR merges. Both concern record_ibkr_breadth_snapshot's new `except (IntegrityError, OperationalError)` concurrent-insert fallback in app/api/routers/ibkr.py.",
  "checklist": [
    {
      "step": "The `except (IntegrityError, OperationalError)` branch's fallback (`winner = db.get(...)`, `assert winner is not None`) assumes any caught `OperationalError` implies a same-key winner committed and is now readable. That's verified true for `IntegrityError` (IBKRBreadthSnapshotORM's only constraint is the composite PK), but SQLite's whole-file write locking (no WAL/busy_timeout, per app/db/session.py) can raise 'database is locked' `OperationalError` from *any* concurrent write anywhere in the file, not only a race on this exact (series_key, snapshot_date) -- in which case `db.get(...)` legitimately returns None and the assert raises AssertionError instead of the graceful fallback the decision describes. Not a behavior regression (pre-fix, an uncaught OperationalError also propagated to a generic 500), but the fallback logs a misleading 'falling back to the concurrently-committed row' warning for a case where no such row exists, and the new decision entry doesn't carry forward the OperationalError-scope caveat app/data/cache.py's own `_upsert` comment makes for the identical exception pair (see docs/tasks/done/data-cache-followups-followups.json's review comment on PR #56, which reviewed and explicitly accepted this exact scope gap as non-blocking for the same race pattern). Worth either narrowing the assumption (return a clean error/None-safe path when no winner row is actually found) or adding the same explicit scope-limitation comment cache.py's OperationalError branch has.",
      "done": false
    },
    {
      "step": "The `assert winner is not None` at app/api/routers/ibkr.py:359 is a bare `assert`, which is fragile under `python -O`/`PYTHONOPTIMIZE` (assertions compiled out) -- in the scenario above, that would silently turn the failure into `row = None` and a later `AttributeError` on `row.count` instead of even the (still-uninformative but at least present) `AssertionError`. Not a live concern today (no evidence anywhere in the repo of running with `-O`), but worth an explicit `if winner is None: raise ...` guard instead of `assert` if this pattern is reused elsewhere, or at minimum a comment noting the reliance on assertions being enabled.",
      "done": false
    }
  ],
  "decisions": [],
  "questions": []
}
