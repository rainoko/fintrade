{
  "id": "backend-support-resistance",
  "title": "Horizontal support/resistance zone detection + false-breakout signal",
  "area": "backend/signals",
  "skill": "add-indicator",
  "state": "planned",
  "depends_on": [],
  "references": [
    "backend/app/signals/triple_screen.py",
    "docs/Analyse.md",
    "docs/ideas.md"
  ],
  "description": "Currently zero support/resistance logic anywhere in this app -- Screen 3/Trigger only ever compares against yesterday's single high/low, never a zone built from repeated touches. Concrete, well-specified rules from docs/ideas.md (Elder ch. 18): a zone is a horizontal congestion area (the edges of where price repeatedly stalled, not the extreme wick), strength scored by length (2wk=minor/2mo=intermediate/2yr=major), height as % of price (~1%/~3%/>=7%), and volume traded within the zone; zones flip roles once broken (old resistance becomes new support); a false breakout (price closes beyond the zone, then closes back inside it) is a specific, high-value reversal signal with an explicit stop-placement rule (near the false breakout's own extreme). This task covers detection + strength scoring + false-breakout flagging + API exposure. Using this to tighten the existing stop-loss formula near a known zone (also logged in docs/ideas.md) is a separate, later task -- note it as a natural follow-up but don't implement it here.",
  "checklist": [
    {
      "step": "Implement congestion-zone detection over the daily series (decide and record the exact algorithm -- e.g. clustering of touches within a tolerance band -- since the book describes the concept precisely but not a mechanical detection algorithm).",
      "done": false
    },
    {
      "step": "Implement the three-factor strength score (length/height/volume) per the book's thresholds.",
      "done": false
    },
    {
      "step": "Implement zone role-flipping (a broken zone keeps existing, inverted role) rather than discarding zones after a breakout.",
      "done": false
    },
    {
      "step": "Implement false-breakout detection with the explicit stop-placement reference point (the failed move's own extreme).",
      "done": false
    },
    {
      "step": "Tests covering: a clean multi-touch zone, a zone that flips role after breaking, and a false breakout that reverses vs. a true breakout that doesn't.",
      "done": false
    },
    {
      "step": "Expose zones + false-breakout flags on GET /api/stocks/{ticker}/analysis; regenerate openapi.json/frontend types; document in docs/Analyse.md.",
      "done": false
    }
  ]
}
