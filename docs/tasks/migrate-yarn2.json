{
  "id": "migrate-yarn2",
  "title": "Migrate frontend package manager from npm to Yarn 2+ (Yarn Berry)",
  "area": "infra",
  "skill": null,
  "state": "planned",
  "depends_on": [],
  "references": [
    "frontend/package.json",
    "Makefile",
    ".devcontainer/post-create.sh",
    "README.md"
  ],
  "description": "Migrate frontend/ from npm to Yarn 2+ (\"Yarn Berry\") via Corepack: replace package-lock.json with a Yarn lockfile (.yarnrc.yml, .yarn/releases), and update every command across the repo that currently shells out to npm — the root Makefile's frontend/install/test/dev targets, .devcontainer/post-create.sh, README.md, and any .claude/skills or .claude/agents files that reference npm commands directly (check add-frontend-feature, static-verify, check-coverage, test-90, revalidate-done-tasks, and any e2e-testing skill/task landed by then) — to their Yarn equivalents. Account for whatever frontend npm scripts exist at the time of this task (including test:e2e if frontend-e2e-tests has landed by then).",
  "checklist": [
    { "step": "Decide the Yarn Berry linker mode (node-modules vs PnP) and record as a decisions entry with rationale — PnP is more idiomatic Yarn Berry but can surface compatibility issues with tools expecting a real node_modules tree (editor tsserver, Storybook, Playwright, etc.); the node-modules linker is the safer default for a migration like this unless there's a clear reason to prefer PnP.", "done": false },
    { "step": "Enable Yarn via Corepack and run `yarn set version berry` in frontend/; remove package-lock.json; commit the new yarn.lock, .yarnrc.yml, and .yarn/releases.", "done": false },
    { "step": "Update every reference to npm commands across the repo: root Makefile (backend/frontend/install/test/dev targets), .devcontainer/post-create.sh, README.md, CLAUDE.md if it references npm, and any skill/agent files under .claude/ that shell out to npm.", "done": false },
    { "step": "Confirm `yarn install`, `yarn build`, `yarn test`/coverage, `yarn lint`, and `yarn build-storybook` all work equivalently to their prior npm counterparts.", "done": false },
    { "step": "Re-run the full frontend suite + coverage gate to confirm no regression from the package-manager switch.", "done": false },
    { "step": "Update .devcontainer/devcontainer.json/post-create.sh if Yarn needs explicit provisioning (e.g. `corepack enable`) so a fresh container gets it automatically.", "done": false }
  ],
  "decisions": [],
  "questions": []
}
