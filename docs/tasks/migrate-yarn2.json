{
  "id": "migrate-yarn2",
  "title": "Migrate frontend package manager from npm to Yarn 2+ (Yarn Berry)",
  "area": "infra",
  "skill": null,
  "state": "implementing",
  "depends_on": [],
  "references": [
    "frontend/package.json",
    "Makefile",
    ".devcontainer/post-create.sh",
    "README.md"
  ],
  "description": "Migrate frontend/ from npm to Yarn 2+ (\"Yarn Berry\") via Corepack: replace package-lock.json with a Yarn lockfile (.yarnrc.yml, .yarn/releases), and update every command across the repo that currently shells out to npm — the root Makefile's frontend/install/test/dev targets, .devcontainer/post-create.sh, README.md, and any .claude/skills or .claude/agents files that reference npm commands directly (check add-frontend-feature, static-verify, check-coverage, test-90, revalidate-done-tasks, and any e2e-testing skill/task landed by then) — to their Yarn equivalents. Account for whatever frontend npm scripts exist at the time of this task (including test:e2e if frontend-e2e-tests has landed by then).",
  "checklist": [
    { "step": "Decide the Yarn Berry linker mode (node-modules vs PnP) and record as a decisions entry with rationale — PnP is more idiomatic Yarn Berry but can surface compatibility issues with tools expecting a real node_modules tree (editor tsserver, Storybook, Playwright, etc.); the node-modules linker is the safer default for a migration like this unless there's a clear reason to prefer PnP.", "done": true },
    { "step": "Enable Yarn via Corepack and run `yarn set version berry` in frontend/; remove package-lock.json; commit the new yarn.lock, .yarnrc.yml, and .yarn/releases.", "done": true },
    { "step": "Update every reference to npm commands across the repo: root Makefile (backend/frontend/install/test/dev targets), .devcontainer/post-create.sh, README.md, CLAUDE.md if it references npm, and any skill/agent files under .claude/ that shell out to npm.", "done": true },
    { "step": "Confirm `yarn install`, `yarn build`, `yarn test`/coverage, `yarn lint`, and `yarn build-storybook` all work equivalently to their prior npm counterparts.", "done": true },
    { "step": "Re-run the full frontend suite + coverage gate to confirm no regression from the package-manager switch.", "done": true },
    { "step": "Update .devcontainer/devcontainer.json/post-create.sh if Yarn needs explicit provisioning (e.g. `corepack enable`) so a fresh container gets it automatically.", "done": true }
  ],
  "decisions": [
    {
      "timestamp": "2026-09-18T13:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Use the node-modules linker (nodeLinker: node-modules in frontend/.yarnrc.yml), not Plug'n'Play.",
      "rationale": "Matches the task's own stated default and this project's tooling: Storybook (@storybook/react-vite), Playwright, and editor tsserver all expect a real node_modules tree on disk. PnP would need extra SDK/editor shims (yarn dlx @yarnpkg/sdks) and has known rough edges with Storybook's Vite builder and some ESM/CJS interop in this dependency set. node-modules keeps the working tree's file layout (and therefore every existing tool config) unchanged, which is the point of a low-risk package-manager swap rather than a bigger tooling migration."
    },
    {
      "timestamp": "2026-09-18T13:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Vendor the Yarn release binary at frontend/.yarn/releases/yarn-4.18.0.cjs with .yarnrc.yml's yarnPath pointing at it (the classic 'committed release' Yarn Berry setup), in addition to keeping packageManager: yarn@4.18.0 in package.json.",
      "rationale": "Corepack 0.34's own `yarn set version stable` only updates package.json's packageManager field and relies on Corepack's own per-user cache/network fetch to resolve that version at run time; it does not vendor a project-local copy. The task's checklist explicitly asks for '.yarn/releases' to be committed, and a vendored release is what makes `yarn <anything>` work offline/hermetically for any contributor or CI runner without a first-time Corepack network fetch (consistent with this repo's no-live-network-call testing philosophy, even though this specific file isn't fetched by tests). Copied the exact 4.18.0 bundle Corepack had already downloaded (byte-identical to what `corepack pack`/a real `yarn set version stable` would fetch) rather than hand-rolling one."
    },
    {
      "timestamp": "2026-09-18T13:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Translated frontend/package.json's npm `overrides` (added in frontend-verify-npm-install, PR #115) to Yarn's `resolutions` field as `\"openapi-typescript/typescript\": \"$typescript\"` (a flat 'package/dependency' key instead of npm's nested-object form), keeping the same `$typescript` self-reference syntax.",
      "rationale": "Verified this resolves correctly: `yarn install` hoists a single top-level node_modules/typescript@6.0.3 (matching the project's own ~6.0.2-range typescript devDependency) with no nested copy under node_modules/openapi-typescript, and `yarn generate:api-types` reproduces src/api/types.ts byte-for-byte identical to the committed file (diffed before/after). Yarn's post-resolution validation still prints a YN0060 peer-mismatch warning ('typescript ... doesn't satisfy what openapi-typescript ... request(s) ^5.0.0') — this is expected and harmless (same category of warning npm's overrides mechanism was working around in the first place: openapi-typescript's own peerDependencies range predates TS 6.x), not a sign the resolution failed to apply."
    },
    {
      "timestamp": "2026-09-18T13:10:00Z",
      "recorded_by": "task-worker",
      "decision": "Pinned devDependencies.jsdom to an exact \"30.0.1\" (previously \"^30.0.1\") instead of letting the fresh Yarn lockfile resolve the range's newest match.",
      "rationale": "A clean `yarn install` from the range initially resolved jsdom to 30.1.0 (vs. 30.0.1 frozen in the old package-lock.json) and that alone broke 9 previously-passing vitest tests (ConfirmDialog, AddPositionDialog) with `TypeError: nodeToRestore.current.focus is not a function` inside MUI's FocusTrap unmount path — a jsdom 30.1.0 behavior change in focus/element handling, not anything caused by Yarn itself. Re-resolving with jsdom pinned back to 30.0.1 made all 269 tests pass again with no other change. Rejected chasing/patching the jsdom 30.1.0 incompatibility itself as out of scope for a package-manager migration (that's real, unrelated frontend maintenance work); rejected leaving it broken since 'no regression from the package-manager switch' is this task's own explicit bar. A future task can deliberately revisit the jsdom upgrade (and whatever MUI/jsdom compat fix it needs) on its own."
    },
    {
      "timestamp": "2026-09-18T14:05:00Z",
      "recorded_by": "task-worker",
      "decision": "Fixed frontend/README.md's Scripts table padding with `yarn format` scoped to just that file (verified via a subsequent isolated `prettier --check README.md`), and did not apply the broader `yarn format --write .` output to the ~22 other files it also touched (RiskPercent.tsx, several stocks-feature components/tests, e2e specs, etc.).",
      "rationale": "Confirmed via `git diff --stat main...task/migrate-yarn2` that this PR never touched any of those other files, and via `git stash` + a repo-wide `yarn format:check` on the pre-fix branch state that all ~22 already fail Prettier's check independent of this PR (pre-existing drift on main, unrelated to the npm->yarn migration). Reformatting them here would be an unrelated, out-of-scope change riding on this PR; left them alone so the diff stays scoped to the review's actual blocking finding (README.md) plus the requested non-blocking comment fixes."
    }
  ],
  "questions": [],
  "git": {
    "branch": "task/migrate-yarn2",
    "pr_number": 116,
    "pr_url": "https://github.com/rainoko/fintrade/pull/116",
    "pushed_at": "2026-09-18T13:08:01Z"
  },
  "review": {
    "reviewed_at": "2026-09-18T13:18:25Z",
    "pr_url": "https://github.com/rainoko/fintrade/pull/116",
    "verdict": "needs_work",
    "comments": [
      {
        "summary": "frontend/README.md's Scripts table (edited by this PR to rename npm commands to yarn) no longer matches Prettier's expected column padding: `yarn format:check` / `node_modules/.bin/prettier --check README.md` fails (exit 1) on this file. Confirmed this is newly introduced by this PR, not pre-existing drift, by checking out the pre-PR revision of frontend/README.md and running the same check (passes cleanly there). Purely cosmetic (`prettier --write` only needs to widen column 1 by one space and narrow column 2's separator by one dash), but this project has direct precedent for treating exactly this failure mode as blocking rather than a follow-up: docs/tasks/done/frontend-api-client.json's review history (PR #63) required a dedicated fix commit for the identical class of README table-padding regression before acceptance.",
        "file": "frontend/README.md",
        "line": 11
      },
      {
        "summary": "backend/app/config.py and backend/app/data/fixture_provider.py still reference `npm run test:e2e` in their docstrings/comments -- missed by the repo-wide npm-to-yarn sweep this task's checklist called for. Comment-only, no functional effect.",
        "file": "backend/app/config.py",
        "line": 19
      }
    ],
    "notes": "Full verification pass, not just a diff read. Confirmed clean from a genuinely fresh install: `rm -rf node_modules && yarn install --immutable` succeeds with no npm involvement. Ran and confirmed passing: yarn build, yarn lint, yarn tsc -b --noEmit, yarn test:coverage (269/269, 99.6%/99.21%/100%/99.6%), yarn build-storybook, make e2e (13/13 specs, real backend+frontend booted via the new yarn dev/yarn test:e2e wiring, including the 'no console errors on load' spec), backend ruff/mypy (clean) and pytest --cov (560/560, 99.86%, no regression either side). Verified the resolutions translation is correct: yarn.lock hoists a single typescript@npm:6.0.3 with nothing nested under openapi-typescript, and `yarn generate:api-types` reproduces src/api/types.ts byte-for-byte identical to the committed file (no API/schema changes in this PR, so no openapi.json staleness concern either). Independently reproduced the jsdom pin's justification: bumping jsdom back to ^30.1.0 and reinstalling reliably reproduces the exact same 9 FocusTrap-related test failures described in the task's decisions entry, confirming it's a genuine unrelated jsdom regression rather than something masking a real Yarn-migration issue. Verified the vendored frontend/.yarn/releases/yarn-4.18.0.cjs is byte-for-byte identical (sha256) to the real upstream https://repo.yarnpkg.com/4.18.0/packages/yarnpkg-cli/bin/yarn.js fetched fresh -- genuine, not tampered. Checked .devcontainer/post-create.sh's corepack enable placement: no automatic frontend-install step exists in that script to race with it, so ordering is fine for a fresh container build. Repo-wide grep for npm run/npm install/npx outside node_modules/.git/docs/tasks (historical records deliberately left as-is) found the two backend comment misses recorded above, otherwise clean and consistent across Makefile, both READMEs, docs/architecture/{Frontend,Testing}.md, and the three .claude/skills/*/SKILL.md files. Ran the code-review skill (medium effort) independently; it found the identical README.md/format:check issue and otherwise cleared the diff (no other correctness bugs, resolutions/jsdom/gitignore pattern all independently confirmed correct). architecture-review checklist walked explicitly: no backend/signal/API changes in this PR (pure build tooling) so verify-elder-signal doesn't apply; no layering/dependency-table/coverage-gate/API-contract issues, vite.config.ts's coverage thresholds and pyproject.toml's gate are untouched (comment-only diffs), no new architectural layer introduced. All three decisions entries are well-reasoned and the checklist's linker-mode 'decide X' item has a corresponding decisions record -- no undocumented judgment calls. No UI feature was added by this PR (pure tooling migration); browser-facing risk was covered via make e2e's real-Chromium run (13/13, including its console-error check) plus manually starting the dev stack via `yarn dev`/uvicorn and confirming both respond correctly -- Playwright MCP tools were not available in this session, so no separate interactive walkthrough was performed beyond that. The one non-blocking finding (stale npm references in two backend comments) is filed as a follow-up on migrate-yarn2-followups. Verdict: needs_work, blocked solely on the README.md Prettier fix -- everything else in this migration (the .yarnrc.yml/.gitignore/.prettierignore Yarn-Berry idiom, the packageManager field, the skill-file and doc updates) is correct and consistent."
  }
}
